## 2026-09-19 · fix(shop): saving a first payment method no longer crashes My Shop; the verification lines read what the shop typed; one-tap doors to "How clients pay you"

- **Crash (prod 15:24 UTC, digest 1227757363).** `payment-options/surface.tsx` is a server component and its Delete button passed an inline `onClick` (a `confirm()`) to the client `SubmitButton`. React cannot serialise a function across the boundary, so the whole shop console fell to its error page the first time a shop HAD a method — prod had none until Saysay saved a QR. The confirm now lives in the existing client `<ConfirmForm>`. New guard `lib/rsc-function-props.test.ts` walks every server file reachable from every route entry (645 entries, 2,077 files) and fails on any inline/local function passed to a `'use client'` component, a client package component or a host element; the payment-options branch is read, not rendered, so it covers every method type and count. Sabotage: restoring the old surface fails it at `surface.tsx:300`.
- **42501 on `loadVerificationIdentityFields`.** It read five tax-identity columns off `vendor_profiles` on the session, but `20271217955839` took them off `authenticated`'s SELECT allowlist, so the whole read was refused and every line beside every paper read "Not filled in yet". It now reads `vendor_profiles_self`, like its sibling `fetchRegistrationNumberState`. `shop-contact-is-not-session-readable.test.ts` now also scans the 12 tax-identity columns (read from prod with `has_column_privilege`) and no longer takes a JSX attribute `admin={admin}` as a binding. Sabotage: pointing the read back at the table fails it.
- **Doors.** `PAYMENT_OPTIONS_HREF` now opens the fold and lands on it (`?open=payments#shop-folds`) instead of the top of My Shop. The nudge now also shows on Today whenever the shop holds an upcoming booking (unless a booking-ask card already carries it), and on the supplier's live ACCEPTED quote card in chat. The client page already carried it for a booked client whose deposit is outstanding.

SPEC IMPACT: None
