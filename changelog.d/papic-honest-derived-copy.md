## 2026-07-20 · fix(papic): every price/capacity claim derived from config, never hardcoded

Owner 2026-07-20 — "make every Papic price/capacity claim honest and derived". Four display surfaces carried hand-typed Papic promises the charge/enforcement path never made good on. Enforcement runs on capture POINTS resolved from the admin-editable `papic_tier_config` (1 photo = 1 pt · one 5-second clip = 3 pts); the copy had drifted away from it.

**What was false (all verified against `origin/main` first):**
- `app/pricing/page.tsx` — "Ltd ₱30 (30 photos + 10 videos) … first 5 free … (Ltd ₱9,000 · Unli ₱15,000)". Wrong rung name (₱30 is Mini/legacy-roll, not Ltd), wrong capacity (roll enforces **20 points**, not 60), wrong free count (**3**, not 5), wrong cap (**₱6,000**, not ₱9,000).
- `app/pricing/_papic-estimator.tsx` — `capPerDay: 15000` applied to BOTH tiers, multiplied by days, covering add-ons, plus an "auto-upgrades to Unlimited with every booster included" claim. None of that matches `computeCameraQuote` (per-tier cap · clamps the whole camera subtotal · weddings only · add-ons excluded · no tier upgrade).
- `app/dashboard/[eventId]/studio/papic/guest-camera-tier-picker.tsx` — "30 photos + 10 clips each, per day".
- `app/_components/home/pricing-data.ts` — "First 5 cameras · 10 photos + 3 videos each", "Papic Ltd · 30 photos + 10×5s", `cap: 9000`, `cap: 15000`, a hardcoded `₱15,000` unlock row.
- `public/llms.txt` (the AI-crawler surface) — "Papic Ltd ₱30 … capped at ₱9,000 per day".
- `lib/v2-catalog.ts` — stale "30 photos + 10 videos" build-status note.

**The fix — derive, don't spell.** New `lib/papic-tier-copy.ts` is the ONE place a Papic capacity/free-camera/cap number may live: `fetchPapicTierConfig()` (public-read RLS, graceful-degrade, never throws), `publicPapicLadder()` (the buyable rungs, `free` + legacy `roll` filtered out, inactive rows drop out so an admin deactivation removes a rung from every surface at once), `papicCapacityPhrase` / `papicCapacityShort` ("about N photos a day — fewer if you shoot clips, since one 5-second clip counts as 3": the honest shape, because photos and clips share ONE points purse and an exact "N photos + M clips" promise is unkeepable by construction), `papicFreeCameraCount`, `papicCapPhrase` / `papicCapLadderPhrase` (per-tier, **weddings-only** — non-wedding events are explicitly stated as uncapped, mirroring `isPapicUncapped`). `lib/papic-tier-config-read.ts` is the server-only admin-client wrapper, kept separate so the pure helpers can be imported by client components.

Every surface now renders from that: /pricing's synthetic Papic row + the estimator (two rungs → the full ladder, per-rung capacity + per-rung wedding cap, cap semantics now mirroring `computeCameraQuote`), the studio picker (`pointsPerDay` passed from the server page), the homepage rows, and llms.txt (rewritten to the three-rung + points model; `lib/llms-price-fixture.ts` updated — ₱9,000 removed, ₱50/₱6,000 added). Also fixed a stray `|| 15000` cap fallback in the studio page (now `PAPIC_UNLI_CAP_FALLBACK_PHP`). A rung whose rate SKU is missing from the catalog DROPS OUT rather than rendering an invented price.

**The durable half — `lib/papic-copy-guardrails.test.ts` (22 tests).** Every enumerated Papic display file is scanned and CI fails if it re-grows a photo/clip split promise, a spelled free-camera count, a spelled cap peso figure (₱6,000/₱9,000/₱10,000/₱15,000), or a spelled points budget. Plus: the copy helpers are asserted to TRACK the budget rather than a number (they read correctly whether roll ends at 20 points or 60 — the grandfathering decision is still open), and `PAPIC_TIER_CONFIG_FALLBACK` is pinned byte-for-byte to the migration seed in `20270821110000` so the last-resort values can never quietly diverge from the DB.

No migration, no schema change, no price/catalog write — copy + derivation only.

Verify: `tsc --noEmit` clean · lint clean (0 errors) · `test:unit` **2287/2287 pass**.

SPEC IMPACT: `0012_papic/Papic_Good_Better_Best_Pricing_2026-07-17.md` + `Pricing.md` — the public Papic surfaces now state the three-rung ladder (Mini ₱30 · Ltd ₱50 · Unli ₱100), 3 free cameras, points-based capacity, and weddings-only per-tier caps (₱6,000 / ₱10,000 / ₱15,000). ⚠ Two owner items surfaced, NOT actioned here (code-only task): (1) prod carries TWO active SKUs both titled "Papic Ltd" — `PAPIC_CAMERA_ROLL_DAY` ₱30 and `PAPIC_CAMERA_LTD_DAY` ₱50; the ₱30 row should be retitled "Papic Mini" or deactivated. (2) `PAPIC_CAMERA_LTD_DAY` is now advertised while its buy path is landing separately — deactivating that catalog row hides the rung everywhere until it is buyable.
