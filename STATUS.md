# Setnayan — Project Status

> Living checkpoint. Refreshed 2026-05-22 (Task #13 — day-of PWA Phase 1).
> Anchor doc — if you're opening this repo cold in a new Claude session, start here.

**Owner deadline anchor:** December 2026 wedding

---

## Where we are right now

V1 web surface is **functionally complete**. Pre-launch sprint closed 2026-05-13 (19 iterations). 2026-05-14 then landed **28 PRs** across two waves.

**2026-06-15 — feat(alaala): the memory pillar is named "Alaala" + Lane 1 spine begins.** Owner locked the Living-Memories pillar's customer-facing name = **Alaala** (Tagalog: memory/keepsake — gathers Papic/Panood/Kwento/Pakanta into one promise) + a 3-lane embed plan (narrative spine → in-app Alaala hub → finish the uncopyable keystones). **Lane 1, increment 1:** the **Studio hub** (`/dashboard/[eventId]/add-ons`) gains an Alaala framing band (names the pillar + states the "never ruins the day" guardrail), and the live `/our-story` manifesto close now names it (*"We call it Alaala"*). Canonical def `03_Strategy/Alaala_Pillar_2026-06-15.md`. **Queued:** onboarding "promise" beat (Lane 1 remainder) · a dedicated in-app Alaala hub w/ the save-the-date→day-of→editorial through-line (Lane 2) · finish the keystones Kwento → Live Photo Wall → produced-output (Lane 3 — the parts competitors can't copy). PR pending (branch `claude/alaala-lane1`, auto-merge). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-15 — feat(pricing): Setnayan AI buy surface (PR 2 of the pricing/payments plumbing fix).** Closes the "no purchase path for SETNAYAN_AI" gap — the entitlement chain was wired but nothing let a couple buy it. New `add-ons/setnayan-ai/page.tsx` (3-state gate: active / owns-but-off / buy) + `shouldOfferSetnayanAiPurchase()` helper (entitlement-keyed → double-charge-safe) + registered as the lead add-on card (`plan_organize`). Buy state dormant until `SETNAYAN_AI_PAYWALL_ENABLED=true` (owner Vercel flip) → then ₱3,999 charges and paying unlocks the ranked match (verified end-to-end). tsc green. Stacks on PR 1 (#1431). **Still queued:** seed missing catalog rows + bundle re-resolution (PR 2b), admin vendor/fees editing (PR 3), payment activation repair (PR 4). SPEC IMPACT → CHANGELOG + DECISION_LOG.
**2026-06-14 — feat(our-story): "Living Memories" brand manifesto shipped.** New `/our-story` page — the umbrella idea over Papic / Panood / Kwento / Editorial (paper albums → digital albums → **living memories**; a wedding was never meant to be frozen, the four features prove it capture→keep) — plus a homepage teaser band routing into it, and a shareable manifesto card (`/api/og/manifesto?format=og|square|story`). Nav "Our story" repointed `/about`→`/our-story`. Companion corpus artifacts: `03_Strategy/Living_Memories_Manifesto_2026-06-14.md` + the 55s film script/storyboard. PR pending (branch `claude/our-story-manifesto`, auto-merge). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG (iteration 0015).
**2026-06-15 — ci(desktop): public Mac/Windows download is now auto-fresh.** The desktop `.dmg`/`.msi` build off `main` used to live only as a 14-day, login-gated Actions artifact; the one public Release (`v0.0.1`) was a manual 2026-05-13 upload, ~a month stale. New `publish-latest` job in `build-desktop.yml` pushes every `main` build to a rolling **`desktop-latest`** pre-release → stable always-current URL. Also hardened `if-no-files-found: warn→error`. **Still owner-blocked for a public "Download for Mac" button:** (1) Developer-ID signing + notarization (Gatekeeper friction until then), (2) no Intel/universal build, (3) no download link on setnayan.com. SPEC IMPACT → CHANGELOG (None — CI plumbing).

**2026-06-14 — fix(pricing): catalog = single source of truth for every charge (PR 1 of the pricing/payments plumbing fix).** From a 3-audit pass (AI charging · admin price coverage · payment flow). `submitOrderAction` now re-resolves the charged price from `platform_retail_catalog_v2` for EVERY catalog SKU (was: pax-priced only → flat SKUs trusted the browser's number, so hardcoded fallbacks set the real bill). Added `SETNAYAN_AI`/`PRO_RSVP`/`EVENT_WEBSITE` to `V2_SKU_CODES` (were live rows but `formatV2Sku()` returned null) + honest BUILD_STATUS. Purged the two divergent hardcoded fallbacks (Custom QR ₱1,499→catalog ₱999, Pakanta ₱1,999→catalog ₱3,499) → catalog-only + graceful. tsc green. **Queued next:** PR 2 = wire the Setnayan AI buy surface + seed the missing catalog rows (Save-the-Date, Patiktok per-day, Panood add-ons, LED) + bundle re-resolution; PR 3 = admin can edit vendor subs/token-packs/fees; PR 4 = payment activation repair (dead `event_software_activations_v2` path for LIVE_WALL/MEDIA_PACK · revoke-on-reject · promote-guard). **Owner action:** the AI paywall only charges once `SETNAYAN_AI_PAYWALL_ENABLED=true` is set in Vercel (after PR 2 ships the buy surface). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-14 — feat(website): seat-reservation delta — "your place is reserved" + couple reserved→seated view (PR D of the couple-website 4-path program).** Owner locked RSVP = seat reservation + attendance confirmation, "holds a place; couple seats them" (no guest seat-picking / conflicts / capacity gate). Small delta: `app/[slug]/page.tsx` RsvpWidget shows a "Your place is reserved" line when the guest is attending (server-render); `app/dashboard/[eventId]/seating/page.tsx` gains a reserved → seated summary strip (Reserved confirmed-attending · Seated in-a-chair · To seat the rest, highlighted), derived from data the page already loads — no new query, no migration. tsc + lint + build green. **Stacks on PR C** (branch `claude/couple-reservation`, base `claude/couple-editorial`) — merge B→C→D. SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-14 — feat(website): love story → pre-event paths; Editorial = pure showcase (PR C of the couple-website 4-path program).** Owner decisions: (1) "no show of what they spent" — Editorial recaps *what was used* (gallery, vendor credits, "used at this wedding", thank-you), no money (it already showed none); (2) "love story applies to save the date, rsvp and event" — moved OFF the post-event Editorial onto the run-up. New `app/[slug]/_components/our-story.tsx` (`OurStory`, self-contained PURE composer weaving only the timeless beats — how-met/proposal/spark + milestone timeline; NOT the editorial's past-tense composeLede; teaser for STD, full for RSVP+Event); reads existing `events.love_story` (no migration). `page.tsx` renders `<OurStory>` in both run-up bodies (only render in rsvp/event phases) + threads the teaser into `SaveTheDateView`. `editorial-content.tsx` stripped of the love narrative (lede prose + "The Story So Far" timeline + unused `Timeline`), keeps the thank-you pull-quote; Editorial now leads masthead + cover photo → showcase. tsc + lint + build green. **Stacks on PR B** (branch `claude/couple-editorial`, base `claude/couple-palette`) — review-then-merge after PR B. Resolves the two open product calls. SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-14 — feat(website): mood-board palette skins the couple site (PR B of the couple-website 4-path program).** Owner: "RSVP adapts to the mood board palette" + "editorial also based on their moodboard." The palette already lived in `events.role_palette` (iteration 0010 mood board) but was never applied to the public `/[slug]` site. New `lib/site-palette.ts` (`buildSitePaletteVars`, unit-tested) maps the decorative palette → UI roles with a **WCAG-AA contrast floor** (accent = boldest color darkened to read on the page; CTA = deep color darkened for light button text; paper = near-white tint or safe default; ink = safe obsidian). Because the couple-site Tailwind tokens (`cream`/`ink`/`terracotta`/`mulberry`+`-600/-700`) are `rgb(var(--color-*))`-backed, theming = overriding those vars on `InvitationShell`'s `<main>` — re-skins the whole site (all 4 phases · STD/RSVP/Event/Editorial) with **no component refactor** and zero dashboard blast radius. Graceful fallback to today's look when there's no mood board. tsc + lint + build + unit test green. PR pending (branch `claude/couple-palette`) — review-then-merge. Follow-up: PR C Editorial content alignment (love story → Event; public/private spend; photo scrims). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG. (PR A — Save the Date + lifecycle-on — merged as #1419.)
**2026-06-14 — feat(homepage): post-hero "Tap to learn more" → "A Place for Each" what-you-get; "What you get" removed from the top nav.** After the hero scrub's "Set na 'yan" end-card the homepage now LOCKS — the visitor can end there or **Tap to learn more ↓**, revealing the what-you-get narrative below the hero. New `PostHeroReveal` (client gate: content renders expanded server-side for SEO/no-JS, collapses on mount so the hero end IS the page bottom = natural lock; fixed pill fades in at the end; tap expands + smooth-scrolls; reduced-motion stays open) + new `WhatYouGet` (the "A Place for Each" 5-beat narrative — how Setnayan helps you, create+run free, soft paid upsell, **price-free by design**) + homepage gains a footer it lacked. "What you get" → /features dropped from the shared nav (desktop+mobile); /features route untouched (dup-content de-dup deferred). tsc + lint (retired/botnav/email-links) + production build all green. PR pending (branch `claude/post-hero-learn-more`, auto-merge). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG (iteration 0015).

**2026-06-14 — feat(website): 4-path couple website — add Save the Date + lifecycle ON for weddings (PR A of the couple-website 4-path program).** Owner reframed the couple wedding website as four named, time-gated paths on the single `/[slug]` page: **Save the Date → RSVP → Event → Editorial**. The phase engine already existed (`rsvp|event|editorial`) but was flag-dark (`WEBSITE_PHASES_ENABLED` off) with no Save the Date. This PR: adds `save_the_date` to `LifecyclePhase` + `STD_THRESHOLD_DAYS=90` (far-future weddings → STD, ≤90d → rsvp) in `lib/invitation-widgets.ts`; new minimal STD view (`app/[slug]/_components/save-the-date.tsx` — monogram/name + date + countdown + add-to-calendar via new pure `lib/calendar-links.ts`); **turns the lifecycle ON for weddings** in `app/[slug]/page.tsx` (`phasesEnabled || event_type==='wedding'`; surface is already wedding-only) + the `save_the_date` body branch in both render paths + `?phase=save_the_date` preview; host-only 4-phase preview chips on the website hub. **Load-bearing flip:** post-event sites now render Editorial, day-of sites the live surface (both already built). tsc + lint + production build all green. PR pending (branch `claude/couple-std-path`) — review-then-merge, verify the four `?phase=` states on the Vercel preview. Follow-ups: PR B mood-board palette → site theming; PR C Editorial alignment (love story → Event, public/private spend). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-13 — refactor(entitlements): shared couple-SKU ownership reader + per-SKU activation dispatcher (seat-finding PR 3 of 6 · paid-layer hardening).** New `lib/entitlements.ts` `checkOrderOwnership(eventId, serviceKey)` — ONE refund-aware, graceful-degrade ownership read replacing 5 copy-pasted `eventOwns*` bodies + 3 inline `CUSTOM_QR_GUEST` gates; new `lib/sku-activation.ts` per-SKU activation dispatcher (extensible map, default no-op) replacing 3 hardcoded `approvePayment` branches (sets up PR 4's seat-pass). **NO migration** (activation state IS `orders.status`); behavior-preserving (99/99 tests incl. new 11-case `entitlements.test.ts`). Built fresh-context + adversarially verified (all PASS). PR [#1373](https://github.com/iscasasola/setnayan-platform/pull/1373), auto-merge armed. Follow-up: ~4 more inline ownership gates undercounted (`find-my-table` INDOOR_BLUEPRINT is next). SPEC IMPACT → CHANGELOG + corpus DECISION_LOG.

**2026-06-13 — refactor(seating): cocktail/waiting area moves into the SAME blueprint (PR A of 3).** Owner adjustment: instead of the same-morning "Areas & booths" multi-area pin overlay, the cocktail/waiting area becomes ONE booths-only room drawn on the existing seat-plan canvas (just outside the reception), size-customizable, with booth + stylist vendors able to arrange it (write access lands PR C). PR A = schema + consolidation: migration `20261218000000` (cocktail_* columns on `event_floor_plan`; `zone`+`event_vendor_id`+coordinator RLS on `event_floor_booths`; folds + DROPs `event_floor_objects`/`event_floor_areas`; `get_vendor_seat_plan` v3), deletes the `/seating/areas` route, reworks the vendor seat-plan viewer. **Migration needs prod apply.** Next: PR B couple editor (cocktail room element + resize + table-blocked-inside), PR C vendor editor + write RPCs. SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.
**2026-06-14 — feat(monogram): chosen lockup now renders EVERYWHERE — PDFs + social/OG card + animated hero (items 2+3 of "do all 3").** Completes the monogram-consistency program (after the chip #1374/#1376 + hero/QR #1402). Built via a 3-agent parallel workflow, then adversarially reviewed (3 reviewers caught real bugs the agents' local checks missed — all fixed). (1) **PDFs** (seating + mood-board concept): new `lib/lockup-pdf.ts` draws the lockup via opentype.js glyph-paths + pdf-lib drawSvgPath (no fontkit dep); review fixes = deriveMonogram label, baseline-pivoted italic shear, ∞ borderWidth (was 4× thin); fonts traced into `.next/standalone` via `outputFileTracingIncludes`; rasterized + visually confirmed all 4 styles. (2) **Social/OG card**: lockup as a native satori element tree (bundled fonts, not sharp substitution); review fix = padded duo's viewBox so glyphs fit the ring. (3) **Animation**: 6 motion signatures rebuilt on the lockup (SSR-safe + reduced-motion), threaded through all 4 callers — the paid public-hero animation now animates the MARK, and the Maker live preview is now WYSIWYG. typecheck+lint+build ✅; social/anim verify on Vercel preview. **Flagged:** no Playfair static TTF in-repo → `duo` substitutes Bodoni (PDF)/Cardo (card); add a Playfair TTF for exact parity. **PR pending (branch `claude/monogram-lockup-everywhere`, auto-merge to arm).** SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(realstories): real-editorial photo OG card + share on `/[slug]` (PR C, couple half).** Makes a REAL couple's own editorial shareable with a photo-based FB/Pinterest card (the consent toggle already existed). `realstory-card.tsx` gains the **photo-background variant** (hero photo smart-cropped 1.91:1 + gradient scrim + white serif names; branded-card fallback) — both variants rendered + eyeballed locally. NEW `app/api/og/realstory-slug/[slug]/route.ts` (renders only when `data.published`, else 302→brand card; consent gates the index, not a couple's own page). `EditorialData` gains `slug` (null for the sample → no double share bar); `editorial-content.tsx` self-renders `<ShareButtons>` for real editorials; `app/[slug]/page.tsx` sets `og:image`+`summary_large_image`. tsc+lint clean. **PR pending (branch `claude/realstories-loop`, auto-merge to arm).** **Vendor half of PR C** ("Featured in Real Stories" + "Share to your Page") still outstanding — its background agent hit a transient rate-limit before opening a PR; to re-run. SPEC IMPACT → CHANGELOG + DECISION_LOG.
**2026-06-14 — feat(realstories): vendor "Featured in Real Stories" + Share to Page (PR C, vendor half — CLOSES the loop).** The last piece. NEW `lib/realstories-vendor.ts` (`loadVendorFeaturedStories` — the vendor's OWN booked events ∩ the RA 10173 public-showcase gate, self-contained, no showcase-db ripple) + NEW `app/vendor-dashboard/real-stories/page.tsx` (per-wedding card → couple's `/[slug]` editorial + reused `<ShareButtons>` with the `/api/og/realstory-slug/[slug]` photo card; vendor-auth-gated; pre-launch empty state) + "Real Stories" nav item in the vendor Grow group. Read-only, no migration. tsc+lint clean. **PR pending (branch `claude/realstories-vendor`, auto-merge to arm).** Closes the full loop: couple publish → admin curate → couple share → **vendor share**. The whole Real-Stories program (A rename · B share cards · D admin curate · C couple+vendor) is now built. SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(realstories): real-editorial photo OG card + share on `/[slug]` (PR C, couple half).** Makes a REAL couple's own editorial shareable with a photo-based FB/Pinterest card (the consent toggle already existed). `realstory-card.tsx` gains the **photo-background variant** (hero photo smart-cropped 1.91:1 + gradient scrim + white serif names; branded-card fallback) — both variants rendered + eyeballed locally. NEW `app/api/og/realstory-slug/[slug]/route.ts` (renders only when `data.published`, else 302→brand card; consent gates the index, not a couple's own page). `EditorialData` gains `slug` (null for the sample → no double share bar); `editorial-content.tsx` self-renders `<ShareButtons>` for real editorials; `app/[slug]/page.tsx` sets `og:image`+`summary_large_image`. tsc+lint clean. **PR [#1405](https://github.com/iscasasola/setnayan-platform/pull/1405) MERGED.** SPEC IMPACT → CHANGELOG + DECISION_LOG. Makes a REAL couple's own editorial shareable with a photo-based FB/Pinterest card (the consent toggle already existed). `realstory-card.tsx` gains the **photo-background variant** (hero photo smart-cropped 1.91:1 + gradient scrim + white serif names; branded-card fallback) — both variants rendered + eyeballed locally. NEW `app/api/og/realstory-slug/[slug]/route.ts` (renders only when `data.published`, else 302→brand card; consent gates the index, not a couple's own page). `EditorialData` gains `slug` (null for the sample → no double share bar); `editorial-content.tsx` self-renders `<ShareButtons>` for real editorials; `app/[slug]/page.tsx` sets `og:image`+`summary_large_image`. tsc+lint clean. **PR pending (branch `claude/realstories-loop`, auto-merge to arm).** **Vendor half of PR C** ("Featured in Real Stories" + "Share to your Page") still outstanding — its background agent hit a transient rate-limit before opening a PR; to re-run. SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(realstories): Facebook-shareable editorials — OG card + share buttons (PR B of the Real-Stories featuring program).** Answers the owner's "will it preview on facebook / how / so the exact editorial is viewable than just a link." The editorials had `og:title`/`og:description` but **no `og:image`** (= bare-link shares); added: NEW `lib/social/realstory-card.tsx` (1200×630 satori+sharp branded share card — gold mat · eyebrow · couple names · venue · palette · wordmark; `heroPhotoUrl` reserved for the real-editorial photo variant), NEW `app/api/og/realstory/[slug]/route.ts` (public, Node, cached; 302→`/brand/og-card.webp` on error), NEW `<ShareButtons>` (Facebook · Pinterest · Copy link — reusable), and wired `og:image`+`summary_large_image`+the share bar into `app/realstories/[slug]/page.tsx`. A shared Real Story now shows the couple/palette/venue and deep-links to that exact editorial. tsc+lint clean (satori/sharp = known local-install gap, in CI; build compiled ✓ on Vercel — a later run OOM'd in the type-check phase, infra not code); card visual to verify on a green preview. **PR [#1400](https://github.com/iscasasola/setnayan-platform/pull/1400), auto-merge armed.** Built in parallel with PR D (admin curate). Next: PR C = couple "Publish to Real Stories" consent + "Share our story" + vendor "Featured / Share to your Page" (reuses `<ShareButtons>` + the card + adds the photo-background variant). SPEC IMPACT → CHANGELOG + DECISION_LOG. Answers the owner's "will it preview on facebook / how / so the exact editorial is viewable than just a link." The editorials had `og:title`/`og:description` but **no `og:image`** (= bare-link shares); added: NEW `lib/social/realstory-card.tsx` (1200×630 satori+sharp branded share card — gold mat · eyebrow · couple names · venue · palette · wordmark; `heroPhotoUrl` reserved for the real-editorial photo variant), NEW `app/api/og/realstory/[slug]/route.ts` (public, Node, cached; 302→`/brand/og-card.webp` on error), NEW `<ShareButtons>` (Facebook · Pinterest · Copy link — reusable), and wired `og:image`+`summary_large_image`+the share bar into `app/realstories/[slug]/page.tsx`. A shared Real Story now shows the couple/palette/venue and deep-links to that exact editorial. tsc+lint clean (satori/sharp = known local-install gap, in CI; build compiled ✓ on Vercel — a later run OOM'd in the type-check phase, infra not code); card visual to verify on a green preview. **PR [#1400](https://github.com/iscasasola/setnayan-platform/pull/1400), auto-merge armed.** Built in parallel with PR D (admin curate). Next: PR C = couple "Publish to Real Stories" consent + "Share our story" + vendor "Featured / Share to your Page" (reuses `<ShareButtons>` + the card + adds the photo-background variant). SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — refactor(explore): the public marketplace URL is now `/explore` (was `/vendors`).** Owner directive ("fix the address to /explore"). `git mv apps/web/app/vendors → apps/web/app/explore` (25 files; no route-handlers/dynamic segments → no shape change) + 115+ internal marketplace-link rewrites + 3 external import repoints, driven by an exhaustive bucketed audit (discovery + adversarial-verify multi-agent passes) so the keeps stayed put (`/dashboard/[id]/vendors`, `/admin/vendors`, `/api/v1/vendors`, `@/lib/vendors*`, `/v/[slug]` all unchanged). **308 permanent redirects** in `middleware.ts` (`/vendors`→`/explore`, `/vendors/categories`→`/explore/categories`, query preserved; the old `/vendors/compare` orphan-guard folded in → `/explore?notice=compare_v1_2`); canonical/OpenGraph/JSON-LD/robots/sitemap-static repointed; `sitemap-vendors.xml` (`/v/` profiles) left untouched. Drive-by fix: `editorial-content.tsx` linked featured vendors to the non-existent `/vendors/[slug]` (404) → corrected to `/v/[slug]`. tsc + lint + build (214/214: `/explore`+subroutes present, no `/vendors` route, keeps intact) + guards clean. Folded into the same PR as the search-first work (branch `claude/vendors-search-only`). SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(explore): bare `/vendors` = search bar only + unified multi-field search.** Owner follow-up to #1382 ("why do i see services here? we just want a search bar … category + vendor + service + place + details, combinable, show all that matches"). #1382 added the search hero but kept the full category catalog rendering *below* it — so services still showed on a bare visit. This finishes it: new `browseMode` flag gates the catalog (icon strip + venue pickers + folder grids) so it renders ONLY on an explicit browse/context visit (`?browse=1`/`?folder=`/`?tile=`/`?from=plan`); a bare Explore landing shows the hero + live "Popular searches" chips (top categories by vendor count) + a "Browse all categories →" link — **demoted, not deleted**. The free-text `q` became a unified multi-field search (`applyMarketplaceTextSearch`): each token matches vendor name, tagline, city, OR a listed service, intersected across tokens ("photographer tagaytay" = photographers in Tagaytay only). No migration. Also fixed: the vendor-grid `StickyMarketplaceHeader` search input had no form, so free-text Enter was a no-op — now wrapped in a GET form so search works on the results page too. tsc + lint + build (214/214) + all guards clean. PR pending (branch `claude/vendors-search-only`, auto-merge to arm). SPEC IMPACT → CHANGELOG + DECISION_LOG. Owner heads-up: venue browsing (Reception facet picker + ceremony-venue cards) now reaches couples via search / "Browse all categories", not on landing.
**2026-06-14 — feat(chrome): chosen monogram lockup now renders on the public hero + QR centers (consistency follow-up).** An audit of every monogram render site (after the switcher-chip fix in #1374/#1376) found the two highest-impact couple-facing surfaces still drew plain initials. Fixed both: (1) the public landing hero (`app/[slug]`) — 3 cascades centralized into new `HeroMonogram` (`app/_components/hero-monogram.tsx`) that draws the chosen lockup (MonogramMark @80px) + framed branch, keeping bespoke + paid-animation + legacy fallbacks; added the 3 design columns to the local `EventRow` type. (2) QR-code centers (`monogramOverlaySvg`) — now draw the chosen lockup inside the cream clearance via a string-SVG twin of MonogramMark (`lockupMarkSvg`); `MonogramConfig` carries the design via `resolveMonogram`; threaded the 3 columns into 5 QR-caller selects; `splitInitials` promoted to `lib/monogram.ts`. Deferred (flagged): animated-hero-as-lockup, PDF/print, maker preview, social card. tsc + lint clean; verified real composited QRs for all styles. SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(realstories): admin curate/feature + hero slot (PR D of the Real Stories featuring program).** New Setnayan HQ surface (`/admin/real-stories`) to FEATURE (pin) + ORDER which published, consent-gated wedding editorials surface on `/realstories`, and which fills the hero. Migration `20261221000000_realstories_featuring.sql` (additive + idempotent · **NOT auto-applied** — owner runs `supabase db push --db-url "$SUPABASE_DB_URL"`) adds two nullable columns to `events` (`showcase_featured_at` + `showcase_feature_rank`) + a partial index — no new table; RLS unchanged (existing `couple_can_update_event` already has `OR is_admin()`, public read uses the admin client). `lib/showcase-db.ts`: `loadPublishedShowcases` orders featured-first + surfaces a `featured` flag (graceful pre-migration fallback to newest-first), + new admin loader `loadShowcaseCandidatesForAdmin`. `app/realstories/page.tsx` renders the leading featured real showcase as a hero (`RealHero`), keeping the labelled sample fallback when there are zero real showcases. Two server actions re-assert `requireAdmin` + the RA 10173 eligibility gate, audit to `admin_audit_log`, and `revalidatePath('/realstories')`. Empty/migration/sample states all honest; sample stays non-featurable. Nav entry added to admin Platform group + `/admin/more`. Did NOT touch PR B's files (`realstories/[slug]`, `realstories/_components/*`, `lib/social/*`, `api/og/*`). tsc (zero errors in my files) + lint clean. **PR pending (branch `claude/realstories-admin-curate`, auto-merge armed). Depends on nothing from B/C.** SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(realstories): rename `/weddings` → `/realstories` (PR A of the Real-Stories featuring program).** Owner: "rename it as /realstories" + wants the showcase turned into a Facebook-shareable featuring loop. The Real Weddings showcase (iteration 0046, seeded with the Maria & Juan sample) is renamed: `git mv app/weddings→app/realstories`, every `/weddings` route ref → `/realstories` across 14 files (route pages · sitemaps · robots · real-weddings/showcase-db · inbound links on privacy/signup/vendors/website-privacy), + **301 redirects** `/weddings`→`/realstories` in `next.config.ts` (preserves indexed ranking + keeps the in-flight nav PR #1391's `/weddings` links alive). SEO copy/keywords unchanged (route-only). tsc + lint clean. **PR pending (branch `claude/realstories-shareable`, auto-merge to arm).** Sequenced next: PR B = per-editorial OG share cards (satori) + FB/Pinterest/Copy share buttons (a shared link shows a beautiful preview, deep-links to the exact story); PR C = couple "Publish to Real Stories" consent + vendor "share to your Page"; PR D = admin curate/feature. SPEC IMPACT → CHANGELOG + DECISION_LOG.
**2026-06-13 — feat(seating): vendor cocktail-area editor + write RPCs (PR 2 of 2 · cocktail-area feature COMPLETE).** The cocktail/waiting-area feature's vendor half — the **first time a vendor WRITES to a couple's planning blueprint** (scoped to the cocktail area + its booths; never reception seating or guest PII). Migration `20261221120000` (additive, **APPLIED TO PROD**, ledgered; **renamed from a colliding `20261219000000`** after a parallel session shipped walkthrough-zones at that stamp): SECURITY DEFINER RPCs gated by `_cocktail_vendor_caps` (booked + eligible category + the couple's `cocktail_vendor_edit` revoke switch). Two tiers — ARRANGE (decor/florist/coordinator/lights = size room + any booth) vs BOOTH (photobooth/bar/cake + performers = own booth only). New `/vendor-dashboard/clients/[eventId]/cocktail` lean drag editor (reception read-only context, optimistic + rollback) + a "Cocktail area" card on the vendor brief when eligible. tsc clean. SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.
**2026-06-14 — feat(seating): cocktail arrival zone (entrance dock + Front Desk booth + wayfinding signs).** Three owner-directed additions to the cocktail/waiting area. (1) **Entrance dock** — `cocktail_linked` (default on) docks the room beside the reception at the entrance door with a drawn doorway connector; connect⇄separate toggle; dragging auto-unlinks; cocktail clamp widened to [-80,180] across couple + vendor + booth write paths; `fitView` frames the off-wall room + auto-fits on enable. (2) **Front Desk** — a `registration_desk` booth auto-seeds into the room (idempotent). (3) **Wayfinding signs** — new `event_floor_signs` (rotatable arrow + label, default Restrooms, ≤24); couple replace-all `saveSigns`; ARRANGE-tier vendor CRUD RPCs (booth-tier rejected); editor drag/rotate/rename/delete + "Add sign". Migration `20261222000000` (additive, **APPLIED TO PROD 2026-06-14**, ledgered): `cocktail_linked`, `registration_desk` CHECK, `event_floor_signs`+RLS, `get_vendor_cocktail_editor` v2, widened clamp + 3 sign RPCs. Designed + adversarially reviewed via workflows (review caught a vendor dock-corruption bug → fixed). tsc clean. **Stacks on PR [#1381](https://github.com/iscasasola/setnayan-platform/pull/1381) — merge that first.** Deferred follow-up: signs/Front Desk → publish print-pack + day-of guest map (authoring only for now). SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.

**2026-06-13 — feat(seating): vendor cocktail-area editor + write RPCs (PR 2 of 2 · cocktail-area feature COMPLETE).** The cocktail/waiting-area feature's vendor half — the **first time a vendor WRITES to a couple's planning blueprint** (scoped to the cocktail area + its booths; never reception seating or guest PII). Migration `20261221120000` (additive, **APPLIED TO PROD**, ledgered; **renamed from a colliding `20261219000000`** after a parallel session shipped walkthrough-zones at that stamp): SECURITY DEFINER RPCs gated by `_cocktail_vendor_caps` (booked + eligible category + the couple's `cocktail_vendor_edit` revoke switch). Two tiers — ARRANGE (decor/florist/coordinator/lights = size room + any booth) vs BOOTH (photobooth/bar/cake + performers = own booth only). New `/vendor-dashboard/clients/[eventId]/cocktail` lean drag editor (reception read-only context, optimistic + rollback) + a "Cocktail area" card on the vendor brief when eligible. tsc clean. SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.

**2026-06-13 — refactor(seating): cocktail/waiting area moves into the SAME blueprint (PR 1 of 2).** Owner adjustment: instead of the same-morning "Areas & booths" multi-area pin overlay, the cocktail/waiting area becomes ONE booths-only room drawn on the existing seat-plan canvas (just outside the reception), size-customizable, with booth + stylist vendors able to arrange it (vendor write access lands PR 2). PR 1 (schema + couple editor, [#1375](https://github.com/iscasasola/setnayan-platform/pull/1375) merged the schema/viewer half, [#1378](https://github.com/iscasasola/setnayan-platform/pull/1378) the couple editor after auto-merge fired early): migration `20261218000000` (cocktail_* columns on `event_floor_plan`; `zone`+`event_vendor_id`+coordinator RLS on `event_floor_booths`; `get_vendor_seat_plan` v3) — **APPLIED TO PROD 2026-06-13** (additive, statement-by-statement, ledger recorded); couple editor "Cocktail area" toolbar button → resizable/draggable room, tables blocked, booths inside; deletes the `/seating/areas` route, reworks the vendor seat-plan viewer. The superseded `event_floor_objects`/`event_floor_areas` tables are orphaned (2 test-event rows each); their **fold+DROP is a deferred owner-gated cleanup** (one-way). SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.

**2026-06-14 — feat(seating): zone walkthrough video (seat-finding PR 6 of 6 · CLOSES the 6-PR program).** The first-person "walk to your table" the market ships nowhere. A coordinator (or a no-coordinator couple's DIY helper) records a short vertical clip from the entrance to a cluster of tables, tags them, and a guest who finds their seat watches the exact walk. Migration `20261219000000` — new `event_walkthrough_zones` (R2 clip + publish gate) + `event_tables.walkthrough_zone_id` (ON DELETE SET NULL); RLS = couple + seat_plan-edit delegate. `public_seat_lookup` gains the published zone clip (LEFT JOIN; all guards preserved); the route presigns it; `/find-seat` shows a lazy "Watch the walk" player. New `/seating/walkthrough` manager (create/tag/upload/publish via shared `<FileUpload>`), linked from the seating header. **Built ungated/free** (coordinator LABOR, delegatable — dual-path parity); whether the tool is monetized = owner's holistic pricing pass. tsc + lint + build + 92 tests + migration guard clean. PR pending (branch `claude/zone-walkthrough-video`, auto-merge to arm); migration applied to prod. SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-14 — feat(seating): live day-of seat-plan propagation (seat-finding PR 5 of 6).** The win over frozen paper — a reseat done during the event reaches guests on its own, **no push/email** (silent-only lock). The data path was already live (finder surfaces are `force-dynamic`); this adds the day-of UX. **No migration.** New `isEventDayActive()` (`live||post` = whole wedding day, so evening receptions count) + `useDayOfLiveTick` (45s + focus + visibility PULL, inert off-day) + `<LiveRefresher>` (router.refresh on `/find-my-table` + check-in desk) + free-finder `NameSearch` self-re-fires its last lookup + a day-of editor banner ("changes reach guests now; printed cards are frozen — the live plan is the source of truth"). tsc + lint + build + 92 tests clean. PR pending (branch `claude/seat-live-propagation`, auto-merge to arm). Open (owner): PR 4 (paid seat pass) still unbuilt — inherits this pattern. SPEC IMPACT → CHANGELOG + DECISION_LOG.
**2026-06-14 — feat(nav): pill-segmented dashboard menus (shared `.sn-seg` template).** Owner "make each menu a pill, not rectangles" — extended the floating-pill bottom-nav language to the in-page section menus. New shared `.sn-seg` / `.sn-seg-item` CSS template (rounded pill track + raised white active pill); converted Services tabs (desktop strip + mobile sticky nav), the Guests carousel menu, the Schedule toggle, and the Guest view-switcher to it (style only — all logic preserved). Built + adversarially verified via workflow (4/4 pass). Verified tsc + lint + guard + build. Memory + DECISION_LOG updated.

**2026-06-14 — feat(explore): search-first reframe of the public `/vendors` (Explore) landing.** Closes follow-up (1) from the 2026-06-13 nav simplify (#1377). The public Explore landing now LEADS with a clean search-first hero (new `explore-search-hero.tsx` on `--m-*` marketing tokens: eyebrow + headline + subhead + one big universal search box + 6 quick-search chips) instead of the pinned `StickyMarketplaceHeader`; the rich category browse (icon strip + folder grids) stays below as breadth. `TaxonomySearch` gained a `variant="hero"`; the icon strip re-pins at `top-0`; catalog-mode FilterDrawer + its faith-option computation retired (filters reappear in vendor-grid mode). No schema/migration — UX reframe only. Flagged to owner: (a) Explore LANDING loses its Filters drawer (deliberate simplify); (b) `setnayan_*` services aren't chipped because `?category=setnayan_papic` is an empty grid on prod (no visible first-party listing yet) → need provisioning to appear in search RESULTS, not just catalog tiles. **PR [#1382](https://github.com/iscasasola/setnayan-platform/pull/1382), auto-merge armed.** Open: follow-up (2) "Real Stories" (`/blog`) editorials-vs-guides direction still an owner decision. SPEC IMPACT → CHANGELOG + DECISION_LOG.
**2026-06-13 — feat(nav): floating-pill bar + consistent pill (finalize the ONE template).** Two owner directives in the single shared `bottom-nav.tsx`: (1) the bar is now a **floating pill** (`left/right-14px`, floating 12px above the safe-area, `rounded-full` + soft shadow) instead of an edge-to-edge bottom strip — that mismatch is why the real app never matched the demos; (2) the active pill is a **consistent ~52px centered capsule** (was cell-filling, so admin's 4 wide tabs made a wider pill than the 5-tab consoles). Tabs still distribute evenly for tap targets. All consoles now render an identical floating-pill nav — same shape/pill/colours/motion, only the role tabs differ. Nothing hardcoded remained to remove (legacy bottom-nav already gone; guard enforces delegation). Verified tsc + lint + guard. Memory + DECISION_LOG updated.

**2026-06-13 — refactor(seating): cocktail/waiting area moves into the SAME blueprint (PR A of 3).** Owner adjustment: instead of the same-morning "Areas & booths" multi-area pin overlay, the cocktail/waiting area becomes ONE booths-only room drawn on the existing seat-plan canvas (just outside the reception), size-customizable, with booth + stylist vendors able to arrange it (write access lands PR C). PR A = additive schema + consolidation: migration `20261218000000` (cocktail_* columns on `event_floor_plan`; `zone`+`event_vendor_id`+coordinator RLS on `event_floor_booths`; `get_vendor_seat_plan` v3) — **APPLIED TO PROD 2026-06-13** (statement-by-statement, ledger recorded); deletes the `/seating/areas` route, reworks the vendor seat-plan viewer. The superseded `event_floor_objects`/`event_floor_areas` tables are orphaned (2 test-event rows each); their **fold+DROP is a deferred owner-gated cleanup** (one-way). Next: PR B couple editor (cocktail room element + resize + table-blocked-inside), PR C vendor editor + write RPCs. SPEC IMPACT → CHANGELOG + DECISION_LOG + 0008 header.

**2026-06-13 — feat(onboarding+chrome): desktop onboarding canvas + the saved monogram IS the event logo.** Two owner asks. (1) **Desktop version** for `/onboarding/wedding`: it was a 430×880 phone card stranded on stone; owner picked "enrich the canvas, keep the frame", so the locked `.onbw>.phone` prototype is untouched and a new desktop-only (`≥1024px`) editorial aside (`desktop-aside.tsx` + `onboarding-desktop.css`, both scoped under `.onbw`, layered over the prototype mirror) fills the space beside it — brand lockup, serif headline, value line, reassurance ticks, "Set na 'yan." sig; phone pinned to its locked 430px in the flex row; <940px-tall fallback to top-aligned scroll; **mobile/tablet byte-for-byte unchanged**. (2) **Saved monogram → event logo:** the switcher logo used the couple's font+ink but only drew initials; new `monogram-mark.tsx` draws their REAL chosen lockup (bar·duo·script·infinity as mark-only inline SVG, faithful to `mono-lockup.tsx`), `framed`+single-name stay letters-forward (ornate ring illegible at icon size). Threaded `monogram_style` through the switcher + 4 layouts (it was selected nowhere in the chrome → type lockups collapsed to the font). Reverses the 2026-06-03 letters-forward lock for the four type-only lockups. No migration (`monogram_style` exists). tsc + lint clean; faithful SVG preview confirmed legibility 28/36/44px. PR pending (branch `claude/onboarding-desktop-monogram-logo`, auto-merge to arm). SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-13 — feat(seating): FREE guest seat-finder (seat-finding PR 1 of 6).** New public `/[slug]/find-seat` — a guest scans the shared venue QR, types their name, sees their table label (no app/login/SKU). New `SECURITY DEFINER` `public_seat_lookup` RPC (migration `20261215000000`) — **APPLIED TO PROD 2026-06-13** (statement-by-statement via `db query`) — published-gate + `{display_name, table_label}` only + min-len 2 + `LIMIT 25`; `/api/seat-lookup/[slug]` route; `name-search` client; "Find your seat" CTA on the anonymous landing. Delivers the design's FREE tier (wedge vs the ₱8,995-rival); the richer map stays the paid Indoor-Blueprint `find-my-table`. tsc + lint + 92 tests + retired-strings clean. PR [#1365](https://github.com/iscasasola/setnayan-platform/pull/1365). SPEC IMPACT → CHANGELOG + DECISION_LOG.

**2026-06-13 — feat(seating): exclusive seating-editor lock (seat-finding PR 2 of 6 · the live-edit gate).** ONE editor at a time per event (co-owners included → 2nd partner view-only; FIRST coordinator-write enablement, gated behind lock + `can_edit_seating`). New `20261216000000_seating_editor_locks.sql` (table + RLS + acquire/refresh/release/assert RPCs · 30s heartbeat / 90s server stale-takeover) — **APPLIED TO PROD 2026-06-13** (statement-by-statement via `db query`, since `db push` is drift-blocked by missing `20261211000000`); `actions.ts` asserts the lock before all 18 mutations (`publishSeating` exempt); new `use-seating-lock` hook + presence-heartbeat broadcast + editor view-only gating + solo-recovery banner + lost-lock→view-only. Built fresh-context + double adversarially reviewed (build→verify→fix→reverify); tsc+lint+build+89 tests clean; concurrency core sound. **PR [#1370](https://github.com/iscasasola/setnayan-platform/pull/1370), auto-merge armed** (migration applied first). Open: action-layer-vs-RLS + co-owner exclusivity; fast-follows: transient-error vs lock-loss on assert + mount flicker.

**2026-06-13 — feat(nav): global bottom nav everywhere.** Owner directive — the customer bottom nav now shows on every surface, retiring the Guests + Services focus-mode suppressions. Services' section tabs (Summary·Shortlist·Build·Compare·Lock) became a sticky-top header; the Guests carousel's 5-pill menu moved to the top of its sheet (sheet docks above the global nav at the 80px `pb-20` clearance, no double bar, no state-lift); the Website tab now opens a real `/dashboard/[eventId]/website` hub (inside the layout, so it has the nav) with a "Launch editor" CTA into the unchanged full-screen `/site-editor` (whose ✕ returns to the hub). Built + adversarially verified via workflows (review caught a 56→80px nav-clearance bug, fixed). Verified tsc + lint + guard + production build. Corpus DECISION_LOG + memory updated.

**2026-06-13 — fix(nav): bolder bottom-nav (visibility follow-up).** The canonical bottom-nav's first cut was too subtle to read as changed on-device (8% pill, light only on hold). Made it unmistakable: active pill 8%→15% ink, `--bn-glow` 1.2→1.5, and the white press-light is now a one-shot bloom that fires on a normal **tap** (not just hold), re-keyed per press. iOS Reduce Motion still freezes the motion parts; the bolder static pill is the motion-independent signal. Verified tsc + lint + guard. Memory baseline updated.

**2026-06-13 — feat(i18n): Taglish `/features` + shared marketing-i18n foundation (dictionary + thin routes).** Owner picked the **dictionary + thin-routes** architecture; this lands it. New `lib/marketing-i18n.tsx` (`MarketingLocale` + `localeAlternates` hreflang helper + `<LocaleSwitch>`). `/features` (copy was spread across **11 section components** — the case an inline twin can't handle) refactored to dictionary-driven: each section reads `COPY[locale]`; shared `_PageBody` threads `locale`; thin routes `/features` (en) + new **`/tl/features`** (tl) can't structurally drift. Mocks/footer stay shared (illustrative). Fixed stale "EN / TL / CEB" → "EN / TL". `/tl/features` → sitemap-static. Verified tsc + lint + build (both routes identical). Next pages reuse the foundation; `/pricing` + home (live data) are the remaining step.

**2026-06-13 — feat(pax): adaptive pax pricing Phase 8 — couple settings (edit deadline + pricing view).** "Guest list & pricing" card on `/details` lets the couple set their `guest_list_edit_deadline` (overrides the 14d default → controls when finalize happens, decision #6) + pick `adaptive_pricing_mode` realtime/final-only (decision #5's toggle). New `updatePaxSettings` action (host-gated, admin-write, audited) + `PaxSettingsCard`. No migration (Phase 1 columns). Deadline override is honored by `ensureFinalized` immediately; the mode value is now stored + selectable — its final-only DISPLAY effect (hiding the realtime budget projection) is the one remaining follow-up, plus a hard post-deadline edit-guard. tsc + lint + build clean. SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — feat(i18n): Taglish `/how-it-works` — localization rolls to the role-map page.** Second page (after `/tl/about`) in the English + Taglish set: new `/tl/how-it-works` Taglish twin of the six-role map + flow + CTAs, reusing the SAME `getVendorPrices()` source as EN (no price drift — only prose translated). Reciprocal hreflang (en-PH/tl-PH/x-default) + EN↔Taglish switcher on both; `/tl/how-it-works` added to sitemap-static, `/how-it-works` lastmod bumped. **`/pricing` intentionally skipped** (live SKU prices from `platform_retail_catalog_v2` → an inline copy would drift; needs the shared-dictionary approach). Inline-per-page still; shared dictionary = scale-up step (owner). Verified tsc + build (both `ƒ` dynamic).
**2026-06-13 — feat(pax): adaptive pax pricing Phase 7 — auto-finalize at the edit deadline (owner decision #6).** The guest count auto-finalizes at the edit deadline (explicit `guest_list_edit_deadline`, else default 14d before the event); `ensureFinalized()` (lazy, cron-free, race-safe) stamps `guest_count_locked_at` + freezes `final_pax = max(estimated_pax, headcount)`; `resolveLivePax()` returns the frozen count once locked so late RSVPs/claims can't move a booked cost (the binding-price guarantee). Migration `20261213000000` (events.final_pax) applied to prod. Guest page shows a "Guest list finalized" banner when locked. Check-in unaffected (writes guest_checkins, not guests). Deferred fast-follows: decision #5's realtime/final display toggle + budget projection, a settings UI to override the 14d default, and a hard edit-guard (freeze already protects money). tsc + lint + build clean; adversarial-reviewed. SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — refactor(i18n): locales = English + Taglish (no Cebuano).** Per owner: `/tl/about` rewritten in genuine Taglish (English-Tagalog code-switch), not formal Tagalog; switcher relabeled Tagalog→Taglish on /about + /tl/about; `/ceb` dropped from robots. `/tl` route + `tl-PH` hreflang kept (closest standard code for Taglish). Verified tsc + build + retired-strings. Follow-up to #1360.

**2026-06-13 — feat(i18n): Tagalog `/about` — first slice of marketing-site localization.** The public site was EN-only (`lib/i18n` is dashboard-only); shipped `/tl/about` (Tagalog brand/entity page, fully static) with the **locale-subpath** URL strategy + reciprocal **hreflang** (en-PH/tl-PH/x-default) on both /about + /tl/about + an EN↔Tagalog switcher. `/tl`+`/ceb` added to robots; `/tl/about` to sitemap-static. Establishes the pattern CEB + more pages follow (shared dictionary = scale-up step). Verified tsc + build (both static ○). Open: scale-up architecture + CEB + page priority (owner).
**2026-06-13 — feat(pax): adaptive pax pricing Phase 6 — HQ audit; PROGRAM COMPLETE.** Append-only `pax_change_audit` table (migration `20261212000000`, applied to prod; admin-read RLS, no FKs so history outlives bookings) + best-effort audit writes from the accept/decline confirm actions + new read-only `/admin/pax-changes` surface (added to the admin sidebar after Disputes). Closes the 6-phase Adaptive Pax Pricing program: schema → couple meter → inquiry snapshot → vendor rate → surcharge+confirm → HQ audit. Both migrations (`20261211000000`, `20261212000000`) live in prod. tsc + lint + build + migration guard clean. SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — feat(pax): adaptive pax pricing Phase 5 — per-vendor surcharge + in-thread confirm (the money-moving core).** When the couple's guest count moves a booked vendor's cost (vendor set a per-guest rate), an Accept/Decline card appears in the chat thread; `total_cost_php` only moves when the VENDOR confirms (symmetric — a drop shows a credit). `computeAddedPaxSurcharge` (floor+block, owner fallback = no rate→no charge) + `fetchVendorPaxProposals` in `lib/pax.ts`; new `pax-actions.ts` (authoritative server recompute, admin-gated write to the couple's event_vendors); vendor thread recomputes live pax FRESH on view (admin client — vendor RLS can't read guests) + renders the cards; couple thread + budget reflect (total auto-updates on accept). 6 unit tests. No migration (Phase 1 columns). Builds the spec'd-but-unbuilt 0021 §12.3. Phases 1-5 of 6 now shipped; Phase 6 (HQ audit) remains. tsc + lint + 6/6 tests + build clean. SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — feat(pax): adaptive pax pricing Phase 4 — vendor per-added-guest rate input.** Vendors can set an optional "Additional cost per added guest (PHP)" on each service (new + edit forms in `/vendor-dashboard/services`; parsed + stored via create/update actions; `VendorServiceRow`/`BASE_COLS` carry `added_pax_price_php`). Input + storage only — no cost effect yet (applying it = Phase 5). Blank = no extra charge (owner fallback). Self-contained (no `lib/pax.ts` dependency; surcharge math deferred to Phase 5). Column live in prod. tsc + lint + build clean. SPEC IMPACT → DECISION_LOG + memory + 0022 AS-BUILT.

**2026-06-13 — feat(nav): canonical bottom-nav template (owner-locked UNBREAKABLE).** The bottom-nav tap interaction is now THE app-wide template, enforced. Upgraded the ONE shared `app/_components/nav/bottom-nav.tsx` (already mounted by customer/vendor/admin) to: a full stadium pill that **travels on release** (spring + subtle stretch), a diffused **white press-light** that blooms under the finger **on press-down** (fills the pill top-to-bottom, feathers only at the ends), and the pressed icon grows. Four motion knobs as `--bn-*` CSS props on the nav root → retune app-wide from one place (locked: 500ms · grow 1.15 · glow 1.2 · stretch 1.1 · white). New `scripts/lint-bottom-nav.mjs` + CI job make it unbreakable (delegation + template-integrity guard). Lights up all three doorways at once; guest landing has no bottom nav. Verified tsc + lint + guard. Corpus `DECISION_LOG` + memory `project_setnayan_bottom_nav_canonical`.

**2026-06-13 — feat(pax): adaptive pax pricing Phase 3 — snapshot pax on new inquiries.** Inquiries now carry the couple's live pax: both inquiry paths (`unlockCategoryWithInquiry`, `startServiceInquiry`) snapshot `chat_threads.pax_at_inquiry` (once) + push `pax_current` (the count the vendor first quoted against); both thread headers (vendor + couple) show "Planning for ~N · was M at inquiry". New `lib/pax.ts` `resolveLivePax()` = max(estimated_pax floor, sure-attending headcount). First runtime use of the Phase 1 columns — **migration `20261211000000` applied to prod 2026-06-13** (only pending migration, clean apply). Read/snapshot only (cost recompute = phases 4-5). tsc + lint + build clean. SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — feat(weddings): in-dashboard Real Weddings consent toggle (RA 10173).** Couples can opt in/out of public `/weddings` showcase listing anytime from `/dashboard/[eventId]/website/privacy` (was signup-only). New host-gated `setShowcaseConsent` action writes `users.public_summary_consent_at` (NOW/NULL) via admin; the page reads + renders the opt-in/out card. Completes the one-click opt-out guardrail for the #1353 consent-gated browse. Verified tsc + build.

**2026-06-13 — feat(weddings): DB-driven Real Weddings browse (consent-gated) — real editorials auto-replace the sample.** New `lib/showcase-db.ts` `loadPublishedShowcases()` (admin-client, consent-gated: `event_type='wedding'` + public slug + T+30d grace + a couple member's `users.public_summary_consent_at` — the RA 10173 gate). `/weddings` index + `sitemap-weddings.xml` now show real published editorials (each linking to the couple's canonical `/[slug]`) when any exist, else the sample. Verified against prod (join clean, 0 rows today → sample shows). The end-to-end auto-yield is now built; first real wedding = the founder's Dec 2026. Verified tsc + build.
**2026-06-13 — feat(pax): adaptive pax pricing Phase 2 — couple guest-list pax meter.** Pax-target meter on the guest list (desktop `SummaryStrip` + mobile carousel Summary panel) showing sure-attending progress toward `events.estimated_pax` (the minimum-pax floor): "Guest target · N of M · X%", flipping to "Now planning for N · K over your M minimum" once attendance tops the floor. New pure helpers `computePaxProgress`/`headcountForBasis` in `lib/guests.ts` + 8-test unit suite. Read-only, couple-facing; independent of the Phase 1 migration (pure compute on existing data) so no prod change needed. tsc + lint + 8/8 tests + build clean. Part of the Adaptive Pax Pricing program (Phase 1 schema = PR #1352; phases 3-6 queued). SPEC IMPACT → DECISION_LOG + memory.
**2026-06-13 — feat(pax): adaptive pax pricing — Phase 1 schema (no behavior change).** Owner feature threading one guest count from the guest list → vendor inquiries → per-vendor pricing → budget; designed + locked 2026-06-13 (6 decisions, `DECISION_LOG.md` "Adaptive Pax Pricing"). Model: `events.estimated_pax` = the minimum-pax floor; `live_pax = max(floor, sure-attending headcount)`; past the floor vendors get the count + couple sees "prices may change"; vendor sets an optional per-added-guest rate (blank = none); count fully live but confirmed cost changes go through a symmetric vendor confirm; couple view realtime-by-default; auto-finalize at the guest-list edit deadline. Migration `20261211000000` adds NULL-safe columns to `events` / `vendor_services` / `chat_threads` / `event_vendors` — ⚠ NOT yet applied to prod (apply when Phase 2 reads them). KEY: the vendor per-pax model (0022 §2.2) + cost-delta flow (0021 §12.3) were spec'd-but-unbuilt, so we're building the spec. Phases 2-6 (couple meter · inquiry snapshot · vendor field · push+recompute · HQ audit) queued. tsc/lint/build unaffected (no TS). SPEC IMPACT → DECISION_LOG + memory.

**2026-06-13 — feat(weddings): sample is now a fallback (real weddings auto-replace it).** Per owner ("our sample until a real wedding is uploaded"), the `/weddings` index + `sitemap-weddings.xml` filter `!isSample` and fall back to the sample only when no real weddings exist — so a real wedding (a non-sample in-code entry, or the deferred DB-driven Phase-4 browse) auto-drops the sample. Conditional intro copy. No change today (only the sample exists). Verified tsc + build.

**2026-06-13 — refactor(weddings): Real Weddings sample now renders the real editorial (follows it).** Per owner ("the sample needs to follow the editorial"), `/weddings/[slug]` no longer uses a bespoke layout — it renders through the SAME `EditorialContent` as a real wedding's Phase-4 editorial, fed a curated Maria & Juan `sampleEditorialData()` fixture via a `loadEditorialData` sentinel (no DB at build). Future editorial changes update the sample automatically; no parallel layout to drift. Real consent-gated editorials unchanged (real ids fall through to the DB loader). Verified tsc + build (sample prerendered ●). Supersedes the bespoke detail from #1349.

**2026-06-13 — feat(weddings): Real Weddings showcase live with a sample (0046 first slice).** `/weddings` (was a "coming December 2026" empty state) now renders a populated showcase + a new `/weddings/[slug]` page, mirroring the `/blog` SSG pattern (in-code constants, no DB, `dynamicParams=false`). Seeded with ONE clearly-labelled SAMPLE (Maria & Juan · Catholic garden · Tagaytay — `isSample`, visible "Sample showcase" label, no real PII, no fabricated vendor names → team links to /vendors categories). `sitemap-weddings.xml` emits hub+sample (was empty); `/weddings` moved out of `sitemap-static` to avoid a dup; added to robots allow-list; `llms.txt` updated. The real consent-gated DB editorials (0046/0002 Phase 4) are unchanged and merge in from December 2026. Verified tsc + build. SPEC IMPACT → CHANGELOG + 0046 header + DECISION_LOG.

**2026-06-13 — feat(blog): Setnayan Journal expanded to 9 articles.** Follow-up to the #1344 first slice: +5 PH-anchored long-tail posts (budget breakdown · marriage-licence step-by-step · venue selection · Catholic-wedding requirements · Filipino traditions) into `lib/blog.ts` (4 → 9). Same engine — no schema, no route change; auto-picks-up the sitemap + JSON-LD + filters. Verified tsc + production build (all 9 prerendered ●).

**2026-06-13 — feat(blog): Setnayan Journal (`/blog`) — iteration 0038 first slice SHIPPED.** New public editorial surface: `/blog` index + `/blog/[slug]` SSG articles, **mirroring the shipped `/help/[slug]` pattern** (in-code typed constants in `lib/blog.ts`, no DB / no CMS / no markdown dep, `dynamicParams=false` → real 404s, no soft-404). 4 PH-anchored long-tail seed articles (12-month planning timeline · supplier costs · civil-vs-church · ninong/ninang entourage), benefits-only copy with NO quoted SKU prices (drift-proof) + internal-link CTAs to /vendors · /how-it-works · /signup (hub-and-spoke). `BlogPosting`/`Blog`/`ItemList`/`BreadcrumbList` JSON-LD; new `sitemap-blog.xml` (honest per-article lastmod) registered in the sitemap index; footer "Journal" link + `llms.txt` GEO entry. `/blog` was already in the `robots.ts` allow-list. Verified: tsc + eslint + retired-strings + production build (all 4 articles prerendered ●). **DEFERRED to later slices:** the 0038 affiliate / recommendations / sponsored-content DB-backed surfaces (need tables + monetization decisions). Zero owner-credential gate — pure code. SPEC IMPACT → CHANGELOG + 0038 AS-BUILT header + `App_Build_Status.md` + `DECISION_LOG.md`.
**2026-06-13 — feat(guests): desktop guest-list redesign — consolidated chrome + unified filters + combinable views/groups.** The Guests tab had accreted to 8 stacked chrome blocks before the list began (list past the laptop fold). Consolidated to 4 rows: a slim `SummaryStrip` (progress bar + 4 RSVP toggle pills that ARE the filter; the duplicate "Invited" total card removed), the lifecycle ribbon, one filter bar (search · sort · list/map), and a new `ActiveFilters` removable-chip row (the single home for every active filter — inline on desktop, **sticky strip above the list on mobile**). Removed the duplicate "Seating chart" card (ribbon Seat step + left nav already reach it); Side moved into the rail; Share folded into a header dropdown. Behavior change: custom **groups split off `view` onto their own `group` param** so role-views (Wedding Party) and custom groups (Cousins) now **combine** instead of replacing each other (back-compat shim for stale `view=group:` links). Ribbon badges clarified to read "258 to send / 192 to seat / arrived" (were bare numbers that read backwards). Param contract otherwise unchanged → server filtering + the rest of the mobile carousel untouched. tsc + lint + build clean. No migration. SPEC IMPACT → DECISION_LOG.
**2026-06-13 — feat(dashboard): 4 zero-cost deterministic customer utilities + planning checklist.** Owner directive — high-utility, no-external-API automated features for the couple dashboard. (1) **RSVP projection** (`lib/rsvp-projection.ts`) — Projected Attendance RANGE in heads (low/expected/high; 85% pending / 50% maybe owner-tunable) on the guests stats strip. (2) **Budget overspend** (`lib/budget-overspend.ts`) — diffs chosen ₱ vs benchmark + greedy absorption plan ("Flowers + Cake can cover your Photography splurge") banner in the allocation planner. (3) **Emcee script** (`lib/emcee-script.ts`) — compiles schedule blocks + wedding-party names into a time-ordered host script (copy/download) on the schedule page. (4) **NEW couple planning checklist** (`lib/checklist.ts` + migration `20261208000000` ⚠ NOT yet applied to prod) — 28-item PH template, `rankUrgentChecklistItems()` surfaces top-3 urgent open items for the runway; "Up next" card streams on event-home via Suspense, coexists with the #1320 "Today's one thing" hero. Reverses part of the 2026-06-03 Today's-Focus retirement (multi-item checklist ≠ retired single-thing wizard). 22/22 unit tests · tsc/lint/build green. SPEC IMPACT → DECISION_LOG + new table.

**2026-06-13 — feat(inquiry): thread_service_interests — multi-service inquiry mapping (link-gated cascade program, final piece).** Owner-locked 2026-06-12 (`DECISION_LOG.md` "🔗 Link-gated build cascade + multi-service inquiry mapping" build ②③). New `thread_service_interests` join table (migration `20261206000000` ⚠ NOT yet applied to prod) records WHICH services a single couple↔vendor thread is inquiring about + WHERE each came from (`initial` / `linked` / `couple_added` / `vendor_offered`), all hanging off the ONE `chat_threads UNIQUE(event,vendor)` thread + ONE burn-on-answer unlock (re-accept is free → cross-sell never double-charges). RLS maps every policy through `chat_threads` (both parties read their own threads — no SECURITY DEFINER reader needed). Capture: the canonical inquiry path (`unlock-category.ts`) records initial+linked; `/v/[slug]` gets a real inquiry composer with "✓ included" linked chips + "Also ask about" opt-in checkboxes; vendor thread gets an "Offer another service" inverse cross-sell. Both thread views render an "Inquiring about: A · B · C" chip row. `lib/thread-interests.ts` graceful-degrades pre-migration. tsc + lint clean. SPEC IMPACT → DECISION_LOG (row pre-existing).

**2026-06-13 — feat(social): auto-publish Phase C — TikTok + 9:16 (corpus § 8.5).** Portrait 1080×1920 story card format added (`renderSocialCardJpeg(ctx,'story')`, route `?format=story`); TikTok adapter (`lib/social/tiktok.ts`) posts the 9:16 card via the Content Posting API **Photo Mode** (DIRECT_POST/PULL_FROM_URL), wired into `dispatchDuePosts`. TikTok auto-post is gated on `isTikTokConfigured()` (per-account OAuth token) + the developer audit; **default = assisted-manual** — a queue panel renders the 9:16 card + caption + download for a 30-second manual post until audit/OAuth land. Real MP4/Reels video = Phase D (needs a render pipeline). No migration. SPEC IMPACT → corpus § 8.5 + DECISION_LOG.

**2026-06-13 — feat(social): auto-publish Phase B — branded cards + Instagram (corpus § 8.5).** Every auto-post now carries a satori+sharp-rendered 1080×1080 Clean-Editorial card (5 layouts, custom-monogram composite), served on-the-fly via public `/api/social/card/[postId]` (zero storage, live admin previews, fetchable by Graph). Facebook posts upgrade to photo posts for free; **Instagram feed posting live** (`lib/social/instagram.ts`, two-step Graph publish) — multi-platform `dispatchDuePosts` posts to every enabled+configured platform and merges per-platform results. IG is now a live Autopilot chip; needs `IG_USER_ID` env. Reels/TikTok video still out of scope. No migration. SPEC IMPACT → corpus § 8.5 + DECISION_LOG.

**2026-06-13 — feat(social): auto-publish pipeline Phase A (corpus § 8 + § 8.3b).** Facebook autopilot on the consent substrate: `social_posts` compose-once-fan-out + cadence governor (FB ≤3/day · PH prime windows · 48-h pull window on couple content) + cron-free `after()` traffic-piggyback flush + Graph-API adapter + auto **milestones** (real-count ladder, watermarked) + **announcements composer** (CHANGELOG suggestions) + **evergreen content floor** (3-day quiet → LRU item, 60-day no-repeat). Social Queue is now mission control (Autopilot strip · Pull/Post-now/Retry · composer · evergreen library). Migration `20261204000000` APPLIED to prod. Ships with the master switch OFF — owner pastes `META_PAGE_ID`+`META_PAGE_ACCESS_TOKEN` (API checklist #21a) and flips it in the Autopilot strip. IG = Phase B, TikTok = Phase C (audit). SPEC IMPACT → corpus § 8.5 + DECISION_LOG.

**2026-06-13 — feat(social): Social Sharing & Featuring Program (corpus `03_Strategy/Social_Sharing_Program_2026-06-12.md`).** Couple "feature us" per-artifact consent (moment-of-delight cards on monogram + save-the-date; first-names/anonymous credit; revoke list in Profile → Privacy & data) with a hard app-side publish gate (`event_date + 7 days`, never before the event) · optional birthday + separate public-greeting opt-in on Profile · vendor verification celebration posts under the owner-locked hybrid (unnamed category mention for Free, named + logo for Pro+; opt-out on /vendor-dashboard/profile) · new `/admin/social-queue` (take-downs 24-hr SLA → ready-to-post → waiting-on-gate → vendors → greetings-this-week), all posting manual, no crons. Migration `20261130000000` APPLIED to prod (statement-by-statement + manual ledger row — known ledger drift). Meta Graph API auto-posting deferred to V1.5. SPEC IMPACT → corpus doc DESIGN → BUILT + DECISION_LOG.

**2026-06-13 — feat(admin): event types are DB-driven — /admin/event-types CRUD, enum→vocab FK, dynamic pickers.** The event-type roster moved from six hardcoded code lists + the `public.event_type` ENUM + two frozen CHECKs into `event_type_vocab` as the single source: migration `20261205000000` (NOT yet applied — needs `supabase db push`) adds emoji/enabled/onboarding_href/hero_photo_url/description, converts `events.event_type` ENUM→TEXT+FK (retired types keep historical events valid), swaps the vendor/notify CHECKs for vocab triggers, drops the enum. New `lib/event-types-db.ts` read-through (constant fallback, request-cached); create-event picker + EventSwitcher sheet + vendor coverage checkboxes + marketplace filter all read it live. New Setnayan HQ surface **/admin/event-types** (create · launch lever "Show in picker" · edit · retire/un-retire, wedding unretirable, audit-logged) linked beside Taxonomy. Live 9 types byte-identical on landing; anniversary/graduation/reunion seeded hidden. SPEC IMPACT → DECISION_LOG (parent session).

**2026-06-13 — feat(social): Social Sharing & Featuring Program (corpus `03_Strategy/Social_Sharing_Program_2026-06-12.md`).** Couple "feature us" per-artifact consent (moment-of-delight cards on monogram + save-the-date; first-names/anonymous credit; revoke list in Profile → Privacy & data) with a hard app-side publish gate (`event_date + 7 days`, never before the event) · optional birthday + separate public-greeting opt-in on Profile · vendor verification celebration posts under the owner-locked hybrid (unnamed category mention for Free, named + logo for Pro+; opt-out on /vendor-dashboard/profile) · new `/admin/social-queue` (take-downs 24-hr SLA → ready-to-post → waiting-on-gate → vendors → greetings-this-week), all posting manual, no crons. Migration `20261203000000` APPLIED to prod (statement-by-statement + manual ledger row — known ledger drift). Meta Graph API auto-posting deferred to V1.5. SPEC IMPACT → corpus doc DESIGN → BUILT + DECISION_LOG.

**2026-06-13 — feat(vendor): returning-customer resync burn + returning-client badge.** Two owner-locked rules (2026-06-12): a vendor accepting an inquiry from a couple it already UNLOCKED on a different event burns FLAT 1 token instead of the 1/2/3 region band (resync of an old customer; all tier gates unchanged; rows stamped `region_slug='__resync__'`), and pending inquiries from a previously LOCKED (CONFIRMED-booked) client show a "Returning client" badge + prior-event sub-line in the vendor inbox + thread page (SECURITY DEFINER `get_returning_client_flags` — vendor RLS can't read the couple's other-event memberships, so the badge must go through the RPC). `vendor_inquiry_received` notification/email enriched. Migration `20261201000000` applied to prod (statement-by-statement — ledger version collision with a parallel session). SPEC IMPACT → DECISION_LOG (rows pre-existing).
**2026-06-13 — feat(delegate): coordinator delegate LIVE — feature-access program Phase 2.** The dormant 0048 multi-host system is wired: accepted `event_moderators` now enter the couple dashboard (layout admission + `event_members` coordinator row on accept), with **per-area RLS grants** (`moderator_area_level`: guest list/seat plan/schedule/vendors Edit · mood board View · budget OFF→View couple-raiseable per locked D1), a **publish guard** (`publish_requires_couple` — QR mint stays with the couple), and a **trigger-based audit stream** into the adopted 0016 `event_action_log` surfaced on /hosts ("your coordinator did X"). /hosts gains "Promote your coordinator" (one-click delegate invite for booked planner_coordinator vendors), grant chips, budget toggle, remove. Migration `20261129000000` APPLIED to prod + 7-assertion impersonation smoke (vendor.test = standing "Test Coordinator" demo). Design: corpus `03_Strategy/Feature_Access_By_Vendor_Category_2026-06-12.md` §3 (owner-locked D1–D5). Deferred: chat join-all (Phase 3/5) · delegate UI polish · invitation delegation (Phase 5). SPEC IMPACT → corpus doc §9 + DECISION_LOG.
**2026-06-13 — feat(seating): multi-area blueprints + booth placement (cocktail areas).** Owner-approved extension: `event_floor_areas` (cocktail/garden/foyer spaces, optionally tied to a schedule block for their live window) + `event_floor_objects` (drag-placed booth/station pins linked to booked vendors). New couple surface `/dashboard/[eventId]/seating/areas` with a touch drag canvas; vendor seat-plan viewer shows every area + a highlighted "YOU" pin + "Your spot" banner. Migration `20261202000000` APPLIED + demo-seeded on the test event (Cocktail garden · caterer's dessert station). Closes the Phase 4 booth-pins deferral without touching the 150KB seating editor. SPEC IMPACT → corpus doc §6/§9 + DECISION_LOG.

**2026-06-13 — feat(seating): vendor seat-plan viewer — feature-access program Phase 4.** Booked floor-touching vendors (Feast/Venue/Design/Booths/Program/Documentary + coordinator) get the PUBLISHED floor plan read-only at `/vendor-dashboard/clients/[eventId]/seat-plan` — positioned map (stage · dance floor · guest + service entrances · table shapes/rotation · seated/capacity) + per-table meal counts for food categories. Publication-gated, counts-only (no guest names), one SECURITY DEFINER RPC (`get_vendor_seat_plan`, migration `20261201000000` APPLIED + smoked). Booth placement pins deferred to an 0008-editor follow-up slice. SPEC IMPACT → corpus doc §9 + DECISION_LOG.

**2026-06-13 — feat(timeline): shared day-of timeline + vendor Suggest — feature-access program Phase 3.** Booked vendors see the FULL live day-of timeline (locked D2) on their Event Brief with per-block "Request a change" + "Suggest a new entry" + per-vendor `.ics` feed; proposals land in `event_schedule_suggestions` (never direct timeline writes — conflict-architecture lock) and surface as a "Vendor requests" queue on the couple’s Schedule Event-Day view where the couple or a schedule-edit delegate Accepts (applies/creates-as-private-draft) or Declines. Migration `20261130000000` APPLIED to prod + impersonation-smoked both directions. SPEC IMPACT → corpus doc §9 + DECISION_LOG.

**2026-06-12 — feat(vendor): Vendor Event Brief — feature-access program Phase 1.** Booked vendors get an aggregates-only brief of the couple's event at `/vendor-dashboard/clients/[eventId]`: live pax + RSVP breakdown, palette swatches, monogram, FULL day-of timeline (locked D2), seat-plan status; meal counts gated to food-relevant categories + coordinator. SECURITY DEFINER RPC `get_vendor_event_brief` (migration `20261128000000`, APPLIED to prod + smoke-tested via test-account impersonation — `vendor.test` is now the test event's booked caterer for demos). Guest PII never crosses (counts only, RA 10173). Design: corpus `03_Strategy/Feature_Access_By_Vendor_Category_2026-06-12.md` (owner-locked D1–D5 same day). Next phases queued: coordinator delegate on `event_moderators` → shared timeline Suggest → seat-plan viewer → invitation delegation. SPEC IMPACT → corpus doc §9 + DECISION_LOG.

**2026-06-12 — feat(vendor): Calendar + Clients surfaces — PR 3 (schedule-pool program complete).** `/vendor-dashboard/calendar` (one tab per schedule pool · month grid with n/capacity + Closed states · capacity editor tier-clamped · block dates pool-scoped/org-wide · import outside client · merge UI "which categories share this team?") + `/vendor-dashboard/clients` (Booked via Setnayan / In conversation / Outside clients). Migration `20261127000000` `import_external_client()` APPLIED to prod — external-client block + 1-token tier-matrix import fee burn atomically. Program PRs: schema #1288 · wiring #1290 · surfaces (this). SPEC IMPACT → architecture doc §11 + DECISION_LOG.

**2026-06-12 — feat(scheduling): per-category schedule pools — PR 1 schema substrate (marketplace architecture).** Owner-locked scheduling model lands: `vendor_schedule_pools` (one schedule per (org, leaf-category); merge = N categories → 1 pool) + `vendor_schedule_pool_bookings` (only consuming rows; released_at status-flip, never DELETE) + pool-scoped/org-wide `vendor_calendar_blocks` with `external_client` imported-client blocks + **`acquire_schedule_pools` multi-pool all-or-nothing atomic acquire** (bundles lock every category pool they span) + `release_schedule_pools` + lazy `resolve_schedule_pool` + `chat_inquiry_status` lifecycle values (displaced/withdrawn/expired). Migration `20261126000000` APPLIED to prod (statement-by-statement + manual ledger row — known parallel-session ledger drift). Next: PR 2 wires finalize/cancel paths; PR 3 ships vendor Calendar + Clients surfaces. SPEC IMPACT → corpus architecture doc §4/§5a + DECISION_LOG.

**2026-06-12 — feat(papic): Salamisim P3 — /live day-of console (0012).** One screen for whoever runs the wall at the venue (couple OR coordinator): `/dashboard/[eventId]/live` composes the shipped P1 controls (screen codes + 60-tile kill-switch strip) + the Kwento approval queue + a new lifecycle mode control (Auto/Teaser/Live/Recap via `events.live_mode_override` — force the wall live early, freeze to recap at program end) + a FaceBlock posture strip (blur-ready coverage at a glance). "Live Wall" nav entry in Share; non-owners get a quiet add-on doorway. Shared actions dual-revalidate both surfaces. No migration, composition only; tsc/lint clean. SPEC IMPACT → DECISION_LOG + 0012.

**2026-06-12 — feat(papic): Salamisim P2 — server-baked FaceBlock blur pipeline (0012).** The wall's public-event SHIP GATE, now LIVE: `lib/face-blur.ts` (self-hosted MediaPipe full-range detector — committed weights ~1.2 MB, tfjs CPU, the nsfw-screen pattern; TILED sweep lifts recall 2/6 → 5/5 frontal on the wedding fixture; sharp bakes every face blurred INTO the pixels, 1.6× expanded boxes) + migration `20261115000604` (applied + prod-smoked): `faceblock_baked_at`/`faceblock_faces_found` provenance, service-role-only `wall_record_bake`, `wall_ingest`+`wall_visible_photos` v2 (FaceBlock event ⇒ per-row BAKED requirement; toggle-ON hides un-baked tiles on the next read — fail-closed by construction). Both capture chains now screen → bake → ingest; couple guest-detail gains the "Blur faces on the Live Wall" toggle whose ON fires a bounded newest-25 re-bake sweep. **FAIL-CLOSED throughout** (any bake error ⇒ photo stays off the wall; the accepted flip side: an occasional candlestick gets blurred). 11/11 new tests · tsc/lint clean. Magazine Variant B's blur prerequisite is now BUILT (consent amendment still pending). SPEC IMPACT → DECISION_LOG + 0012.

**2026-06-12 — feat(faiths): worldwide religion expansion (9 → 17).** Aglipayan (IFI) · LDS · SDA · Jehovah's Witnesses · Hindu · Sikh · Buddhist · Orthodox added end-to-end (vocab + 5 CHECKs + launch-gated coming_soon cards + TS layer across 20 files) and **every audit journey gap closed with content** (catalog 199 → 223): rabbi + chuppah (Jewish was a dead end), Chinese tea-ceremony/qipao/double-happiness (was zero), Christian counseling, Muslim groom attire, Hindu set, debut attire (event-scoped), Trophies & Awards tile ({tournament, corporate, graduation}). Migrations `20261120000000`/`20261120000100` applied + prod-verified. Owner activates each new faith in /admin/wedding-types. Wave 2 = venue types per faith · onboarding grid layout · per-faith wizard copy. SPEC IMPACT → DECISION_LOG.

**2026-06-11 — feat(papic): Kwento Magazine Variant A (0012).** The free couple-private A4 keepsake, with the Kwento weave included (photo_messages shipped first): `lib/kwento-magazine.ts` (deterministic gap-bucketing — curation owns the cap, "a Kwento earns its photo a slot", WinAnsi-safe text, full renderer cover→prologue→PH-titled chapters→Mga Boses→Salamat) + the couple-gated download route (same editorial frame as the recap, ≤48 curated images, sharp-resized, SSRF-guarded) + the MagazineCard on the Papic add-on page. **9/9 new tests incl. a real 12-page render with 14/14 content assertions on the decompressed streams**; all other suites unchanged (17+11+29); tsc/lint clean. No migration. Variant B (shareable) stays gated on the blur pipeline + consent amendment; POD pricing batched. SPEC IMPACT → DECISION_LOG + 0012.

**2026-06-11 — feat(papic): Kwento P0–P2 — guest photo messages (0012).** The guest-voice layer, owner-locked (text-only · free for all · one-tap wall gate): migration `20261113000972` (applied + prod-smoked) ships `photo_messages` (polymorphic anchor, consent-at-insert, DB interlocks wall_needs_clean/approved_needs_screen) + `guest_message_blocks` + the service-role `submit_photo_message` (cap/burst/edit-resets-moderation) + `guest_visible_messages` + the one-tap `wall_approve_caption`/`wall_clear_caption`. Tier-1 EN+TL+CEB moderation lexicon + PH-PII gate (`lib/kwento-moderation.ts`, 17/17 tests). Guest author sheet on the disposable camera (post-shot "Ano'ng nangyari dito?" + consent tick), couple review queue on the moderation page (Approve/Reject/Show-on-wall/Block), and the wall projection now renders the approved caption as a lower-third. ACLs verified: guest paths service-role-only. SPEC IMPACT → DECISION_LOG + 0012.

**2026-06-11 — feat(admin): /admin/taxonomy ergonomics.** Answering the owner's "easy to update/use/navigate?" — navigation was the honest gap (199 rows, one ~18k-px scroll, no search, saves snapped to top). Now: sticky search + view chips (faith-tagged/event-scoped/unfiled w/ counts) + folder jump pills · 54 tiles collapse to a ~4k-px badge-scannable overview (`wedding-only`/`N faith`/`empty` visible without expanding) · parent-grain **bulk event-set** (one audit row, double-guarded after the critique caught the untouched-default-wipes-scopes trap) · **every save returns you to the exact tile you edited** with filters preserved (`redirectBack` across all 61 action exits) · reads parallelized (~7 serial RTTs → 2) · zero client JS. tsc/lint/40-tests green. SPEC IMPACT → DECISION_LOG.

**2026-06-11 — feat(papic): Salamisim Live Photo Wall P1 (0012).** The Phase-1 output-layer centerpiece, dark-launched: migration `20261112000545` (applied + smoke-verified on prod) ships the gate-chain RPCs — `wall_ingest` (LIVE_WALL → NSFW **allowlist** via the shipped nsfwjs screen → **FaceBlock fail-closed withhold** → consent veto), the audited `wall_visible_photos` reader, the `wall_retract`/`wall_unhide` kill switch (couple/coordinator, wall-only vs also-gallery), single-use screen-code claim. `/wall/[eventId]` = anonymous venue projection (claim code → masonry collage + live count + gold-ring new-tile + 12s/60s reconcile + freeze-on-drop + wake-lock + teaser modes); Papic add-on page gains the wall control card (codes + hide/unhide strip); both capture paths ingest after the NSFW screen. **11/11 new tests + 29/29 bridge suite + tsc/lint clean; prod ACLs verified service-role-only on the feed path.** P2 blur pipeline = the public-event ship gate. SPEC IMPACT → DECISION_LOG + 0012.

**2026-06-11 — feat(taxonomy): couple-side shared filters (mixed-faith union · civil · event gate).** New `lib/taxonomy-filters.ts` — ONE predicate module for both couple surfaces (`/vendors` + dashboard category search, which previously had NO taxonomy-level filter). Fixes: Mixed couples now see BOTH rites' specialist services (was primary-only scalar); civil couples no longer see religious-tagged services (code now matches its documented intent); non-wedding events never faith-narrow (wedding guard); tile-level multi-event gate live (fail-open). 17 new unit tests (23/23 total) + live-data probe of 5 couple scenarios verified. SPEC IMPACT → DECISION_LOG.
**2026-06-11 — feat(papic): Camera Bridge M1 — S0 sink + O1 offline transit + U1 pairing UI (mock-driven, dark-launched · 0012).** The demoable zero-hardware chain on the C1+C2 core: `papic-sink.ts` (DI'd deliverCapture over the SHIPPED seat pipeline — presign→R2→recordSeatCapture; infra failures queue offline, server rejections never do) · the real `camera_bridge` offline drain (stub replaced; Blob/ArrayBuffer payloads) · `InternalCameraBridge` (phone camera as the 5th impl = the real FSM fallback) · the seat-page bridge panel behind `?bridge=demo` (pair Demo DSLR → live-view → still/5s-clip → real gallery → simulate drop → instant phone-fallback banner + null-stamped gap shots → restore). `recordSeatCapture` +kind param (additive). **29/29 unit tests**, tsc+lint clean. Remaining now-track: K1a SKU schema (owner Q1-Q3) + Patiktok/Panood mock adapters; gated track (Canon SDK, EOS body, native binary) on owner sign-offs. SPEC IMPACT → DECISION_LOG + build-plan doc.

**2026-06-11 — feat(taxonomy): Phase 2 faith reconcile — faith_vocab + admin write control.** Faith vocabulary is now a DB table (`faith_vocab`, 9 title-case keys incl. `Civil`; migration `20261109000000` applied + prod-verified) replacing the 5-value CHECK with an FK (Chinese/Jewish/Born Again now taggable; in-use keys delete-protected; 21 tagged rows untouched — zero data mutation, storage stays title-case). Admin gets the faith **write control** the column never had: editable dropdown per service row + faith in Advanced-add, vocab-validated + audit-logged + a dietary-guard (food can never be faith-gated again). `vendors/page.tsx` FaithKey now derives from the lib union (drift fails typecheck). Re-seed clobber fixed in `20260803001000` (`faith=EXCLUDED.faith` dropped). 6/6 unit tests + tsc clean. Next: couple-side event filter + the mixed-faith SET rewrite. SPEC IMPACT → DECISION_LOG.
**2026-06-11 — feat(papic): Camera Bridge core C1+C2 + WiFi transport correction (0012).** First now-track slice of the Camera Bridge build plan (corpus `Camera_Bridge_Build_Plan_2026-06-11.md`; owner extended the bridge to 3 surfaces: Papic + Panood + **Patiktok**; research verdict: **V1 = Canon-only** — Sony/Nikon have no mobile SDK, Fuji is Android-USB-only + warranty-void). New `lib/camera-bridge/`: brand-agnostic `CameraBridge` protocol + `MockBridge` (zero-hardware root) + `DslrPairingController` FSM (immediate phone-fallback inside the locked 3s bar · gap-captures stamped null · 5s auto-retry · per-surface semantics incl. patiktok keep-the-take seams + panood zero-grace continuity · 1-phone:1-DSLR guard) — **17/17 deterministic unit tests**, tsc+lint clean. Transport drift corrected: `apps/mobile` BLE perms → WiFi perms, bluetooth-le dep fully removed (package+gradle+lock), stub comment WiFi-SDK-only. Next now-track: S0 minimal Papic sink → O1 transit → U1 pairing UI (mock-driven); gated track (Canon SDK + EOS body + native binary) starts on owner sign-offs. SPEC IMPACT → DECISION_LOG 2026-06-11 ×2 + the 0012 callout.

**2026-06-11 — feat(compliance): the app-store readiness trio SHIPPED + prod-verified (PRs #1229 · #1230 · #1231/#1234).** The three pre-launch store-compliance gaps from the mobile proposal (`Mobile_Native_Features_Tier1_2_Proposal_2026-06-10.md` §7, corpus) are now live: **(1) in-app account-deletion request flow** (Apple 5.1.1(v)) — Profile → "Request account deletion" → queued `account_deletion_requests` (S89X- ids, one-pending-per-user) → `/admin/account-deletions` approve (reuses existing delete/blacklist) / reject; replaces the immediate self soft-delete. **(2) UGC moderation** (Apple 1.2 / Play UGC) — `user_reports` (couple + `/admin/user-reports` queues), event-scoped `event_blocked_users` (keys on `blocked_guest_id` — uploader is a guest, not a users row), one-time upload terms gate (`guests.ugc_terms_accepted_at`), all enforced inside `papic_record_guest_capture` (gate order: owned → blocked → terms → quota). **(3) Web Push + offline shell Phase 1** (Apple 4.2) — `push_subscriptions` (owner-only RLS), VAPID-gated `sendWebPush` no-ops until env keys set, wired to `chat_message` + `vendor_inquiry_received` alongside email, `sw.js` v3 (push/notificationclick/offline.html/day-of SWR cache/guestbook sync stub). Migrations `20261106/20261107/20261108` applied + ledger sequential. **Verified against prod** (rolled-back RLS probes incl. full guest lifecycle: terms_required→accept→ok→blocked; report→dedup→queue; admin via real `account_type='admin'`). **Incident absorbed:** a 3-way parallel-session timestamp collision on `20261105000000`/`...106` (defaith vs account-deletion vs UGC) — resolved by re-timestamping (#1234 + `20261108000000_ugc_moderation`); watch-out: parallel sessions must re-check free migration timestamps at merge time, `migration list` matches on VERSION not content. **Known gap surfaced:** the NSFW/objectionable-content filter the corpus claims is "shipped" was NOT found in the capture path — flagged as a follow-up build item (Apple 1.2 requires a filter, not just report/block). Owner action: generate VAPID keys (see OWNER_ACTIONS.md 2026-06-11). SPEC IMPACT → corpus proposal §7 + DECISION_LOG 2026-06-11.

**2026-06-11 — test: unit-test foundation (node:test via tsx).** First unit tests in the repo — `apps/web/lib/taxonomy.test.ts` (5 taxonomy-invariant guards incl. a de-faith regression guard that fails on the pre-fix code, naming all 4 offenders, and passes after). `pnpm test:unit` (`tsx --test`) + a CI "Unit tests" step in the typecheck+lint job. **Zero new deps** (Node built-in runner + existing tsx). Foundation for the dietary grade-comparator + filter tests to come. SPEC IMPACT: none.

**2026-06-11 — fix(taxonomy): de-faith food canonicals (live halal-hiding bug).** `halal_catering` (faith=Muslim) + 3 `mocktail_*` (faith=INC) were hidden from non-matching couples by the include-only marketplace faith filter (a never-subtract-lock violation). Cleared `faith` on all 4 (DB migration `20261105000000` + the hardcoded tags in `lib/taxonomy.ts`); `halal_catering` stays a faith-neutral "Halal Catering Specialists" canonical visible to all (option 1c). `dietary` kept as a future capability signal. Verified on prod (4/4 faith=NULL, 0 residual). First slice of the dietary-capability model; the graded capability + matcher wiring follow. SPEC IMPACT → DECISION_LOG.

**2026-06-11 — feat(papic): Live Photo Wall "Salamisim" — P0 schema (0012).** First build step of Papic's in-venue real-time projection (Phase 1 of the output layer; the moat per the competitive strategy). Migration `20261104000959` (applied to prod): wall-state columns on both capture tables (`moderation_state`/`wall_safe_r2_key`/`wall_hidden_at`) + `guests.faceblock_enabled` + `events.timezone`/`live_mode_override`/`live_photo_wall_visibility` + new tables `photo_tags` (guest-in-photo, polymorphic), `wall_feed` (durable public-broadcast mirror — safe-derivative-key only), `wall_display_sessions` (venue-screen claim) + `wall_feed`→realtime publication. RLS at create (couple/coordinator/admin; NO anon; writes service-role/RPC-only). **Schema only** — the gate-chain RPCs, `/wall` route, and control surface are P1. Corrected 2 corpus drifts: `coordinator` IS a shipped `member_type` (no `thread_join_authorizations` table needed); `events` had no `timezone`. SPEC IMPACT → DECISION_LOG 2026-06-11 + 0012.

**2026-06-11 — feat(taxonomy): admin event-type assignment UI.** `/admin/taxonomy` tile editor now has a per-tile **"Events:"** control to scope a category to specific event types (none checked = universal). New `setCategoryEventTypes` action (audit-logged) + `tileEventTypes` on the taxonomy snapshot. Directly the owner's ask "assign which category serves which event." Couple-side filtering (never-empty-careful) is the next step. No migration (columns from Phase 1). SPEC IMPACT → DECISION_LOG.

**2026-06-11 — feat(taxonomy): Phase 1 multi-event applicability.** Added `applicable_event_types TEXT[]` to `service_categories` (tile, primary control) + `canonical_service_taxonomy` (per-service override) + an `event_type_vocab` table (12 events) + a validation trigger. **FAIL-OPEN** (NULL = universal → all 64 rows unchanged-for-weddings). Admins can now structurally scope a category to specific events; the read-through + admin UI + couple-side filter are the next (wiring) phase. Migration `20261104000000` applied + verified on prod. SPEC IMPACT → DECISION_LOG.

**2026-06-11 — feat(taxonomy): Phase 0 single-source unification — refinements anchored to the tree.** Added `onboarding_refinements.tile_id` FK → `service_categories` (tier-2 tile) + NOT NULL, backfilling all 38 leaves (27 exact + 11 alias; **no `leaf_key` rename** → saved couple picks intact). The couple-facing onboarding refinements can no longer drift from the marketplace taxonomy, and event-type/faith visibility can later inherit from the anchor tile. Migration `20261103000000` applied + verified on prod (38/38 anchored). Foundation for the multi-event `applicable_event_types` + wedding faith-exclusivity design (`Taxonomy_Event_Faith_Scoping_Design_2026-06-10.md`). Next: Ph1 event-type cols · Ph2 faith reconcile · Ph3 de-hardcode + admin write + photo enforcement. SPEC IMPACT → DECISION_LOG 2026-06-10.

**2026-06-10 — feat(guests): privacy-first invite-claim + email-OTP (no auto-admit).** `/join/[eventId]` no longer silently auto-admits a signed-in user whose email doesn't match a seed-list row. New flow: exact-email → link; single confident fuzzy name-match w/ a seed email + Resend → email-OTP handshake; everything else (ambiguous/no-match/no-email) → a couple **review queue** (`/dashboard/[eventId]/guests/claims`, banner on the guest list). New `guest_claims` table + `finalize_guest_claim()` + `register_guest_claim_otp_attempt()` RPCs (migration `20261102000000`), `lib/guest-claim*.ts`. Hardened post-review (14 findings, incl. a pre-existing `member_can_self_join` privilege-escalation). tsc + lint clean. SPEC IMPACT → DECISION_LOG 2026-06-10.

**2026-06-09 — feat(marketplace): near/far vendor coverage + "show vendors farther away."** The tier service-radius (entry/verified 20km · Pro 50km · Enterprise nationwide, from `vendor-tier-caps.serviceRadiusKm`) was already a silent hard filter on the couple's category search — far vendors were just excluded. Now: in-range vendors badge **"✓ Serves your area"**; a **"Show vendors farther away"** expander reveals out-of-range vendors labelled **"~X km · beyond their N km range · travel fee likely."** `category-search.ts` results carry `withinRadius` + `serviceRadiusKm`; `wizard-recommendations.ts` gains `includeOutOfRadius`; overlay reworked. No migration (radius in tier caps, coords exist). tsc clean. Radii unchanged/confirmed. Map view (vendor pins) is a deliberate follow-up ("labels now, map next"). SPEC IMPACT: confirms locked radii + adds near/far surfacing → DECISION_LOG; supersedes stale "10/25/100 CONFIRM" memory note.

**2026-06-09 — feat(demo + marketplace): full-taxonomy demo coverage + linked-services-on-card.** "Create demo vendors" is now driven by the live taxonomy DB — `fetchCoverageNodes` = every `canonical_service_taxonomy` row ∪ every empty `service_categories` leaf (182 nodes today; grows with the taxonomy), so **every** node gets a demo vendor (was ~192 schema-only, missing any node without a schema row). Blended topology: ~1-in-6 vendors are multi-service "studios" — either extra priced listings or **linked-services-on-card** (`is_linked_only` siblings + the new `vendor_service_links` table → couple card shows **"✓ comes with X · Y · Z"**, the locked-but-unbuilt concept now BUILT). New migration `20261014000000` (table + `vendor_services.is_linked_only`, applied to prod). Couple card read-side wired (`fetchVendorPhotoMaps` → `linked_services`); vendor `/vendor-dashboard/services` gains a "Comes with" picker (`setServiceLinks`). tsc clean; coverage + row-shapes verified against prod. SPEC IMPACT: linked-services-on-card → BUILT in corpus (DECISION_LOG + 2 spec docs + budget forward-flag resolved).

**2026-06-09 — feat(mood-board): "concept book" PDF export (0010).** Couples download a printable concept book from the mood board — Cover (monogram·names·date·hero) · The Concept (the stylized reception they designed; auto-upgrades to the photoreal render once "Make it real" ships) · How you made this (palette · per-part design choices · custom-template thumbnail · uploaded inspirations). Free, ₱0, **no migration** (reads existing columns); reuses pdf-lib+sharp+qrcode (mirrors the seat-plan PDF). New `lib/concept-pdf.ts` + `concept-pdf/route.ts` (auth+RLS, `maxDuration=20`) + `concept-pdf-button.tsx` + reusable `lib/safe-image-fetch.ts` (SSRF-guarded fetch — the inspiration `image_url` column can by-schema hold a pasted URL). tsc/lint/build green; 3 pages visually verified (ñ names, long-name shrink-to-fit, empty-palette skip); SSRF guard unit-tested; 4-lens adversarial review fixed (fetch timeouts, shrink-to-fit, iOS objectURL revoke). The paid photoreal render + saved-render galleries + ₱500/5-render credits + consent-bonus stay owner-gated design locks (pending the image-provider API key). SPEC IMPACT: 0010 concept-book PDF → DECISION_LOG.

**2026-06-09 — vendor tier: founder vendor overrides (owner).** Founder vendor `Setnayan Founder · Ice` → **verified** + **unlimited categories/services** + **full token-gate bypass**. New `vendor_profiles.is_founder` flag (migration `20261013000000`, applied to prod) on the one founder account; `unlock_vendor_event` founder branch (unlimited free unlocks — no FREE block / weekly cap / burn); `createVendorService` lifts parentCategories + servicesPerLeaf to ∞ for the founder. Other caps stay verified; searchability gate still flag-dark. `tsc`/`lint` clean; auto-rollback smoke test confirms bypass. SPEC IMPACT: → DECISION_LOG + matrix + memory.

**2026-06-09 — fix(brand): correct logo on seat-plan PDF + desktop icon + keynote; PDF "Created by WWW.SETNAYAN.COM" footer.** Owner: the seat-plan PDF logo (and "the loading" logo) was wrong. The canonical gold-mark SVG + favicons/manifest/web-splash + mobile splash were already correct; 3 raster/desktop derivatives were stale — `setnayan-mark-512.png` (old **black pin+star**, embedded in the PDF), `keynote/setnayan-mark.png` (old orange), `src-tauri/icons/icon.svg` (wrong glyph → desktop app icon). Regenerated all from the canonical SVG via a new **`pnpm brand:icons`** script (`sharp`; also syncs the byte-identical SVG copies) → future logo changes = swap `public/brand/setnayan-mark.svg` + run it. Seat-plan PDF: logo fixed, the mis-placed header logo (overlapped the monogram) removed, and a centred **"Created by WWW.SETNAYAN.COM"** footer (with the gold mark) added to every page. tsc/lint clean; PDF + PNGs visually verified. SPEC IMPACT: None → DECISION_LOG.

**2026-06-09 — onboarding: hot-date demand "heat" layer on the wedding date calendar.** The date `DateCalendar` now tints cells by **predicted demand** (on-brand gold→terracotta warm ramp) and shows a demand chip ("In-demand"/"Hottest") in the existing "why this date" nugget — couples see demand alongside fit. Deterministic & cold-start-safe (peak month × Saturday × repeating-number); the observed inquiry / relative-to-supply escalation (Date-Aligner spec §L.2) is **deferred** (founder-only marketplace = no data). Presentation-only — no migration/schema. `tsc`/`lint`/`build` + full CI green. SPEC IMPACT: builds Date-Aligner §L (predicted half) → DECISION_LOG.
**2026-06-09 — vendor tier: subscription bundle tokens are LIFETIME (owner).** The free token bundle granted with a paid Pro/Enterprise subscription (Pro 5/50 · Ent 10/100) is now **never-expire + granted in full on purchase** — credited to `vendor_wallets.purchased_tokens` (was an expiring 28–365-day earned voucher). New reusable idempotent `grant_vendor_lifetime_tokens` RPC (migration `20261012000000`, applied to prod); `_apply_subscription_credit` + `setVendorTier` both switched to it. Burn drains purchased_tokens after earned vouchers, so they're fully spendable on the answer-inquiry burn. `tsc`/`lint` clean; auto-rollback smoke tests confirm +5 (Pro-mo) / +100 (Ent-yr) land in purchased_tokens, 0 expiring vouchers, idempotent. SPEC IMPACT: → DECISION_LOG + matrix + memory.

**2026-06-09 — chore(db): prevent recurring migration timestamp collisions.** Duplicate 14-digit migration prefixes have half-applied prod + blocked every open PR 4× (hand-typed `YYYYMMDD000000`, two people pick the same date; CI only catches it reactively). Added **`pnpm migration:new`** (`scripts/new-migration.mjs`) — a **monotonic** allocator (prefixes have drifted ~4mo ahead of wall-clock, so a real-timestamp would sort *before* existing ones) that consults local ∪ `origin/main` and loops on prefix-level uniqueness + writes an idempotent stub — plus a pure-sh **pre-push hook** (`.githooks/pre-push`, wired via `core.hooksPath` by a `prepare` script) that blocks a colliding push by inspecting the **pushed commits ∪ origin/main** (not the working tree → no false-block on WIP; catches already-merged collisions pre-push), and `pnpm migration:check`. 4-dimension adversarial review (19 agents): 9/15 findings confirmed + all fixed. No migration; tooling only. SPEC IMPACT: None → DECISION_LOG.

**2026-06-09 — admin: recent-admin-activity feed on Home.** Completes the command-center Home (redesign §3.4 — the last deferred piece). A "Recent admin activity" card reads the last 8 `admin_audit_log` rows (real data) with resolved actor names + friendly action phrases (humanizes unknown codes) so admins see who-did-what and don't collide on the same row. Read-only; no schema/route/data change. `tsc`/`next lint` clean. SPEC IMPACT: §3.4 → DECISION_LOG. **Admin nav redesign Home is now fully built (command center: all queue lanes + platform stats + activity feed).**
**2026-06-09 — vendor tier: subscription token-bundle reprice (owner).** Per-period free-token bundle changed to **Pro 5/50 · Enterprise 10/100** (was 30/300 · 100/1000). Updated in both synced places: `TIER_SUBSCRIPTION_BUNDLE_TOKENS` (display + admin tier-set grant) + the money-path RPC `_apply_subscription_credit` via migration `20261011000000` (applied to prod, auto-rollback smoke test confirms 5 + 100). Subscription PRICES unchanged (₱6,000/₱60,000 · ₱10,000/₱100,000). `tsc`/`lint` clean. SPEC IMPACT: → DECISION_LOG + matrix + memory.

**2026-06-09 — vendor tier #5: self-serve subscription checkout → tier program (#1–#5) COMPLETE.** Final "do 1–5" build (Phase D). Vendors self-serve upgrade Pro/Enterprise via **apply-then-pay → admin approve** (writes `tier_state` + grants the per-period token bundle); lapse auto-downgrades on next login (cron-free). Migration `20261010000000` (applied to prod): `vendor_subscriptions` + `vendor_profiles.tier_expires_at`/`tier_billing_cycle` + 6 SECURITY DEFINER RPCs. **Security:** credit-core + webhook-confirm REVOKEd from authenticated (no self-grant); approve gated `is_console_admin()`. Price from DB catalog (₱6,000/₱10,000). New `/vendor-dashboard/subscription` + `/admin/subscriptions`. Built by an impl agent (8 verifier fixes) + hand-reviewed migration + **auto-rollback money-path smoke test** (tier→pro, +30 tokens, idempotent, lapse→free — zero prod mutation). Deferred: import-customer charge. `tsc`/`lint` clean. SPEC IMPACT: → DECISION_LOG. **Full vendor-tier matrix (#1 services-per-leaf · #2 daily capacity · #3 Enterprise time-slots · #4 Phase-C gates · #5 subscriptions) is SHIPPED.**

**2026-06-09 — feat(seating): tables never overlap (collision avoidance · 0008).** Owner: "the tables will never overlap each other." Auto-grow spaced *new* tables apart but a **drag** could still cover another and a wide 16-seat family-head table could touch neighbours. Added footprint-aware AABB collision (`footprintPx`/`overlapsAny`, 10px gap) + `nearestFree` (spiral + guaranteed grid-scan fallback): a dragged table now **slides around** others instead of covering them, and a layout-effect auto-places every table at a non-overlapping home (saved tables anchor; up to 3 cleanup passes resolve stragglers) with a smooth slide transition. Sized rooms get a footprint-aware shelf-pack base (`venueShelfBase`) so to-scale tables fit whenever the room is physically big enough. `tsc`/`lint`/`build` clean; **46/46** collision-invariant test (real geometry: free board 3–50 tables incl. family-heads · tight→extreme sized rooms · drag-onto) + visual before/after (16 overlaps → 0). SPEC IMPACT: 0008 collision avoidance → DECISION_LOG.

**2026-06-08 — feat(seating): auto-grow board for the free (no room size) plan (0008).** Owner: with no room size, the board should "expand as we add more tables" (0008 `venue_known=false`). New tables now sit at FIXED comfortable spacing (`defaultTablePosition(spread=true)`) so the board grows outward (positions can exceed 100%) instead of crowding; the view auto-fits on table-count change; ZOOM_MIN→0.1. Venue mode unchanged (packs in the room). 14-agent adversarial review caught that >100% positions break the 0–100%-assuming renderers — fixed with a shared `fitFloorTransform` (no-op in-bounds) applied to the PDF export + day-of wayfinding/find-my-table, and one shared `defaultTablePosition` across editor/PDF/map (consistent un-arranged layout; PDF no longer centre-stacks null tables). `tsc`/`lint`/`build` clean; repro-verified (before/after spread + spread-position PDF on-page). SPEC IMPACT: 0008 auto-grow → DECISION_LOG.

**2026-06-09 — admin: onboarding settings grouped into `/admin/onboarding`.** Owner: with multiple onboardings coming, group the onboarding (wedding) custom settings — like the background music. New type-organized **Onboarding** admin surface: a **Wedding** section houses the background music (relocated from the generic `/admin/settings`, same `platform_settings` columns → `/onboarding/wedding` read path unchanged, zero risk) + links to the catalogs the flow pulls from (Songs/Refinements/Wedding types) + a "more onboarding flows" placeholder for future event types. Added to the desktop Platform group, mobile More accordion, and bottom-nav. No migration. Per-type storage (`onboarding_settings` table) deferred until a 2nd flow needs its own knobs (UI already type-organized so it's additive). `tsc`/`next lint` clean. SPEC IMPACT: new `/admin/onboarding` surface → DECISION_LOG.

**2026-06-09 — admin nav redesign PR 4: two-admin (four-eyes) approval queue.** Final core piece of the redesign (after PR 1 re-bucket · PR 2 command-center Home · PR 3 mobile accordion). Builds the §9.1 four-eyes primitive the audit found **unbuilt**: new `admin_approval_requests` table (migration `20260930000000`, idempotent, admin-RLS, four-eyes CHECK) + `/admin/approvals` queue (New request form · Pending approve/reject — **disabled on your own row** · Recently decided · <2-admin bootstrap banner) + server actions with four-eyes enforced 3× (atomic-claim `.neq` + DB CHECK + UI) and an atomic `UPDATE…WHERE status='pending' AND expires_at>now AND initiated_by<>me` claim (decided once, never after expiry, no double-execute; execute-failure rolls back; all audit-logged). V1 actions = privilege grants (internal §10a / team-pool §10b / promote-to-admin). Surfaced in sidebar Work + mobile Work tab + `/admin/work` feed + Home "Approvals & support" lane. `tsc`/`next lint` clean; adversarial 4-lens security review. **Owner action:** apply migration `20260930000000` to prod to activate (page degrades to empty until then). **This completes the admin nav redesign's core scope.** SPEC IMPACT: PR 4 → DECISION_LOG + 0023 §4.
**2026-06-09 — vendor tier #4 Phase C gates, PR-a (chat · editorial · slug).** First of 3 #4 PRs. Three clean zero-migration gates: **chat FREE-block** (`lib/chat-actions.ts` — FREE vendors can't message couples; closes the admin demo-accept service-role bypass), **editorial tag-gate** (`editorial/data.ts` — `verified` now a plain credit, logo+badge+link showcase is pro/ent only; free still hidden), **custom-slug = PRO/ENT** (`vendor-dashboard/actions.ts` + `profile/page.tsx` — rejects slug *change* for FREE/VERIFIED, advisory disabled input). `tsc`/`lint` clean. Next: PR-b (names + reviews + radius + searchability flag-dark) · PR-c (Enterprise video chat via public Jitsi, ₱0). SPEC IMPACT: → corpus DECISION_LOG.

**2026-06-09 — admin nav redesign PR 3: mobile "More" → 3-section accordion.** Third slice of the owner-approved redesign (after PR 1 re-bucket + PR 2 command-center Home). The mobile **More** tab's flat 24-card grid becomes a grouped, **collapsible accordion** (Insights · Money & Catalog · Platform) — never a flat dump; sections start expanded, each collapses via a chevron. New client `mobile-landing-accordion.tsx` reuses the existing `m-card` item markup; `more/page.tsx` splits its items into the 3 sections (keys mirror the desktop sidebar groups). No schema/route/data change. `tsc`/`next lint` clean. **Redesign remaining:** `/admin/approvals` two-admin queue (now unblocked — migrations clean again). SPEC IMPACT: PR 3 of the redesign → DECISION_LOG.

**2026-06-09 — vendor tier #3: Enterprise time-bound slots (✗/✗/✗/∞).** Build #3 of "do 1–5". Enterprise vendors plot **named time windows per service, each with its own allotment** (hotel rooms across a day); Pro stays flat 3/day, FREE/VERIFIED unchanged. Migration `20260928000000` (applied to prod): `vendor_service_time_slots` (RLS at CREATE) + `event_vendors.service_time_slot_id` + atomic `acquire_service_time_slot` RPC (couple-only auth, `FOR UPDATE` lock, write-in-lock → no TOCTOU, reads `events.event_date` gated on `event_date_precision='day'`). Vendor services page gets a `SlotEditor` (ADD Enterprise-only; daily-capacity input disabled when slots exist); `finalizeVendor` gains slot/`slot_full` paths + a couple `<select>` picker on all 3 lock surfaces; #2's `wedding_date` reads repointed → `event_date`. `tsc`/`lint` clean; design adversarially verified (banked spec). Next: #4 Phase C feature gates · #5 Phase D self-serve checkout. SPEC IMPACT: → corpus DECISION_LOG + tier matrix.

**2026-06-09 — admin refinements editor ([#1136](https://github.com/iscasasola/setnayan-platform/pull/1136) · follow-up to items 8/9).** New `/admin/refinements` (Platform group, by Taxonomy): a collapsible per-leaf editor for the onboarding "what kind of X?" catalogue — edit label/description/main photo/retire + per-option emoji/label/photo/retire + add/remove, with **R2-backed photo uploads** (seeded `/public` photos stay until replaced; `getOnboardingRefinements` resolves `r2://` refs at render, a no-op when none). Admin-gated (layout 404 + `requireAdmin` + RLS) and edits go live immediately (`revalidatePath('/onboarding/wedding')`). `tsc`/`lint` clean + adversarial-review-workflow'd. This closes item 9's "full" admin surface — **the entire onboarding punch-list (items 1–10 + monogram-icon) is now shipped.**

**2026-06-09 — "Maria & Jose" full demo wedding + tier-aware editorial showcase.** Owner wanted to see all 3 website phases on a real, photo-rich wedding with Free/Pro/Enterprise vendors. Seeded the `[TEST] Maria & Jose` event (slug `test-maria-and-jose`, dated 2026-06-01 → Editorial phase) with full content: rich `love_story`, special message, what-to-bring, dress code, photo moments, 6 schedule blocks, 30 guests (24 attending), 9 vendors (6 #1-match) incl. 3 tiered marketplace profiles (Free/Pro/Enterprise, `is_demo`, hidden from browse), `event_editorial` snapshot (→ "Jewel-box" archetype), + 9 AI-generated (Recraft) Filipino-wedding photos committed to `public/demo/maria-jose/`. Code: relaxed the `our_photos` renderer to accept non-`r2://` URLs (so `/demo/...` renders); built the **§3 tier-gated editorial vendor showcase** (Pro/Enterprise = featured card w/ logo + badge + profile link; **Free hidden from editorial entirely**) — was a deferred D gap; editorial hero now falls back to the website hero photo. No migration; demo data disposable. View at `setnayan.com/test-maria-and-jose` (Editorial needs `WEBSITE_PHASES_ENABLED` on). SPEC IMPACT: §3 showcase + hero fallback → DECISION_LOG.
**2026-06-09 — admin nav redesign PR 2: command-center Home.** Second build slice (after the PR-1 re-bucket) of the owner-approved redesign. The admin Home now surfaces **all 11 pending queues grouped by lane** (Trust & supply · Money to reconcile · Recourse · Support), not just 4 — "what needs admin action right now" at a glance. The **Money to reconcile** lane (Payments · Payouts · Token sales) is the always-visible one-stop money view that **satisfies the Money-lane sign-off condition** from the dissolved Money group. Each tile is a live count → its queue with the matching filter (adversarially verified against every queue page). Two-admin approvals not shown (table unbuilt). `tsc`/`next lint` clean. **Next (committed follow-ups):** `/admin/approvals` two-admin queue (needs schema + actions + UI — sensitive, its own PR), mobile-More 3-section accordion, optional recent-admin-activity feed. SPEC IMPACT: PR 2 of the redesign → DECISION_LOG.

**2026-06-09 — vendor tier #2: per-service daily booking capacity (✗/1/3/∞).** Build #2 of "do 1–5". A vendor declares bookings/day per service (2 photobooths → 2); tier caps the max (FREE 0 / VERIFIED 1 / PRO 3 / ENT ∞). Migration `20260925000000` (applied to prod): `vendor_services.daily_capacity`; capped on create+edit; tier-aware "Bookings per day" input on both forms; `finalizeVendor` blocks once the service has `daily_capacity` confirmed bookings on the wedding's date. Enterprise time-bound slots (#3) layer time-of-day on top later. `tsc`/`lint` clean. Next: #3 · #4 Phase C · #5 Phase D. SPEC IMPACT: → corpus DECISION_LOG. Build #2 of "do 1–5". A vendor declares bookings/day per service (2 photobooths → 2); tier caps the max (FREE 0 / VERIFIED 1 / PRO 3 / ENT ∞). Migration `20260925000000` (applied to prod): `vendor_services.daily_capacity`; capped on create+edit; tier-aware "Bookings per day" input on both forms; `finalizeVendor` blocks once the service has `daily_capacity` confirmed bookings on the wedding's date. Enterprise time-bound slots (#3) layer time-of-day on top later. `tsc`/`lint` clean. Next: #3 · #4 Phase C · #5 Phase D. SPEC IMPACT: → corpus DECISION_LOG.
**2026-06-09 — onboarding refinements: DB-backed + 4:3-photo cards + 232 generated photos (punch-list items 8/9/10).** Every "what kind of X?" refinement card now shows a **4:3 main photo on top + a description + a 4:3 option carousel** (item 8). The ~40-leaf hardcoded `REFINEMENTS` const is lifted into a data module + **DB-backed** tables (`onboarding_refinements`/`_options`, migration `20260927000000`, seeded 37 leaves · 206 options in prod) read **DB-first with module fallback** (`getOnboardingRefinements`), so refinements are no longer hardcoded + admin-editable (item 9); cards still only show for chosen services. **232 on-brand 4:3 photos** (37 mains + 195 options) generated via a 37-agent Recraft workflow, `sharp`-compressed to 5.3 MB total, committed under `public/onboarding/refinements/` (item 10 · ~₱530, not the bad ₱11.5k estimate). `tsc` clean + the cake card screenshot-verified (hero + description + carousel). **Remaining:** an admin editor UI for the refinements (DB-editable via SQL today) + the monogram→event-icon polish. SPEC IMPACT: starts V1.x DB-backed taxonomy → DECISION_LOG.

**2026-06-08 — fix(seating): smooth pan + tables visible in to-scale mode (0008).** Two owner-reported bugs in the venue/to-scale editor, root-caused via a 6-agent workflow + render-repro. (1) Pan/drag STOPPED at the canvas edge — `onPointerLeave={onCanvasPointerUp}` killed the gesture (capture doesn't suppress pointerleave); replaced with `onPointerCancel` → smooth pan. (2) Tables "not showing" — scale math was fine but the room-aspect `w-full` canvas ballooned tall (portrait → ~1000px) so tables sat below the fold + a first-paint canvasW=0 flash; fixed by capping canvas height (64vh budget drives width, centered → all tables in one overview), measuring canvasW in useLayoutEffect (+ venue dep), resetting to a zoom-1 overview on toggle, and making fitView/puck use the on-screen scaled sizes. `tsc`/`lint`/`build` clean; repro-verified. SPEC IMPACT: none.

**2026-06-09 — vendor tier #2: per-service daily booking capacity (✗/1/3/∞).** Build #2 of "do 1–5". A vendor declares bookings/day per service (2 photobooths → 2); tier caps the max (FREE 0 / VERIFIED 1 / PRO 3 / ENT ∞). Migration `20260925000000` (applied to prod): `vendor_services.daily_capacity`; capped on create+edit; tier-aware "Bookings per day" input on both forms; `finalizeVendor` blocks once the service has `daily_capacity` confirmed bookings on the wedding's date. Enterprise time-bound slots (#3) layer time-of-day on top later. `tsc`/`lint` clean. Next: #3 · #4 Phase C · #5 Phase D. SPEC IMPACT: → corpus DECISION_LOG.

**2026-06-09 — admin console nav: ops-shaped re-bucket (PR 1 of N).** First build slice of the owner-approved nav redesign (`Admin_Console_Nav_Redesign_2026-06-08.{md,html}` · conditional sign-off "as long as it's easier to manage"). The admin console is regrouped by **verb** (act / find / tune): a 3-item spine (**Home · Work · Directory**) + 3 collapsible tune-groups (**Insights · Money & Catalog · Platform**), replacing the 6 noun-domain groups. Mobile drops to a **4-tab spine** (Home · Work · Directory · More); new `/admin/work` triage feed (adds Payouts + Token sales); `/admin/queues`+`/admin/money` redirect for bookmark continuity. **Drift fixes:** orphaned `/admin/notifications` gets a home; Token sales now lights a mobile tab; wedding-types/traditions move to Platform. Group keys preserved → localStorage open-state survives. `tsc`/`next lint` clean (only pre-existing unrelated errors). **Next (committed follow-ups):** Work master-detail + **Money-lane filter** (the sign-off condition), full command-center Home, net-new `/admin/approvals` two-admin queue UI, mobile-More accordion. SPEC IMPACT: PR 1 of the redesign → DECISION_LOG.
**2026-06-09 — onboarding background music, admin-uploaded ([#1122](https://github.com/iscasasola/setnayan-platform/pull/1122) · punch-list item 1).** Owner's design: an **admin uploader** (not a committed file) so the owner uploads an **owned / AI-generated** track (e.g. a Suno instrumental) that the ~15-min wedding onboarding streams. New `/admin/settings` "Onboarding background music" card (audio ≤40 MB + enable toggle + owned-track helper) → `platform_settings.onboarding_bg_music_r2_key`/`_enabled` (migration `20260925000000`, applied to prod directly — orphan `20260924000000` mood-board ledger entry blocked `db push`). Onboarding resolves the URL server-side and shows a header mute/unmute **pill**; starts on first tap, loops, low volume, mute remembered; **unset/disabled → silent**. `/api/upload` audio cap 20→40 MB. `tsc` clean + pill layout screenshot-verified. **Remaining punch-list:** items 8/9 DB-backed refinements + 4:3 card (next), item 10 generate photos via Recraft (~₱530 — corrected down from the bad ₱11.5k estimate), monogram→icon polish. SPEC IMPACT: new admin music uploader → DECISION_LOG.
**2026-06-07 — admin pricing: per-SKU cost + margin column, editable cost + active toggle (0023).** `/admin/pricing` already SELECTed `saas_overhead_cost_php` but never showed/wrote it. Each customer-SKU row now displays **Cost / event** + a computed **Margin %** (divide-by-zero guarded → "—" for FREE), the header stats gain an **Avg margin (paid)** tile, and the single-row edit form gains a `saas_overhead_cost_php` input (the `is_active` deactivation toggle was already present — left intact so the 3 retired SKUs are deactivatable). `updateRetailSku` now validates + writes the cost alongside the price, preserving the audit field + revalidatePaths. **No prices changed, no rows added/removed, no DB write/migration** — costs + deactivations are entered later by the owner via this UI. `pnpm typecheck` clean. SPEC IMPACT: None — pricing model already landed in the corpus (Pricing.md § 00).

**2026-06-08 — seating: A4 PDF export — mood-board / blueprint + monogram + QR (0008).** Owner-specced. New `lib/seating-pdf.ts` (`buildSeatingPdf` via `pdf-lib`) + `/dashboard/[eventId]/seating/export` route (Node runtime) + an "Export PDF ▾" toolbar menu. **A4**; header on every page = couple monogram (text) + names + date + Setnayan logo + website **QR** (`{appUrl}/{slug}`); **page 1 = floor plan** (to-scale when a venue size is set, via `TABLE_FOOTPRINT_M`; round→circle, banquet→rect; stage + entrance); **pages 2+ = seating arrangements** (per-table guest lists w/ roles, 2-col, paginated). Two modes: **mood-board** (colours from `event_moodboard_saves.palette_snapshot`) or **blueprint** (blue line-art). Reuses existing `pdf-lib`/`qrcode`/`events.slug`; no migration. `tsc`/`lint`/`build` clean; **both modes + both page types verified by rendering the real PDF**. Completes the seating arc (chair-level→names→mobile list→zoom/pan→markers→venue-scale→PDF). SPEC IMPACT: 0008 print/PDF export → DECISION_LOG.

**2026-06-08 — seating: venue dimensions + to-scale tables (0008).** Owner: "set the room's width × length, keep tables at their right size." `event_floor_plan` gains nullable `venue_width_m`/`venue_length_m` (migration `20260923000000`, applied to prod). A "Room size" toolbar panel (toggle + W/L metres) makes the canvas take the room's aspect ratio + draw walls/metric labels, and each table renders at its **true footprint** (`TABLE_FOOTPRINT_M`; round 8/10/12 ≈ 2.5/2.8/3.1 m) via a ResizeObserver `pxPerMeter` × per-table `scale()` — composes with the zoom/pan + LOD. No room size → unchanged. `tsc`/`lint`/`build` clean. **Next — PR D:** A4 seating PDF (mood-board/blueprint modes; monogram + names + date + Setnayan logo + website QR; floor-plan page + arrangement pages). SPEC IMPACT: 0008 venue to-scale mode → DECISION_LOG.

**2026-06-08 — onboarding punch-list, Phases A–C ([#1114](https://github.com/iscasasola/setnayan-platform/pull/1114)).** Owner walked `/onboarding/wedding` (~15 min) and filed 11 items; this lands the structural + clarity ones. **Date now precedes the love story** (`name → date → love stage → region → pax → budget`); **love story split to one-story-per-page** (`love_spark` "How you two met" + `love_almost` "The almost", clear titles); the love **timeline year follows the wedding date and shows both years** ("2026 / 2027") until candidate dates narrow to one; **monogram must be finalized** ("Use this monogram") before the name screen's Continue unlocks; **guests start at 200**; **budget opens at the pax-range midpoint**; the **song step shows the playable Top-100 recommended list again** with a pinned search bar (**reverses the 2026-06-05 search-only lock**). `tsc` clean + browser-verified via seeded drafts. **Remaining punch-list (later PRs):** DB-backed refinements + 4:3 carousel card (items 8/9), generate missing photos via Recraft (item 10), background-music player (item 1), monogram→event-icon polish. SPEC IMPACT: item-5 lock reversal + flow reorder → DECISION_LOG.

**2026-06-08 — website Increments C + D: lifecycle phase engine + Editorial recap page (FLAG-DARK).** The interdependent centerpiece of the wedding-website lifecycle, built in parallel (two isolated-worktree agents) + integrated. **C** = the date engine that auto-switches the site RSVP → Event → Editorial by date and gates widgets per the §2 phase matrix; **D** = the post-wedding "newspaper front page" (masthead · headline · template-composed lede · By-the-Numbers impact metrics M1/M2/M3 · milestones timeline · couple pull-quote · always-flattering identity archetype from guest-count × spend). **Behind `WEBSITE_PHASES_ENABLED` (env, default OFF)** → live sites byte-for-byte unchanged until the owner flips it + previews on a real event. **No migration** (uses the `20260912000000` foundation columns + `event_editorial`/`event_vendors.selection_match_rank`/`papic_photos`). `EditorialContent` never throws (every section degrades). Known v1 gaps (flagged in CHANGELOG): LLM auto-composition (v1 = deterministic template), reviews empty-state (system §3 unbuilt), M1 denominator + archetype spend-axis pending data, full-bleed newspaper width + post-event interview + T+3 launch gating = follow-ups. SPEC IMPACT: §1–2 + §6.3–6.8 → DECISION_LOG. **This completes the website build sequence (A.1/A.3/A.4 content · B chrome · C engine · D editorial).**

**2026-06-08 — seating: floor-plan markers — draggable Stage + Entrance door (0008).** Owner: "set the entrance door." New singleton `event_floor_plan` (migration `20260922000000`, applied to prod): stage_x/y + entrance (enabled + x/y), percent coords, Pattern B RLS. The fixed stage banner becomes a **draggable Stage** (Auto-seat now anchors its rings on the placed stage via `computeAutoSeat(stage)` + `fetchFloorPlan`); an **"Add entrance"** button drops a draggable `🚪 Entrance` marker (one, per owner; × to remove). Both persist through the existing Save-layout flow (`saveFloorPlan`); drag generalised to kind table|stage|entrance. `tsc`/`lint`/`build` clean. Completes the seating arc: chair-level → names → mobile list → zoom/pan → markers. SPEC IMPACT: 0008 event_floor_plan (single-entrance) → DECISION_LOG.

**2026-06-08 — vendor tier #1: multiple service listings per leaf (cap 2/2/5/∞).** Owner-clarified "Creating Package" = # of service listings per leaf category. Dropped `vendor_services` 1-per-category UNIQUE + added a per-listing `title` (migration `20260922000000`, applied to prod); `servicesPerLeaf` cap enforced on `createVendorService`; services-page picker now allows re-adding a used category (shows "N added") + a "Service name" input + title on rows; fixed a `lib/budget.ts` map collision (now keeps the cheapest listing per category). First of the owner's "do 1–5" tier queue. `tsc`/`lint` clean. Next: #2 daily capacity · #3 enterprise time-bound slots · #4 Phase C feature gates · #5 Phase D checkout. SPEC IMPACT: → corpus DECISION_LOG.

**2026-06-08 — seating: growable floor plan, zoom + pan + level-of-detail (0008 · PR 2 of 2).** The spatial canvas now scales to many tables: scroll/pinch **zoom**, drag-to-**pan**, **+/- / Fit** controls (Fit frames all tables), and **level-of-detail** — tables become compact pucks (number + fill + group-colour halo) when zoomed out, chairs+names when zoomed in. World transform written to the DOM via refs (no per-frame re-render of 50 tables); table-drag is zoom/pan-aware. `tsc`/`lint`/`build` clean; no migration. **Next — PR 3:** floor-plan markers (draggable **Stage** + **Entrance door**) via a small `event_floor_plan` table; auto-seat then anchors rings to the placed stage. SPEC IMPACT: 0008 auto-growing canvas → DECISION_LOG.

**2026-06-08 — seating: mobile table-card list + Floor-plan/List toggle (0008 · PR 1 of 2).** The chair-level canvas can't fit many tables on a phone (~2-3 max), so small screens now default to a scrollable **table-card list** — each card = colour dot · name · type · fill chip · seated-guest avatars · "Seat here" (when a guest is picked) · expand-to-unseat · delete. Scrolls to any number of tables (50+ fine). Added a **Floor plan / List** toggle (mobile→List, desktop→Floor plan via `matchMedia` on mount; both available everywhere). Reuses the same pick-then-place + group-colour model. `tsc`/`lint`/`build` clean. **Next — PR 2:** canvas pinch/scroll **zoom + pan + level-of-detail** (pucks zoomed out → chairs zoomed in) so the spatial view also scales. SPEC IMPACT: builds 0008's intended mobile surface → DECISION_LOG.

**2026-06-08 — website Increment B: Music & Video hero chrome (LIVE).** Site-chrome layer of the lifecycle (§6.2), after the A.1/A.3/A.4 content blocks — a looping **background song** (opt-in, off by default, never autoplays: a visible "Play music" control lets each guest start/pause it) + a **video hero** that loops behind the monogram (muted/inline, photo becomes its poster). Built on the foundation columns (`20260912000000`) — **no migration**. New `BackgroundMusic` client component + `HeroBackgroundMedia` helper thread through both render paths; new editor `/dashboard/[eventId]/website/site-chrome` (song ≤20 MB + video ≤60 MB uploads + enable toggle), linked from `site-editor`. The shared `/api/upload` route was widened (audio/video MIME + per-type size caps: video 60 MB · audio 20 MB; **image cap stays 10 MB**; existing flows byte-identical) — the owner-approved shared-route change. SPEC IMPACT: §6.2 → DECISION_LOG. Remaining website pieces: the per-phase Date engine (C) + Editorial page (D).

**2026-06-08 — Setnayan AI build PR-5: dependency-awareness engine (§4B).** Encodes the locked planning cascade as DATA: new pure `lib/dependency-graph.ts` (`DEPENDS_ON` one-way edges per category w/ H/s prominence + `MUTUAL_PAIRS` + `resolveDependency`). `buildPlanBudgetModel` builds a satisfied-node set (finalized categories from the model + `wedding_date` + `mood_board`; guest/seating nodes fail-open) and attaches `AccordionChild.dependency`, surfaced as a soft `DependencyNudge` row per category ("⏳ Lock your reception venue first — your Catering matches better once it's set" / "✓ Ready — time to book"). ALWAYS SOFT (never a gate); shown only when Setnayan AI is on AND the category is in its action window (quiet otherwise). No migration (reuses `events.mood_board_updated_at`). `tsc`/`lint`/`build` clean + 14/14 resolver checks. **Deferred:** real detection for the fail-open decision nodes + dependency-aware reminders. Build remaining: code-identifier migration (`todays_focus→setnayan_ai`). SPEC IMPACT: §4B → DECISION_LOG + build-state 🟡.

**2026-06-08 — Setnayan AI build PR-4: last-minute editors (feature now switch-on-able).** The two config surfaces that turn PR-3's dormant engine ON (no migration — PR-3's `20260920000000` is applied to prod). **Admin** (`/admin/taxonomy`): a "Last-minute window start" section to set/clear each category's START months (`setLastMinuteStart`/`clearLastMinuteStart` → `planning_deadlines` kind=`last_minute_start`, audit-logged; filtered out of the deadlines list). **Vendor** (`/vendor-dashboard/services`): a "Last-minute bookings" block (END months "accept until" + 0–100% surcharge) in the Add+Edit forms, wired through `create`/`updateVendorService` + `fetchVendorServices`. End-to-end now: admin sets a category START → vendor sets cutoff/surcharge → couples inside the window see the gold "Last-minute" badge (AI on) or an empty category (free search). Stays dormant until an admin sets a START. `tsc`/`lint`/`build` clean. Build remaining: dependency-awareness engine (§4B) · code-identifier migration. SPEC IMPACT: completes §4 editors → DECISION_LOG + build-state ✅.

**2026-06-08 — website Increment A.4: Our Photos couple gallery (LIVE).** Fourth content block on the lifecycle foundation (after A.1 Special Message + A.3 What to Bring) — a **couple-curated** photo gallery (engagement / pre-wedding shots) the couple uploads themselves, rendered on the live invitation site (`OurPhotosWidget`, responsive grid, both authed-guest + anonymous paths; empty → section hides). Distinct from the existing `your_photos` widget (guest-tagged photos). Migration `20260919000000` adds `events.our_photos JSONB` (array of `r2://` refs, `jsonb_typeof='array'` CHECK) + the `our_photos` widget_type (CHECK recreated cumulatively across all 15 types; seed + backfill) so it joins the show/hide/reorder editor; new editor at `/dashboard/[eventId]/website/our-photos` (multi-image upload, ≤24, host-membership gated). Reuses the already-whitelisted image upload path (zero `/api/upload` change) — sequenced ahead of the Music/Video-hero block, which needs a shared-route MIME/size change. Built independent of the parallel onboarding session. SPEC IMPACT: §6.5 → DECISION_LOG.

**2026-06-08 — Setnayan AI build PR-3: last-minute mechanic (dormant-by-default).** Implements §4 of `What_Is_Setnayan_AI_2026-06-08.md` (owner-locked §9.3) after PR-1 (governing gate) + PR-2 (paid entitlement). New pure engine `lib/last-minute.ts` (months-to-wedding + the three zones Normal/Last-minute/Expired, range `[platform leaf START → vendor service END]`, AI-gated visibility, the AI-off-empty-category edge, surcharge). Wired into the candidate-discovery overlay (`category-search.ts` reads START from `planning_deadlines` kind=`last_minute_start` + END/surcharge from `vendor_services`; drops Expired for all, Last-minute when AI-off, empties a fully-last-minute category in generic search) + a gold "Last-minute" badge in `category-search-overlay.tsx`. Migration `20260920000000` (additive: new `kind` + 2 `vendor_services` columns, **no seed → dormant**). **Zero prod behavior change** until an admin sets a START (the START months are owner-set, not invented); the search only touches the new columns when configured, so it's safe before the migration lands. **Deferred (PR-4):** admin START editor (`/admin/taxonomy`) + vendor END/surcharge editor. **Migration PENDING:** `db push` blocked by parallel-branch ledger drift (`20260919000000`/`20260916000000`) — not force-repaired; apply when the ledger realigns, before configuring START. `tsc`/`lint`/`build` clean + 21/21 engine checks. Build remaining: dependency-awareness engine · identifier migration. SPEC IMPACT: §4/§9.3 → DECISION_LOG + build-state flip.

**2026-06-08 — vendor token purchase: webhook + notifications + history.** Follow-ups to the purchase flow. (1) **Payment webhook** `app/api/webhooks/token-purchase` — HMAC-verified (`TOKEN_PURCHASE_WEBHOOK_SECRET`), auto-confirms a paid `TKN-` reference via a new service-role-only `confirm_vendor_token_purchase_by_reference` RPC (migration `20260918000000` also extracts a shared idempotent credit core + hardens grants: REVOKE from anon/authenticated, closing a self-credit hole). Inert/503 until the owner sets the secret + registers the provider. (2) **Notifications** (`lib/token-purchase-notify.ts` + 2 new `notification_type` values, migration `20260918000100`): admins pinged on a new pending order, vendor pinged (in-app + email via `emitNotification`) on a fresh credit. (3) **Purchase history** card on `/vendor-dashboard/tokens`. `tsc`/`lint` clean; migrations applied to prod; smoke-tested (rolled back). Owner action: provision `TOKEN_PURCHASE_WEBHOOK_SECRET` + provider webhook to activate auto-confirm. SPEC IMPACT: extends 0034 + 0022 → DECISION_LOG.

**2026-06-08 — seating chart: chair-level visual editor + auto-seat (0008).** Brought the seat plan up to a polished reference the owner shared — and, in doing so, built two long-deferred 0008 spec sections. The plain table-shapes + dropdown assigner are replaced by a chair-level canvas (`seating-editor.tsx`): every table draws its seats around a hub, each chair shows the guest's photo/initials in a side/group-coloured ring + name; tap-to-seat / move / unseat; table reposition by hub drag. New grouped, colour-coded sidebar (Tables · Individual Members · custom Member Groups w/ eye-toggle) + "only unseated" filter + search + inline Add-table. **Auto-seat** (`computeAutoSeat` + `autoSeatGuests`) fills unseated *attending* guests into the nearest tables to the stage by role tier (idempotent; skips sweetheart + the couple). No migration (`seat_number` + guest groups already existed). `tsc`/`lint`/`build` clean. **Deferred (the "full rebuild" not picked this pass):** Add-Group colour-picker modal, two-tab layout, zoom, dedicated mobile cards, publish-QR/print pack, serpentine wedge geometry. SPEC IMPACT: 0008 "Chair-level interaction" + "Auto-fill" now built → corpus DECISION_LOG + 0008 AS-BUILT header.

**2026-06-08 — Setnayan AI build PR-2: per-event paid entitlement, behind a default-off flag.** Migration `20260917000000` (prod): additive `events.setnayan_ai_active boolean`. `lib/setnayan-ai.ts` gains `SETNAYAN_AI_PAYWALL_ENABLED` (default OFF) — off = free toggle (PR-1, unchanged), on = requires the purchased entitlement. Activation hook in `admin/payments/actions.ts` stamps the flag on a confirmed `SETNAYAN_AI` order (flat, no expiry). 3 event selects fetch the column. **Inert until the flag flips** — deferred to flip-time: SKU on /pricing+onboarding, buy-routing, `V2_SKU_CODES` sync, homepage "from ₱3,999" copy, and verifying the checkout `order.service_key`. Build remaining: last-minute · dependency engine · identifier migration. SPEC IMPACT: implements the §2/§9 paid gate.

**2026-06-08 — vendor token-pack purchase flow LIVE (apply-then-pay).** Owner: "make purchasing available too" + "manual now, automated later." Vendors can now buy token packs from `/vendor-dashboard/tokens` (was a read-only "purchase opens this week" placeholder). Migration `20260916000000` (prod): `vendor_token_purchases` + `create_`/`approve_`/`reject_` SECURITY DEFINER fns + `is_console_admin()` helper. Vendor starts an order (DB-priced from `vendor_billing_catalog`, `TKN-` ref) → pays BDO/GCash (from `platform_settings`) → admin confirms at new `/admin/token-purchases` queue → wallet credited to the **never-expire `purchased_tokens`** bucket (idempotent). `approve_` is the exact seam a future Maya/PayMongo webhook calls to auto-credit — Phase D automation = a webhook handler, no rebuild. Buy card de-hardcoded (was ₱180–250/token ladder → live ₱100/token). Verified via rolled-back transactional smoke test (price correct · non-admin blocked · +25 purchased / earned untouched · idempotent). This also closes the **Phase D buy-token** item flagged across the 2026-06-07 vendor-tier rows. SPEC IMPACT: 0034 + 0022 + Pricing §0.C → DECISION_LOG.

**2026-06-08 — Setnayan AI governing gate (build PR-1 of N).** New `lib/setnayan-ai.ts → isSetnayanAiActive(event)` is the single chokepoint the whole app consults for "is Setnayan AI on?" Routed the 3 scattered `planning_mode==='manual'` checks through it + closed 2 leaks (reception-proximity sort + "eyeing" nudge now gate on it) so **AI-off → genuinely generic (region-only) site-wide**. Sequenced "govern now (free), monetize next" — PR-2 swaps the gate to the paid per-event entitlement without touching call sites. Remaining build: paid entitlement · last-minute range+badge · dependency engine · code-identifier migration. Spec: `What_Is_Setnayan_AI_2026-06-08.md`. SPEC IMPACT: implements the free-floor↔AI boundary.

**2026-06-08 — "Today's Focus" → "Setnayan AI" UI rename (owner naming lock).** Display-name-only scrub across **66 `apps/web` files** (homepage hero, retired-SKU concierge/settings copy, admin labels, i18n nav, help, metadata, aria-labels) + 3 coherence comment fixes. Prices (₱1,499 prose) and snake_case code identifiers (`todays_focus`/`TODAYS_FOCUS` SKU key, `events.todays_focus_*` columns, `/today` route) **left as-is** — they mirror live DB/catalog/routes, so a true rename is a separate migration (owner to greenlight). Corpus already scrubbed (`What_Is_Setnayan_AI_2026-06-08.md` + DECISION_LOG). SPEC IMPACT: app↔corpus naming aligned.

**2026-06-08 — onboarding prices de-hardcoded → live admin catalog (owner directive).** Owner: "our pricing must not be hardcoded but taken from the admin pricing page." The onboarding services screens (15/16) read SELLING prices from a hardcoded `SVC` constant; they now read the SAME live catalog `/pricing` does. New pure module `onboarding-pricing.ts` (`buildOnboardingPricing` → a plain-JSON `OnboardingPricing` view-model); `page.tsx` fetches `fetchV2CustomerCatalog` + `fetchV2BundleCatalog` in its `Promise.all` (now `force-dynamic`) and passes a `pricing` prop; the shell's `SVC` table is DELETED and every price reads `pricing.svc[k].label`/`.set` with a ₱0 fallback (pax SKU shows "from ₱2,999"). Checkout routing (`INAPP_TO_ADDON_SLUG`) + the authoritative server-side pax charge (`resolvePaxPricedOrderCentavos`) are byte-identical — this is display-only. `tsc`/`build` clean. **Flagged for owner:** bundle membership (`BUNDLE_MEMBERS`) is author-curated (no DB source) and the bundle UI is **deferred** (view-model built but unconsumed); `out` market anchors kept as a labeled illustrative constant. SPEC IMPACT: onboarding now reads live prices from `platform_retail_catalog_v2` + `platform_package_catalog` → DECISION_LOG.

**2026-06-08 — pure-moment conversational onboarding intro (prototype→prod port).** Closed a spec/code drift: the owner-2026-06-05 "pure-moment" welcome (Setnayan speaks line-by-line · role/kind/faith asked inline · no Continue) lived only in the production-mirror prototype `Onboarding_Wedding_Flow_2026-06-01.html`; the live React onboarding still opened on a static hero + 3 Continue screens. Ported faithfully: new `welcome-moments.tsx` (self-contained moment player, live active-faith set, civil-skips-faith, tap-to-skip), step-0 plays the conversation then hands off to the Name screen, bars hidden during it, plain hero on back-nav re-entry; standalone role/kind/faith screens kept as edit targets. `tsc`/`lint` clean. **Flagged (not faked):** the prototype's over-budget venue copy needs per-venue pricing the live venue search doesn't return — deferred for an owner call. SPEC IMPACT: none (matches a locked prototype decision).

**2026-06-08 — website Increment A.3: What to Bring block (LIVE).** Third content block on the lifecycle foundation (after A.1 Special Message) — a couple-curated gift / registry / no-gift note, rendered on the live invitation site (`WhatToBringWidget`, both authed-guest + anonymous paths; blank → section hides). Migration `20260918000000` adds the `what_to_bring` widget_type (CHECK recreated cumulatively with all 14 types; seed + backfill; reads `events.what_to_bring`) so it joins the show/hide/reorder editor; new editor at `/dashboard/[eventId]/website/what-to-bring`. Built fully independent of the parallel onboarding session (own column, own editor). Next blocks (Our Photos · per-phase engine) follow the same pattern. SPEC IMPACT: §6.5 → DECISION_LOG.

**2026-06-08 — website Increment A.1: Special Message block (LIVE).** First content block on the lifecycle foundation — the couple's note to guests, rendered on the live invitation site (`SpecialMessageWidget`, both authed-guest + anonymous paths; blank → section hides). Migration `20260913000000` adds the `special_message` widget_type (CHECK + seed + backfill; reads `events.special_message`) so it appears in the widget show/hide/reorder editor; new editor at `/dashboard/[eventId]/website/special-message`. Next content blocks (Our Love Story · What to Bring · Our Photos) follow the same pattern. SPEC IMPACT: §6.5 → DECISION_LOG.

**2026-06-07 — wedding-website lifecycle FOUNDATION (schema · PR #1060).** Owner locked the couple's event website as **ONE site, three date-driven phases** (RSVP before · Event during · Editorial after) — canonical spec `Wedding_Website_Lifecycle_Spec_2026-06-07.md` + DECISION_LOG. Shipped the safe additive schema (`20260910000000`, applied to prod): `events.*` shared chrome (looping bg music `site_bg_music_source`, scrub-video hero) + auto-editorial storyline inputs (`love_story`/`special_message`/`together_since`/`editorial_tone`/`language`); `event_vendors.selection_match_rank` (Editorial first-pick stat, forward-only); new `event_editorial` snapshot table (draft + frozen impact metrics, RLS couple/moderator/admin). **Deferred:** `invitation_widgets` per-phase (renderer-coupled) + event-review table (reconcile vs `vendor_reviews`). Pricing for the website locked to the 2026-06-07 ladder (RSVP/Event Website ₱1,999 · Editorial ₱7,999 · Essentials ₱12,999 · Complete ₱27,999). SPEC IMPACT: lifecycle model in DECISION_LOG; 0002/0021/0031 fold-in pending.

**2026-06-07 — vendor tier Phase B: count caps (agents · portfolio · parent categories).** Enforced 3 of the 5 matrix caps (app-layer, helper-driven, no migration): **agent accounts** (FREE 0 / VERIFIED 1 / PRO 3 / ENT ∞) on team invite; **portfolio photos** (30/50/100/∞) on profile save + tier-driven `maxFiles` UI (was hardcoded 10); **parent categories** (1/3/3/∞) on service create (via `tilesForVendorCategory`→`TILE_PARENT`). **Deferred (blocked — owner input needed):** packages-per-leaf (`vendor_services` already UNIQUE 1/leaf; `vendor_packages` has no vendor create path) + slots-per-day (no slot ledger; `slotsTimeBounded` needs schema). `tsc`/`lint` clean. Next: Phase C feature gates, Phase D self-serve checkout. SPEC IMPACT: → corpus DECISION_LOG.

**2026-06-07 — FREE may buy tokens (client import).** Owner clarification: FREE stays blocked from in-app customers (already enforced — no change), but FREE may now BUY tokens to import their clients (overrides the sheet's "Not Allowed"). `canBuyTokens()` returns true for all tiers; FREE's only sink is the 1-token Import Customers gate (in-app stays tier-blocked). Buy-token checkout + import token-charge = Phase D. `tsc`/`lint` clean; no migration. SPEC IMPACT: matrix doc buy-token row → corpus DECISION_LOG.

**2026-06-07 — vendor tier sheet REISSUE: reprice + verified-free + token bundle.** Owner reissued the sheet (prices ↑, capabilities same) + confirmed two points: verified is FREE (revert Phase A's verified-burn), and grant the subscription token bundle on admin tier-set now. Migration `20260911000000` (prod): reprice Pro **₱6,000/28d·₱60,000/yr**, Ent **₱10,000/28d·₱100,000/yr** (round, breaks charm-lock — owner-set); `unlock_vendor_event` now **FREE-VERIFIED = free (≤10/wk, 0 tokens)**, only PRO/ENT burn; `setVendorTier` grants the bundle (Pro +30 / Ent +100, idempotent). Helper updated (prices, `verified.inAppGated=false`, `TIER_SUBSCRIPTION_BUNDLE_TOKENS`, `canBuyTokens` FREE=✗). `tsc`/`lint` clean. Buy-token flow (₱100/tok, FREE-not-allowed) + per-renewal bundle = Phase D. SPEC IMPACT: tier matrix doc updated → corpus DECISION_LOG.

**2026-06-07 — vendor tier matrix Phase A: foundation + pricing + tier-gated burn.** Owner provided the canonical 4-tier capability matrix (FREE/FREE-VERIFIED/PRO/ENTERPRISE); audit found ~13/19 rows unenforced + `tier_state` inert. Owner: build everything (phased), matrix prices win, all paid tiers burn (only FREE blocked). **Phase A of 5 shipped:** `lib/vendor-tier-caps.ts` (the matrix in code — single source for all phases); migration `20260910000000` aligns catalog prices (Pro ₱3,999/mo·₱39,999/yr, Ent ₱9,999/mo·₱99,999/yr; Pro caps 3 cats/3 seats) + **tier-gates `unlock_vendor_event`** (FREE blocked · VERIFIED ≤10 new unlocks/week + burns · PRO/ENT unlimited + burns; re-accept free); `acceptInquiry` shows tier-appropriate messages; **admin tier control** (`setVendorTier` + selector on `/admin/vendors/[id]/tokens` — the only way to reach Pro/Enterprise until self-serve checkout). Migration applied to prod; `tsc`/`lint` clean. **Next:** Phase B count caps · C feature gates (incl. name-reveal/searchability by tier_state) · D self-serve subscription checkout. SPEC IMPACT: tier matrix canonical (supersedes tier-ladder) → corpus DECISION_LOG.

**2026-06-07 — login-driven ghosting nudges, no cron (PR 2 of 2).** Owner: check at login, not via a background cron (won't scale to 250k vendors / 1M accounts). The previously-dead `users.last_login_at` is now written at login (`stampLastLogin` in password sign-in + `/auth/callback`); a new `last_ghost_check_at` gates the work to once per login. `runLoginGhostingCheck` (`lib/ghosting.ts`) runs from the customer + vendor dashboard layouts via `after()` (post-response): finds `inquiry_status='pending'` threads older than 48h (indexed accept-gate signal) and nudges — couple ("a vendor hasn't replied → explore alternatives", `inquiry_no_response`) or vendor ("inquiries awaiting your reply", `inquiry_awaiting_reply`). Replaces the never-built cron-based `vendor_unresponsive_48h`. Migration `20260909000000` applied to prod. `tsc`/`lint` clean. SPEC IMPACT: 0028 ghosting = login-driven → corpus DECISION_LOG.

**2026-06-07 — token economy connected: burn-on-answer + anti-merge (PR 1 of 2).** Owner: "connect the tokens properly." The vendor token economy was inert (vendors answered inquiries free). Wired the owner-locked burn-on-answer: accepting an inquiry burns ONE idempotent unlock per (vendor, event) — banded by the wedding's region (₱100/200/300 = 1/2/3 tokens via the new admin-editable `token_burn_bands`, seeded + editable at `/admin/token-bands`) — covering ALL the vendor's services for that event; insufficient balance blocks the accept with a top-up prompt; re-accept never double-charges. New `vendor_event_unlocks` (idempotency) + `unlock_vendor_event` RPC (atomic, ownership-checked, rolls back on shortfall). **Free tokens for new vendors (strict):** the 100-on-verification grant already ships (verified-gated, idempotent); this PR adds the "never merged" half via immutability triggers making `vendor_id` un-reassignable on all four token tables (wallet/vouchers/grants/redemptions). Migration `20260908000000` applied to prod. ⚠ Owner to ratify the band→region map + confirm which of the two "approve" surfaces is the canonical verification gate (only the `verification_state` one grants). Next: PR 2 = login-driven ghosting (no cron). SPEC IMPACT: vendor token model → corpus DECISION_LOG.

**2026-06-07 — app-wide loader sweep (customer + vendor + admin).** 3-agent completeness audit after the one-off misses. Coverage is strong — 164/167 loading.tsx use the shared system; every customer + vendor data route has a matched loader (vendor side clean, 28/28). One real gap found + fixed: `admin/notifications` (async, fetched notifications, no loading.tsx → blank) now re-exports `ListPageSkeleton`. Two bespoke vendor-page spinners were left as-is (owner-designed 2026-06-05, already narrate via LoadingStatus). SPEC IMPACT: none.

**2026-06-07 — notifications loading: branded, layout-matched shell.** Customer flagged Notifications showed "no load" — the route had only a bare `ListPageSkeleton` (generic blocks, no narration) that flashed imperceptibly. Swapped for a purpose-built `loading.tsx` mirroring the page (back link + header + rows) + the branded `LoadingNarration` strip. The "More" tab was also flagged but is a static link-menu (no data fetch → nothing to load → intentionally no loader). `tsc`/`lint` clean. SPEC IMPACT: none.

**2026-06-07 — onboarding-completion overlay → `<SDLoader>`.** Owner spotted (live) that the post-onboarding "Creating your personalized dashboard" overlay still used the OLD loader (thin ring spinner + static mark + cycling sub) — a bespoke loader the targeted rollout had missed. Swapped it for the brand `<SDLoader>` (steps = Analyzing/Matching/Building, hint "Personalizing"), keeping the premium serif title; removed the redundant `finStage` cycling + dead `.fin-mark/.fin-spinner/.fin-sub` CSS. `onboarding-shell.tsx` + `onboarding.css`; `tsc`/`next lint` clean; harness-verified (mobile). SPEC IMPACT: none.

**2026-06-07 — cross-actor signal wiring: closed the silent one-way breaks (0022/0023/0028).** A dedicated cross-actor audit (customer action → vendor signal → admin control) found the only two-way couple↔vendor channel was the inquiry chat; finalize/book, review, and cancel all wrote `event_vendors` (couple-only RLS) and never reached the vendor. **Wired 4 vendor signals** via the canonical dual-channel `emitNotification` (new enum values, migration `20260907…` **applied to prod**): `booking_confirmed` (marketplace lock — the #1 break), `review_received` (also makes the Reviews-page "we notify you" claim true), `booking_cancelled` (consolidated off the prior email-only path), `dispute_filed` (named vendor on a scoped force-majeure flag). **Admin governance:** `/admin/disputes` is now resolvable (was read-only → "edit in Supabase Studio"), and a new `/admin/notifications` reader + top-bar bell surfaces the rows admins already received but couldn't see. **Scoped-not-built** (need owner sign-off): token burn-on-answer, two-admin approval gate, anti-fraud fake-event surface, chat moderation, vendor-unresponsive/ghosting escalation. `tsc`/`lint`/email-link guard green. SPEC IMPACT: 0028/0022/0023 → corpus DECISION_LOG.

**2026-06-07 — app cold-start ("initialization") splash (web + native).** Closes the boot moment: the animated brand mark on Warm Alabaster over the first app-route / native-shell paint of a session, then fades. One impl covers both — the native Capacitor shell loads the hosted web app, so the SSR boot splash becomes the native cold-start splash (static native OS splash bridges launch→WebView, `NativeBridge` hides it, animated splash takes over; no native rebuild). apps/web only: inline gate script in `layout.tsx` (sets `data-sn-boot` on app routes `/dashboard`·`/vendor-dashboard`·`/admin` OR native, once per session via sessionStorage), `#sn-init-splash` markup, `AppInitSplash` client remover (450ms web / 750ms native hold), `globals.css` styles + CSS-only ~4s failsafe. **Gated OFF marketing/legal** → SSR content + Lighthouse/LCP untouched. `tsc`/`next lint` clean; harness screenshots (mobile + desktop) confirm cover + fade-reveal. SPEC IMPACT: none.

**2026-06-07 — first-load gap sweep: skeleton `loading.tsx` on 8 in-app data routes.** Follow-up to `<SDLoader>` — closed the screens that flashed an empty area on first load. Audit: 158 routes already had a loader; the gaps were admin/vendor data surfaces. Added layout-mirroring **skeletons** (not the brand loader — principle: skeleton for content/list/form, brand loader for personalized/blocking, nothing for static/print): `admin/{budget-planner,growth}` (Grid), `admin/{payment-options,wedding-traditions,wedding-types}` (List), `admin/connection-logs` (Table), `vendor-dashboard/branches` (List), `vendor-dashboard/payment-options` (Form). One-line re-exports matching the `admin/telemetry` convention; purely additive. Skipped static marketing/legal + print + `api/v1` + the `for-you` redirect (no real gap); the 4 `Suspense fallback={null}` are intentional invisible widgets. `tsc`/`next lint` clean. SPEC IMPACT: none.

**2026-06-07 — shared brand "thinking" loader (`<SDLoader>`) from the Organic loaders handoff.** Native React port of the owner-supplied loader (gold particles → mark · twin orbit · narrated steps · `Ready ✓` ring). Gold bound to the locked `--m-orange` (#C5A059, not the handoff's #c69a4b); reuses the app's narration fade + global reduced-motion freeze. New `components/sd-loader/` (`SDLoader` + `LoaderOverlayProvider`/`useLoader()` + editable `LOADER_STEPS`). **Targeted rollout (owner choice), NOT a literal "replace all 158 loading states"** — the layout-mirroring route skeletons + `LoadingNarration` stay (replacing them would regress route UX); the brand loader covers full-screen blocking + heavy "personalized work" moments. Wired into: app-wide overlay (`providers.tsx`), **login boot** ("Signing you in…", via a `useFormStatus` bridge — no auth-action change), **order-and-pay completion** (`inline-checkout-drawer.tsx` → `Ready ✓` "Order sent"), and a new **monogram route loader** (additive). Copy lives in `loader-steps.ts`. `tsc`/`next lint` clean; fidelity confirmed via harness screenshot. SPEC IMPACT: none (UI infra; no schema/SKU/scope change).

**2026-06-07 — Connection Logs batch 2: server-action save failures wired (0023/0035).** Second instrumentation batch. Batch 1 + #1046 covered client-side; this covers **server-side save failures** via `insertFaultLog()` (the app is server-action-heavy, so that's where DB writes actually fail — the whole `actions.ts` layer was uninstrumented). 8 parallel discovery agents → **21 sites across 8 files**, all tapping existing supabase-write error branches: money (checkout order/payment/voucher · orders create/log/self-comp · admin payments approve/promote/confirm), booking (vendors finalize/cancel/add), event core (save date/ceremony/step), guest-facing (RSVP submit — was silently swallowed · quick-add guest/role), profile (save info/password). Purely additive (`await insertFaultLog` before existing return/throw; ids-only payloads, also redacted by #1046's chokepoint). No migration; CI typecheck = gate; SPEC IMPACT none.

**2026-06-07 — native store-prep: plugins + Android BACK fix + branded icons + signing/deep-link scaffolds (0052).** Owner: "prep for both app stores." After a 42-finding multi-agent audit (remote-URL approach validated; OAuth-in-WebView latent behind off flags; the one real defect = Android BACK button exits the app). **apps/mobile:** +4 plugins (`@capacitor/app`/`splash-screen`/`status-bar`/`keyboard`), **branded launcher icon + splash** via `@capacitor/assets` from the real PWA icon (replaces the stock robot), **deep-link intent-filters** (App Links `/dashboard` + `setnayan://`), **release signingConfig** reading a gitignored `keystore.properties`, splash/keyboard config — `gradlew assembleDebug` BUILD SUCCESSFUL. **apps/web (zero new deps, runtime `window.Capacitor` global):** `NativeBridge` fixes the BACK-exits bug + hides splash + status-bar insets + `appUrlOpen`; `'capacitor'` client-type → 10-yr cookie/aggressive refresh; `.well-known/{assetlinks.json,apple-app-site-association}` scaffolds + middleware exclusion + AASA `application/json` header — `tsc` + lint green. **Deferred (device/owner-gated):** Papic native camera wiring (needs device), OAuth-via-system-browser (before enabling social login), iOS project (Xcode + CocoaPods), release keystore + Apple enrollment + real `.well-known` hashes. Build env reused from `~/.setnayan-toolchain`. SPEC IMPACT: 0052 → corpus DECISION_LOG + 0052 rewrite.

**2026-06-07 — Connection Logs: 5 more buttons wired, additive to #1046 (0023/0035).** Parallel follow-up to the Connection Logs tracker. Wired `trackFailure()` into 5 high-value client-side error branches that PR #1046's 19-site sweep did **not** cover (verified non-overlapping): **chat send** (send form, vs #1046's stream/receive side), **payment-order submit** (checkout / money), **LED save**, **moodboard pick save**, and **add-guest**. Purely additive (taps existing `catch`/`!ok` branches; ids-only payloads, which #1046's `insertFaultLog` redaction also scrubs). Rebased clean on #1046 — the onboarding-commit site I'd also picked was already done there, so I took theirs (no dup). No migration; SPEC IMPACT none.

**2026-06-07 — Connection Logs wired into 19 call sites + Sentry capture gaps fixed + payload PII redaction (0035).** Follow-up to the Connection Logs feature below. (1) **Sentry** was not broken as the task assumed (config is sound + LCP-optimized) but had two real gaps, now fixed: `app/global-error.tsx` never called `Sentry.captureException` (root-layout crashes were unreported despite the "we've logged it" copy); and `deferred-observability.tsx` set `replaysOnErrorSampleRate` without registering `replayIntegration()` (replays never recorded). (2) **PII redaction** added to `insertFaultLog()` (new `lib/telemetry/redact.ts`) — the single `app_telemetry_logs` write chokepoint now strips PII-shaped keys + caps sizes, closing the RA 10173 gap (the ingest route previously stored payloads verbatim). (3) **19 call sites across 13 files** instrumented with `trackFailure()`/`insertFaultLog()` — badges, chat, uploads, vendor planning, wizard cards, attire guide, onboarding — completing the "call sites not yet instrumented" follow-up. **No `client_interaction_errors` table** (the task's literal ask): the Connection Logs substrate already covers it; building it would duplicate + re-introduce the rejected anon-`.insert()` (owner decision 2026-06-07). `typecheck`/`lint`/`build` green. **Owner action:** set `SENTRY_DSN` + `NEXT_PUBLIC_SENTRY_DSN` in Vercel Production to actually enable capture (OWNER_ACTIONS #19e). Guide: `TRACKING_STATUS.md`. SPEC IMPACT: 0035 → corpus DECISION_LOG.

**2026-06-07 — native mobile shell bootstrapped: Capacitor remote-URL wrapper, Android APK built (0052).** Owner started the Capacitor native-app setup. Caught that the tutorial's `output: 'export'` (static export) would break the server-driven app (111 Server Actions · 60 API routes · middleware auth) — left `apps/web`/`next.config.ts` untouched and instead added a self-contained **`apps/mobile/`** package (npm-managed, **excluded from the pnpm workspace**) using the **remote-URL** pattern: the native WebView loads the hosted app (`https://www.setnayan.com`) with Camera/Network/BLE plugin bridges. Android project generated + permissioned + offline-fallback-wired (`MainActivity` → `BridgeWebViewClient`) and **built green** (`gradlew assembleDebug` → BUILD SUCCESSFUL; `app-debug.apk` 7.8 MB, verified perms + bundled offline page + baked `server.url`). Toolchain (JDK 21 + Android SDK 36) installed user-local under `~/.setnayan-toolchain/` (no sudo). **iOS deferred** — needs the owner to install Xcode (App Store) + CocoaPods; `cap add ios` then mirrors the Android setup. **Known gaps:** offline fallback compile-verified not runtime-tested (no AVD); web-side `@capacitor/core` bridge (Papic capture first) is a future `apps/web` change. SPEC IMPACT: 0052 — remote-URL shell (not static export); native = V1.5/Phase 2 → corpus DECISION_LOG.

**2026-06-07 — Connection Logs: real-time admin fault tracker (0023/0035).** New internal observability surface at **`/admin/connection-logs`** — captures front-end faults (broken buttons · failed Supabase saves · blank fallbacks) via a client-safe `trackFailure()` helper and streams them live to admins with a resolve/ignore/auto-clear lifecycle. Standalone by design (complements Sentry + the backend `telemetry_events` viewer). New table `app_telemetry_logs` (RLS admin-read; service-role insert only — no anon-writable table); ingest via `/api/telemetry/client-fault`; code-level auto-clear via `/api/telemetry/auto-resolve` (`{file_path}`). Adapted the brief's two off-pattern bits (owner-confirmed): `src/utils/` → `lib/telemetry/` (App Router); direct anon `.insert()` → server-route + service-role. Guide at `ADMIN_LOGS_GUIDE.md`. **Migration `20260902000000_app_telemetry_logs.sql` must be applied** (`supabase db push`). CI = proof (fresh worktree, no node_modules). **Call sites now instrumented** (19 sites, 2026-06-07 — see the entry above) + payloads PII-redacted server-side. SPEC IMPACT: 0023 (+1 admin surface) / 0035 → corpus DECISION_LOG.

**2026-06-06 — onboarding Purchase Now jumps to the in-app checkout card (0016).** Owner — Purchase Now (step 16) should land on a payment card, not the generic Services tab. Prod check ruled out the "VendorDirectPay" route (all 225 `is_setnayan_service` vendors have 0 payment methods → empty card); owner chose the in-app BDO/GCash checkout card (works today via platform config + 0034 order/reference). New `INAPP_TO_ADDON_SLUG` map → `goToDashboard` routes Purchase Now to `/add-ons/[slug]` (`InlineCheckoutDrawer`) for the first picked service with a built checkout page (papic_seats/animated_monogram/panood/custom_qr/indoor_blueprint); the rest payable on the Services tab; falls back there when no pick is mappable. `onboarding-shell.tsx`; no migration; Vercel preview = proof. SPEC IMPACT: 0016 §3.2 16 → corpus DECISION_LOG. Follow-up: build checkout pages for the other 9 in-app services.

**2026-06-05 — onboarding end screens: match-gated inquiry stepper (no toggle) + Boost/Picks fit-to-screen (0016).** Owner punch-list. **Reach my best matches (14):** dropped the on/off toggle — the 1–5 "inquiries per category" stepper shows directly; the card is gated on REAL AI matches (reuses `getOnboardingVendorCounts`, which returns null when none → `matchAvail`), and `sendTopInquiries` is driven from it (auto-on iff matches exist, else hidden + no fan-out; RA 10173 — disclosed, no opt-out now beyond "no matches"). **Boost & enhance (15):** the big poster stretches to fill + the carousel snaps to the bottom (budget/role photo-fill pattern). **Your picks (16 · last page):** fits the viewport — grand-total pinned top, picks list framed + scrolls in between (`svc-rows-scroll`), totals+buttons pinned bottom; removed "You save ₱… vs hiring elsewhere" (+ the unused `saveTotal`). `onboarding-shell.tsx` + `onboarding.css`; no migration; Vercel preview = proof. SPEC IMPACT: 0016 §3.2 rows 14/15/16 → corpus DECISION_LOG.

**2026-06-05 — onboarding canonical-fields close-out: role · area picks · services-to-look-for · basic moodboard now persisted + every-leaf recommendations (0016).** Owner locked the onboarding data contract (19 outputs + #20). Closed build-plan **G1–G4** (zero migration): **role** → `event_moderators` (signing user = the event's first host, bride/groom/helper from screen 2 — was dropped); **area picks** → `style_preferences.search_areas`; **services-to-look-for** (taxonomy picks) → `style_preferences.interested_categories`; **basic moodboard** → `style_preferences.basic_moodboard` (the `FEELS` palette). **#20:** `PICK_TO_INAPP` expanded to all ~53 leaves + recommendations uncapped (dedup bounds to ≤14) so every chosen leaf surfaces a matched in-app add-on. `actions.ts` + `onboarding-shell.tsx`; no migration; Vercel preview = proof. SPEC IMPACT: 0016 §3.0a (G1–G4 closed) → corpus DECISION_LOG.

**2026-06-05 — onboarding congrats (13) decluttered: removed savings stat-strip · inquiry opt-in · personalization note (0016).** Owner — the "You did the hard part" screen keeps the full data recap + live countdown but drops three blocks layered over it: the savings stat-strip (₱/hours/"N that fit your wedding · from M"), the "Keep Setnayan AI helping finish your wedding" inquiry opt-in, and the "✦ … Personalize my matches" note. Savings still shows on Your Plan (14) + services grand total (16); the inquiry opt-in still lives on Your Plan (14) binding the same state (no functionality lost). Also removed the now-dead `vendorCounts` state + `getOnboardingVendorCounts` fetch (sole consumer was the removed tile). `onboarding-shell.tsx`; no migration; Vercel preview = proof. Partly reverses the 2026-06-05 "also surface inquiry opt-in on congrats." SPEC IMPACT: 0016 §3.1a 13 → corpus DECISION_LOG.

**2026-06-05 — desktop guest list = row/table; mobile stays the photo grid (0001).** Owner: *"guest on desktop mode will be row/table style not grid style."* Reverted desktop (sm+) from the photo-card grid back to a **row/table** (`DesktopRow` + a round `RowAvatar` showing the photo, initials fallback). Importance order carries over (Bride #1 · Groom #2 · then role); when grouped the table breaks into tier sections with a header row, else flat. Thead checkbox = select-all (separate header removed). **Mobile unchanged — still the tiered photo grid** (couple 2-up · roles 2-up · guests 3-up). Same `sections` + `guestSelection` store → SelectionBar / carousel lockstep / select-all / swipe untouched; dropped the dead desktop grid `cols`. `tsc`+`lint`+`build` green; no migration. SPEC IMPACT: 0001 → DECISION_LOG + 0001 amendment.

**2026-06-05 — onboarding end screens: congrats full recap + live countdown · services summary 20% promo + grand total + pick-matched recommendations (0016).** Owner punch-list. **Congrats (13)** now summarizes EVERYTHING gathered (couple + helper · type + religion · dates · budget · target locations · taxonomy picks · reception/ceremony/catering/photo-video · song list · mood board · shortlisted) + a live HH:MM:SS countdown to the nearest picked date. **Services summary (16):** pick-matched recommended add-ons pre-added (removable, tagged "Recommended") next to the couple's picks · a 20% onboarding promo (struck total → discounted) · a grand total of money + hours saved + the onboarding duration ("you did all this in X minutes") · free link reworded "Will purchase later, continue for FREE." `onboarding-shell.tsx` + `types.ts` (startedAt + servicesSeeded) + `onboarding.css`; no migration; Vercel preview = proof. SPEC IMPACT: 0016 §3.1a 13/16 + §3.2 → corpus DECISION_LOG.

**2026-06-05 — onboarding Style steps: reception/ceremony hero-on-top · persistent carousel end-line · service-style 2×3 grid (0016).** Owner punch-list on screen 9 (picker) + 10 (Style sub-stepper): (1) photo-card rows now keep a **vertical end-line after the last card even when the row fits** — re-shown on `.pickrail`/`.car`/`.strip`, chip rails stay clean, line stretches to card height. (2) **Reception + Ceremony** re-laid out **like the Kind screen** — the SELECTED option's photo as a captioned hero on top (updates on tap), the choices as the smaller `strip` carousel below (was a big-card `.car` carousel). (3) **Service-style** is now a **2×3 grid of equal-size buttons** (Plated·Buffet·Family-style / 🕌 Halal·Alcohol-free·Stations), replacing the two chip carousels. `onboarding-shell.tsx` + `onboarding.css`; no migration; Vercel preview = proof. SPEC IMPACT: 0016 prototype + Blueprint §3.1a → corpus DECISION_LOG.

**2026-06-05 — Guest grid tiers by importance, per-tier densities (0001).** Owner: *"bride and groom will share same row / wedding party will be 3 per row / Guests will be 4 per row."* On the importance sort (the default), the photo grid now breaks into **role-tier sections** with subtle headers: **Bride & Groom share a 2-up row**, every special-role tier (VIP family → Wedding Party → sponsors → bearers/flower girl → officiants) runs **3-up**, plain **Guests 4-up** (desktop; mobile scales to 2-up roles / 3-up guests). Empty tiers skipped; any non-importance sort = one uniform grid. New `importanceGroupOf` keeps sections consistent with the sort for multi-role guests. Every card still drives the same `guestSelection` store → SelectionBar / carousel lockstep / select-all / swipe untouched. `tsc`+`lint`+`build` green; no migration. SPEC IMPACT: 0001 → DECISION_LOG.

**2026-06-05 — Guest list defaults to importance order (0001).** Owner: *"guest is always arranged based on their importance in the wedding. Bride will always be #1 then groom. then everyone else follows depending on their role."* New canonical `ROLE_IMPORTANCE` in `lib/role-groups.ts` (Bride → Groom → VIP family → wedding party → principal → secondary sponsors → bearers/flower girl → officiants → guest; mirrors the existing `RoleGroup` order so sort + sidebar + bulk picker agree). **Importance is now the DEFAULT sort**, and **Bride #1 / Groom #2 are pinned first under EVERY sort** (the couple = the event foundation). A guest ranks by their *most important* role (primary or extra); ties break by name. Retired the alphabetical-by-enum "Role" sort (meaningless for a wedding). Photo grid + mobile carousel inherit the order for free. `tsc`+`lint`+`build` green; no migration. SPEC IMPACT: 0001 → DECISION_LOG.

**2026-06-05 — Guest list is a photo grid; guests bring their own photo (0001/0012).** Owner: *"guest list will be grid style now. since we want them to have photos."* The couple's guest list (desktop table + mobile stacked list) → a responsive **photo-card grid** (`GuestCard`, portrait photo-or-initials; all selection / bulk role·side·group·delete / swipe-to-delete lockstep preserved). Photos are **guest-supplied**: a **Gmail-login avatar** (captured at join, priority-guarded so it never clobbers a selfie) OR an **RSVP selfie** taken front-camera on the `/[slug]` invitation — prominent but **skippable**, behind a separate RA 10173 biometric-consent checkbox. The selfie is **face-recognition grade** (advisory in-browser MediaPipe gate — 1 face/size/frontal/brightness — dynamically imported so it stays off the 200KB shared bundle) and lands in a new per-event `guest_face_enrollments` table that **Papic (0012) will consume** to auto-tag candids (matching/embeddings stay a later build; schema ready). Withdrawal both sides: guest "Remove my photo & face data" + the couple's existing `photo_consent` toggle now revokes the enrollment. 2 migrations applied to prod; `tsc`+`lint`+`build`+`bundle-size` (199.1KB/200KB) green. SPEC IMPACT: 0001/0002/0012/0025 + DECISION_LOG → corpus.

**2026-06-05 — Home: live HH:MM:SS countdown + roadmap shows 3-at-a-time with overdue flag.** Owner punch-list on the couple Home. **(1) Countdown** — under the big day count, a per-second **HH:MM:SS** ticking the time left in the current PH day (i.e. when the day number drops). Confirmed the countdown already anchors on **PH-midnight (12MN)** of the date (`…T00:00:00+08:00`), never a ceremony/church-schedule time — so only the ticker was new; `aria-hidden` (no per-second SR spam), SSR-seeded (no hydration mismatch). **(2) Things to complete** — now **3 at a time**, **overdue-first**, refilling as items complete; an item past its ideal band gets an amber **Overdue** badge (owner: "badge + always surface"). `lib/wedding-roadmap.ts` gains `idealByMonths` + `overdue` + a `limit` param; the "X/11 done" count still spans the full flow. `tsc`+`lint` clean; **17/17** roadmap assertions + countdown math checked (Jun-5→Dec-8 = **186 days**, matches the owner's screenshot); no migration. Builds on the #1032 auto/manual split. SPEC IMPACT: 0021 Home → corpus DECISION_LOG.

**2026-06-05 — onboarding: heart (save) button on the in-app services carousel.** Owner wanted a heart on each in-app-service poster (alongside what-it-does · benefits · outside price · Setnayan price — all already shown on the "Boost & enhance" carousel, screen 15). Added a heart-toggle overlay on the detail poster, reworded the CTA `+ Add` → `♡ Save / ♥ Saved`, and swapped the carousel-chip `✓` → `♥` — all binding the SAME `interestedServices` save state (no new state; drives the screen-16 summary). `tsc`+`lint` clean; no migration. SPEC IMPACT: none → corpus DECISION_LOG.

**2026-06-05 — onboarding: inquiry opt-in also surfaced on the congrats screen.** Owner wanted "Keep using Setnayan AI to help finish your wedding" + the 1–5 inquiry question on the "You did the hard part" screen (13). The control already lived on Your Plan (14) — `sendTopInquiries` toggle + 1–5 `inquiriesPerCategory` stepper (default 3, auto-inquires best-fit per category at terminal commit). Owner chose "also surface on congrats": a 2nd instance on step 13 binding the SAME state (no new state/CSS; commits once → no double-send). Stays opt-in. `tsc`+`lint` clean; no migration. SPEC IMPACT: none (presentation of an existing control) → corpus DECISION_LOG.

**2026-06-05 — onboarding: every Style-step selector is a swipeable carousel.** Owner: *"make these carousel … we will not have buttons anymore … like service style — make them all carousels."* Reception + Ceremony photo grids → big-card `.car` carousels (one tall venue card + peek, fills the viewzone); Service-style / dietary / photo-need / coverage chip rows → horizontal chip rails — all via the existing `<Rail>` (fade + chevron "more →"). **Continue stays.** Generalises the faith-chip carousel (2026-06-04) to the Style steps; Catering/Photo cuisine·look strips were already carousels. Verified real CSS × real DOM in a static harness at 402px — immersive swipe cards + chip rails, no vertical scroll. `tsc`/`lint`/`build` via CI. SPEC IMPACT: prototype Reception/Ceremony grids + wrapping chips superseded → corpus DECISION_LOG.

**2026-06-05 — Onboarding venue search: serviceability rings (region rings, no longer hard-drops).** Owner design lock. Find-first-vendor (step 12) now shows in-area venues under "★ Matches your preference", then **"Farther afield — outside your area"** behind **Expand search** — out-of-region venues that still pass every other leaf dim (capacity/ceremony/venue_type/date, all already hard-removed) are no longer dropped; region **rings** instead of hard-filtering. `searchOnboardingReceptionVenues` runs a 2nd unscoped pass → `tier:'native'|'travel'`; render splits + adds a 🚫 remove-note and a reception-anchor bridge note (later vendors ringed by who can reach the venue, far ones flagged "travel fee may apply"). `tsc`+`lint` clean; no migration. Founder-only marketplace → travels usually empty until density grows. SPEC IMPACT: region hard→ringed (corpus DECISION_LOG + prototype step 13). Deferred: budget demote + style ring-split (need price/setting fields).

**2026-06-05 — fix: un-stretched the Church ceremony photo (onboarding Style step).** Owner flagged the **Church** card looking stretched (couple too tall/narrow). The five `ceremony_*.webp` were squished from 1820×1024 into 520×520, baking a ~1.78× vertical stretch; `.pimg.haspic` is `cover`, so it's the asset not the CSS. Regenerated `ceremony_church.webp` (+ proto mirror) by reversing the squish (×1.778 → cover-crop to 520²) — same approved photo, natural proportions, 43 KB. Only church read as broken; the other 4 untouched. No code/migration. SPEC IMPACT: corpus copy + DECISION_LOG (squish→cover-crop) refreshed directly.

**2026-06-05 — Your Plan v2: à-la-carte in-app services + 1–5 inquiry stepper (Screens 14–16).** Owner punch-list. **Bundle retired**; new **Screen 15** (in-app-services carousel + per-service detail with savings, multi-select) + **Screen 16** (interested summary + **Purchase Now** → commits + persists picks to `style_preferences.interested_services` + routes to the dashboard Services tab to pay per service via 0034; + continue-free). Inquiry opt-in → **"Reach my best matches" + 1–5 per-category stepper** (default 3); fan-out inquires top-N per category via `unlockCategoryWithInquiry({count})` (count default 1 → dashboard caller unchanged). Flow 15→17; terminal commit moved 14→16. `tsc`+`lint`+`build` green; no migration. Owner-checked before merge. PR #1021. SPEC IMPACT: Blueprint §3.2 + prototype + DECISION_LOG (corpus).

**2026-06-05 — vendor payment-options fast-follow: server-side QR decode (0034).** The QR method's destination is now **decoded server-side** (`sharp` → `jsQR` on the uploaded image) so it's Setnayan-verified, not vendor-typed — the anti-swap guarantee; unreadable images fall back to the vendor's note + route to admin review. Added `sharp` dep + `serverExternalPackages`. Full CI green; decode proven end-to-end. The other deferred fast-follow (workspace-page settlement mount point) was already wired in main. PR #1019.

**2026-06-05 — Wedding Roadmap · free "things to complete" on Home (0021).** Owner: *keep the roadmap, drop the automation.* After removing paid Today's Focus, the couple Home now has a simple **"Things to complete"** list — the ordered wedding tasks (11 items · 12+/9–12/6–9/4–6/2–4 month bands), timed by months-to-EARLIEST-date, where each item is done when **auto-confirmed OR tapped done** (persisted in `events.roadmap_completed` · **applied to prod**). **Hybrid auto/manual — refined same day (owner: "some manual, some automatic · like date, finalize venue"):** 8 of 11 items auto-check from hard structural signals (date committed · a vendor at status contracted+ in that category · budget / guest / table counts · a paid capture order); the 3 soft items (reception look · save-the-dates · invitations) plus any auto item the app can't confirm (e.g. civil / same-venue → no ceremony-venue signal) keep the manual Done button, so nobody is stuck. Still NOT Today's-Focus inference — deterministic facts only. New `lib/wedding-roadmap.ts` (pure: months-out due + `RoadmapSignals` auto/manual done + count) + self-fetching `wedding-roadmap-async.tsx` + `toggleRoadmapItem` action; **replaces the "Up next" hero**; hidden in Manual mode. `tsc`+`lint` green. SPEC IMPACT: 0021 roadmap → corpus DECISION_LOG.

**2026-06-05 — Your Plan: powerful Freebies value block (Screen 14).** Owner: *"more modern than just frames — a powerful way to present the Freebies."* `FreeValueSlider` rebuilt into a gold value-showcase — big serif **₱-total + hours hero** (counts up) over a meter, cards that **strike the elsewhere price → mulberry *Free*** (gold spine + ghost index), mulberry seal tally; replaces the plain rail + `.plansave`/`.planfree`. Free **Monogram/Website** relabeled **"Basic"**; the opt-in **toggle is now a real pill** (was a circle). Mockup-verified before porting. `tsc`+`lint`+`build` green; no migration. Next (PR-b): remove bundle + 1–5 inquiry stepper + in-app-services screens 15–16. SPEC IMPACT: Blueprint §3.2 + prototype (corpus).

**2026-06-05 — Onboarding "Your Plan" reframed free-first (Screen 14).** Owner: show free value in a slider (time saved + price-if-bought-elsewhere), then ask to keep the free AI guidance + to send the first inquiry to the top-3 matches. New **`FreeValueSlider`** renders the per-tool breakdown of the locked **§H/§I** Time-&-Money model (`computeOnboardingSavings` brought current — was on the superseded §D ₱32,992/745h; now ~₱63.5K/~290h; headline + Congrats auto-update). Two opt-in toggles in `OnboardingState`: **guidance** (free deadline timeline · default ON · NOT the retired paid Today's Focus · → `style_preferences.guidance_opt_in`) and **send-top-3 inquiries** (default OFF · RA 10173). The commit's old unconditional per-category inquiry fan-out is now **gated** behind the opt-in and **capped to ≤3** (idempotent; faster than before). Paid bundle demoted below the asks. `tsc`+`lint`+`build` green; no migration. SPEC IMPACT: Blueprint §3.2 + prototype + DECISION_LOG (corpus).

**2026-06-05 — Onboarding "Set the mood" → swipeable carousel (UX).** Owner — *"set the mood must be carousel as well."* The wedding-onboarding palette step picked the feel via flat chips while its sibling Style steps (Cuisine, Photo & Video) already used the swipeable photo-card film-strip. Now the feel picker is a **`.pgrid.strip` carousel of feel cards** (single-select · budget-tiered feel photos · "Swipe a feel"), reusing the proven strip CSS. `tsc` green; built off `origin/main`. ⚠ Not visually verified (app needs Supabase env locally). The corpus prototype specifies the richer `.pgrid.car` (fills-screen, no hero) variant — the app ships the lighter film-strip per owner's pick. SPEC IMPACT: none (corpus already specifies a carousel here).

**2026-06-05 — Today's Focus REMOVED (customer-facing · iteration 0016 retired).** Owner: "remove it completely — we don't want this anymore." The retired AI-planner SKU (already off in-app via `CONCIERGE_ENABLED=false`) is scrubbed from every customer/vendor surface: dropped from **/pricing** (+ /for-vendors productions + admin discount picker) via a `fetchV2CustomerCatalog` `.neq('service_code','TODAYS_FOCUS')` filter (no DB write); TF copy removed from home metadata / signup / features / privacy / marketing FAQ + sections; **/for-vendors** "Today's Focus matching" → brand-free "Couple matching" (feature kept); the free cockpit hero label "Today's focus" → "Up next". `tsc`+`lint` green; no migration. Dead concierge machinery + admin "brain/abuse" tooling + corpus sweep left as follow-ups. SPEC IMPACT: 0016 retired → corpus DECISION_LOG (logged).

**2026-06-05 — Monogram Maker (new couple surface · `/dashboard/[eventId]/monogram`).** A standalone, returnable home to craft the wedding monogram — initials + 1 of 5 lockups (bar/script/duo/framed/infinity) with a live `AnimatedMonogramHero` draw-on preview; persists the SAME columns onboarding writes (`monogram_text/color/style/font_key/frame_key`) so the design round-trips everywhere — **no migration**. Nav entry in "Share"; "How it animates" upsells the paid `ANIMATED_MONOGRAM` SKU (₱2,499) + teases the wider animation library. `tsc`+`eslint` green; built off `origin/main`. The 23-style animation **picker** + `monogram_animation_key` stay a tracked expansion (`Monogram_Maker_Plan_2026-06-05.md`). SPEC IMPACT: new surface → corpus.

**2026-06-05 — Narrated loading on Guests + Website (UX).** Owner follow-up to the Services narrated loader → both routes now *tell what they're doing*. New shared **`LoadingNarration`** (gold spinner + cycling `LoadingStatus`) added over each route's existing page-shaped skeleton: **Guests** (*"Loading your guest list… → Counting RSVPs… → Organizing tables & sides…"*) and the **Website** editor at `/site-editor/[eventId]` (the real target of the "Website" nav doorway; *"Opening your website editor… → Loading your design… → Bringing in your photos…"*). Reduced-motion-safe; `tsc`+`lint` green; no migration. SPEC IMPACT: none.

**2026-06-05 — Manual mode · Services accordion deep-gate (0021 · PR2 of 2 · COMPLETE).** Finishes Manual planning mode after PR1 (#1002). In Manual mode the Services plan+budget accordion now also turns off: the per-candidate **"% match" pills** (`VendorCardAtom` + `CompareSheet` + the category-search overlay), the per-category **deadline chips**, and the **"Do this next" / lock-next** nudges — via a `personalizationEnabled` flag threaded through `buildPlanBudgetModel` (per-child + model-level); the timeline math + budget + result order are untouched. Manual mode is now fully consistent across Home + Services; the vendor directory stays usable (search/browse/compatibility/neutral order). `tsc`+`lint` green; no migration (reuses `events.planning_mode`). SPEC IMPACT: completes 0021 Manual mode (logged in corpus DECISION_LOG).

**2026-06-05 — CI fix: duplicate migration timestamp `20260826000000` resolved.** Two migrations shared the prefix (budget_planner + songs-iTunes-cache), reddening the **migration timestamp guard** on every PR. Verified via `supabase migration list` (one `20260826` applied, one pending) + DECISION_LOG: budget_planner is the applied one; the **songs twin was never applied**. Pure `git mv` renamed the songs file → `20260828000000` (idempotent, so safe to re-apply; budget_planner untouched at `20260826`). Guard now green. **⚠ Known prod gap (now actionable):** prod is still missing `songs.apple_track_id`/`preview_url`/`artwork_url` + the 390-song seed — the renamed migration is pending and should be applied on the next `supabase db push`. (Separately still pending: `20260817` monogram migration.)

**2026-06-05 — CSV import dedupe (0001).** Closes the last gap from the name-quality review: `import/actions.ts` now skips **exact-normalized duplicates** (within the uploaded file AND against the existing list) before insert — re-importing a file no longer doubles the list. Exact-only (shared `norm`); fuzzy stays with the interactive forms. `skipped` = invalid rows; duplicates counted + shown separately in the success banner ("Imported N · skipped M duplicates"). `tsc`+`lint`+`build` green. SPEC IMPACT: 0001 CSV dedupe → corpus. **The guest-list name-quality pass is now complete** (normalize on all 3 paths · dedupe on quick-add + detailed form + CSV import).

**2026-06-05 — Guest-name hygiene (0001).** Owner Q on preventable name issues → shipped the two lowest-risk wins. New `lib/guest-name.ts` `normalizeGuestName()` (NFC + drop zero-width/bidi/control + fold all Unicode whitespace + clamp 80; casing untouched) wired into **all 3 guest write paths** (detailed form, quick-add, CSV import) — kills dedupe/search/sort misses from pasted junk. The nickname/typo duplicate matcher was extracted to shared `lib/guest-dedupe.ts` (zero behavior change) and the **detailed Add-guest form now shows the same live duplicate warning** the quick-add sheet had (non-blocking, links to matches, no PII in client props). `tsc`+`lint`+`build` green; normalize 15/15 + dedupe 8/8 unit tests. SPEC IMPACT: 0001 → corpus DECISION_LOG + 0001 .md. **Next (not built):** CSV import has no dedupe yet (within-file + against-existing) — the biggest remaining gap.

**2026-06-05 — Services tab UX: no-tilt cards + tap-to-open loading + Vendors route loader (0021/0022).** Owner report on the couple Services tab (Plan + Budget accordion): (1) the coverflow **`rotateY` tilt is removed** — its per-frame sign-flip near rail-center read as "shaking"; the centered-card **scale enlarge is kept**. (2) **Tapping a vendor/service card now enlarges it ("dig deeper") behind a full-screen loading overlay** (lifted to root so `position:fixed` escapes the curve-transformed ancestors), then opens the detail; a new `workspace/loading.tsx` continues the loading screen. (3) The **home → Services blank flash** is fixed — the route's generic `ListPageSkeleton` `loading.tsx` is **rewritten to a Vendors-shaped loader** (black budget bar + spinner, hides the top-nav so there's no header swap), so the hop shows a continuous loading state with nothing half-rendered tappable. (4) **Narrated loading** — a new `LoadingStatus` client cycler makes every loading screen *tell what it's doing* (e.g. *"Downloading your information… Activating your personalized refinements…"*) across the Vendors loader, the new `workspace/loading.tsx`, and the card-tap overlay. `tsc`+`lint` green; no migration. SPEC IMPACT: coverflow tilt retired + tap-load + narrated-loading added → corpus DECISION_LOG.

**2026-06-05 — Manual planning mode · foundation + toggle (0021 · PR1 of 2).** Owner asked for an off-switch for Setnayan's automated layer — personalization + per-category/statutory deadlines + auto-tasks — leaving a self-driven app + a usable vendor directory. PR1 lands the foundation: migration `20260827000000` (`events.planning_mode` · default 'guided' · **applied to prod**), a `setPlanningMode` action, the **Guided⇄Manual toggle** on the "Matching you on" strip (Manual collapses it to a slim "you're driving" bar), and **Home gating** (Manual hides Today's Focus + Upcoming-schedules/deadlines; keeps countdown + activity). `tsc`+`lint` green. **Owner-accepted reversal:** Manual hides even the LEGAL/statutory dates, no warning (logged in corpus DECISION_LOG). **PR2 (next):** the in-accordion % match-pill + taste-sort deep-gate (`VendorCardAtom`/`CompareSheet` + `category-search`) + per-service deadline chips, so Manual is fully consistent on Services. SPEC IMPACT: new 0021 planning mode → corpus DECISION_LOG + 0021.

**2026-06-05 — Budget Planner UI shipped · couple planner + admin pages (0007/0023).** The full loop on the 2026-06-05 allocation engine (PR #996). **Couple:** a "Suggested budget split" on the budget tab — per-service ₱ + range + confidence chip, cushion / shortfall, a peso-pin tilt sheet (Splurge / Standard / Save dial + free ₱ + reset), Save → behavioral snapshot; the **pure engine runs client-side** so every tilt is instant. **Admin** `/admin/budget-planner`: benchmark seeding (26 leaves; admin fills prices — never invented), engine knobs, **de-identified min-N-gated insights** (admins never see raw rows). New `lib/budget-allocation-data.ts` resolver + migration `20260826000000` (config + benchmarks; **APPLIED to prod via monogram-isolation**, the owner's `20260817` left untouched). `tsc`+`lint` green. **SPEC IMPACT: builds 0007 planner + 0023 controls → corpus.**

**2026-06-05 — Branch-scoped service grouping (0022 · Branches V1.x complete).** Second half of the V1.x "yes": migration `20260825000000` (**applied to prod**) adds nullable `vendor_services.branch_id` → `vendor_branches` ON DELETE SET NULL; the services editor gains a "Branch" picker **gated to Enterprise vendors with ≥1 branch** (every other vendor's form unchanged), each service card shows its branch, and agents inherit scoping via their assigned services. Resilient select (pre-migration fallback) + `resolveBranchId` (own-branch-only). `tsc`+`lint`+`build` green; DB-verified (owner sets branch_id ✓ · ON DELETE SET NULL ✓). **This fully completes the Branches feature (create/pay/activate · auto-lapse/Renew · ₱999 · branch-scoped services).** SPEC IMPACT: 0022 grouping BUILT → corpus.

**2026-06-05 — Branches V1.x: ₱999 charm + auto-lapse + Renew (0022).** Owner follow-ups to #986: (4) price corrected to **₱999 (charm)** — aligns code to Pricing.md §0.C (already ₱999); supersedes the ₱1,000 in the entry below. (3) the deferred lifecycle is now built: a branch's status is **derived from its latest activation order** (paid + in the 28-day `orders.expires_at` window → Active; past it → **Expired**; unpaid → Pending), so **auto-lapse is automatic at read time — no cron** ([[cron-free]]); plus a one-tap **Renew** (`renewBranch` → fresh ₱999 apply-then-pay → admin approval reactivates with a new window). Code-only; `tsc`+`lint`+`build` green; no migration. Pricing.md §0.C reconciled (₱999 · Enterprise) per owner authorization. SPEC IMPACT: 0022 price ₱999 + lapse/renew BUILT → corpus.

**2026-06-05 — Budget allocation engine + behavioral capture · foundation (NEW · 0007/0025/0023).** Owner design session: a top-down budget *allocation* layer atop the existing tracking ledger — recommend a ₱ target + shopping range per service *before* the couple picks anyone, from the median of solo vendor prices, proportioned + budget-scaled, a **guide not a rule**. Shipped the foundation only: pure **`lib/budget-allocation.ts`** (`computeBudgetAllocation` — median→proportion→₱ spine · fixed-then-proportion · cushion/slack-first · soft-floor-warn · p25–p75 band · `surplusMode` park|distribute) + Layer-1 capture migration **`20260824000000_budget_allocation_decisions`** (couple-own-only RLS · **admins no blanket read by design** · RA 10173 erasable). `tsc`+`lint` green · runtime harness **20/20** (the worked example reproduces exactly). **⚠ Migration NOT applied** — `db push` would co-apply the owner's pending `20260817` monogram migration, and the version was renamed off a remote-only `20260823` collision (vendor_self_comp_caps); ships ahead of application (nothing consumes it yet). No UI yet (planner surface = follow-on). Design: corpus `Budget_Planner_Allocation_Engine_2026-06-05.md`. **SPEC IMPACT: NEW capability → corpus (0007/0025/0023, direct-edit this session).**

**2026-06-05 — vendor_self_comp_caps RLS fix (0022).** Owner follow-up to the "RLS-enabled-no-policy" flag. Of the 4 flagged objects, **3 are VIEWS** (`vendor_active_ads` / `vendor_active_tools` / `vendor_market_stats`) — no RLS by design (false alarm). Only `vendor_self_comp_caps` was a real table with RLS on + zero policies (service-role-only), so a vendor couldn't read an admin-raised comp cap (reader fell back to default 12). Migration `20260823000000` (**applied to prod**) adds owner+admin read-own (`current_vendor_profile_ids()`) + admin-manage (`is_admin()`). RLS-only; DB-verified (owner reads own ✓ · stranger blocked ✓). SPEC IMPACT: none.

**2026-06-05 — Vendor Branches shipped · Enterprise sub-accounts (0022).** The last item of the multi-user vendor workspace ("multiple accounts depending on plans"). Owner picked build-now · **₱1,000/28d · Enterprise-only** (settling the live-site price/gate contradiction). New `/vendor-dashboard/branches` (owner/admin + Enterprise gated): list / add / cancel branches, each a ₱1,000 apply-then-pay charge via the existing 0034 flow (branch inactive → `orders` + pending `payments` → admin approves at `/admin/payments` → an additive `approvePayment` hook flips it active). New `lib/vendor-branches.ts` + `branches/actions.ts` + a Business-group nav entry (owner/admin only; agents never see it). `tsc`+`lint`+`build` green; **DB-verified via rolled-back impersonation** (owner creates branch+order+payment ✓ · activation flip ✓ · agent blocked + sees 0 ✓). **No migration** (vendor_branches RLS already owner+admin; price passed explicitly, no service_catalog SKU). Flagged V1.x: auto-renewal/lapse is manual; branch-scoped service grouping deferred. **SPEC IMPACT: 0022 Branches BUILT (price/gate owner-locked 2026-06-05) → corpus.**

**2026-06-05 — Vendor admins see everything · fast-follow (0022).** Closes the tail of #972: a set of the vendor's OWN tables still gated vendor access owner-only (or on the PLATFORM `is_admin()`, not the vendor's own team-admin), so a vendor-team ADMIN couldn't see the business's packages / contracts / calendar / payouts / ad-subs / tax / tokens. Migration `20260822000000` (**applied to prod**) adds one ADDITIVE `<table>_team_admin` policy on **12** owner-only vendor tables via `current_vendor_profile_ids()` (owner+admin) — FOR ALL where the owner had write, FOR SELECT where read-only; existing owner policies untouched (permissive OR → zero-regression). **DB-verified via rolled-back impersonation:** 12/12 policies valid · owner unregressed · admin GAINED parity · agent stays scoped out · stranger locked. Applied via **monogram-isolation** (the unapplied out-of-order `20260817` monogram migration left as-is — owner's to deploy). RLS-only; no app code (#972 already routes admins to their vendor). **Flagged:** 4 tables (`vendor_active_ads` / `vendor_active_tools` / `vendor_market_stats` / `vendor_self_comp_caps`) have RLS-enabled-but-no-policies — separate pre-existing issue. **This completes the multi-user vendor workspace: owner+admin see everything, agents see only their assigned services + customers. SPEC IMPACT: 0022 admin parity → corpus.**

**2026-06-05 — Admin mobile Queues triage feed (0023).** Delivers the change the admin remap (#963) explicitly deferred: `/admin/queues` is no longer a flat 7-card menu but a **live, prioritized triage action feed** — every queue with its open-count (payments/verify/disputes/force-majeure/reviews/help/Today's-Focus abuse, each using that queue's exact filter), **busiest first**, each a 64px tap-target straight into the work; champagne-gold count pill when open, check when clear, degrades to a chevron if a count is momentarily unavailable (row still routes — no 500). Header tally "N need you / all caught up". Stays `lg:hidden` (desktop = sidebar tree, untouched). New `_components/queues-triage-feed.tsx` + page rewritten as the data layer (reuses the `/admin` Home count pattern). `tsc`+`lint`+`build` green; no migration. **SPEC IMPACT: 0023 §5 mobile Queues = triage feed (supersedes card menu) → corpus.**
**2026-06-05 — dead-code sweep: PersonalizedMenu + unused menu builders removed (0021).** Closes the follow-up flagged when PR #978 moved personalization onto the Services "Matching you on" strip. The old `PersonalizedMenu` card was rendered nowhere (home → cockpit; `/for-you` → redirect) and survived only to export `TasteChip`; that type moved into `lib/personalized-menu.ts` and the strip repointed to it, then the component + the now-orphaned `buildServiceFeatures`/`buildWeddingDetailRows` (+ their `ServiceFeature`/`WeddingDetailRow` types + private helpers + the dead `style_preferences` field) were deleted — **all with zero importers verified**. Kept everything `buildTasteChips`/`/details` still use. **−379/+11** lines; `tsc`+`lint` green; no behavior change; no migration. SPEC IMPACT: None.

**2026-06-05 — Layer-B "matches your preference" match-read shipped (matcher · 0044 / Vendor_Match_Personalization).** The couple-pref ⋈ vendor-facet match-read — the activation the `event_vendor_preferences` (`20260721000000`) + `vendor_service_attributes` (0044) **foundation** migrations were built for. New `lib/preference-match.ts` (`fetchPreferenceMatches` · per-dimension **array overlap** · returns matched vendors only · graceful-degrades to an empty map on every missing-table/empty-data path) wired into `fetchWizardVendorRecommendations` (over-fetch → float matched up by `matchedDimensions` → optional `preference_matched` fields), an emerald **Sparkles "Matches your preference"** pill on the vendor card, and `scripts/seed-preference-match-demo.ts` for reproducible end-to-end proof on test/staging (frequency-ranks demo-vendor facets → writes one overlapping couple pref; prod-ref safety gate). Mirrors the proven **song-overlap** re-rank, generalized music → every category. **Inert in prod** until vendors carry facet tags (founder-only marketplace) — then auto-activates; **zero regression by construction**. `tsc`+`lint`+`build` green; mock-client runtime harness (7 cases / 14 assertions) green; **no migration** (both tables exist); worktree off origin/main. Shipped as **PR #980**. SPEC: activates `Vendor_Match_Personalization` §8/§9 read → DECISION_LOG (deeper .md/.docx "read SHIPPED" sync deferred while inert).

**2026-06-04 — Services "Matching you on" criteria strip + /for-you retired (0021).** The planned **PR2** after the cockpit. The couple's match criteria (date · region · ceremony · venue · guests · style · budget — what filters + sorts the vendor search) now surface as a compact **"Matching you on"** strip atop the **Services/Vendors** tab (new `match-criteria-strip.tsx`, reusing `buildTasteChips`), with a **Refine** → the editable Personalization page (`/details`). The orphaned standalone **`/for-you`** page is **retired** (permanent redirect to Services; `loading.tsx` deleted) and its bottom-nav entry pruned. The now-unrendered PersonalizedMenu card is kept only for the `TasteChip` type (dead-code removal flagged). `tsc`+`lint` green; worktree off origin/main; no migration. **SPEC IMPACT: 0021 personalization placement (strip-on-Services, /for-you retired) → corpus DECISION_LOG.**

**2026-06-04 — Home countdown now ticks live (0021).** Follow-up to #968/#970: the cockpit countdown is a live **days · hrs · min · sec** timer (new client `live-countdown.tsx` — ticks every second, `tabular-nums`, no jitter; "Today" / "Just married" at the date). The server header resolves the target (earliest chosen date → PH-midnight `+08:00`) and seeds the client with the server clock so it hydrates without mismatch. `tsc`+`lint` green; no migration.

**2026-06-04 — Vendor agents: role-aware RLS scoping · Phase 2b (0022).** The multi-user payoff. The vendor data layer was owner-only at RLS; now role-aware: **owner/admin see everything; agents see only their assigned services + the customers tied to them** (`event_vendors.service_id`). Migration `20260821000000` (**applied to prod**): redefines `current_vendor_profile_ids()` owner→owner+admin (propagates admin access to chat/follows/branches/boosters), adds `agent_assigned_service_ids()`/`agent_customer_event_ids()`, makes `vendor_services`+`chat_threads`/`chat_messages` admit agents by assignment, `vendor_profiles` member-read. Code: `fetchOwnVendorProfile` membership-aware; agent nav → Services/Bookings/Messages; `/team` kept owner-only (admin-client email exposure). **DB-verified via rolled-back impersonation:** owner unregressed (191 svcs), non-member blocked, agent=1 assigned + 0 money + 0 manage-all, admin sees vendor. `tsc`+`lint`+`build` green. **Fast-follow:** admin access to remaining owner-direct tables (earnings/tokens/contracts/etc.). **⚠ Prod migration drift found:** 20260820 vendor-payments applied-but-unmerged; 20260817 monogram merged-but-unapplied (monogram likely half-deployed) — owner to reconcile. **SPEC IMPACT: 0022 role-aware vendor RLS → COWORK_INBOX.**

**2026-06-04 — off-platform vendor payment options ("How clients pay you") shipped (NEW feature · 0022/0025/0007/0034/0023).** Vendors publish their own payment destinations (link / QR / bank details) so couples pay them **directly, off-platform** — Setnayan takes 0% + never holds the money (RA 11967). Migration `20260820000000` (applied to prod): `vendor_payment_methods` (RLS owner-only) + `event_vendor_payments.proof_r2_key`. Vendor editor `/vendor-dashboard/payment-options` (link/QR/bank; **links gated to Pro & Enterprise**; allowlist + shortener block) · couple `VendorDirectPay` settlement rail (**always-on "Setnayan doesn't control vendor payments" vigilance disclosure** · copy bank · QR modal w/ decoded destination · leaving-Setnayan interstitial · receipt upload) · admin `/admin/payment-options` moderation queue. `tsc`+`lint`+boundary green; shipped as **PR #969**. Owner-locked: links = Pro/Enterprise only; vigilance disclosure = platform-wide standing rule. SPEC: landed direct in corpus (0034 schema+rules · 0022 vendor surface · 0025 tab · 0007 settlement · 0023 admin) — owner authorized direct corpus edits.

**2026-06-04 — Home countdown → earliest chosen date (0021).** Follow-up to the cockpit (#968): the countdown now targets the **earliest chosen date** (committed `event_date` → earliest `date_candidates` → `date_window_start`) so candidate/window-mode couples get a live "days to earliest" + date-state label ("3 possible dates" / window range / single candidate) instead of "add your date"; it settles to "days to go" once a single date is committed. `tsc`+`lint` green; no migration.

**2026-06-04 — couple Home cockpit redesign (0021 / 0016).** Owner — *"fix the first page (customer dashboard home): less text; updates + guides + a quick what-to-do-next."* After a side-by-side prototype review, the lean 3-block home is reshaped into a **cockpit** answering "what now?": **Countdown** (new `event-countdown-header.tsx` — names + days-to-go + "X/N vendors locked") → **Today's Focus** (re-wired the dormant `TodaysOneThing` single-action hero — the lightweight vendor-derived one, **not** the retired wizard/Concierge) → **Needs you** (`UpcomingSchedules` reframed via new headingLabel/emptyLabel props) → **Recent activity** → a "Browse your matched services" doorway. The text-heavy **"Your wedding details" recap is removed from Home** (+ its orphaned compute/imports); it returns at the top of **Services** as an editable "Matching you on" strip (next PR), with the full editable record staying at `/details`. `tsc`+`lint` green; worktree off origin/main; no migration. Shipped as **PR #968**. **Next: PR2 — the Services "Matching you on" criteria strip + Refine → /details.** SPEC IMPACT: reverses the 2026-06-02 lean-home shape (0021) + re-surfaces a Today's-Focus hero (0016) → COWORK_INBOX.

**2026-06-04 — Vendor agents: per-service assignment · Phase 2a (0022).** Phase 2 of the vendor multi-user workspace (after the Phase-1 role-aware shell #962). Confirmed the customer↔service link exists (`event_vendors.service_id` → booked `vendor_services`), so "agents see only their services + customers" is feasible. Phase 2a = the assignment foundation: new **`vendor_service_agents`** table (migration `20260816000000`, **applied to prod**; RLS = members read, owner/admin manage via `current_vendor_ids('admin')`) + `lib/vendor-team.ts` fetch helpers + a `setVendorAgentServices` action + a per-agent service-checkbox UI on the Team page. `tsc`+`lint`+`build` green. **Phase 2b (next):** agent-scoped dashboard reads (Services/Bookings/Messages → assigned only) enforced via RLS, admins-see-everything data resolution, agent nav expansion, route guards. **SPEC IMPACT: 0022 §2.6a `vendor_service_agents` now BUILT → COWORK_INBOX.**

**2026-06-04 — event-type "feel photo" picker (replaces the bars) + per-event onboarding step study (0000).** Owner reversed the same-day bar picker → **photos of how each event feels, no carousel indicators** (no dots/arrows/bars); tap the centered, fully-visible photo to begin; it snaps card-to-card. New `event-type-photo-picker.tsx` (scroll-snap deck of `/public/event-types/*.webp` via next/image; peeking dimmed neighbours; "Begin →" only on the centered card; tapping a side photo snaps it to center) swapped into `event-type-picker.tsx`; deleted `event-type-bar-picker.tsx`. Same onboardingHref routing (Wedding → `/onboarding/wedding`; others fall back to the inline form). `tsc`+`lint` green; worktree off origin/main. **Per-event step study (sourced): 8/15 steps universal; Kind+Faith+ceremony-venue+wedding-docs drop for all except christening; per-event = Role + Identity + picker + style via 2–3 swap-ins; one parameterized shell.** Next: finalize wedding onboarding (owner), then build the shared engine + Debut. SPEC IMPACT: 0000 picker = feel-photo deck (supersedes bars) + per-event step recommendation → COWORK_INBOX.

**2026-06-04 — Admin dashboard remap: 6 groups + mobile table/orphan fixes (0023).** Companion to the vendor remap. Desktop sidebar **8→6 groups**: Home · Queues · Directory · Money · **Insights** (Growth · Funnels · Operations & Hiring · Telemetry · Offline — absorbs old Operations) · **Manage** (Taxonomy · Website · Ads · Brain · Moodboard · Songs · Settings · Demo mode — merges old Content+Settings, collapsed by default); group keys reused (funnels→Insights, content→Manage) so open-state survives, item keys unchanged. **Mobile fixes:** the 4 viewport-overflowing tables from the dashboard study now scroll (operations-hiring wrapped; demo-vendors/inquiries/offline-diagnostic `overflow-hidden`→`overflow-x-auto`); `/admin/songs` mobile orphan now reachable (More tab + landing). `tsc`+`lint`+`build` green; no migration. **Deferred (flagged, own PR):** the unified mobile Queues triage feed. **SPEC IMPACT: 0023 nav remap (8→6) + mobile fixes → COWORK_INBOX.**

**2026-06-04 — minimal event-type "bar picker" + tap-to-onboarding (0000 · P1 of per-event onboarding).** Owner — the create-event picker should be "nothing but the choice of events": a minimal bar strip (one bar/type, ‹ › chevrons, gold focused bar); tap a bar → jump STRAIGHT into that event's onboarding. New `event-type-bar-picker.tsx` replaces the hero-photo carousel on the full-page create-event surface (the in-chrome add-event sheet keeps the carousel). `onboardingHref` per type: Wedding → `/onboarding/wedding` now; the rest fall back to the inline name form until their tailored onboarding lands. Dropped the dead `WeddingTypePicker`/`launchStatus` path. `tsc`+`lint`+CI green; worktree off origin/main. **Phase 1 of a larger approved build: each event type gets its own fully-tailored onboarding mimicking the wedding flow's concept (shared `app/onboarding/_shared/` engine + per-event route/palette/content/commit; event-specific data in additive `events.attributes JSONB`) — Debut exemplar next, then one type per iteration. Plan: `.claude/plans/curious-swimming-journal.md`.** SPEC IMPACT: 0000 picker redesign + per-event onboarding roadmap → COWORK_INBOX.

**2026-06-04 — Vendor dashboard remap (4 groups) + role-aware nav shell · Phase 1 (0022).** Owner directive to simplify the vendor + admin dashboards and make the vendor account a multi-user workspace (owner/admin see all; agents see only their services/customers). Phase 1 (remaps-first, owner-sequenced): desktop sidebar **6→4 groups** (Home · Work · Grow · Business; group keys reused so open-state survives, item keys unchanged); new `lib/vendor-role.ts` resolves the member role (`vendor_team_members`) + centralizes the nav policy; sidebar/bottom-nav/more-landing/layout are now role-aware (agent/viewer → scoped nav = Overview only + a "you're on the team" home; owner/admin → full). **No data exposure** — agents already resolve NULL via the owner-only `fetchOwnVendorProfile`, so this is purely structural; **Phase 2** adds `vendor_service_agents` + RLS (agents see only assigned services/customers), admins-see-all data resolution, and route guards. `tsc`+`lint`+`build` green; no migration. Admin remap (groups 8→5 + unified mobile Queues feed + fix 4 overflow tables) is the next PR. **SPEC IMPACT: 0022 nav remap + role-aware shell → COWORK_INBOX.**

**2026-06-04 — taxonomy governance: vendor request-a-category + admin 4-outcome review (0023 §3.2c / 0022).** Closes the last taxonomy gap. New `taxonomy_category_requests` table (migration `20260811000000`, applied to prod; RLS = vendor manages own, admin resolves all). Vendors get a "Don't see your service?" form on the services editor (`proposeCategory`) + a live status badge on their own requests. Admins get a ghost-card queue on /admin/taxonomy with the four outcomes — **promote** (mints a canonical leaf under a chosen tile), **map** (→ existing canonical; repeat-maps surface a **demand signal**), **keep-private**, **reject** — all audit-logged. `tsc`+`lint`+`build` green. **The expandable-taxonomy loop is now end-to-end: grow (add-leaf) · reshape (rename/remap/reparent-via-reorder/delete) · request (vendor) → resolve (admin).** **SPEC IMPACT: 0022 (request entry) + 0023 §3.2c (review + demand signal) → COWORK_INBOX.**

**2026-06-04 — fully taxonomy-driven onboarding · PR-1 (expand · 0006/0000).** Owner ratified going **fully taxonomy-driven** (picker + couple's stored category + auto-inquiries all derive from the live taxonomy; new tile → auto-appears, no deploy) — **reverses the locked "couple-side `vendor_category` does NOT auto-expand" decision**. Spec: `Onboarding_Taxonomy_Driven_Spec_2026-06-04.md` (corpus · converged §0: onboarding=available-only, dashboard=show-all, listings supply-gated, EN-only). **PR-1 of 4 (expand-only, no behavior change):** migration `20260815000000` adds nullable `event_vendors.category_key TEXT` + **FK → `service_categories(id)` ON DELETE RESTRICT** (also guards "a running event can't lose a chosen category if its tile is deleted") + index, and **backfills** from the legacy `vendor_category` enum via the authoritative bridge (24 clean · 2 alias→primary · 4 exempt→NULL; `EXISTS tier-2` guard + idempotent). Legacy `category` enum UNTOUCHED = source of truth until PR-4. **Drift found:** PG enum has 36 values, TS type only 30 (6 attire alters) — backfill covers all 36. Not yet applied to prod. Next: PR-2 picker-derive + adaptive rendering; PR-3 reader cut-over; PR-4 drop enum. **SPEC IMPACT: reverses couple-side-curation lock + `category_key` on 0006 → COWORK_INBOX.**

**2026-06-04 — light-locked the app (theme).** Owner — *"the app used to adjust automatic to light and dark theme. disable this and just always keep it light theme."* Reverted the 2026-05-22 Light/Dark/Auto brand pivot: `ThemeProvider` is hard-locked to light (never adds `.dark`, `setMode` no-op, bootstrap script strips `.dark`); `viewport.themeColor` pinned to `#FFFFFF`; removed the profile **Appearance** theme picker (section re-headlined "Feedback", Haptics toggle kept) + deleted `theme-mode-picker.tsx`; removed the in-editor **Theme** card from the Reels site-editor. The `users.theme_preference` column + `updateThemePreference` action + `html.dark` CSS are left **dormant** — light by construction (`darkMode:'class'` + no `prefers-color-scheme` rule → `.dark` never present). `tsc`+`lint`+`build` green; isolated worktree off origin/main. **SPEC IMPACT: reverses the Light/Dark/Auto lock → 0021 + 0025 + DECISION_LOG → COWORK_INBOX.**

**2026-06-04 — Growth surface follow-ups: demo data + CSV export + event breakdowns (0023).** On top of the just-shipped `/admin/growth` (PR #938): (b) **demo-mode preview** — `buildDemoGrowthStats` renders deterministic illustrative curves (badged "Illustrative demo data") when an admin has demo mode on, so the surface shows shape pre-pilot with zero DB writes; (c) **CSV export** — admin-gated `GET /admin/growth/export` returns tidy/long-format CSV (population + curves + conversion + breakdowns; honors range + demo) + an "Export CSV" button; (c) **breakdowns** — Events-by-type (enum) + Events-by-region (`events.region` slug) composition cards. `tsc`+`lint`+`build` green (both routes ƒ dynamic). (a) live admin screenshot handled against the deployed site. **SPEC IMPACT: extends the 0023 Growth subsection → COWORK_INBOX.**

**2026-06-04 — global security headers (pre-public-pilot hardening · B1 · PR #939).** `next.config.ts` shipped zero security headers; a global `source:'/(.*)'` block now adds HSTS · `nosniff` · `X-Frame-Options: SAMEORIGIN` + CSP `frame-ancestors 'self'` (external clickjacking blocked, same-origin preview iframe preserved) · `Referrer-Policy` · `Permissions-Policy` (camera/mic/geo = self, Topics off). A resource/script CSP is deliberately deferred (would break the inline-Babel keynote decks + needs full origin enumeration — tested follow-up). Part of corpus `Pre_Public_Pilot_Hardening_2026-06-04.md`; § B2 rate-limiting routed to Cloudflare edge (owner console, no app code). **SPEC IMPACT: none** (posture lives in the corpus doc + DECISION_LOG).

**2026-06-04 — Admin Growth & Population surface (/admin/growth · 0023).** Owner — make statistics of the progress of the app: *actual population* (current totals) + *growth over time* for vendors · services · events · customers · guests, plus *guest → account-holder conversion*. New `lib/admin/growth-stats.ts` (population head-counts; 12-bucket cumulative + net-new series per entity, from each table's `created_at` via exact per-boundary `count` queries — no migration, no 1000-row truncation; conversion via `event_members.guest_id`+`member_type='guest'`, cumulative by `joined_at` + all-time rate + median days-to-convert) + `app/admin/growth/page.tsx` (server component: range picker 3/6/12mo · population tiles · per-entity cards with hand-rolled SVG sparkline + net-new bars · conversion section; no chart lib). Nav: Funnels group → **Insights** (Funnels + Growth) in sidebar + bottom-nav More + mobile More landing. Conversion definition owner-locked = **"any linked account"**. `tsc`+`lint`+`build` green; `/admin/growth` builds as ƒ dynamic. Isolated worktree off origin/main. **SPEC IMPACT: 0023 gains a 29th surface (Growth) + Funnels group→Insights → COWORK_INBOX.**

**2026-06-04 — taxonomy adaptability: add-new-leaf editor + couple-side anchoring + /vendors read-through (0023/0044/0006).** Owner — "set the taxonomy to grow + reformat; the app relies on it for vendors, services, onboarding." Three gaps closed on top of the already-shipped DB-backed stack (2a read-through · Phase 3 editor): (1) **add-new-leaf** — new `createCanonicalLeaf` mints a brand-new bookable canonical service from /admin/taxonomy (writes the `canonical_service_schemas` stub → onboarding picker + the `canonical_service_taxonomy` mapping → /vendors bucket), with an optional starter refinement; audit-logged, no deploy. The editor's first leaf-minting action (prior ones only remapped existing canonicals). (2) **couple-side anchoring** — `lib/vendor-category-taxonomy.ts` maps the legacy 30-value `vendor_category` to canonical tiles (24 clean · 2 aliases · 4 exempt), compile-time-drift-proof + a runtime validator shown on the admin page. (3) **/vendors child comps** (`category-tile`/`folder-vendors-section`, now async) read folder labels from the snapshot so parent renames reflect (Phase 2b·2 had deferred these). `tsc`+`lint`+`build` green; no migration (foundation already on prod). **SPEC IMPACT: editor can mint new leaves (0023 §3.15) + couple-side anchored to canonical (0006) → COWORK_INBOX.**

**2026-06-04 — Schedule dimension wired: ALL SIX leaf-match dims now live (0016).** The last deferred dimension. The availability infra already existed (`lib/vendor-availability.ts` · `vendor_calendar_blocks` · batched `getBatchVendorAvailableDays`, cron-free + failing-open) and the public `/vendors` marketplace used it — now the matcher + onboarding do too. New optional `availableDateKeys` engine arg keeps a vendor only if FREE on ≥1 candidate date (drops venues booked on all). Failing-open: no-calendar = available, so Setnayan always-on services + un-marked vendors pass through (no special-case). Onboarding venue search passes `state.dateCandidates` (only in `dateMode==='specific'`). **Scope: schedule filters the browse LIST, not the congrats count** (count stays structural — schedule is transient). No migration (table exists); activates on real vendor calendars (demo has no blocks → no demo narrowing). `tsc`+`lint` green. **The Hybrid leaf-match is now COMPLETE — Location · Event-Type · Religion · Venue-setting · Pax · fine Venue-type · Schedule.** Follow-ups: dashboard `category-search` adopting `availableDateKeys`; the 0044 refinement-schema formalization. **SPEC IMPACT: onboarding now scopes by Schedule; reuses existing `vendor_calendar_blocks` (no new schema).**

**2026-06-04 — leaf-match: fine venue_type refinement + dashboard parity (0044/0016 · apply-everything 3-4/4).** Part A: migration `20260810000000` (applied to prod) adds nullable `vendor_profiles.venue_type` (fine: hotel_ballroom/events_place/restaurant/garden/beach/heritage/resort — the precise reception pick the coarse `venue_setting` collapses). New optional `venueType` engine arg, resolved in the SAME candidate-pool lookup as capacity; onboarding `RECEPTION_TO_VENUE_TYPE` map + search/count pass it; seed's `venueTypeFor` fans `banquet_hall` into hotel/events_place/restaurant. So "hotel ballroom" stops surfacing events places. Part B: dashboard marketplace (`category-search.ts` + `wizard-actions.ts`) now passes `event_type` + `pax` (region stays client-picker-driven; venue_type is onboarding-only). `tsc`+`lint` green; migration applied + confirmed. **The full Hybrid leaf-match now filters Location + Event-Type + Religion + venue-setting + Pax + fine venue_type; Schedule is the one deferred dimension (needs the vendor calendar populated).** **SPEC IMPACT: venue_type + capacity shipped as `vendor_profiles` columns (not 0044 attribute_payload) → COWORK_INBOX/0044 to ratify the venue_type vocabulary + reconcile the 3 venue vocabularies.** Owner: re-Create demo vendors to see venue_type/capacity narrowing.

**2026-06-04 — /admin/taxonomy editor: reorder tiles (Phase 3c · 0023).** New audit-logged `moveTaxonomyNode` (swaps a tile's `sort_order` with its adjacent sibling) + ▲▼ buttons per tile; the catalog reads order from the snapshot so the marketplace re-orders live (no deploy). The editor now does the full structural set: **rename · re-map · add · delete · reorder**. **Taxonomy track wrap-up:** 2b·3 (client nav read-through) is a no-op (those comps read editor-immutable folder slugs/short-labels); remaining spec items — two-admin gate, drag-to-move, grandchildren/leaf↔branch (modest value over existing admin-gate+audit+orphan-guards), and §3.2c vendor-request review (Phase 4, upstream-blocked on 0022) — are larger/blocked/sign-off. typecheck + lint green. SPEC IMPACT none.

**2026-06-04 — Pax dimension: venue capacity filter (0044/0016 · leaf-match · apply-everything 2/4).** Migration `20260809000000` (applied to prod) adds nullable `capacity_min/max` to `vendor_profiles` + partial index — NOT to the `vendor_market_stats` view (matcher reads capacity via a candidate-pool lookup, so the live view is untouched). New optional `pax` arg on `fetchWizardVendorRecommendations` drops venues with `capacity_max < pax` (Hybrid: NULL capacity = admitted); `searchOnboardingReceptionVenues` + `getOnboardingVendorCounts` (now sourced from `vendor_profiles` for the capacity column) pass `state.pax`. Seed gives each demo venue a setting-correlated capacity (`venueCapacityFor`). So a 225-pax couple drops venues that can't fit (after a demo re-Create; existing NULL rows admitted meanwhile = no regression). `tsc`+`lint` green; migration applied + confirmed on remote. **SPEC IMPACT: new capacity columns + Pax filtering; note on COWORK_INBOX/0044 that capacity is a column (not attribute_payload).** Next: dashboard parity (3/4), venue_type refinement (4/4); Schedule deferred.

**2026-06-04 — demo venue-setting diversification + BGC region fix (0044/demo · leaf-match follow-up).** PR #915 wired venue + region filtering, but the demo data couldn't show it: every demo venue shared `['banquet_hall','garden','heritage']` (so a "garden" pick matched all 1,801 venues), and that uniform array on NON-venue vendors wrongly excluded all photographers/caterers from beach/destination weddings. Now reception venues get ONE city-correlated setting (`venueSettingFor`, deterministic so it doesn't perturb the RNG stream) and every other vendor gets NULL = "works anywhere". Also plugged a region-filter hole: `BGC` wasn't in `regionForCity` → added bgc/bonifacio global city/fort bonifacio → NCR. Venue filter now bites + service vendors stop being beach-excluded (after demo re-Create); BGC fix live now. `tsc`+`lint` green; no migration. **SPEC IMPACT: none** (demo realism + region-map completeness; venue refinement schema still → COWORK_INBOX/0044). Part of "apply everything" (1/4): next = Pax/capacity (migration), dashboard parity, venue_type 0044 refinement.

**2026-06-04 — /admin/taxonomy editor: add + delete tiles (Phase 3b · 0023).** The expandable-taxonomy core. New audit-logged actions `createTaxonomyNode` (add a tile under a parent · slugified id/slug · collision-guarded) + `deleteTaxonomyNode` (orphan-guarded — refuses if children or mapped canonicals; parents not deletable). Tree UI gains ＋add-tile per parent + ✕delete per tile. With 2b·2 in, a new tile renders live on `/vendors` (no deploy), ready for re-mapped canonicals. Staged (Phase 3c): drag-to-move, grandchildren/leaf↔branch, two-admin gate, §3.2c request-review. typecheck + lint green. SPEC IMPACT none.

**2026-06-04 — placeholder photo extended to the vendor detail page (0006).** Completes the #917 follow-up: `/v/[slug]` hid its Portfolio section when a vendor had no photos. Now, with no portfolio, a hero banner renders the bundled `VENDOR_PLACEHOLDER_PHOTO` at the top (above logo+name); vendors with photos are unchanged. Real + demo. `tsc`+`lint` green. **SPEC IMPACT: none.**

**2026-06-04 — generic placeholder photo for photo-less vendors (0006/0021).** Owner: "vendors with no photo for their service must have at least a generic placeholder photo." Added a bundled neutral wedding-venue placeholder (`public/placeholders/vendor.webp`, Recraft, ~73KB) + `VENDOR_PLACEHOLDER_PHOTO` constant; the marketplace card (`VendorHero`) + the category picker now fall back to it instead of an initials monogram. Always-available, CDN-served, never rate-limited. `tsc`+`lint` green. **SPEC IMPACT: yes (minor)** → `COWORK_INBOX.md`. (Detail page `/v/[slug]` gallery = optional follow-up.)

**2026-06-04 — marketplace catalog reads the DB taxonomy, Phase 2b·2 (0023/0044).** Completes the public read-through: `CatalogView` (the `/vendors` tile-grid) fetches `getTaxonomy()` once and locally shadows the 10 taxonomy maps (cast to exact-key types) → labels + order + structure come from the DB snapshot; `ScopedFolderBanner` made async too. So admin renames/re-orders (Phase 3 editor) now show on the live marketplace browse, not just re-mapping. Slug routing + autocomplete + SEO JSON-LD stay on the constant (stable; small follow-up). Behavior-preserving (DB seeded from constant; fallback on error). typecheck + lint green. SPEC IMPACT none.

**2026-06-04 — onboarding leaf-match: location + event-type filters, Hybrid (0016/0044).** Owner audit — the "Find your first vendor" step showed out-of-region venues (Boracay/Tagaytay for a Metro Manila couple) and the congrats tile read "1,801 of 1,801", because the vendor search + count filtered only on (demo-uniform) ceremony + venue_setting. Owner locked **Hybrid** semantics (hard-filter objective dims, rank the sparse ones, never empty) + "quick wins now, spec the rest." Wired OPTIONAL `region` (PSGC; effective = `hq_region` ?? `regionForCity(location_city)` — the fallback is required since demo rows have NULL `hq_region`) + `eventType` args into `fetchWizardVendorRecommendations`; the onboarding venue search + counts now pass `state.region` + `wedding`. `getOnboardingVendorCounts` rewritten to a single JS pass so region/event-type narrow `matched` below `total` (a real "N of M"). The other 5 engine call sites are untouched (args optional). `tsc`+`lint` green; no migration. **SPEC IMPACT: per-leaf refinement layer (venue type/capacity) + `venue_setting`↔`venue_directory.venue_type` reconciliation + the Hybrid leaf-match contract → COWORK_INBOX → 0044.**

**2026-06-04 — /admin/taxonomy editor: rename + re-map (Phase 3 MVP · 0023).** The admin-facing payoff of the ♾️ lock. New audit-logged, RLS+role-gated server actions `renameTaxonomyNode` (parent/tile labels in `service_categories`) + `remapCanonical` (canonical→tile in `canonical_service_taxonomy`); the page gains a live rename tree + per-row re-map select + result banner + `force-dynamic`. Closes the loop: rename → `getTaxonomy()` live; re-map → `getCanonicalBuckets()` re-buckets `/vendors` live — no deploy. Staged (Phase 3b): drag-to-move, add/delete, leaf↔branch, §3.2c request-review ghost cards, two-admin gating. typecheck + lint + full CI green (#913). SPEC IMPACT none.

**2026-06-04 — demo seed images from a small Picsum pool (so demo photos load).** "Why are there no photos?" The seed gave every vendor a unique `picsum.photos/…/800/600` image; ~4,900 unique large requests rate-limited Picsum → all failed. Fix: pool the URLs — `snl${i%40}` (≈40 logos @400×300) + `snp${(i*4+j)%60}` (≈60 photos @600×400), reused everywhere → browser caches ~100 images, Picsum stops throttling. **Owner action: tap Create demo vendors again to re-seed with pooled URLs.** `tsc`+`lint` green. **SPEC IMPACT: none.**

**2026-06-04 — vendor-pick logos: graceful initials fallback (0021).** With the badge fixed, the picker showed broken-image icons — the demo seed's `logo_url` is `picsum.photos/.../800/600` and ~4,900 vendors rate-limit picsum (the overlay uses a raw `<img>`, so `next.config`'s allow-list doesn't cover it). Added `failedLogos` + `onError` → falls back to the initials tile instead of a broken icon (host-agnostic). Root follow-up (separate): shrink the seed's picsum sizes + re-Create so real photos load. `tsc`+`lint` green. **SPEC IMPACT: none.**

**2026-06-04 — vendor-pick badge distortion: real fix (rename collision class) (0021).** The #908 portal didn't resolve it (badge still ballooned with the `.pbacc .v` signature — likely stale PWA JS, and the portal was fragile). Root, source-level fix: the overlay badge was `className="badge v"`, so the accordion's card rule `.pbacc .v {min-height:300px;flex:1 1 auto;flex-direction:column}` matched it by the `v` class alone. Renamed badge modifiers `v→vrf`, `b→bst` (CSS+JSX) so `.pbacc .v` can never match — immune to nesting/portal/specificity. Hardened `.csov .r .badge` (`flex:0 0 auto;min-height:0`) too. `tsc`+`lint` green. **SPEC IMPACT: none.** (Owner tip: full-reload the app — old cached JS won't show the fix.)

**2026-06-04 — global top loading bar · future-proof nav catch-all (perf; 0000).** Owner: "we want it to be future proof." New `app/_components/nav-progress.tsx` — a client-side slim gold (`--m-orange`) top progress bar mounted once in the root layout: shows on EVERY path-changing navigation (link click / back-forward), debounced ~120ms (instant staleTimes revisits show nothing), completes on pathname change with a 10s safety timer. Covers every current + future route with ZERO static-generation impact — unlike a root `app/loading.tsx` skeleton, which would prerender ~55 service-role admin/dashboard pages at build and break it. Pairs with the per-route skeletons (#892). typecheck + lint + build (117/117) green. SPEC IMPACT none.

**2026-06-04 — DB-backed taxonomy marketplace bucketing, Phase 2b·1 (0023/0044).** The live `/vendors` marketplace now buckets vendors (canonical→tile/folder) via the DB snapshot: new `getCanonicalBuckets()` in `vendor-counts.ts` (cached, snapshot-derived, same cross-view logic; the IIFE constants stay as sync fallback) + `findTopVendorsByFolder/Tile` + the page's two `CANONICAL_SERVICES_BY_TILE` sites flipped. Behavior-preserving (DB seeded from the constant; fallback on error). Admin re-mapping a canonical now re-buckets the marketplace with no deploy. Staged: the page's ~45 tile-label/slug sites (sync helpers) = Phase 2b·2; 7 client components = Phase 2b·3. typecheck + lint + full CI green (#906). SPEC IMPACT none.

**2026-06-04 — loading shells for auth + onboarding entry points (perf; 0000).** Continuing "every gap gets a loading screen." Added `loading.tsx` for /login + /signup (auth-card skeletons matching the centered .m-*-card layout) + /onboarding/wedding (full-screen phone-frame placeholder for the first render). Guest landing + venue + receipts + vendor-claim were already covered by #892. **A root app/loading.tsx global fallback was intentionally NOT added** — a root loading boundary triggers Next static-shell generation app-wide, which runs /admin/taxonomy's top-level DB fetch at build → build fail (the page lacks force-dynamic). A true catch-all needs those build-time-fetching pages force-dynamic'd first (deferred follow-up). typecheck + lint + build (117/117) green. SPEC IMPACT none.

**2026-06-04 — vendor-pick "Add to your plan" overlay no longer distorts (0021).** Owner screenshot: the category picker's VERIFIED badge ballooned into a giant cream pill (surfaced once demo vendors populated the marketplace). Root cause = generic-classname CSS bleed: `CategorySearchOverlay` injects a global `<style>` and renders inside the plan-budget-accordion (`.pbacc`), whose card rule `.pbacc .v {min-height:300px;flex:1 1 auto}` matched the badge's `class="v"`. Fix: portal the overlay to `<body>` (mount-guarded) so it's no longer a `.pbacc` descendant → all `.pbacc *` bleed (badge, name, image) gone at once. `tsc` + `lint` green. **SPEC IMPACT: none.**

**2026-06-04 — dashboard scale consistency · Guests + Website editor (0021/0001).** Owner: *"keep our dashboard design consistent — use that kind of height and icon size and font size for guests, websites."* Aligned both surfaces to the canonical dashboard card metric (`customer-mobile-landing.tsx`): `h-10 w-10 rounded-md` icon chip · `h-5 w-5` glyph · `text-base font-semibold` label · `text-xs` sub · `min-h-[44px]` tap targets. Touches the Website **site-editor** (cards, stat rows, theme toggle, all buttons, empty-state) + the Guests page (Seating + Share-invite rows). The editor's 4-up tab bar + mobile carousel menu were left at the dashboard's `BottomNav` scale (forcing the card scale would overflow the grid); data rows / stat tiles / empty-state hero icons keep their own class. typecheck + lint green. **SPEC IMPACT: none.** No owner action.

**2026-06-04 — stop unexpected mobile zoom (native-app feel · 0000).** Owner: *"screen sometimes zooms in unexpectedly … we want it to feel like an app."* CSS-only hardening in `globals.css` (unlayered, so it beats Tailwind utilities): a `pointer: coarse` 16px floor on `input/select/textarea` kills iOS focus-zoom (root cause — inputs `font: inherit` shrink to 14px inside `text-sm` wrappers and Safari zooms in on focus); `html { touch-action: manipulation }` drops double-tap-zoom + the 300ms delay; `html { overscroll-behavior: none }` removes the pull-to-refresh bounce. Pinch-zoom left **enabled** (`maximumScale: 5`, WCAG 1.4.4). Viewport + manifest were already correct (no change). **SPEC IMPACT: none.** No owner action.

**2026-06-04 — DB-backed taxonomy read-through, Phase 2a (0023/0044).** New `lib/taxonomy-db.ts` `getTaxonomy()` (cached) reconstructs the taxonomy snapshot from `service_categories` + `canonical_service_taxonomy` (Phase 1 tables, applied), mirroring the `lib/taxonomy.ts` constant shapes, with **fallback to the constant** on error/unseeded (behavior-preserving). `/admin/taxonomy` flipped to it (first consumer · admin-only · shows DB-vs-fallback source). High-risk consumers (the live `/vendors` marketplace + `vendor-counts.ts` + 7 client components) staged as Phase 2b. typecheck + lint green. No migration. SPEC IMPACT none.

**2026-06-03 — demo-vendor Create now reliably passes the prod demo-mode gate (0023).** Follow-up to the same-day prod-under-demo-mode change, which still 403'd on the live admin because the `setnayan_demo_mode` signal wasn't reaching the seed POST (httpOnly cookie not surviving the fetch). Fix: the page computes demo mode server-side (`isAdminDemoModeOn()`, same cookie+admin check as the sitewide banner) and passes it to the Create button, which now sends `demoMode:true` in each request body; the seed route honors that explicit flag (admin still enforced). Also replaced stale "staging/dev only" admin copy. `tsc` + `lint` green. **SPEC IMPACT: none** (hardening of the already-logged decision).

**2026-06-03 — admin-editable wedding traditions table (0043/0023).** Step 3 of the per-religion work. New `wedding_tradition_items` table (migration `20260807000000`, owner-push) + `/admin/wedding-traditions` editor (Directory nav): per-religion edit/add/remove/reorder + "Load starter content" (copies the code defaults in, idempotent, never clobbers edits). `lib/wedding-traditions.ts` `fetchTraditionItems()` reads the table; `/paperwork` renders table items when present, else the code `WEDDING_TRADITIONS_GUIDE` (graceful fallback pre-migration). This is the validation path for the per-religion content (owner corrects INC/Muslim/Cultural/Chinese in-app, no deploy). typecheck + lint green. **Owner action: push `20260807000000` + Load starter content + validate.**

**2026-06-03 — haptics Settings toggle + 8 query-waterfall folds (UX/perf follow-up to #892).** Two owner-requested follow-ups ("both"). (1) A **Haptic feedback** toggle (iOS-style switch) now lives in customer Profile → Appearance next to the theme picker; it writes the `setnayan-haptics` localStorage key and `GlobalHaptics` was made reactive (re-reads live on a `setnayan-haptics-change`/`storage` event) so it toggles without a reload. (2) **8 independent query waterfalls folded into `Promise.all`** — papic add-on (4→1), vendor manpower (3→1), vendor bookings/repertoire, couple hosts/sponsors, admin vendors/disputes (each 2→1) — each verified independent (guard/auth chains + dependent reads left sequential). The audit confirmed event-home + both dashboard layouts were already parallelized (`site-editor` too, via a concurrent PR whose version merge took). typecheck + lint + build green. **SPEC IMPACT: none.** No owner action.

**2026-06-03 — DB-backed taxonomy tree, Phase 1 foundation (0023/0044).** Owner "build it" (the `/admin/taxonomy` editor + DB-backed taxonomy from the ♾️ "Finalize = permanent publish" lock). New migration `20260803000000` (owner-push) adds `service_categories` (10 parents + 54 tiles · self-ref `parent_id`/`tier`/`sort_order` + `scope`/`merged_into_category_id`/`sample_photo_r2_key`) + `canonical_service_taxonomy` (199 canonical→tile mappings + facet flags), seeded FROM `lib/taxonomy.ts` via a new generator (`apps/web/scripts/gen-taxonomy-seed.ts`) with an FK-integrity guard. RLS = public read / admin write (mirrors 0044). **Non-breaking** — nothing reads the tables yet; `lib/taxonomy.ts` stays the source of truth. Remaining: Phase 2 read-through + 19-consumer flip (high-risk · checkpoint first), Phase 3 visual editor + Finalize, Phase 4 §3.2c request review. **Owner action: push `20260803000000`.**

**2026-06-03 — per-religion vendor-readiness gate + admin control (0043/0023).** Owner: "INC needs INC-compatible services before we open it." New `/admin/wedding-types` (Directory nav) shows each religion's readiness — published vendors + ceremonial venues tagged `compatible_ceremony_types ⊇ religion`, vs an editable threshold — with Open/Hold/Disable writing `wedding_type_launch_status`. New `lib/religion-readiness.ts` (`fetchReligionReadiness` admin counts · `fetchActiveCeremonyTypes` gate). Gate now enforced couple-side: the onboarding faith picker is data-driven from launch status (greyed when not active), matching the create-event picker. Graceful fallback to all-available on read error. All religions stay live (owner kept everything live) — this is the control surface. No migration. typecheck + lint + full CI green.

**2026-06-03 — instant tab revisits (router-cache window) + site-editor fetch parallelization (perf; 0000/0021).** Complements the app-wide-skeletons work — those fix the wrong-shape flash on *first* visit; this fixes the RE-LOAD on *revisit* + the Website tab's slow first paint. Two changes: (1) `experimental.staleTimes {dynamic:60,static:300}` in `next.config.ts` — Next 15 defaults the client Router Cache to 0s, so re-tapping a tab seen seconds ago refetched + re-skeletoned; now revisits within 60s are instant (no round-trip, no skeleton). Safe — every mutation `revalidatePath()`s (100+ sites), busting the cache on real changes; the window only affects passive re-navigation. (2) `site-editor/[eventId]/page.tsx` — the Website-tab editor's 6 sequential Supabase awaits collapsed into one `Promise.all` (6 → 2 phases), pairing with its `BoardPageSkeleton` shell. `tsc --noEmit` + `next lint` + `next build` green; staleTimes confirmed in Next 15.5.18's config schema. SPEC IMPACT none.

**2026-06-03 — demo-vendor "Create" works on production under demo mode (0023).** Owner tapped Create demo vendors on the live `/admin/demo-vendors` and the progress bar "ends and does not complete" — its first request was 403'd by the prod safety guard (demo vendors were staging-only). Owner had **demo mode ON** (banner + Dec 1 2026 cleanup deadline) and was deliberately populating the live deploy. Scoped the guard (`apps/web/app/api/admin/demo/seed/route.ts`): non-prod always allowed; **prod allowed only while admin demo mode is on** (`isDemoMode` — `setnayan_demo_mode` cookie / `?demo=1`); demo-off prod stays blocked. Safe — the marketplace only shows `is_demo` rows under demo mode, so real-vendor posture is unchanged; Cleanup ALL wipes them. `tsc`+`lint` green. **SPEC IMPACT: yes** (staging-only guard scoped) → `COWORK_INBOX.md` + `DECISION_LOG.md`.

**2026-06-03 — app-wide loading skeletons + global tap haptics (UX polish; 0000/0001/0021/0022/0023).** Owner report ("why is it so slow to transfer to guests from summary") → the lag was mostly *perceived*: only 4 segment-level `loading.tsx` existed, so ~160 child routes froze on their server reads with no instant feedback. Added a shared skeleton kit (`components/skeletons`, 8 server-component templates + a `.skeleton` shimmer in globals.css, reduced-motion safe) and **151 route-local `loading.tsx`** (4 → 155) covering every customer/vendor/admin dashboard route + guest-facing dynamic routes — each mirrors its page shape; static marketing, onboarding (preloaded) + print/api excluded. Wired **global tap haptics** (one passive `pointerdown` listener → light `tick` on any control, reusing `lib/haptics.ts`; opt-out via `[data-no-haptic]`/`localStorage`) so haptics fire app-wide instead of in just 3 vendor components. Also folded the Guests `fetchJoinUrl` into its `Promise.all` (one fewer Singapore RTT). typecheck + lint + build green. **SPEC IMPACT: none.** No owner action.

**2026-06-03 — wedding onboarding caters all faiths (0016).** Owner ("fix all gaps… cater all different religious weddings"). Faith-adaptive ceremony venue — `ceremonyOptsFor(faith)` swaps the house of worship (Catholic/Christian→Church · INC→Chapel · Muslim→**Mosque** · Chinese→**Temple** · Cultural→outdoor/ancestral) + universal Garden/Beach/Civil/Same; two matching photos generated via Recraft. De-churched copy ("A faith ceremony" / "Where will you hold your ceremony?" / venue blurb / groom role). Chinese activation = #889 (deduped on merge; my redundant migration dropped). `tsc --noEmit` exit 0.

**2026-06-03 — per-religion wedding traditions guide (0043).** Owner: "create onboarding that follows the traditions of each religion." Built on the existing per-religion doc/deadline engine (`lib/paperwork.ts` already gives each ceremony_type its own documents + lead-time deadlines flowing into /paperwork + the /schedule agenda + Home reminders). New `lib/wedding-traditions.ts` (`WEDDING_TRADITIONS_GUIDE` per ceremony_type) drives a "What to expect — your {religion} wedding" section on /paperwork — overview + officiant/ceremony/food/custom items + "confirm with your {officiant}." Chinese seeded forward-compat. Content is honest STARTER guidance (NEEDS owner/clergy validation, flagged for Cowork). typecheck + lint green. No migration.

**2026-06-03 — event_type enum guarantee + create-event copy aligned to "all live" (0000/0041).** Follow-up to "keep everything live." Migration `20260805000000` (owner-push) belt-and-suspenders the event_type enum (`ADD VALUE IF NOT EXISTS` debut + gender_reveal — already in prod per #884 — plus NEW seedable anniversary/graduation/reunion). `create-event/page.tsx` copy de-stale'd: the header + `invalid_type` error no longer say "only weddings live" or "tap to be notified" (no notify flow exists). The 3 new types are enum-only (not in the picker roster). typecheck + lint green. **Owner action: push `20260805000000`.** Spec-0000 reconciliation (all-live, no notify) logged for Cowork.

**2026-06-03 — Chinese wedding added as a coming-soon ceremony type (0043).** Owner: "on weddings, also add chinese wedding." Chinese (Tsinoy — tea ceremony + Chinese customs) joins the wedding-type lineup as the lone **coming_soon** faith (the others were unlocked the same day) — surfaced in the create-event picker (greyed + notify-me) and onboarding (greyed chip), gated until vendor density can cater it. Migration `20260804000000` (owner-push) widens the 4 ceremony CHECK constraints to permit `chinese` + seeds its launch-status row coming_soon (widening-only → activation later is migration-free). 9 code touchpoints (shared radio options, picker secondary label, notify list, launch-status fallback, onboarding faith type/photo/chip, edit-modal disable, admin venue constants + label map) + new hero `public/onboarding/wed_chinese.webp`. Admins can already tag Chinese-compatible venues/vendors to build supply. typecheck + lint green. **Owner action: push `20260804000000`.**

**2026-06-03 — in-app service tiles made clickable for checking (0004/0010/0021).** Owner directive ("for now we want to unlock all to check"). After religions + events, the remaining services-catalog locks were 3 coming-soon tiles: **Monogram Creator** repointed to the real `animated-monogram` studio (0004), **Mood Board** surfaced from its real `/add-ons/mood-board` route (0010, was built-but-unsurfaced), and **Landing Page** + **Music Creator** made clickable to their polite `[addon]` info pages. Catalog-only (`lib/add-ons-catalog.ts`) → propagates to the /add-ons grid + the Services-tab rails. `tsc --noEmit` exit 0. **Left intentionally gated** (hide unbuilt/partial features — flipping would surface stubs/broken flows): the 8 not-built pricing SKUs, Concierge (`CONCIERGE_ENABLED=false`, retired), OAuth sign-in (needs real credentials), offline daemon (upload stubs). Those need building, not a flag flip.

**2026-06-03 — one-click "Create demo vendors" on /admin/demo-vendors (chunked seed · admin console 0023).** Creating demo vendors was CLI-only (Regenerate just cleaned + printed the command). Now a Create button clicks once → the browser loops category-by-category against a new chunked API (`/api/admin/demo/seed` · start+chunk) until the marketplace is seeded, with a progress bar. Refactored the seed core to be importable WITHOUT moving it: extracted `export seedCategory()` + exported the fetch/cleanup helpers + `isNonProdUrl`, and guarded the CLI entrypoint so importing never auto-runs (per-category RNG ⇒ chunked output == CLI output; CLI behavior preserved). Non-prod-gated (refuses prod → 403 on the live deploy; works on staging). No migration. typecheck + lint green; refactor-safety smoke tests pass. **Owner:** on staging, click Create on /admin/demo-vendors. CI gates the production build (route bundles the scripts/ import).

**2026-06-03 — onboarding completion hang fixed (0016).** The final "Creating your personalized dashboard" overlay could strand a couple forever (owner report, production iPhone): `handleFinish` awaited the commit server action with no try/catch, so a rejected action (500 / serverless function timeout / dropped RSC transport on mobile) left the blocking overlay up with the retry guard locked. Fixed across the three weak points — try/catch around the commit (unwind + show the existing retry error on reject) + a hard-`window.location.assign` watchdog if the client router wedges or `router.push` no-ops + a 2s `AbortController` on the awaited PostHog `captureEvent` fetch (it could drag the serverless function to its timeout) + the post-insert shortlist/anchor seed wrapped non-fatal (a throw there rejected the commit *after* the event existed → duplicate-on-retry). typecheck + lint green. SPEC IMPACT none. Follow-up flagged: the commit is still non-idempotent on the other failure branches (needs a client idempotency key + server dedup).

**2026-06-03 — all event types unlocked (0000/0041).** Owner directive ("unlock all events"): the seven "Coming soon" event types (Gender Reveal · Birthday · Celebration · Travel · Corporate · Tournament · Christening) are now creatable alongside Wedding + Debut. Two code gates: `EVENT_TYPES[].enabled` (event-types.ts — drives both the create-event picker AND the add-event sheet) + `ALLOWED_TYPES` (create-event/actions.ts server validation). No migration — the `public.event_type` enum already held all 9 values (verified against prod); the `isWedding` branch already NULLs wedding-only fields for non-wedding events; they redirect to the standard dashboard (the path debut already exercises). `tsc --noEmit` exit 0. **Downstream caveat:** non-wedding events get the wedding-tailored planning surfaces until per-type flows land (V1.2+).

**2026-06-03 — all wedding faiths unlocked (0043/0016).** Owner directive ("unlock all religions first"): Christian / INC / Muslim / Cultural flipped from "Coming Soon" to **active** across all five gates — the onboarding faith chips (`FAITH_CHIPS` `soon:false`), both `ALLOWED_CEREMONIES` server constants (the onboarding commit no longer silently coerces non-Catholic → catholic), the create-event launch-status fallback, and the canonical `wedding_type_launch_status` table (migration `20260803000000`, every row → active; verified applied to prod by direct query). The DB CHECK requiring a Muslim/Cultural tradition sub-type is satisfied in onboarding by defaulting (`general_muslim` / `other`) since onboarding has no tradition picker (create-event collects it). Overrides the per-region vendor-density activation gate (global unlock). Full CI green. No owner action (migration already applied).

**2026-06-03 — in-app services nested INTO the Vendors-tab category rails (0021).** Retired the standalone in-app-services launcher grid; Setnayan services now render as ✦ Setnayan supplementary cards (float-to-top) inside their canonical category — Save-the-Date/Papic/Panood → Photography & Video · Patiktok → Photobooth · LED → LED Background · Animated Monogram → a new Design › Digital Services rail — with a compact "Tools & extras" strip for non-category tools. Single `category` field on the add-ons catalog drives placement. typecheck + lint + runtime partition check green; visual check on the Vercel preview. **Follow-ups:** §3 vendor-model convergence + fleshing out the Digital Services rail (Pakanta / Pro Website / Live Wall need catalog entries + routes).
**2026-06-03 — event-type picker → swipeable hero-photo carousel (0000).** Owner ask (screenshot of the add-event sheet): *"we want a carousel but like hero photos. let them scroll all the possible events."* New shared `event-type-carousel.tsx` — a horizontal scroll-snap filmstrip of full-bleed Recraft hero photos (one per event type), swipe to browse all; arrows + dots track the centred card. Live types (Wedding/Debut) = colour + gold "Available" + tap-to-continue; coming-soon = grayscale + inert + "Coming soon". Both the event-switcher sheet AND the full `/dashboard/create-event` page now use it (old emoji-tile `Tile`/`ArrowButton` deleted). 9 hero photos at `public/event-types/` (15.5 MB → 541 KB). Switcher subtitle copy de-promised the never-built "notify me" line. `tsc --noEmit` + `next lint` clean; live preview not run (auth+DB-gated, no `NEXT_PUBLIC_SUPABASE_*` here). No migration.

**2026-06-03 — customer top nav slimmed: Marketplace + Switch View icons removed (0000/0021).** Owner directive ("remove these 2 on top nav", both circled on mobile): dropped the 🏪 Marketplace (`/vendors`) link + the 👤﹀ Switch View `RoleSwitchPill` from the customer **top bar** — event-scoped (`[eventId]/layout.tsx`) AND non-event routes (`outer-dashboard-header.tsx`). Top bar is now event-switcher monogram · Messages · Bell · Profile-monogram. The desktop **left sidebar** keeps both per the owner's scope choice. Nothing orphaned (Marketplace via the home tease-strip / "Browse matched services" / plan cards / sidebar; role-switch via the event-switcher dropdown + sidebar). `next lint` + `tsc --noEmit` clean. No migration.

**2026-06-03 — admin song dedup/merge tool · COMPATIBILITY BUILD COMPLETE (PR 6 · 0023/0006).** `/admin/songs`: search the master catalogue + merge near-duplicates (Duplicate ID → Canonical ID; `mergeSongs` re-points repertoires + picks then deletes the dup) + remove junk. `requireAdmin`-gated actions; "Songs" nav item by Taxonomy. typecheck + lint + build green. **This completes the compatibility build (PRs 1–6)**: schema+seed · vendor "Your repertoire" · couple picks → event_song_picks · the overlap score+cue on the music cards · the admin dedup tool. **Owner action remains: push migration `20260731000000`.** Open refinements: explicit Best/Next-best section-headers, the marketplace/Category-Search cue, the catering food-look parallel.

**2026-06-03 — music compatibility score live on the wizard music cards (compatibility PR 4 · 0006/0016).** `fetchWizardVendorRecommendations` now (for music categories with a matched event) re-ranks vendors by song overlap (couple `event_song_picks` ∩ vendor `vendor_songs`) — matches float to the top, never excluded — and each card shows "♪ Best match · plays N of your M songs" (≥90% = best). Optional `matchEventId` arg + optional score fields (24 callers safe); over-fetch→stable-sort→trim; the 2 music cards + in-card search pass it through. typecheck + lint + build green. **Owner action:** push migration `20260731000000` to light it up. Next: PR 6 admin dedup. (Explicit Best/Next-best section-headers + the marketplace/Category-Search cue = noted refinements.)

**2026-06-03 — couple onboarding music picks → event_song_picks (compatibility PR 3 · 0016/0006).** The onboarding music picker now also writes the couple's chosen songs to `event_song_picks` (the couple side of the music overlap), alongside the display-only `music_playlist_seed`. `lib/songs.ts` `syncEventSongPicks` (find-or-create each "Title|Artist" → upsert), called from `commitOnboardingWedding` with the service-role client, wrapped so it can never fail the commit (graceful before the 20260731000000 migration is pushed). typecheck + lint + build green. Data only. Next: the compatibility score in the recommender + the 90% split.

**2026-06-03 — vendor "Your repertoire" capture (compatibility PR 2 · 0022/0006).** Music acts (band/choir/orchestra/singer/DJ) now build their song set list at `/vendor-dashboard/repertoire` — search the master library (the seeded MUSIC100), add existing or new songs (new ones join the deduped catalogue), manage/remove. The vendor side of the music compatibility overlap. `lib/songs.ts` helpers (RLS-safe select-then-insert find-or-create) + the page/actions + a Pipeline nav item; gated to music vendors with a clear explainer for others. typecheck + lint + build green. Next: onboarding picker → event_song_picks → the score.

**2026-06-03 — chrome monogram = the full framed onboarding monogram + exact fonts + event logo (0000/0021).** Follow-up to PR #863 closing the 3 parked items: the switcher icon + the upper-right profile avatar now render the couple's ACTUAL framed onboarding monogram (gold frame webp + initials in the chosen font + ink — the onboarding medallion scaled down), not letters-forward; the exact display faces (Cinzel · Playfair · Great Vibes) are loaded in the chrome; and the profile avatar is now the event's logo (owner "that will be the logo of the event"). typecheck + lint + build green. Flagged for owner eyeball (frame legibility at the small 28/36px switcher sizes; the 44px avatar reads best).

**2026-06-03 — vendor-compatibility build started: master song list foundation (PR 1 · 0006/0044).** Owner-locked the compatibility model (corpus `Vendor_Compatibility_and_Master_Songlist_2026-06-03.md`): vendors place the songs they perform → compiled into a shared **master song list**; couples pick from it; music-vendor **compatibility = song overlap**, matches float up, nobody hidden, `<90%` = "next best options." PR 1 lands the substrate — migration `20260731000000`: `songs` (master, seeded from the curated MUSIC100, deduped), `vendor_songs` (repertoire), `event_song_picks` (couple picks); RLS at create. This is the data the parked **"best-match recommender (PR D)"** needed. Foundation only — no app behavior change. **Owner action:** push `20260731000000`. Next: vendor "Your repertoire" capture → picker→master → the score.

**2026-06-03 — Admin demo-vendor inquiry responder + unique demo emails (admin console 0023 · chat 0019).** So the team can test the customer↔vendor inquiry round-trip from one place. Demo vendors were unclaimed (no one receives inquiries) AND shared one contact_email (ambiguous `.maybeSingle()` lookup → couples couldn't start a thread). Now: (1) the seed gives each demo vendor a unique `${slug}@demo.setnayan.local` so a couple's Message flow resolves to exactly one; (2) new `/admin/demo-vendors/inquiries` (+ `/[threadId]`) lists demo-vendor inquiry threads and lets an admin Accept/Decline/reply **as the vendor** via the service-role client — double-gated (admin + is_demo only). Reply posts `sender_role='vendor'`; Accept fires the existing name-reveal trigger. No migration; reuses chat tables. typecheck + lint green. **Owner action:** re-seed staging, then run the round-trip (couple Message → admin responds). **Parked:** best-match recommender (PR D) until taxonomy settles.

**2026-06-03 — onboarding free monogram → event-switcher icon (0000).** The couple's free onboarding monogram (chosen frame + font, persisted `events.monogram_frame_key`/`monogram_font_key`) now renders as their **event-switcher icon** instead of a plain initials circle — `EventMonogram` renders letters-forward in the couple's chosen font + ink (the ornate gold frame is illegible at ~28px, so it's reserved for the larger onboarding medallion / Website editor). Threaded the two columns through the switcher data path (`lib/events.ts` · both `/dashboard` layouts · `outer-dashboard-header` · `event-switcher`); all new fields optional → admin chrome + non-onboarding events unchanged. typecheck + lint + build green. **Open fork:** initials-in-font+ink vs the literal framed mini-monogram (fast follow). Spec correction logged in `COWORK_INBOX.md` (corpus rows name `events.monogram_svg`; real columns are `monogram_frame_key`/`monogram_font_key`).

**2026-06-03 — Demo-vendor testing tools: calendar blocks + claim helper + 20–50 default (marketplace simulation · follow-up).** Unblocks testing the two real flows with demo data. (1) The seed now gives each demo vendor 2–8 full-day `vendor_calendar_blocks` so the **mutual-schedule narrowing** (`lib/vendor-availability.ts`) is exercisable — previously demo vendors had no blocks (always free) so the intersection never narrowed. (2) New `scripts/claim-demo-vendors.ts` claims ONE demo vendor to a vendor user (`user_id`+`is_demo=false`+unique `contact_email`; 1:1-guarded) so it can **receive/reply to inquiries** — demo vendors are otherwise unclaimed (`user_id=NULL`) and inquiries orphan. (3) Seed default bumped 5–10 → **20–50 vendors/category** (owner's testing target; ~4–9.6k/run). Seed/scripts only, no migration. typecheck+lint green; block harness confirms the 30-min/zero-second CHECK holds across timezones. **Owner action:** re-seed staging; for Q5 lock 2–3 demo vendors and watch the schedule narrow; for Q4 run the claim helper + the inquiry round-trip. **Next:** best-match recommender (PR D).

**2026-06-03 — Demo vendors get reviews/ratings, district addresses & real names (marketplace simulation · follow-up).** So demo vendors can exercise find → compare → "pick the best." Seed-only (no migration): each vendor gets a hidden baseline quality + 0–10 synthetic reviews (five 1-5 sub-axis ratings, ~60% with a Filipino-voice body, ~20% vendor reply) — `couple_user_id=NULL` (self-review trigger short-circuits) reusing the archived `TEST-REVIEW · %` event pool (migration 20260607000000) for the NOT-NULL event_id, bulk-inserted so the `vendor_review_stats` matview refreshes only a few times; ratings now differentiate vendors (per-vendor mean ⭐ spread 3.0–5.0). Addresses go district-level (`{District}, {City}, Philippines`); the `Demo ·` name prefix is dropped (is_demo flag unchanged). typecheck + lint green; offline harness clean. **Owner action:** re-run the seed on staging, then check `/vendors?demo=1&sort=highest_rated` + compare + a demo `/v/[slug]`. **Next:** the "best match" recommender (4th ask) builds on these ratings.

**2026-06-03 — Demo vendors get real per-category details, richer packages & images (marketplace simulation · 0044/0022).** Owner flagged that the admin Demo Vendors seed should make synthetic vendors *"provide the details and customization for each of the categories."* The seed (`scripts/seed-demo-vendors.ts`) was writing one generic 5-field blob for all 192 categories + hard-coding `completeness_score:75`/`meets_visibility_minimum:true` (and never filling the real `service_regions` minimum field). Now: a **schema-driven generator** loads each `canonical_service_schemas` row + inherited shared groups and fills realistic, schema-valid per-field values, with **honest** completeness + visibility scoring (minimum fields always filled → still visible, now earned; ~18% of optional fields left unset → ~80-100 variance). **Richer packages** — 7 new `priceProfileFor()` buckets so niche categories (booths/stations, keepsakes, accessories, rentals, ceremony paperwork, wellness, food carts) get category-appropriate tiers instead of the generic catch-all. **Images** — seed sets `logo_url` + `portfolio_r2_keys` to deterministic picsum URLs; `vendor-card.tsx`'s image guard now allows picsum (already in `next.config.ts`). **Public profile render** — `/v/[slug]` gains a **Details** section (per-category attributes) + a **Portfolio** gallery (reuses `fetchVendorServiceAttributes`/`fetchSchemaWithSharedGroups`/`displayUrlForStoredAsset`); benefits real vendors too. typecheck + lint green; offline generator harness confirms minimum-fields filled + honest scoring + `required_if`. **Owner action:** run the seed on **staging** (`SUPABASE_URL` + service key for a non-prod project; the script refuses prod) to repopulate the demo batch, then spot-check `/vendors?demo=1` + a demo `/v/[slug]`.

**2026-06-03 — Typed Preparation items: meeting & payment schedules (couple + vendor · 0021/0022/0007).** Owner follow-up to PR #845. The hand-added prep items on the couple's `/schedule` **Preparation** agenda can now be **typed**: a couple or a booked vendor places a **Task**, a **Meeting**, or a **Payment**. Meeting rows render with the SAME Meeting tag/icon as the autofilled `vendor_meetings`; Payment rows render with the SAME Payment tag/icon **+ the ₱ amount** (formatted like autofilled vendor-payment rows); Task rows keep the prior manual style. Both add modals gain a shared Task/Meeting/Payment segmented picker (`prep-kind-picker.tsx`) + a conditional Amount (₱) field (Payment only); the couple + vendor actions validate amount > 0 and stamp `kind`/`amount_php`. Backed by NEW additive migration `20260730000000_event_preparation_item_kinds.sql` (**owner-push** — adds `kind` + `amount_php` columns, **no RLS change**; #845's policies already cover them). **GRACEFUL DEGRADE preserved:** `SELECT *` + `kind ?? 'task'` / `amount_php ?? null` so pre-migration rows coalesce to plain tasks, and the whole source still returns `[]` if the #845 table is absent. Autofill + Event Day mode untouched; no 4th Home block. **Schema reason:** typed items live on `event_preparation_items` (correct `vendor_profile_id` RLS) because `event_vendor_line_items` + `vendor_meetings` key to the TEXT-named `event_vendors.vendor_id`, which a platform vendor can't be RLS-scoped to. **Limitation:** prep-payments show on the schedule only, NOT the couple Budget ledger (0007) — candidate follow-up. typecheck + lint + build green. **Owner action:** push `20260730000000`.

**2026-06-03 — Onboarding congrats vendor stat → REAL marketplace counts (onboarding · 0015/0016).** The `/onboarding/wedding` "You did the hard part" congrats screen's third stat tile no longer fabricates **"N best-fit vendors from 2,400+"** (was `picked-categories × 5` floored at 12 + a hardcoded "2,400+"). New criteria-based server action `getOnboardingVendorCounts` returns two real `vendor_market_stats` head-counts — `matched` (published vendors in the couple's picked categories that fit their ceremony/venue) and `total` (the published pool for those categories) — using the **same published-pool definition as the `/vendors` marketplace** (`lib/vendor-counts.ts`), so the tile matches reality. Renders "{matched} that fit your wedding · from {total}" and **auto-hides** when uncomputable (never fabricates). Money + hours tiles unchanged. Ships the never-merged `4af4f6c` (cherry-picked onto fresh `main`; the original branch had no PR and went 66 commits stale). typecheck + lint green. **No migration.**

**2026-06-03 — Photo Delivery "Release to Drive" made functional (cron-free).** The release action now drains via Next 15 `after()` instead of the dormant `photo-delivery-tick` cron (which never had a scheduler), so releasing actually copies to Drive. Pairs with Phase 2's capture auto-sync — the whole Drive surface is cron-free. `oauth-refresh` cron left dormant (redundant; consumers refresh on-demand).

**2026-06-03 — Schedule Preparation is now HYBRID (couple + vendor manual items · 0021/0006/0022).** Completes the fast-follow #840 deferred. The couple's `/schedule` **Preparation** agenda gains a write path on top of the read-only autofill: couples can **+ Add to schedule** their own dated items (modal: label / date / notes; bottom-sheet on mobile) and delete items (incl. dismissing vendor-added ones); booked vendors get an **Add to prep schedule** control on each **accepted** booking in `/vendor-dashboard/bookings` (+ a delete list of their own additions). Backed by NEW table `event_preparation_items` (migration `20260729000000`, **owner-push**) with RLS-at-create: couple full CRUD via `current_couple_event_ids()`; vendor SELECT on accepted-thread events, INSERT only for accepted threads stamping their own `vendor_profile_id`, UPDATE/DELETE own rows only. New `'manual'` source in `lib/preparation.ts` merges into the date-sorted agenda and **graceful-degrades** (catches `42P01` → autofill-only) so the deploy is safe pre-migration. Autofill + Event Day mode untouched; Home 3-block rule respected. typecheck + lint + build green. **Owner action:** push `20260729000000`.

**2026-06-03 — Drive-copy Phase 2 (Papic auto-sync, cron-free).** Papic captures (paparazzo + guest camera) now `enqueueDriveCopy('papic')` + drain in the background via Next 15 `after()` → couple's Drive (same folder as the manual release, deduped). No cron (the repo's existing cron endpoints have no scheduler anyway). 5 other feeders await their services' pipelines. `readR2Object` now strips `r2://` prefixes (latent fix).

**2026-06-03 — Messages icon unread badge (delta #2 follow-up · 0019/0021).** The icon-only `MessageSquare` link from PR #837 now carries an unread count, mirroring the bell. New per-user/per-thread read marker `chat_thread_reads` + `count_unread_message_threads()` RPC (NEW migration `20260728000000_chat_thread_reads.sql` — additive, RLS-at-create, **owner-push**). `countUnreadMessages()` (`lib/chat.ts`) drives the badge; `markThreadRead(threadId)` (`lib/chat-actions.ts`) stamps `last_read_at` on render of both the couple + vendor thread pages; new client component `unread-messages-badge.tsx` resyncs via Realtime on `chat_messages` INSERT. **Graceful-degrades to 0 pre-migration** (mirrors `countUnread`) so the deploy is safe before the owner applies it — badge reads 0, opening a thread never fails. One SQL fix vs draft: `current_vendor_ids()` (a NULL stub) → `current_vendor_profile_ids()` for the vendor-side count. typecheck + lint + build green.

**2026-06-03 — Schedule Preparation⇄Event Day toggle (chrome redesign delta #3 · 0021).** The couple's `/schedule` page now carries a URL-driven `Preparation | Event Day` segmented toggle. **Event Day** = the existing editable day-of blocks UI (untouched — lifted verbatim into an `EventDayView` helper). **Preparation** = a NEW read-only, month-grouped agenda that auto-fills from EXISTING dated data via new `lib/preparation.ts`: vendor **payment** due dates (`event_vendor_line_items`, fully-paid lines dropped), **paperwork** "complete by" deadlines (`event_paperwork` + `lib/paperwork.ts`), vendor **meetings** (`vendor_meetings`), and statutory **milestones** (PSA/license/Pre-Cana windows computed from `event_date`+`ceremony_type`). **No new table/migration** — pure aggregation. **Deferred:** manual user-added prep items (needs a table → `COWORK_INBOX`). **Absent (documented):** orders have no due-date column (`expires_at` is renewal billing, omitted); Concierge/Today's Focus has no per-step dated milestone. **Home untouched** — the lean-home 3-block rule is respected. Final delta of the **4-delta** 2026-06-03 chrome-redesign port. typecheck + lint green.

**2026-06-03 — In-app add-ons surfaced inside Services tab (chrome redesign delta #4 · 0006/0021).** The "Services" tab (`/vendors`) now shows a compact "In-app services & add-ons" section (horizontal-scroll mini-card grid on mobile, 4-col on desktop) below the vendor plan+budget accordion. Reuses shared `lib/add-ons-catalog.ts` (extracted from add-ons/page.tsx). Canonical `/add-ons` route untouched. All 4 chrome-redesign deltas are now shipped. typecheck + lint green.

**2026-06-03 — Drive-copy Phase 0 (OAuth consolidation).** Collapsed the two per-event Google Drive connections (Papic `provider='drive'` + Photo Delivery `provider='drive_photo_delivery'`) into ONE: the Photo Delivery connect now routes through the canonical Drive consent + redirect URI + `provider='drive'` grant; release worker + disconnect + the drive-copy layer all read the one grant. Migration `20260727000000` is a safety-net data backfill. **Owner action:** push `20260727000000`; register only `GOOGLE_DRIVE_OAUTH_REDIRECT_URI` (retire `PHOTO_DELIVERY_OAUTH_REDIRECT_URI`). Next: Phase 2 (wire the 6 feeders).

**2026-06-03 — Messages icon in top bar (chrome redesign delta #2 · 0019/0021).** `MessageSquare` link added to the event-scoped top bar right cluster, adjacent to the bell, linking to `/dashboard/[eventId]/messages`. No unread badge (no `read_at` column in `chat_messages` in V1 — badge deferred to a follow-up). typecheck + lint green.

**2026-06-03 — Home "Your wedding details" card (chrome redesign delta #1 · 0021).** Event Home now surfaces the couple's onboarding details as one compact kv card (Location · Venue · Guests · Budget · Style · Cuisine · Photo & video) with a "See all wedding settings →" link to `/details`. Reshapes the existing `PersonalizedMenu` preview (chips → card); `/for-you` unchanged. First of **4 deltas** porting the 2026-06-03 chrome redesign to live — an audit found most of the redesign (5-tab nav, Website tab, `/details` settings, Messages, top-bar Switch/bell) was **already shipped**; the remaining 3 deltas are the top-bar Messages icon, the Schedule Preparation⇄Event Day toggle, and the Service+Add-ons merge. typecheck + lint green.

**2026-06-03 — Drive-copy layer keystone (storage lock).** Shipped Phase 1 of the 2026-06-03 storage architecture: R2 = system of record, Google Drive = the couple's permanent copy of 6 artifacts (Papic · Patiktok · Pabati · Pakanta · Monogram · QR codes; Panood carved out → YouTube only). New `lib/drive-copy.ts` `pushToDriveCopy()` + `drive_copy_folders`/`drive_copy_artifacts` schema (migration `20260726000000`), plus a behavior-identical extraction of the R2→Drive primitives into `lib/drive-upload.ts` (shared with the live 0009 flow). Additive + pilot-safe; no feeders wired yet. **Owner action:** push migration `20260726000000`. Follow-ups: OAuth consolidation (Phase 0), 6 feeders, cron tick, R2 3-month compress, Drive quota fallback. Design + worklist: corpus `Storage_and_Drive_Copy_Architecture_2026-06-03.md`.

**2026-06-03 — mobile Guests carousel reshaped (0001).** Customize panel is now select-and-assign (tap Select → card checkboxes → select-all + live count + Assign → bottom sheet: Side / Role / Group, with create-new-group); the View/Groups/Tags filters folded into Search & sort; sort gains Side/Role/Group; mobile header removed (Summary carries the count); carousel docked as a clean raised sheet (no doubled border). Desktop unchanged. Spec follow-up logged in `COWORK_INBOX.md` (`[PENDING] 2026-06-03`).

**2026-06-03 — bride & groom are the event foundation (0001).** Couple auto-Attending (DB trigger `20260725000000` + app read-coercion), can't be deleted (single + bulk guard), still renamable, and Bride/Groom hidden from the role pickers (role + RSVP locked on their detail page). Owner action: push the migration (`OWNER_ACTIONS.md` 2026-06-03) so the stored RSVP matches the UI.

**2026-06-03 — couple detail kept simple (0001).** The briefly-shipped editorial live-view iframe was reverted per owner clarification — the bride/groom detail just shows their info, like any other guest (foundation locks retained: auto-Attending, can't-delete, role/RSVP locked). "Editorial" = the `/[slug]` page's post-wedding recap state (day-of lifecycle, 0031), which activates at the end of the wedding — nothing separate to build.

**Task #13 (2026-05-22) — day-of PWA Phase 1.** Public guest surface at `/[slug]` flipped from `force-dynamic` to ISR (`revalidate = 60`); day-of-mode lifecycle branches (`pre` / `live` / `post`) wired via `getDayOfPhase`; new `GuestPreload` client component posts `PRELOAD_ASSETS` to the SW on hydration so guest at venue with weak WiFi sees the invitation from SHELL_CACHE on reload instead of a blank page. Live phase pins schedule + green "Live now" banner to top; post phase shows quiet "Thank you for celebrating" header. Manual offline test recipe added to `OWNER_ACTIONS.md` (Playwright deferred — no test infrastructure in repo yet). Phase 2 per-guest table-assignment preload deferred to V1.1 per Task #9 audit.

**Wave 1 — launch hardening (PRs #1–#23):** landing-page conversion upgrades, full observability (Sentry + PostHog), R2 storage migration, day-of mode + event-day pre-load, account-lifecycle redesign (Delete vs Blacklist), persistent login, caching foundation, Services → Add-ons rename, Phase 1 placeholder routes, 7 of 10 V1 email templates wired through Resend, status doc refresh.

**Wave 2 — Phase 2 closed (PRs #24–#28, merged 08:40–08:43Z):** 5 background agents shipped in parallel:
- **#24** vendor marketplace at `/vendors` + reviews system (couple form, vendor reply, public profile section)
- **#25** vendor dashboard expansion — services editor + bookings inbox + 4-role team + earnings rollup
- **#26** admin force-majeure queue + couple-side dispute filing + admin funnel analytics (`/admin/funnels`)
- **#27** read-only public API (`/api/v1/events|guests|vendors`) + scope-gated `sk_live_*` keys
- **#28** EN/TL dashboard locale toggle + 2 more email templates (event-wired count 7 → 9)

Email-wired count is now **9 of 10** V1 templates. Only Phase 3 decision-gated items below remain.

> **🔴 Active prod-deploy gap (Task #49, 2026-05-22):** Guest-list edit form throws `invalid input value for enum guest_role: "bride"` / `"groom"` because `20260530020000_guest_role_add_bride_groom.sql` (committed 2026-05-21) hasn't been applied to prod. Run `supabase migration list --linked` from your local checkout to see all unpushed migrations from the last 12 days, then `supabase db push --linked` to apply them. Full step-by-step in `OWNER_ACTIONS.md` punch-list item #9.

> **🔴 Before next session:** run `npx supabase db push --db-url "$SUPABASE_DB_URL"` to apply 6 unpushed migrations (`blacklisted_emails`, `vendor_reviews`, `vendor_dashboard_expansion`, `force_majeure_flags`, `notification_type_additions`, `api_scopes`). The new surfaces will 500 against prod DB until pushed.

---

## 2026-05-14 — full PR run

Merged commits on `main`, newest first:

| PR | Commit | What |
|---|---|---|
| #28 | 9a966d0 | 0025+0028 — EN/TL dashboard locale toggle + 2 more email templates (`help_ticket_replied`, `vendor_inquiry_received`) |
| #27 | 0fbd6f7 | 0033 — read-only public API (`/api/v1/events|guests|vendors`) + scope-gated `api_keys` |
| #26 | 4bc0af3 | Admin force-majeure queue + couple disputes + 3 Supabase funnels + 4 PostHog funnel links at `/admin/funnels` |
| #25 | 9f44813 | 0022 — vendor dashboard expansion (services editor + bookings inbox + 4-role team + earnings rollup) |
| #24 | cfa9402 | 0006 + vendor-reviews — public marketplace at `/vendors`, couple review form, vendor one-time reply, public profile section |
| #23 | c6d45ca | docs(status): EOD 2026-05-14 refresh (anchor doc) |
| #22 | e74b169 | Phase 1 placeholder routes + nav (10 new surfaces, 2 add-ons grid entries) |
| #21 | 4833541 | Landing page conversion upgrades — split CTA, trust signals, pricing transparency |
| #20 | 4941a6f | 0028 RSVP-received email + in-app notification |
| #19 | 124b6e4 | PostHog wiring + 3-event funnel (signup_completed, event_created, order_paid) |
| #18 | 351715b | R2 storage migration (uploads off Supabase Storage) |
| #17 | 835aeef | Sentry error tracking wired |
| #16 | 7349666 | Add-ons status pills (Web V1 / Coming soon) + admin-only dev mode |
| #15 | 7dc9aa2 | CI build job — catches Next compile-time errors before merge |
| #14 | eec8fd2 | Vercel build fix — split client-safe query keys out of server-only event-preload |
| #13 | 65f8f68 | Services → Add-ons rename across the couple dashboard (308 redirects) |
| #12 | b049f99 | 0036 event-day pre-load — couple + vendor (T-3d → T+1d CTA, T-24h auto) |
| #11 | 327b489 | 0031 day-of live mode — auto-activation + 6 cards |
| #10 | c582a4d | Caching foundation — TanStack Query + persister + route-scoped SW |
| #9 | fac3e75 | Account lifecycle redesign — Delete vs Blacklist + migration |
| #8 | 865ea46 | Resend env var name fix + signup post-confirm redirect |
| #7 | 4e41f83 | Delete users from /admin/users (superseded by #9) |
| #6 | fade56a | Persistent login hardening — cookie defaults + proactive refresh + client-aware sessions |
| #5 | 8f761d6 | Auto-format BIR TIN with dashes |

Earlier 2026-05-14 work (before the PR run): public-repo flip + AGPL-3.0 + security hardening (#4), short-URL alias (#3), QR auto-crop (#2), monogram QR fix (#1).

---

## Phase 3 — decision-gated (waiting on owner)

Each of these requires a strategic call from the owner before code can ship:

| Item | The decision | Effort once decided |
|---|---|---|
| **Save-the-Date render pipeline** | Browser-canvas + MediaRecorder (free, ~1 day) OR server FFmpeg (needs Workers Paid plan + Hetzner VM pool, ~3 days) | 1–3 days |
| **Panood (live stream)** | Provision Cloudflare Stream Live + YouTube Data API + master `@SetnayanWeddings` channel | 5–7 days |
| **Marketplace commission model** | Free-listing forever / commission per booking / paid tier — pick before launch advertising | Pricing call only |
| **Daily.co video meetings (0019)** | Sign up + paste API key | 2 days code |
| **Anthropic Claude API (0032 Contract Intelligence)** | Sign up + spend cap → unblocks paid SKU | 3 days code |
| **Apple Developer Program** | $99/yr enrollment | 2-5 day approval; signed `.dmg` + future iOS Papic |
| **Render pipeline infra** | Cloudflare Workers Paid ($5/mo) + Hetzner Cloud VMs (€15/mo) — shared by 0011 / 0012 / 0017 / 0024 | 2 days code once provisioned |
| **Public website visual redesign** | Owner provides direction on what to change. Current state (post PR #21): split-CTA hero + trust signals + pricing table + compact roadmap — functional but generic. Owner queued this for a later session — needs a specific brief ("hero feels weak", "want a hero image not a device mock", "needs a how-it-works section", etc.) before code can ship | 0.5–2 days depending on scope |
| **Monogram tier system + AI-automated Bespoke flow (queued for new session)** | Owner locked the 3-tier model + Bespoke pricing + UX rules 2026-05-14 (reference images shared). **Free / Basic**: 2 letters + `&` (e.g. `J & S`), 1-2 default fonts, simple geometric frame. **Pro (₱99 widget upgrade per 0004)**: 2 letters OR full names, ornamental + heritage frames, 8+ premium fonts. **Bespoke (₱2,999, NEW SKU)**: AI-generated interlocked letterforms — DALL-E 3 HD behind the scenes, branded as **"Setnayan AI"** in all customer-facing copy (DALL-E never named in the UI). Replaces the old `Custom Monogram Pack — remove watermark` SKU. **CUSTOMER UX (in-app, live render — no external tools)**: (1) Guided brief form: initials/names + 3 personality words + motif preference + style direction + **reference image upload area** for couples to share inspiration. (2) Pay ₱2,999. (3) App fires the first generation server-side — DALL-E 3 HD × 4 variations stash to R2 — customer sees live thumbnails in ~30 sec. (4) **Refinement loop, up to 30 re-renders included in the SKU**: customer types text feedback ("more delicate", "more gold", "swap wreath for crest") + each refinement re-fires DALL-E with the SAME locked brief + appended feedback. Counter visible: "X refinements left." (5) After 30 re-renders OR customer hits "Accept final": top result auto-vectorizes via vectorizer.ai → SVG goes live event-wide. **🔴 ANTI-ABUSE RULE**: the brief inputs (initials/names/connector) **lock after the first generation** and cannot be edited. Re-renders REFINE the existing concept; they do NOT restart from scratch. This prevents 1 transaction → multiple distinct logos (e.g., logo for self + logo for sister + logo for friend). **Optional add-on SKU (V2)**: `+10 re-renders` pack at ₱199 (multi-buy allowed) if a customer exhausts their quota. **Cost ceiling**: 30 × $0.08 HD = ~₱135 max per customer; avg ~₱45 (most use 5-15 rerenders) → 95% margin at ₱2,999. **Owner signups needed**: OpenAI Platform (`OPENAI_API_KEY`), Vectorizer.ai (`VECTORIZER_API_KEY`). **Branding**: all UI strings say "Setnayan AI" not "DALL-E" or "OpenAI"; server-side calls only; no API key in client bundle | 5-7 days code once API keys are in Vercel |

---

## Locked architectural decisions (no further owner input needed)

### Time-limited services — **no cron**

For services with a paid time budget (Panood live stream, Papic camera-seat session, future limited-duration SKUs), use database-state + on-access checks. Owner locked 2026-05-14: no Vercel Cron, no Supabase `pg_cron`, no Cloudflare Cron Triggers.

**Pattern:**
- `service_sessions` row stores `scheduled_for`, `start_window_opens` (= `scheduled_for - 30 min`), `start_window_closes` (= `scheduled_for + 2 hours`), `duration_minutes`, `started_at`, `expires_at`, `status`
- Couple hits **Start** between `start_window_opens` and `start_window_closes` → server sets `started_at = now()`, `expires_at = now() + duration_minutes`, flips status to `active`
- Every read of the service surface validates `now() < expires_at` server-side; flips status to `expired` lazily on next access if exceeded
- Client tracks the countdown locally from `expires_at` for the visible timer; polls every 30 sec to revalidate
- When countdown hits 0 client-side, UI swaps to "session ended" state immediately
- **Resource teardown** (stopping the Cloudflare Stream broadcast, releasing Papic seats, etc.): hybrid client-driven + lazy admin sweep:
  - **Client-driven (primary)**: countdown hits 0 → client fires `/api/sessions/[id]/teardown` → server calls the external API (Cloudflare, etc.) to stop the resource
  - **Lazy sweep (backup)**: any couple/admin page load sweeps `WHERE expires_at < now() AND status = 'active'` and fires teardown — covers the case where the broadcaster's browser is offline

Applies to: 0011 Panood, 0012 Papic, future time-budgeted SKUs. **Does NOT** apply to bookings or events themselves (those use absolute date scheduling).

---

## Owner-side blockers (must act, no code can replace)

- **🔴 BLOCKING — `supabase db push`** — 6 migrations on disk are not yet applied to prod: `blacklisted_emails` (#9), `vendor_dashboard_expansion` (#25), `api_scopes` (#27), `notification_type_additions` (#28), `vendor_reviews` (#24), `force_majeure_flags` (#26). New surfaces will 500 until pushed. Run `npx supabase db push --db-url "$SUPABASE_DB_URL"` once to apply all in one shot.
- **Sentry / PostHog smoke test** — trigger one error in production, sign up one fresh user, confirm both show up in their respective dashboards
- **Resend domain verification** — done; just confirm a fresh signup welcome email lands at a non-account-holder Gmail
- **Supabase Sessions config** — Inactivity timeout already at "never" by default (free plan); JWT expiry can be bumped to 7-30 days when found in dashboard
- **Cowork spec reconciliation** — `COWORK_INBOX.md` now carries `[PENDING]` entries for the full Phase 2 surface: 0006 reviews, 0019 force-majeure, 0022 vendor dashboard, 0025 locale, 0028 emails, 0033 API scopes, 0036 event-preload, caching strategy. Walk each via Cowork; tick `[DONE <YYYY-MM-DD>]` as you go.

---

## Stack quick-reference

- **Repo:** https://github.com/iscasasola/setnayan-platform (public, AGPL-3.0)
- **Hosting:** Vercel (Hobby plan), auto-deploys from `main`
- **Domain:** `setnayan.com` (Vercel-managed SSL)
- **DB:** Supabase (Singapore region) — **31 migrations on main as of EOD 2026-05-14** (last 6 are unpushed to prod, see Owner-side blockers)
- **Storage:** Cloudflare R2 — 4 PH-region buckets (live writes from PR #18), plus Supabase Storage for the `platform-assets` bucket (legacy)
- **Email:** Resend — domain `setnayan.com` verified, `noreply@setnayan.com` from-address, **9 of 10 V1 transactional templates wired**
- **Observability:** Sentry (errors) + PostHog (3 funnel events) + 3 Supabase-side funnels at `/admin/funnels` (signup→event→paid order, vendor signup→profile→booking, week-over-week)
- **Native:** Tauri 2 desktop wrapper (unsigned macOS .dmg on GitHub Releases v0.0.1); iOS/Android deferred to V1.0+

---

## Quick-jump anchor docs

- **`HANDOFF.md`** — cold-start handoff with the verification flow, all live routes, locked decisions
- **`OWNER_ACTIONS.md`** — step-by-step phased launch checklist (Phase 1-7)
- **`CHANGELOG.md`** — every meaningful commit with `SPEC IMPACT` callout
- **`COWORK_INBOX.md`** — `[PENDING]` worklist of spec-corpus updates owed back to `~/Documents/Claude/Projects/Setnayan/`
- **`README.md`** — public-facing overview
- **In the Cowork corpus at `~/Documents/Claude/Projects/Setnayan/`:**
  - `CLAUDE.md` — status anchors auto-load on every Cowork session
  - `App_Build_Status.md` — spec-vs-code audit (regenerated EOD 2026-05-14)
  - `V1_Gap_Analysis_Status.md` — Tier 1/2/3 spec landing audit
  - `Installed_Stack_Inventory.md` — 10-pass audit of installed deps, migrations, env vars
  - `API_Integration_Checklist.md` — external service prereqs

---

## Sprint 0 history (closed 2026-05-13)

Sprint 0 was the platform foundation — Next.js 15 + Tauri 2 + Supabase + Cloudflare R2 + GitHub. All Sprint 0 acceptance criteria passed:

- Vercel project connected, env vars set, deploys clean
- Supabase Singapore region, base schema migration `20260512000000`, 5 RLS helpers, on_auth_user_created trigger
- R2: 4 PH-region buckets (`setnayan-media`, `-thread-files`, `-vendor-contracts`, `-samples`)
- Auth: email/password + magic-link; owner email auto-flagged `is_internal=TRUE`
- Tauri 2 scaffold, GitHub Actions matrix building `.dmg` + `.msi` artifacts
- PWA manifest + service worker scaffolded (replaced in PR #10 with the full caching foundation)

Verification probes that passed: `/health` 200, `/`, `/login`, `/manifest.json`, all icons, RLS denies anon, `generate_public_id` produces valid S89X- IDs.

Then the 19-iteration pre-launch sprint (closed 2026-05-13) shipped the couple/vendor/admin core surfaces. Then 2026-05-14 happened (see PR run above).
