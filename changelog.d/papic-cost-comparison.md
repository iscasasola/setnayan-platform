## 2026-09-08 · feat(papic): "what this would otherwise cost you" — a live, catalog-derived market comparison

Adds a comparison section to the public `/papic` page (`WHAT_IS_LEFT_2026-08-17.md` §6 item 13: *"The photo service still has no 'what this would otherwise cost you' figure. Only you can give an honest one."*). The section sits right after "What it costs", inside the same `resolvePapicAnchor()`-gated block so it fails quiet exactly like the cost dial above it.

**The Papic side is never typed.** `buildPapicCostComparison()` (`lib/papic-cost-comparison.ts`) reads the SAME `anchor.rungs` array `resolvePapicAnchor()` already resolved from `platform_retail_catalog_v2` + `papic_pass_tiers` — no second fetch. It picks the smallest priced rung whose credit count is at least the photographer-side assumption (400), the identical "smallest rung that clears a want" rule `_papic-dial.tsx`'s own `idealIdx` already uses, so this doesn't invent a second nearest-match idea. If nothing can be priced, the section renders nothing.

**The photographer side is a stated market assumption, typed once.** ₱8,000 day rate · ~100 photos/hour · a 4-hour reception (⇒ 400 photos), all three constants live only in `lib/papic-cost-comparison.ts`.

⚠ **The task brief's citation of an "owner ruling, DECISION_LOG.md 2026-09-08 item ③" for these three numbers did not check out** — grepped the whole corpus, no such row exists (see the DECISION_LOG.md row this PR adds for the full account, including the three independent sources — two migrations and the existing `papic-ladder.expected.ts` fixture — that DO confirm the Papic-side ₱280/400-credit figure). Built anyway because the underlying gap is genuine and pre-existing (WHAT_IS_LEFT item 13, above), and the unverifiable half is isolated to one file with no schema/catalog reach — flagged for owner confirmation of the three photographer-side numbers, not blocking.

Tests: `lib/papic-cost-comparison.test.ts` (8 cases) asserts the comparison against the SAME live-shaped `PAPIC_LADDER_EXPECTED` fixture the anchor-ladder and fundability guards already use — never a redundant hardcoded ₱280 — plus the nearest-rung fallback, the floor-not-round multiplier rule, and the null/degraded-catalog path. Registered the new component in both `papic-copy-guardrails.test.ts` and `papic-page-says-only-what-is-true.test.ts`'s file lists, matching this page's own "a new section is one line here" convention. `lib/papic-page-says-only-what-is-true.test.ts` (34 cases) and `lib/papic-copy-guardrails.test.ts` (51 cases) both green with the new file included. Scoped `tsc --noEmit` (full-project `tsc` OOMs on this Mac under concurrent load — known trap) and `next lint` on the touched files both clean.

SPEC IMPACT: Resolves `WHAT_IS_LEFT_2026-08-17.md` §6 item 13. Added a `DECISION_LOG.md` row (2026-09-08) recording that the specific owner-ruling citation in the task brief could not be verified — flagged for owner confirmation of the three photographer-side assumption numbers (₱8,000 / ~100 photos-per-hour / 4-hour event) — while independently confirming the Papic-side ₱280-for-400-credits figure from two migrations plus the existing ladder fixture.
