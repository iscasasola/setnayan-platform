## 2026-07-17 · feat(sharing): vendor featured-story card + editorial story button + chapter OG card (share-asset completion)

The three deferred follow-ups from the owner-approved social-sharing program (2026-07-16 Social_Share_Settings_Council_Verdict), all riding shipped machinery — the satori/sharp OG card family (`lib/social/`), `lib/save-to-device.ts`, and the recap page's "Save story card" pattern (#3294). No new dependencies, no migration.

- **Vendor "featured wedding" story card** — the #1 vendor share gap closed: vendors had URL shares only, nothing IG/TikTok-postable. `app/vendor-dashboard/recaps/page.tsx` + `app/vendor-dashboard/real-stories/page.tsx` now render the reused `SaveStoryCardButton` beside ShareButtons on each published card — it fetches the couple's 1080×1920 story asset (`/api/og/recap/[slug]?format=story` · `/api/og/realstory-slug/[slug]?format=story`) and hands it to `saveImageToDevice` (native share sheet on mobile, download on desktop). Rides the couple's publish/consent gates those pages already enforce; the story card carries the existing "made with Setnayan" mark from #3294 — no new branding.
- **Editorial-page story button** — the couple's public editorial masthead (`app/[slug]/_components/editorial/editorial-content.tsx`, beside the compact ShareButtons in the dateline) gains a slim `compact` variant of the same button. Backing asset: `/api/og/realstory-slug/[slug]` did NOT support formats — the route + `lib/social/realstory-card.tsx` gained the `?format=og|square|story` param mirroring #3294's DIMS pattern in `lib/social/recap-card.tsx`, with the "made with Setnayan" mark on the story/square photo variants. Renders only for REAL published editorials (samples + unpublished get nothing; the unpublished monogram fallback stays 1200×630).
- **Chapter OG card** — new `app/api/og/chapter/[publicId]/route.ts` + `lib/social/chapter-card.tsx` (mirrors `profile-card.tsx` from #3303): 1200×630, chapter title + storyteller byline + the Storyteller badge mark (renders `CREATOR_BADGE_LABEL` — follows any owner rename) over the YouTube-derived thumb (`youtubeThumbFromEmbedUrl`, the Storytellers-shelf rule). New `fetchPublishedChapterForShare` in `lib/creator-public.ts` applies the full public gate (published + embed + owner profile public/non-deleted/slugged) in one place; anything short 302s to the static brand card — never a leak. Wired into the chapter page's `generateMetadata` (openGraph + twitter images) only; the page body is untouched.

Verified: typecheck + lint clean; all six card variants rendered via the modules directly (satori+sharp) — valid JPEGs at 1200×630 / 1080×1080 / 1080×1920 with badge + watermark confirmed visually; `migration:check` green (no migration).

SPEC IMPACT: Social share program — deferred follow-ups (vendor story asset · editorial story button · chapter og:image) now shipped; DECISION_LOG.md row added in the corpus.
