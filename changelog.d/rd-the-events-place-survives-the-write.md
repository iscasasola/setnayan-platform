## 2026-09-23 · fix(venues): an events place survives the write — one reception vocabulary, forward-only

Owner, 2026-09-23, on a supplier's "Venues you work" card: *"Venues here are different from our options. Where is the event place. this means the data is wrong."* Measured: the couple's onboarding offers seven reception settings (`setting_*`: Hotel ballroom · Events place · Heritage · Restaurant · Garden · Beach · Resort/destination) and `lib/vendor-venue-type.ts` matches them key for key — but `events.venue_setting` had no `events_place`, so the onboarding write map collapsed **both "Events place" and "Restaurant" onto `banquet_hall`** (restaurant needlessly: legal since 20271114090000). A couple who chose an events place was stored as a hotel ballroom and no supplier could ever be matched to it. The supplier card then showed the third list's words ("Hotel Ballroom / Banquet Hall", "Garden Estate", "Heritage / Hacienda", "Outdoor Tent") beside the couple's.

- **Migration `20271244482831_venue_setting_events_place.sql`** — widens `events_venue_setting_check` with `events_place` (DROP + ADD, the 20271114090000 shape; post-conditions assert the value is present, nothing was lost, and `civil_registrar` did not return). Forward-only: no row rewritten.
- **`lib/venue-settings.ts`** — `events_place` joins `VENUE_SETTINGS`; every label now uses the couple's own onboarding words (Hotel ballroom · Events place · Restaurant · Garden · Beach · Resort / destination · Heritage venue · Outdoor tent), so `VenueMatchCard` ("Venues you work"), Explore's facet chips and the couple's Details picker read the same list. Scene phrase, scene family (`hall`) and 3D archetype (drawn as the hall, **named** in `archetypeFor`, not fall-through) added; directory type maps to `hotel_ballroom` until the venue directory learns the word (recorded in the map).
- **`lib/reception-pick-to-venue-setting.ts` (pure)** — the onboarding pick → `venue_setting` table; every pick maps to its own value, the default is the documented `AMBIGUOUS_VENUE_SETTING`. `app/onboarding/wedding/actions.ts` uses it and its own collapsing map is gone. Test executes it and reads the writer; sabotage (collapse restored) watched red.
- Allow-lists widened in `create-event/actions.ts` and `[eventId]/actions.ts`; the Details picker is now **derived** from `VENUE_SETTINGS` (the way the ceremony picker already was); Explore's facet keys and its two label maps carry `events_place`. `venue-settings.test.ts` now pins the reception and ceremony constraints to their own migrations; sabotage (`events_place` removed from the migration) watched red.
- ⚠ **BACKFILL DELIBERATELY NOT DONE — owner decision.** Measured 2026-09-23: `events.venue_setting` = 9 × `banquet_hall` · 1 × `heritage` · 2 × NULL. The nine are ambiguous by construction (events place, restaurant and hotel ballroom all landed on the same bytes), so no rewrite can know what each couple chose. Options: leave them (they keep matching hotel-ballroom suppliers, as today), re-ask those couples, or accept a default. Re-measure: `select venue_setting, count(*) from events group by 1`.
- Follow-up, not built: an `events_place` of its own in `venue_directory_type` and its own 3D look.

SPEC IMPACT: `DECISION_LOG.md` — the reception vocabulary is the couple's onboarding list; `events.venue_setting` gains `events_place`; backfill of the 9 ambiguous rows is an open owner call (to be recorded with the 2026-09-23 My Shop rows).
