## 2026-06-25 · feat(onboarding): per-type persona packs — the generic plan now leads with type-appropriate categories (flag-gated, ships dark)

Iteration 0053 Phase 3 follow-up. The Event-Type Engine is plumbing-complete, but the generic `/onboarding/[type]` flow derived its starter plan from a flat taxonomy-top-N (`deriveGenericPlan`) — every event type AND every persona got the same categories in the **wedding-shaped** sort order, so a birthday opened with "Reception · Ceremony · Coordinator · Cake…". This restores the per-persona richness the wedding wizard already has (`EXP_ESSENTIAL_PICKS` + persona `extras`, effort-scaled), scoped per event type.

- **`lib/onboarding/persona-packs.ts`** (new) — `PERSONA_PACKS`, pure data keyed by `personaPackKey` (= `event_type_profiles.onboarding_flow_key`, which is the event-type key for all 8 seeded non-wedding profiles). Each pack lists the type's lead `essentials` (4 core) + per-persona `extras` (the 6 `EXP_PERSONAS` keys). `derivePackPlan(packKey, personaKey, tiles, effort)` orders essentials → persona extras → the rest of the taxonomy, **intersected against the type's real `tiles`** (a category not applicable/active for the type is silently dropped), then sizes by the effort axis (simple=4 / balanced=6 / allout=9). Category ids are `service_categories.id` slugs — the same id space as `OnboardingPickChip.cat`, so `picks` → `interested_categories` stays consistent with PR3.
- **Graceful fallback:** a packKey with no pack ('generic', or any type enabled before its pack is authored — e.g. the 4 disabled types) falls straight back to `deriveGenericPlan` — the exact PR3 behaviour. Empty tiles → empty plan.
- **`lib/onboarding/generic-plan.ts`** — exported the shared `EFFORT_LIMIT`/`DEFAULT_LIMIT` + an `effortLimit()` helper so both derivers size identically (no drift). `deriveGenericPlan` output unchanged (its suite stays green).
- **`app/onboarding/[type]/page.tsx`** — passes `personaPackKey={flow.personaPackKey}` to the shell.
- **`app/onboarding/[type]/_components/generic-onboarding.tsx`** — the reveal/commit plan now comes from `derivePackPlan(...)` instead of `deriveGenericPlan(...)`.
- **`lib/onboarding/persona-packs.test.ts`** (new) — essentials-lead-not-wedding-order, persona differentiation at balanced, effort scaling, drop-non-applicable, missing-essential backfill, no-pack fallback parity, dedupe + label alignment, structural completeness (every enabled type has a pack with all 6 personas).

Wedding is untouched and unaffected — it has its own wizard and never routes through `/onboarding/[type]`. The whole generic flow remains **dark** behind `NEXT_PUBLIC_EXPERIENCE_QUIZ_ENABLED` (the owner go-live switch), so there is no user-visible change until that flag flips. No migration; pure code + data.

SPEC IMPACT: onboarding (iteration 0053 Phase 3) — per-type persona/plan content. Engine seam (`personaPackKey`) unchanged; this fills it with data. Logged in `DECISION_LOG.md`.
