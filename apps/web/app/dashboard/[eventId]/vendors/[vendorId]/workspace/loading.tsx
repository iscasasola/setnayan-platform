/* Loading shell for dashboard/[eventId]/vendors/[vendorId]/workspace — owner perf pass 2026-06-03 (instant animated skeleton). */
import { DetailPageSkeleton } from '@/components/skeletons';

export default function Loading() {
  return <DetailPageSkeleton title />;
}
