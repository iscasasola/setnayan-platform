# S0 — the spike that selects the path

**Model:** Opus 5 · **Effort:** high · **Wave:** FIRST · blocking for S4+ · S1–S3 may start in parallel

Measured against `origin/main` @ `bd1c1a39f` on 2026-09-05. Re-fetch before you act.

Part of the S-series encoder plan — `build-sessions/encoder/README.md`. Supersedes E0.

Start a new Claude Code session and paste EVERYTHING below the rule.

---
---

Read the repo's own CLAUDE.md and the corpus CLAUDE.md first, then follow RULE 0: assume what you
are about to build already exists, and locate it before writing anything. On this stream RULE 0 has
now paid SEVEN times — "invite an off-platform supplier", "a supplier can only tap six fixed
messages", "the camera screen says 3 cameras free to test with", the host stranger-copy defect, the
NPC residency rows, the camera-claimer name, and the ENTIRE captured-by-person build were all
reported as missing and ALL already ship.

0a. SPAWN FROM origin/main. The prompt that created you was `cat`-ed from a file, and that file
   rots like any other. Paste with
   `git fetch -q origin && git show origin/main:build-sessions/encoder/<S>.md | pbcopy`.
   If your prompt's header and its body disagree, the body was corrected and the header was not —
   STOP and re-read from origin/main rather than guessing which half is stale.

0. MEASURE AGAINST origin/main, NEVER A LOCAL CHECKOUT. `git fetch` first, then read with
   `git grep <pattern> origin/main -- <path>`. The main checkout on this machine was 2237 commits
   behind while reporting itself as main, and it produced four false findings in one hour —
   including two sessions scoped to build things that already shipped.
   NEVER ANCHOR ON A LINE NUMBER. Grep for a string. Line numbers rot between fetches.

0b. A `git add` THAT STAGES NOTHING REPORTS SUCCESS. This repo's root is an ALLOWLIST: `.gitignore`
   line 18 is `/*`, so EVERY new top-level file or directory is ignored unless a `!/path` line below
   it says otherwise. `git add <new-top-level-path>` then exits 0 having staged nothing, `git status`
   shows nothing, and the work never enters the repo. `.gitignore`'s own header records this
   swallowing a README once already. If you create ANY new top-level path, add its `!/path` line in
   the SAME commit and verify with `git check-ignore -v <path>` before believing it is staged.
   Nested paths (under apps/, supabase/, changelog.d/ …) are unaffected.

0c. A CHECK THAT CANNOT FAIL IS NOT A CHECK. Twice in one day a verification command returned a
   confident FALSE answer because of its own shape, not the repo's state:
     · `for f in $list; do git cat-file -e "ref:$f"; done < <(...)` — a command inside the loop
       consumed the loop's stdin, so every probe failed and reported "97 files absent upstream".
       All 97 were present.
     · `git check-ignore -v X | head -2 || echo "not ignored"` — `||` binds to the PIPELINE, whose
       status is `head`'s zero, so the fallback branch can never run and BOTH outcomes print nothing.
     · `timeout 60 <cmd>` printed `DB_EXIT=127 elapsed=0s` — **`timeout` does not exist on macOS**,
       so the command never ran. 127-in-zero-seconds reads as a fast decisive result if only the exit
       code is printed. 🔑 **PRINT ELAPSED TIME BESIDE EXIT STATUS.** Duration is a cheap, general
       detector for "the command did not run", which no exit code alone can distinguish from "the
       command ran and failed".
   Before believing a verification, ask what output the FAILING case would produce and confirm the
   command can produce it. Prefer an explicit `if cmd; then … else … fi` over `&&`/`||` after a pipe,
   and prefer set arithmetic (`comm`, `sort -u`) over per-item loops that shell out. If a result is
   implausible — everything present, everything absent, silence from both branches — suspect the
   check before the repo.

WORKING RULES — every one has cost this project real work before:

1. Branch, then `git worktree add` IMMEDIATELY — beside the repo, NEVER in /tmp, and branch FROM
   origin/main. A finished, proved change was lost to a /tmp worktree on 2026-08-28.
2. `pnpm install` in the worktree BEFORE running anything. A run in an uninstalled worktree means
   nothing.
3. `git fetch` before branching. origin/main moved 2237 commits ahead of a checkout that still
   called itself main.
4. PUSH THE MOMENT IT TYPECHECKS. Do not batch a session's work into one commit at the end.
5. Typecheck with the exit code printed beside the error count:
   NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json > /tmp/tsc.log 2>&1; \
     echo "TSC_EXIT=$?"; grep -c 'error TS' /tmp/tsc.log
   An EMPTY log is NOT a clean one — tsc exits 134/143/144 on abort and that reads as zero errors.
   Require TSC_EXIT=0 printed beside ERROR_LINES=0; either one alone is a lie. Never run two.
6. Require `# tests` to be NON-ZERO before believing any pass. Zero-tests-zero-failures is
   byte-identical to success and exits 0. A --test glob that matches nothing behaves identically.
7. Mutation-test every assertion you add and PRINT THE OCCURRENCE COUNT before → after. An
   unmeasured sabotage proves nothing. If a well-formed sabotage reports GREEN, suspect the
   sabotage before the guard.
8. Read the live object, never a migration comment or a docblock. A migration comment is not
   evidence; neither is a decision log; neither is this prompt.
9. Add a changelog fragment in changelog.d/ — never edit CHANGELOG.md or STATUS.md directly.
10. Auto-merge is the standing default: `gh pr merge <n> --auto --merge` right after creating it.

AUTONOMY RULES — how this session finishes rather than stalls:

11. DONE MEANS MERGED. After arming auto-merge, poll until the PR reads MERGED. If a required
    check fails, read the failure, fix it, push again. Do NOT hand back a red or open PR and call
    the session complete. If the same check fails twice for the same reason, STOP and escalate.
12. NEVER STALL ON A GATE. If a feature flag's production value is unknown, build behind the
    EXISTING flag, defaulted off, and record the open question in your handback. Do not stop and
    wait for an answer you were not promised.
13. WRITE STATE AS YOU GO. Create the changelog fragment on your FIRST commit, not your last. If
    you are running low on context, commit and push everything already proved, and write the next
    concrete step into the fragment so a fresh session resumes instead of restarting.
14. IF THE DEFECT IS NOT THERE, SAY SO AND STOP. Do not invent adjacent work to justify the
    session. Report what you measured, with the command and its output. That is a complete and
    successful session — two of this program's ten original sessions ended that way.
15. ONE SESSION = ONE BRANCH = ONE PR. If the work genuinely needs a second PR, say so in the
    handback before opening it.
16. HAND BACK IN THIS EXACT FORMAT so the overseer can verify without re-reading everything:

    SESSION: <C-id>
    PR: <#number> <MERGED|OPEN|BLOCKED>
    MEASURED-AGAINST: origin/main @ <sha>   (must be a fetched sha, not a local HEAD)
    TSC_EXIT=<n> ERROR_LINES=<n>
    TESTS: <# run> passed, <# run> total   (must be non-zero)
    MUTATION: <assertion> — before <n> occurrences, after <n>
    PREMISE: <HELD | FALSE — with the command that showed it>
    OWNER QUESTION: <none, or the one thing you could not resolve>
    LEFT UNDONE: <none, or exactly what and why>

ENCODER-PROGRAM RULES — measured 2026-09-05, every one of these was a false premise in the E-series:

17. THE PLAN OF RECORD IS `build-sessions/encoder/README.md` (S-series). The E0–E9 prompts are
    retired; if you find one, do not act on it. Design: `Live_Studio_Encoder_Scope_2026-09-03.md`
    § "Corrections 2026-09-05" and the Encoder Replan artifact linked from the README.
18. OVERLAYS ARE `ResolvedOverlays` (`lib/live-studio-overlays.ts`), ALREADY on the controller
    page as `airOverlays`. `ProgramFrame.overlay` / `WatermarkReason` is RETIRED (2026-07-25);
    the unified bridge publishes `overlay: false` unconditionally. Never guard it.
19. THERE IS NO PROGRAMME AUDIO. Each phone's mic is a separate track, may be absent, changes on
    every cut. S3 builds the mixer. Nothing before S3 may assume a programme audio stream.
20. THE DEFAULT TIER STREAMS ON THE COUPLE'S OWN CHANNEL BY HAND (`lib/live-studio-manual-air.ts`):
    the key is theirs. Setnayan pool channels are only the hosted-channel add-on. Any key logic
    must handle BOTH sources.
21. SPLIT-SCREEN HAS NO LIVE PUBLISHER — `ProgramBridgeHost` hard-codes `secondaryStream: null,
    splitRatio: 0.5`. Code it, unit-test it with a synthetic frame, never make it an acceptance step.
22. TAURI IS 2.11.1, `src-tauri/src/lib.rs` is 14 lines, ZERO custom commands, no HTTP/TLS/RTMP
    crate, `capabilities/default.json` grants only core/event/oauth/opener to `remote.urls`
    setnayan.com. Every new command needs a capability entry; the same JS ships to plain browsers,
    so gate on `window.__TAURI__`.
23. THE IPC IS BINARY ONLY UNTIL THE FIRST CUSTOM-PROTOCOL FAILURE. After one, Tauri's
    `ipc-protocol.js` JSON-stringifies every payload forever (~3.6×). The CSP draft in
    `next.config.ts` lacks `ipc:` / `http://ipc.localhost`. S5 owns the guard; nobody streams
    without it.
24. `lib/live-studio-ingest-health.ts` (LS4, PR #5122) IS the health surface. Extend
    `decideIngestHealth`; never build a second decider. YouTube `noData`/stale always beats a
    local "publishing".
25. THE OS FLOOR IS Apple-silicon macOS 14 + Safari 26's WebKit (`AudioEncoder`), Windows 10/11
    with hardware H.264. Do not spend a day making macOS 13 or Intel work.
26. NO CALENDAR WAITING. Owner ruling 2026-09-05: "we will finish everything asap. no need to wait."
    If a session is blocked on a cert or an owner answer, build behind the existing flag, default
    off, record the question in the fragment, and hand back — do not idle.

---

TASK — S0: produce a FINDING, not a feature. Every session from S4 on assumes its answers.

🛑 This session writes a document (`build-sessions/encoder/S0-FINDING.md`) and, if needed, a
throwaway probe harness on its branch. It does not build the encoder.

WHAT IS ALREADY KNOWN (do not re-derive; verify):
  · WebCodecs in WKWebView is documented ON — WebKit `UnifiedWebPreferences.yaml`:
    `WebCodecsVideoEnabled` defaults to WebRTC-available on Cocoa, `WebCodecsAudioEnabled`
    default true. The gate is WebKit VERSION: `VideoEncoder` since Safari 16.4, `AudioEncoder`
    ONLY since Safari 26 (webkit.org/blog/17333). E0's "undocumented either way" was wrong.
  · WebView2 tracks Chromium; WebCodecs on since Chrome 94; HW H.264 via Media Foundation, SW
    fallback OpenH264 = Constrained Baseline only; HW accel sometimes off on fresh installs.
  · Tauri IPC: raw `Uint8Array` body via `invoke` in Tauri 2 → `InvokeBody::Raw`. Measured
    ~5 ms/10 MB macOS, ~200 ms/10 MB Windows (tauri discussions/11915). Fine for encoded chunks
    (~10 KB/frame); fatal for raw pixels. Native VideoToolbox/MF encode is therefore NOT a
    drop-in fallback — the only real fallback is window capture, which scope § 5 rejects.
  · build-desktop ran twice on 2026-09-05: Windows OK both times; macOS failed at notarization
    (403, unaccepted Apple agreement — owner item in X0). Question 2 of E0 is answered.

QUESTION 1 — THE OS MATRIX. Run the probe INSIDE THE REAL APP (`src-tauri`, debug build is fine)
FROM THE REAL ORIGIN https://setnayan.com — not Safari, not a file:// page. Build a probe harness
on your branch: a debug-only Tauri command `probe_report(json)` (capability entry for
setnayan.com) and a `webview.eval()` of the probe after page load, printing to stdout. Probe:
      typeof VideoEncoder, typeof AudioEncoder, navigator.userAgent, `WebKit ${version}`
      await VideoEncoder.isConfigSupported({ codec:'avc1.42E01F', width:1280, height:720,
        bitrate:2_500_000, framerate:30, hardwareAcceleration:'require-hardware' })
      same with 'prefer-hardware'; same at 1920×1080
      await AudioEncoder.isConfigSupported({ codec:'mp4a.40.2', sampleRate:48000, numberOfChannels:2 })
      typeof MediaStreamTrackProcessor (window AND worker), typeof OffscreenCanvas, AudioWorklet
Machines — record OS build, WebKit/WebView2 version, chip, for each:
      · THIS Mac (macOS 26.6.2, arm64) — run first, today
      · macOS 14 or 15 WITHOUT the Safari 26 update · WITH it (borrow/VM; expected: AudioEncoder absent → present)
      · macOS 13 (expected: no AudioEncoder — confirms the floor)
      · Windows 11 with edge://gpu "Hardware accelerated" · one GPU-blocklisted or VM box
Then a 60-MINUTE ENCODE on this Mac and one Windows box: a 1280×720 OffscreenCanvas in a worker
drawing a moving test pattern at 30 fps → VideoEncoder (config above, CBR, keyframe every 60) →
count chunks, log `encodeQueueSize` every 10 s, drops, and `pmset -g thermlog` / thermal
pressure. Report fps held, queue growth, thermal state at 10/30/60 min.

QUESTION 2 — THE IPC PATH STAYS BINARY. From the same harness: 30 invokes/s of a 10 KB
`Uint8Array` for 60 s; Rust reports body type (`Raw` vs `Json`) and received length. Then
enforce a CSP on the page WITHOUT `ipc:`/`http://ipc.localhost` in `connect-src` and show the
fallback engaging (payloads arrive as JSON arrays). Record both outputs. This is the guard S5 ships.

QUESTION 3 — TWO SPIKES, FOR THE RECORD (one day, both):
  a. HLS-CORS: from a page, `fetch(PUT)` one MPEG-TS segment to a live YouTube HLS ingest URL
     (owner's own channel; YouTube Studio → Stream settings → HLS). Read the preflight in the
     Network tab. Pass = 2xx with CORS headers. Record verbatim.
  b. WHIP: `canvas.captureStream()` from the REAL controller page → MediaMTX on a ₱580 VPS
     (Hetzner, hourly; destroy after) → `ffmpeg -c:v copy -c:a aac -f flv rtmps://…` → a watch
     URL plays. Record the WebRTC encoder's actual bitrate/resolution under 2 % packet loss
     (`tc netem`) — this is the quality argument against Path B.
  Owner ruling 2026-09-05: native is the plan of record regardless of (a)/(b) unless Question 1
  fails on the accepted floor. Run them anyway; the exit must be measured, not assumed.

OUTPUT — `S0-FINDING.md`: the matrix table with versions; Q2 outputs; Q3 verbatim results; the
60-min encode log summary; and ONE paragraph: "the floor holds / does not hold, because…".
If the floor does not hold on Apple-silicon macOS 14 + Safari 26, STOP and say so — the plan
branches to B-remux at S3 and this finding is what triggers it.

HAND BACK in rule 16 format. PR: the finding file + the probe harness (debug-only, behind
`#[cfg(debug_assertions)]`, capability entry removed or debug-gated). Auto-merge.
