import { IterationPlaceholder } from '../_components/placeholder';

export const metadata = { title: 'Vendors' };

export default function VendorsPage() {
  return (
    <IterationPlaceholder
      iteration="Iteration 0006"
      title="Vendor list"
      blurb="28 canonical service categories + custom rows · 6-stage readiness tracker · flexible payment milestones · crew meals · meetings."
      hint="Pairs with iteration 0007 Budget under the same Vendors tab."
    />
  );
}
