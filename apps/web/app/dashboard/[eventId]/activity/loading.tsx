/* Loading shell for dashboard/[eventId]/activity — owner perf pass 2026-06-03 (instant animated skeleton). */
import { FeedPageSkeleton } from '@/components/skeletons';

export default function Loading() {
  return <FeedPageSkeleton actions={1} />;
}
