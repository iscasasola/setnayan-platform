// Vendor claim finalize — form shell while the claim completes.
import { FormPageSkeleton } from '@/components/skeletons';

export default function Loading() {
  return <FormPageSkeleton title />;
}
