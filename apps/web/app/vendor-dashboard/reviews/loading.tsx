/* Instant loading shell for /vendor-dashboard/reviews — client reviews list. */
import { ListPageSkeleton } from '@/components/skeletons';

export default function Loading() {
  return <ListPageSkeleton title />;
}
