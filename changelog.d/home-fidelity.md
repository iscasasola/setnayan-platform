## 2026-07-15 · feat(home): pixel-fidelity pass — 67 audit gaps closed against the approved prototype

- **Bug fixed:** the Watch tile's "quiet" dots used `bg-success`, which has no DEFAULT key in the Tailwind ladder and compiled to nothing — the dots were invisible. Now `var(--sn-success)` (proto #5E7C52) at the proto's 7px, with the live `sn-pulse`.
- **Glass everywhere:** every glass surface (event cards, Watch, Spaces, command bar, This-year strip, ⌘K palette) now carries the proto's real frosted recipe — white .5 + `backdrop-blur(22px) saturate(1.5)` + white .72 hairline + the warm tile shadow — via a single `.sn-tile-glass` kit utility; hover lifts via `.sn-lift-2/3/4` deepening to `--sn-sh-hi`.
- **Exact obsidian Alaala tile:** gold-glow radial over `--sn-glass-dark-bg`, 18px padding, gold-100 type (21px w800 headline, gold-300 eyebrow), exact lens chips (gold-500 active fill on `--sn-ink-black`, filled inactive state, no size jump), 46px face orbs with name captions, 64px min-height lens body.
- **Entrance choreography:** staggered `sn-reveal` rise cascade (brand head .12s → hero .24s → command bar .32s → Events .4s → cards .5s+.08s·i → ghost → Watch .78s → Spaces .9s), the Alaala tile's `sn-bloom` blur-rise at 1.05s, `CountUp` numerals (hero · Watch · watch rows · ring % labels, SSR-final + reduced-motion bail), progress-ring `sn-ring-sweep` (opt-in prop; other ring mounts untouched), Life-Flash veil lift + capiz shimmer sweep (`display:none` under reduced motion), lens-body cross-fade + chip pop, ⌘K scrim fade + panel pop-in, `.sn-press` press feedback on all Link cards.
- **Mobile composition:** primary event as the dark `.mhero` card (gold eyebrow · mono facts · 6px shimmer bar), remaining events as 2-col compact chips, the warn nudge row carrying the Watch signal (Watch tile is `lg`-only, per proto), row-shaped New-event ghost, compact Alaala tile with lenses ≥sm + static People/With-me rows below, tightened base rhythm, 22px hero type with hidden soft tail.
- **Exact type/color/spacing values:** sans-extrabold hero (36px, −.03em), sentence-case section headers with gold sub + base hairline treatment, gold-700 tile eyebrows (10.5px, .14em), 26px mono Watch numeral, amber `--sn-warning` attention pill on `--sn-warning-soft`, 9px mono badge pill in gold-700, 44px square monogram chip (opt-in `shape` prop), 44px ring with frosted inner disc, true-terracotta `--sn-terra` star, gold "Show all"/"Hide finished" link (same `?show=all` semantics), 760px command bar with proto placeholder + kbd, frosted top-bar utility pill with pulsing bell (opt-in `pulse` prop), 1.3fr bento, max-w-7xl canvas with xl 4-col events grid, proto ambient wash.
- **All values tokenized through the `--sn-*` kit / Tailwind ladders (owner: no hardcoded dependent values):** repeated recipes live once in globals.css (`.sn-wash-home`, `.sn-tile-glass`, `.sn-tile-obsidian`, `.sn-texture-band`, `--sn-sh-tile/--sn-sh-hi`); proto colors map to kit vars (gold-100/300/500/600/700 · ink-500/black · warning/success/info pairs · `--sn-line`); two proto values had no slot and were added as named kit vars (`--sn-ink-400` #8A857B, `--sn-terra` #C08A4A); counts and stagger delays computed, never literal.
- **Intentional deviations:** the infinitely-animating 72px-blur ambient drift blobs are NOT implemented (real GPU cost on low-end Android; the static proto wash shipped instead — LOW gap, flagged per its own caveat). Radius-token substitutions per the repo's `--m-r-*` lock (no arbitrary radii): proto 18/20px tiles → `rounded-2xl` (22px), 12px monogram / 10px space chip / 9px palette chip → `rounded-xl`/`rounded-md` (14/8px), proto 14px command bar → `rounded-xl` (14px, exact).

SPEC IMPACT: None — full-fidelity implementation of the already-logged approved design.
