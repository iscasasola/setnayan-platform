## 2026-07-23 · feat(privacy): register guest_columns + papic_pool_gallery as fail-closed DPO controls at /admin/data-privacy

The two guest-content features that landed today — **Guest Columns** (PR #3583 · guest-authored text published to the couple's public site after couple approval · env `GUEST_COLUMNS_ENABLED`) and the **Papic Shared Pool Gallery** (PR #3581 · guests browse the whole event capture pool + self-link · env `NEXT_PUBLIC_PAPIC_POOL_GALLERY` + `events.pool_gallery_open`) — are now Data-Privacy CONTROLS on the owner-DPO board, following the exact #3549 (`vendor_ai_autoreply` / `vendor_deep_search`) pattern: the owner approves activation in-app, the approval is the RA 10173 audit trail, and every feature surface fails closed until then.

- **Seed migration `20270919527984`** (via the `pnpm migration:new` allocator): inserts `guest_columns` + `papic_pool_gallery` into `data_privacy_controls` as `'inactive'`, with descriptive comments (what each lane exposes, the RA 10173 basis, what activating means). `ON CONFLICT DO NOTHING` keeps any admin edit. Replayed green by the PGlite harness.
- **Catalog** (`lib/data-privacy-controls.ts`): both defs added under a new board section `guest_content` ("Guest content & publication"), ordered between vendor-mediated and automated-AI. The console renders sections generically from `PRIVACY_CONTROL_GROUP_ORDER`, so the new section needs no page change.
- **Coverage map** (`lib/privacy-coverage.ts`): both declared `privacySensitive: true, declaredIn: []` — ON PURPOSE, the honest #3549 posture: they surface as DRIFT on the Coverage & drift tab until the public `/privacy` notice + the ROPA actually cover them.
- **Gates — control check ALONGSIDE the env flag, env short-circuits FIRST** (no DB read added anywhere while a flag is off; control read is request-cached + fail-closed):
  - `lib/guest-columns-gate.ts` (new, server-only) `guestColumnsActive()` = env AND control; wired into the `/api/guest-columns` route (POST + DELETE), the studio review queue page, the guest-site `GuestColumnCard`, the editorial "Letters to the Editor" reader, and the two couple-dashboard doorways (website QuickLink + `guestColumnsOn` prop).
  - `lib/papic-pool-gate.ts` (new, server-only) `papicPoolGalleryActive()` = env AND control; wired into the three guest-pool routes (`guest-pool` / `guest-pool-link` / `guest-pool-unlink`), the couple-only `setPoolGalleryOpen` host toggle action, the `/papic/pool` page, the Me-page `PoolDoorway`, and the host `PoolGalleryCard` — every server surface that read the env flag now reads BOTH.
- Everything stays inert on merge: both controls seed `'inactive'`, both env flags default off — zero behavior change until the owner flips a flag AND approves the control.

SPEC IMPACT: None (repo-side privacy-control registration; the corpus privacy docs get their /privacy + ROPA coverage in a follow-up — the coverage map's empty `declaredIn` is the tracked drift).
