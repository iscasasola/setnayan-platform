// Loading shell mirroring the Hosts route (invited co-host roster list).
import { ListPageSkeleton } from '@/components/skeletons';

export default function Loading() {
  return <ListPageSkeleton actions={1} />;
}
