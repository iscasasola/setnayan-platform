## 2026-07-22 · fix(pricing): apply the safe 2026-07-22 pricing decisions (cleanups + two reprices)

The self-contained, non-Papic half of the owner's 2026-07-22 pricing answers (DECISION_LOG 2026-07-22 · Pricing.md § 00.G). The cross-surface bundle restructure (Website PRO ₱3,500 · Editorial PRO / Cinematic Reveal / Live Background → bundle-only · Monogram Pro ₱1,000 + Live Background) is a separate follow-up PR — it touches ~8 buy/display surfaces + `lib/v2-catalog.ts` and is deliberately not folded in here.

- **Music Creator retired → Pakanta** (owner #7). The orphan card (no browse surface of its own; the ~400-track `songs.ts` unused) is removed from the catalog. Its `addOnHref` alias + the `/studio/[addon]` redirect map keep any lingering `music-creator` link resolving to `/studio/pakanta` (the "301"). Removed its now-orphaned `add-ons-detail` entry, `studio-recommendations` priority, and the retired `alaala` showcase chip.
- **Photo Delivery off the free layer** (owner: "Photo Delivery on Papic"). Moved to `studioGroup: 'utility'` so it drops from the Suite free layer + grids (both filter `!== 'utility'`) while its `/studio/photo-delivery` page stays reachable by deep link.
- **Legacy `/studio` hub retired → Suite** (owner: "retire those not needed"). The hub index now redirects to `/dashboard/[eventId]/suite` when Suite is on (same `NEXT_PUBLIC_SUITE` flag the page + nav swap key on). The hub still serves as fallback when the flag is off, and every `/studio/*` tool sub-route is untouched.
- **Reprices** (migration `20270914120000`, idempotent): Patiktok `PATIKTOK_COMPILER` ₱1,499 → **₱1,500/day** (owner #6); Pakanta `PAKANTA` ₱2,499 → **₱2,500** (owner #7). Pure price updates on active rows — no entitlement/grandfathering impact (ownership reads `orders.status`).
- **Guardrail tests updated + a resolution locked**: the two audit-known gaps (photo-delivery, music-creator) are resolved, so `KNOWN_GAPS` is now empty; the free-layer snapshot drops both keys; a rewritten test locks the resolutions (photo-delivery must stay out of the free layer; music-creator must stay retired with its Pakanta alias intact).

Verified: `tsc --noEmit` clean · `next lint` clean · **all 2753 unit tests pass** (incl. the 14 Suite doorway guardrails and the add-ons-detail orphan check) · migration-timestamp guard passes.

SPEC IMPACT: Pricing.md § 00 / § 00.G — will be mirrored after merge (§ 00 rule: code lands first, the doc mirrors). Records: Patiktok ₱1,500 · Pakanta ₱2,500 · Music Creator retired→Pakanta · Photo Delivery on Papic · legacy Studio hub retired. The bundle-restructure § 00.G items (#1–#3) remain open pending the follow-up PR. ⚠ The catalog reprices take effect on prod only after `supabase db push` (migrations do not auto-apply).
