## 2026-08-20 · docs(claude): point cold sessions at the 2026-08-20 handoff

The owner is transferring to a new Claude account, which gets no memory files and
nothing local — only what is committed and pushed. The auto-loaded pointer now
names `WHATS_NEXT_HANDOFF_2026-08-20.md` (spec corpus), and the essentials are
inlined here because this file's own START HERE block warns the corpus is "a
second repo you may not have".

Records: the 11-item build list is COMPLETE; what is small and left; the measured
readiness state (0 orders ever, marketplace empty-not-broken); the new
payment alert; and the trap that produced a wrong report to the owner —
**a flag's default in code is not its value in production.** He caught that one,
not me.

SPEC IMPACT: Applied — new handoff added to the corpus, registered at the top of
`WHATS_NEXT_INDEX.md`, the 2026-08-19 handoff marked superseded at its top, and a
`DECISION_LOG.md` row appended.
