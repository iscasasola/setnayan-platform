## 2026-07-17 · fix(seating-3d): flag legacy round-vs-round overlaps in the 3D lab + tested spawn/create parity

Owner evidence (2026-07-17 3D-lab screenshot): two 10-seat ROUND tables interpenetrating in a figure-8 (chair rings tangled, ~40% overlap). Post-#3307 standalone rounds must ALWAYS collide, so this state should be uncreatable — and if it is legacy data, it should be VISIBLY flagged. Audited all three possible holes:

- **Live 3D drag (hole 1) — already CLOSED, no change.** The move path feeds `oraclePose`/`scaleOfTable` → `checkPlacement`. For `round_10` the footprint is chair-inclusive by construction: `scale = TABLE_FOOTPRINT_M.round_10 / tableGeometry('round',10).box.w = 2.8/222`, so `obbOf` yields a circle of **r = 1.4 m (Ø 2.8 m)** — the `box.w` cancels: `(222/2)·(2.8/222) = 1.4`. Two cap-10 rounds are INVALID (kind `overlap`) for any centre-distance < 2.8 m (< 3.4 m with the 0.6 m walkway). At the owner's ~1.7 m figure-8 the body penetration is 1.1 m — deeply refused. Identical footprint to the 2D editor (same helper, `pxPerMeter` folded to 1).
- **Live 3D create (hole 2) — already CLOSED; extracted for testability.** The spawn spiral used the same chair-inclusive `round_10` scale and only ever returned a `checkPlacement(...).valid` pose. Extracted that inline spiral into a pure, exported `firstFreeRoundSpawnPct(others, zones, room, gapPx)` in `lib/seating.ts` (behaviour-identical) so CREATE parity is now a tested invariant, not component-local logic. `seating-lab-3d.tsx`'s `spawnRoundPct` is a thin wrapper over it.
- **Legacy render gap (hole 3) — REAL, fixed.** Saved anchors are never force-moved on load, so a room persisted before the shared oracle (a pre-#3307 round "kiss") can sit body-deep in an overlap the current paths would refuse — and the 3D lab drew NOTHING for it (`layoutViolations` was never called there; the warm-red ground ring only appeared mid-drag). Added a read-only 3D twin of the 2D mount audit: a pure `useMemo` (`violatingIds`) runs the SAME `layoutViolations` over the SAME chair-inclusive poses + zones + 0.6 m walkway, gated to a sized room + build mode; each flagged table renders a static warm-red ground ring (`#d9534f`, the #3285 escape colour) in `TableMesh`. No movement, never raycasts; the drag ring supersedes it. Monotone-escape already lets an overlapped round be dragged APART to heal (verified + regression-tested).

The owner's screenshot is **legacy data**, not a live-creatable state: neither the 3D drag (chair-inclusive oracle + monotone escape) nor the 3D create (oracle-gated spawn) can produce a fresh round-vs-round overlap.

Tests — new `lib/seating-3d-round-collision.test.ts` (11 cases, all through the exact lab bridge): (0) round footprint is Ø 2.8 m chair-inclusive; (a) owner figure-8 → `checkPlacement` invalid/overlap + the 2.8 m / 3.4 m refusal bands; (b) `firstFreeRoundSpawnPct` never returns an overlapping pose (empty room, blocked base, dense grid, no-go zone); (c) the flagged-set (`layoutViolations`) marks a known-bad fixture and leaves a clean room empty; (4) monotone-escape lets an overlapped round move out but not deeper. Full `lib` suite green (1933), typecheck + lint clean.

SPEC IMPACT: None
