## 2026-07-23 · feat(plan3d): chibi avatar foundation — schema + reusable ChibiFigure kit, flag-dark (Build ② PR-1)

WAVE-2 LANE G · the FIRST of Build ②'s PRs (`OnTheDay_App_Build_Studies_2026-07-23.md § 2`, `Chibi_Rig_Production_Spec_2026-07-19.md`): the foundation only — NOT the maker UI, NOT the venue-walk rig swap, NOT crowd integration (later PRs). Zero visible change: nothing mounts the component, nothing reads the columns, the flag defaults OFF.

**Schema (INERT on merge — zero readers/writers)** — `supabase/migrations/20270918210897_chibi_avatar_foundation.sql`: `guests.avatar_config JSONB` + `guests.avatar_updated_at TIMESTAMPTZ` + a 2048-byte size CHECK + COMMENTs (the study § 2.3 sketch verbatim). guests RLS untouched (zero-account subjects; the future write path is the session-verified server action per the submitRsvp pattern). NULL → hash-derived default look. RA 10173: rides guests' existing export/erasure lifecycle; reset = NULL. Surfaced spec divergence: rig spec § 11.4 names account-level `users.avatar_parts` — guests are zero-account, so `guests.avatar_config` is the store of record; copy-on-account-claim is a later PR.

**`lib/chibi-config.ts` — THE one whitelist** (pure, unit-tested ×13): the `ChibiAvatarConfig` shape the migration comment declares ({v, bodyType, skinTone, hairStyle, hairColor, eyes, mouth, mark, outfit, outfitColor, accessory, colorMode}); catalogs re-activate the dormant figure-rig look tables (SKIN_TONES verbatim; HAIR_COLORS 4→6 per rig spec § 3); 11 hair styles · 4×4×4 face parts (FACES ARE IN, § 10) · 10 outfits · 8 curated outfit colours · 6 accessories. `validateChibiConfig` = the fail-closed write gate (unknown keys, off-palette hex, out-of-catalog → reject); `resolveChibiConfig` = the never-throw read repair (junk falls back per-field to FNV-1a hash defaults — same id, same chibi, forever); `darkenHex` + `effectiveChibiColors` = the single colour-derivation source both the individual figure and the future instanced crowd share. Privacy fence: bodyType is COSMETIC, derived only from the opaque id hash — never from `users.sex`.

**`lib/chibi-geometry.ts` + `kit/chibi-figure.tsx` — the reusable figure** (pure geometry module + thin client component): ports the corpus prototype recipes (`3D_Avatar_Maker_2026-07-19/chibi_studio_prototype.html` — closedLathe, buildFace, two-piece hair cap incl. the 2026-07-21 placement clamp, outfit lathes, accessories) RE-AUTHORED per the owner-corrected § 11 V5 silhouette law — **integral part geometry, NOT the retired "overlap" draft**: arms emerge from inside the torso lathe (no shoulder pivot), hands are the arms' own coaxial same-radius rounded ends (no mitten-ball joints), body top terminates inside the head sphere, ears fold into the head buffer; head stays a separate group (`userData.headGroup`) for PR-2's idle tilt. 100% procedural in-code geometry — CSP-safe by construction (no external models/fonts/textures). Parts are grouped by COLOUR REGION with `ChibiPaint` descriptors — the documented instancing contract for the later crowd PR (one InstancedMesh per part buffer, per-instance colour via instanceColor; NEVER merge whole-figure look-combos). Merge-gate tests land here (×10): closed-lathe law on every profile, the § 11.2 **no-exposed-cap junction audit** (mechanical containment margins for every bodyType × outfit — body-top→head, arm-root→torso, hand→sleeve, leg-stub→hem), hair-cap clamp invariants, shared-cache identity, full catalog construction sweep.

**Flag** — `NEXT_PUBLIC_FIGURE_CHIBI` (default OFF, `.env.example`): declared + gate helper exported (`FIGURE_CHIBI_ENABLED`); nothing consumes it in this PR.

Verified: typecheck clean · lint clean · production build green · 24 new unit tests green (chibi-config + chibi-geometry, incl. the § 11.2 junction-audit and closed-lathe merge gates) · full migration-replay DB suite green (photo-tag-cap replays all migrations incl. the new one; columns + size CHECK confirmed live in the replayed schema).

SPEC IMPACT: None (implements the corpus study/spec as written; the § 11.4 `users.avatar_parts` divergence is surfaced in the migration header + PR body, not silently resolved).
