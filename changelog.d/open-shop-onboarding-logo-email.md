# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-05 · feat(vendors): /open-shop onboarding — six basics + location (adds logo + company email, drops website + social)

The `/open-shop` vendor on-ramp (the "List your business for free" CTA target) is trimmed to the owner-locked onboarding set: **shop name · shop logo · owner name · contact number · company email · main service · location**. This adds the two owner-required basics the shipped 2026-07-03 wizard omitted — the **mandatory logo** (§2.1b) and the **company email** — and, per owner 2026-07-05, **removes the website and social fields from onboarding** (those move to the dashboard). The underlying `vendor_profiles.website` column and the `social_media` verification slot are untouched — just no longer collected at onboarding.

- **`app/open-shop/_components/open-shop-wizard.tsx`**: Step 1 carries the mandatory **logo** upload (reusing the shared `<FileUpload bucket="media" variant="square" qrGuard>` → R2 pattern from My Shop, identical props to `vendor-dashboard/profile/page.tsx`); Step 2 is **owner name · contact number · company email** (all required) + **location** (optional). Website + social inputs removed. Closing note updated: *"You can upgrade your business further anytime — add your website, social links, photos, services and pricing from your dashboard."*
- **`app/open-shop/actions.ts` `becomeVendor`**: validates + upserts `logo_url` (r2:// ref, same `parseLogoValue` shape as My Shop) and `contact_email` (light shape check) onto `vendor_profiles`, alongside `business_name` / `business_owner_name` / `contact_phone` / `services` / `location_city`. Owner name, contact number, and company email are now hard-required with inline error redirects. The website write + the `social_media` verification-slot seeding (and their now-unused `cleanUrl` / `buildSlotValue` / `resolveApplicationFeeCentavos` imports) were removed. Provisioning path unchanged: idempotent find-or-create of the owned `vendor_profiles` row + founding `vendor_team_members` admin seat via the admin client (a fresh signup's trigger-provisioned bare shop is UPDATEd; a logged-in account with no shop gets one INSERTed). No publish/verify here — new shops keep default visibility.
- **`app/open-shop/page.tsx`**: selects `logo_url` + `contact_email` (dropped `website`), pre-resolves the logo display URL via `displayUrlForStoredAsset` so an already-uploaded logo paints on load, and passes `vendorProfileId` (scopes the logo R2 prefix) into the wizard. The logged-out auth gate redirects to `/signup?as=vendor&next=/open-shop` so the return loop is explicit (covers the already-logged-in `?as=vendor` path, not just fresh signups whose vendor default already lands on `/open-shop`).

Column names written: `business_name`, `logo_url`, `business_owner_name`, `contact_phone`, `contact_email`, `location_city`, `services[]` (primary category leads the array). No new migration — every column already exists on `vendor_profiles`. **Website + social remain fully editable in the dashboard:** `website` at `vendor-dashboard/profile/page.tsx` (Website field → `saveVendorProfile`), social links at My Shop via the `social_media` slot's 9 labeled platform inputs (`vendor-dashboard/shop/_components/docs-body.tsx`).

SPEC IMPACT: The `/open-shop` onboarding route already existed on origin/main (owner 2026-07-03) and is already wired from the vendor hero / tier-ladder / deep-dive CTAs and the signup vendor-redirect — the "dead `/open-shop` links in how-it-works / explore" premise was stale (those pages link to `/signup?as=vendor`, which forwards vendors to `/open-shop`; the links resolve). This change is a field-set correction to the shipped flow: adds the owner-required logo + company email, drops website + social from onboarding (owner 2026-07-05), keeps location. Record the field-set decision at the bottom of `~/Documents/Claude/Projects/Setnayan/DECISION_LOG.md`.
