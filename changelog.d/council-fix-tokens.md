## 2026-07-22 · fix(vendor): sweep the dead/false token surfaces stranded by token retirement + free answering — and free the manpower gig accept

Companion to `stop-token-pack-sales.md` (packs retired · answering made free). Retiring the packs and neutralising the inquiry burn left a fleet of vendor surfaces still asserting the dead token model (the "orphaned peso card" pattern) — plus one LIVE functional stranding. Nothing consumes tokens on the vendor answer/spend paths anymore, so this cleans the copy/UI and fixes the one broken flow. The token wallet / bundle-grant / burn DB plumbing is left DORMANT (untouched); no prices changed.

**FUNCTIONAL — Manpower gig accept is now FREE.** `app/vendor-dashboard/manpower/actions.ts` `acceptManpowerGig` still called `consume_vendor_assets(p_spend_amount: 2)` and raised *"You need 2 tokens… buy a token pack"* on shortfall — but packs are retired (no token source) and the remedy pointed at a deleted redeem page, so any token-less vendor was STRANDED on a LIVE surface (mounted in `shop/page.tsx`). Mirroring how the inquiry burn was neutralised, the 2-token consume is DROPPED: the atomic pending→accepted claim now runs directly, the gig records 0 handshake tokens, and ownership stamping is unchanged. Removed the `insufficient_tokens` result variant; swept the token copy across `surface.tsx` (header "2 tokens to accept" → "Free to accept", removed the wallet-balance tile + "Buy tokens →", BIR note "2-token handshake" → "Accepting a gig is free") and `_components/gig-card.tsx` (removed the insufficient-tokens banner + "Buy tokens →", button "Accept · 2 tokens" → "Accept gig", success/race copy de-tokenized, dropped the now-unused `Link` import).

**COPY/UI SWEEP — surfaces that asserted the dead model:**
- `app/vendor-dashboard/earnings/surface.tsx` — removed the live "About tokens / Buy tokens →" CTA card (FALSE: "tokens are used only for certain vendor add-ons" — the four add-ons are apply-then-pay PHP, nothing spends tokens) and the "…and token packs" clause; dropped the unused `Coins` import.
- `lib/chat-actions.ts` — the `INSUFFICIENT_WALLET_BALANCES` branch is dead on the live path (`unlock_vendor_event` forces the burn to 0). Kept the branch defensively for the dormant HOLD path but replaced the impossible "Top up your token balance" remedy with honest generic copy. Marked the burn-model comment block as HISTORICAL (answering is free) and de-tokenized the perk-reveal comment.
- `app/vendor-dashboard/subscription/_components/token-wallet-section.tsx` — REDUCED to a dormant read-only balance panel: reads only the wallet balance, renders NOTHING when it's zero (the common case), and otherwise shows a "retained balance" note (no buy CTA, no spend copy, no pending/purchase-history/team-buy). Tokens are now "receive-only" (still grantable via plan bundles + admin grants, but nothing buys or spends them), so the granted-balance read is kept. Purchase components (`buy-tokens-cta` etc.) are no longer mounted (left in place, dormant).
- `app/vendor-dashboard/subscription/page.tsx` + `tokens/page.tsx` + `_components/vendor-sidebar.tsx` + `earnings/surface.tsx` + `deep-search/page.tsx` — renamed the **"Plan & tokens"** hub label to **"Plan"** everywhere user-facing (metadata titles, eyebrow, sidebar row, back-labels, in-copy link).
- `app/_components/home/vendor-benefits.ts` — removed the Custom-tier "monthly included inquiry tokens" marketing (tagline + benefits array) and the dead **"Peso-per-lead scorecard"** Solo benefit (its RPCs were dropped in `20270911189993`).
- `app/vendor-dashboard/subscription/_components/price-position-card.tsx` — fixed the stale "beside the Peso-per-lead card" comment reference.
- `app/vendor-dashboard/messages/[threadId]/page.tsx` — de-tokenized the one stale "Post-accept (token burned)" comment (user-facing accept copy already said "it's free").
- `apps/web/VENDOR_TIERS_AND_BENEFITS.md` — added a prominent SUPERSEDED banner: every "subscriptions + tokens", "burns a region-banded token", `inAppGated`, Boosters, and "inquiry tokens" claim is historical; answering + manpower are free; revenue = subscriptions + four apply-then-pay PHP add-ons; code is canonical.
- `apps/web/public/llms.txt` — removed the token-only claims (the "…and vendor token packs" index line, the "100 complimentary founder tokens" bonus, the Boosters Q&A, the branch "token wallet" mention). Only token lines touched (the vendor-NAME line is owned by a concurrent PR). `lib/llms-price-drift.test.ts` stays green — the removed lines carried token COUNTS, not ₱ figures.

Verification: `lib/**/*.test.ts` all pass (2709, incl. `llms-price-drift`), `tsc --noEmit` clean, `next lint` clean on touched files, `lint:retired` clean.

SPEC IMPACT: No new product surface, price, or schema. Behavior changes surfaced for owner sign-off: (1) accepting a **manpower gig is now free** (was a 2-token handshake) — matches "tokens retired / answering free"; and (2) tokens are now **receive-only** on the vendor side (grantable via plan bundles + admin grants, but unbuyable/unspendable). Remaining owner-facing inconsistency left intentionally IN SCOPE-BOUNDARY (plan model, not touched here): the subscription plan cards + pitch still advertise "bundle free tokens each cycle" as a Pro/Enterprise/Custom perk while those tokens are now unspendable — flag for the owner to decide whether to keep bundling them. Corpus lives outside this worktree and was NOT edited; the pricing corpus / token memory note should reflect free manpower accept + receive-only vendor tokens.
