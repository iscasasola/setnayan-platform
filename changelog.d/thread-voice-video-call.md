## 2026-07-11 · feat(calls): free 1:1 voice/video call in accepted threads

Adds a free 1:1 voice/video CALL inside an accepted vendor↔couple chat thread (Relationship_Workspace_and_Appointments_2026-07-11.md · "Call"; PR 10 of the Vendor↔Customer master build plan). Additive only — the existing chat composer, message stream, and thread accept-gate logic are untouched; the call entry renders solely in the `inquiry_status === 'accepted'` branch of both thread pages.

- **Reuses the existing transport as-is.** Media is free peer-to-peer WebRTC over an ephemeral Supabase Realtime broadcast channel (`lib/call-webrtc.ts`, unchanged) — STUN-only, no TURN, so it costs ₱0 per call and never touches a Setnayan server. A rare both-sides-hard-NAT pair fails cleanly with a hint (a TURN relay can be added later without touching this feature).
- **New table `thread_calls`** (migration `20270715600000_thread_calls.sql`) — ring/session metadata ONLY (never any media): `kind` voice|video, `status` ringing|active|ended|missed|declined, started_by/at, ended_at, plus thread/event/vendor scoping columns. RLS at CREATE time MIRRORS the `chat_messages` membership pattern exactly (the two thread parties read/insert/update: couple via `current_couple_event_ids()`, vendor via `current_vendor_profile_ids()`); admin read via `is_admin()` (matches the sibling `event_appointments` read policy). Opted into the `supabase_realtime` publication (idempotent) so the callee's open thread page rings live. Distinct from `event_appointments` (scheduled meetings) — this is the live-session log.
- **Server actions** `app/_actions/thread-call-actions.ts`: `startThreadCall` (verifies the caller is a thread party via the same `fetchThreadById` + couple/vendor probe as `chat-actions.ts`, gates strictly to accepted threads, inserts a `ringing` row under the caller's own RLS — never the admin client — then rings the OTHER party via `emitNotification` type `chat_message`, title "Incoming voice/video call"). `endThreadCall` flips the row to `ended` (RLS-scoped, best-effort).
- **Client**: `thread-call-room.tsx` (self-starting WebRTC room: local+remote video tiles for video, avatar placeholders for voice; mute / camera / hang-up; teardown = leave() + stop tracks + endThreadCall), and `thread-call-launcher.tsx` (start voice/video buttons + a live "Incoming call — Join" banner from a server-fetched + Realtime-subscribed ringing row).

Verified: `tsc --noEmit` (zero errors in changed files) + `next lint` clean on every changed .tsx. Live 2-peer WebRTC can't be exercised headlessly (needs two devices + camera), so verification is compile + lint + render-shape only.

SPEC IMPACT: Relationship_Workspace_and_Appointments_2026-07-11.md (PR 10)
