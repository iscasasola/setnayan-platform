# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-16 · feat(creator): chapter report path + share buttons (Storytellers S0 safety floor)

Phase S0 of the Storytellers × Editorial architecture council verdict (2026-07-16, §5 item 1 + §7): the public creator chapter pages `/u/[userSlug]/c/[chapterId]` are ALREADY LIVE (published chapter + `public_profile_enabled` owner) with no abuse-report path and no share doorway. This closes both, independent of every later Storytellers phase — no gate.

- **Migration `20270817834616_chapter_report_target.sql`**: widens the `user_reports.target_type` CHECK to add `'chapter'` (target_id = `creator_chapters.public_id`, S89C-…), following the exact drop-by-definition-then-readd pattern of 20270812329751. A chapter report is not event-scoped, so the `user_reports_event_id_required_check` guard is re-added as `target_type IN ('user_profile','chapter') OR event_id IS NOT NULL` — chapter reports carry NULL `event_id` and are admin-only under the existing RLS (no policy change). Table COMMENT updated.
- **`lib/reports.ts`** (`fileReport`): accepts `target_type='chapter'`, validating the target against a PUBLISHED `creator_chapters.public_id` (a draft is never publicly reachable) and filing with `event_id = NULL`. Same service-role write path, signed-out reporters supported, same open-report dedup.
- **Chapter detail page** (`app/u/[userSlug]/c/[chapterId]/page.tsx`): gains a quiet actions row above the footer — the reusable `ShareButtons` (FB / Pinterest / copy-link, pointed at the chapter's canonical URL with the profile OG card as the pin image) + the reusable `ReportPageButton` (`target_type='chapter'`, `target_id=public_id`). The entire page is the public branch (`resolve()` 404s anything unpublished or on a non-public profile), so neither control can render on a private surface. Luxury layout untouched — the row sits below the substrate behind a hairline rule.
- **`/admin/user-reports`**: renders the new target type — "reported a creator chapter" phrasing, chapter title + "Open chapter page" deep link (resolved via a batched `creator_chapters` + `users.slug` read; falls back to "no longer published" / "no longer exists"). Chapter reports resolve via the existing **Escalate / Dismiss** lanes; no Hide button is offered for chapters yet — `actions.ts` carries the documented **Phase S1 seam**: once `creator_chapters.showcase_featured_at`/`showcase_feature_rank` exist, the hide resolution must atomically clear `showcase_featured_at` in the same action (columns deliberately NOT added now).

SPEC IMPACT: Yes — implements Phase S0 of `~/Documents/Claude/Projects/Setnayan/Storytellers_Editorial_Architecture_Council_Verdict_2026-07-16.md` (§7 Phase S0, §5 item 1, §4 step 5). DECISION_LOG.md row appended in the corpus.
