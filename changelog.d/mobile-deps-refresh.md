## 2026-07-16 · chore(mobile): clear 8 dependabot alerts — tar/minimatch transitive bumps

`apps/mobile` (the Capacitor shell) is a standalone npm project excluded from the pnpm workspace, and held the mobile side of the open dependabot alerts — 7 HIGH (node-tar path-traversal ×6 + minimatch ReDoS) + 1 moderate (tar PAX size-override smuggling). All eight lived under the dev-only `@capacitor/assets@3.0.5`, which bundles an old `@capacitor/cli@5.7.8` (pins `tar@^6.1.11` → 6.2.1) and pulls `@trapezedev/project → replace@1.2.2` (pins `minimatch: 3.0.5`). `@capacitor/assets@3.0.5` is already the latest published release, so bumping the package could not resolve them.

- `apps/mobile/package.json`: added an npm `overrides` block scoped entirely under `@capacitor/assets` — `@capacitor/cli → tar` forced to `^7.5.16` (dedupes to the top-level `tar@7.5.16` already in the tree) and `@trapezedev/project → replace → minimatch` forced to `3.1.5`. No production/runtime dependency touched; the pnpm workspace is untouched.
- `apps/mobile/package-lock.json`: regenerated in place (npm 10) so only the tar/minimatch subtrees churn — the old tar-6-only deps (chownr@2, minizlib@2, minipass@3, yallist@4, fs-minipass@2, mkdirp@1) drop out and `minimatch@3.0.5 → 3.1.5`. Everything else (including `typescript`, which a naive full re-resolve would drift to the broken 7.0.2 TS-Go preview) stays pinned at its pristine version.
- Validation (no CI gates on this project): `npm install` clean · `npm audit` HIGH count 6 → 0 (4 moderate remain — the dev-only `xcode → uuid` chain, out of scope) · `npx cap sync android` succeeds · `npx capacitor-assets generate --android` runs clean (86 assets) exercising the tar-7-forced bundled cli + the `replace`/`minimatch` path. Android/iOS generated artifacts from the validation runs were reverted; only the two dependency files ship. The Android SDK/gradle toolchain is not installed in this environment, so `assembleDebug` was not attempted.

SPEC IMPACT: None
