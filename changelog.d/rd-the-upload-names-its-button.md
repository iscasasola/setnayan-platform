## 2026-09-23 · fix(upload): a fresh upload names the button that will save it, in that button's own words

Owner, 2026-09-23, on /open-shop step 1 with the logo just uploaded: *"asking me to press save when there is no save."* The chip read **"NOT SAVED YET — PRESS SAVE BELOW"**; the button on that step is Continue and the shop is created four steps later by "Open my shop". The sentence was written for /dashboard/profile (2026-08-19, whose button IS Save) and then rendered verbatim on every one of the widget's 37 form-bound mounts — the You card (Done), the receipt log (Log), the dispute (File flag), the checkout (Submit request), the story editor (a publish rung).

- **`FileUpload` gains `unsavedHint`, REQUIRED by type whenever `name` is passed** (`FileUploadFormBinding`, a discriminated prop: a widget that feeds a form must say what happens to a fresh upload; a widget with no `name` refuses the hint). The widget keeps the half that is true everywhere — *"Not saved yet — "* — and the caller supplies the tail: *"press Done below"*, *"it goes in when you open your shop"*, *"press Publish service or Save as draft below"*. `unsavedHint={null}` is for the three mounts whose parent saves the upload itself on change (verification documents and pairs, the story cover): the widget then makes no claim and the parent's own Saving…/error stands.
- **Every mount read, not guessed.** 37 `<FileUpload name=…>` mounts across 26 files, plus the `ShowcaseMediaFields` wrapper (its four parents thread the hint, because only the parent knows its button). The wizard says the logo goes in when the shop opens; the profile, the website editor and the admin taxonomy keep "press Save … below" because there the button really is Save.
- `lib/upload-unsaved-line.ts` (pure): `unsavedTail` normalises the tail (trims, strips a pasted "Not saved yet —", refuses empty); `pressBelow` builds the common shape. `lib/upload-unsaved-line.test.ts` EXECUTES it.
- Guard `app/_components/the-upload-names-its-button.test.ts` WALKS app/ and lib/: every form-bound mount carries a hint (41 blocks measured, count printed), no hint repeats the widget's claim, a `{null}` mount must carry an onChange and its file a save/transition, and the widget's one-size sentence must not return. Sabotages watched red: a fresh `name=` mount with no hint (tsc AND the walk), `unsavedHint=""`, the literal "press Save below" restored in the widget. `save-cannot-outrun-the-upload.test.ts` stays green untouched — the widget still prints "Not saved yet" and still keys on `seed-` ids.

SPEC IMPACT: None (copy is derived from each page's existing button; no product decision changed).
