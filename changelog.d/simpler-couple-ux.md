## 2026-07-10 · feat(studio): "Recommended for you now" — Studio leads with the couple's next step, roadmap-aligned & progress-aware

The couple's core-loop entry (5-tab nav · Home's "Today's one thing" + roadmap-throttled-to-3 · free-by-default Setnayan AI) is already tuned for "easy and simple." The Studio hub was the one surface still reading as a **store**: header "Everything you can make with Setnayan" over ~24 buyable/free add-ons across 4 tabs, with no sense of what to do *first*. That's the paradox-of-choice moment that undoes the guided spine.

Studio now **leads with the 2–3 add-ons that fit where the couple actually is**, then drops the full catalog under a "Browse everything" divider. Crucially, "where they are" is the **same roadmap state the free Home list reads** — so the two surfaces can't contradict each other.

- `lib/wedding-roadmap-signals.ts` (new): the single source of truth for the couple's roadmap state — months-to-date + the hard structural signals (date locked, venue booked, guests added, capture owned). **Extracted from `wedding-roadmap-async.tsx`**, which built it inline; that component now imports `fetchRoadmapState` (behaviour byte-identical — all 1372 lib tests green). Pure `resolveEarliestDate` + `deriveRoadmapSignals` are unit-testable; the earliest-date anchor is precision-agnostic on purpose (planning timing starts at any target).
- `lib/studio-recommendations.ts` (new · pure): ranks by **(1) following the roadmap** — an add-on that advances an OPEN roadmap item surfaces in the roadmap's overdue-first order (behind on save-the-dates → Save the Date leads) — and **(2) respecting readiness** — a prerequisite gate holds day-of capture out of the lead until the date is locked, and a seat plan out until there are guests. Non-roadmap "delight" add-ons (monogram, Pakanta, LED, music) fill remaining slots by peak-month proximity. Free picks recommendable on purpose. `STUDIO_PEAK_MONTHS` / `STUDIO_RECOMMEND_EXCLUDED` are **drift-guarded** against the catalog by a test, so a new add-on can't silently vanish from recommendations.
- `lib/studio-recommendations.test.ts` (new · 8 tests): readiness gate (capture withheld until date locked, then surfaces), roadmap-follow (overdue save-the-dates → Save the Date), owned/ineligible drop + backfill, limit, excluded-never-appears, and the two drift guards.
- `app/dashboard/[eventId]/studio/page.tsx`: one shared `fetchRoadmapState` read folded into the existing `Promise.all` (replaces the bare `event_date` read); "Recommended for you now" strip reuses `StudioAppRow` + the live pill/href logic, placed **after** the human coordinator/vendor suggestion strips (a person's targeted pick outranks a timeline guess) and before the catalog. Header softened "Everything you can make with Setnayan" → "Your Studio".

Proof (nothing owned, all eligible): `2mo NOT ready → mood-board, rsvp, playlist` (no capture) vs `2mo date-locked → mood-board, save-the-date, papic` — same calendar position, different picks by actual readiness. Typecheck + lint clean; 1372 + 8 tests green.

Gaps this closes from the prior review: progress-awareness (recs were date-only and could contradict the roadmap), event-date-precision handling (now via the roadmap's own earliest-date anchor), committed test + drift guard, and human-suggestions-above-algorithmic ordering. Still deliberately out of scope: "Browse everything" breadth, the Services/Vendors-tab presentation, and the ~45-route depth (structural follow-ups).

SPEC IMPACT: None (in-app Studio UX + a shared internal lib; no SKU, price, schema, or catalog-content change).
