## 2026-09-22 · feat(signup): the small card — /signup is the sign-in card, then one "You" card

Owner, 2026-09-22, on the approved one-door prototype: *"approved. account inside step 3, consent per event, small card for signup"*. This is the third of those three (Redesign bundle · build `rd-signup-small-card`; build 3 landed in wave 1 as #5876).

- `/signup` becomes the SAME greige card `/login` wears (`.home-reskin-ov` › `.hr-ov-card.sn-signin-terra`) over the same paper base: Google / Apple by /login's shell gate, email, password, the Terms box (CTRL-B3, unticked, required, above the submit), Stay signed in, *Create account · free*, *Have an account? Sign in*. Measured before: 136 words (couple) / 107 (vendor); the card is ~40.
- **Removed from /signup, each with a named home:** the v2.1 brand panel (wordmark, "Set your day in motion.", four bullets incl. the live verified-vendor count), the "RA 10173 compliant" line → gone, /privacy carries the promise · `first_name` / `last_name` → the **You card** · `public_summary_consent` → the **You card (interim)**, couples only, unticked, in event-neutral words; its final home is event creation, per event (build 2). `signup-contract.test.ts` moved with them, deliberately; `doors-are-designed.test.ts`'s clone bill shrank by the /signup line.
- **New: `/signup/you`** — "How should we call you?": photo (optional), display name (required), the **@account name with a live availability check** (`checkAccountName` → the one `findSlugConflict`, failing CLOSED as *"Could not check right now"*), set only while the account has none (renames stay on the profile page behind their cap and log), the formal name folded (`<details>`, the 2026-09-21 five parts), phone. Done saves through the profile page's own plan builder (`planPersonalInfoPatch`); Later continues. Reached from `signUp` (couples; vendors go to /open-shop, whose step 3 asks the name) and from the OAuth callback for a brand-new customer account — both through `lib/signup-landing.ts`.
- Guards: `lib/signup-landing.test.ts` EXECUTES landing, brand-new, and the You plan (incl. consent never for a vendor); `app/signup/you/the-you-card-is-a-first-run-subset.test.ts` pins the form, the gates, the fail-closed check and both doors. Sabotages watched red: vendors routed to the card; consent pre-ticked; callback routing everyone.

SPEC IMPACT: `DECISION_LOG.md` 2026-09-22 🚪 ONE DOOR row (already recorded); `prototypes/one_door_FINAL_2026-09-22.html` is the approved drawing. The consent's interim home on the You card (not in the drawing) is flagged for the owner in the build report.
