## 2026-07-15 · feat(design): Atelier-Glass foundation — warm wash, kit primitives, motion library, violet retirement (Glass PR-1)

The app-wide foundation for the Atelier-Glass rollout (App_Wide_Glass_Rollout_Plan_2026-07-15 §5 PR-1 row). No page recompositions — this warms the whole app up at once, coherently, so every intermediate state stays coherent (opaque cards on the warm wash read as intentional paper-on-paper). PR-0 (#3247) already landed the launcher kit values this references; this generalizes them.

- **Warm wash, one canvas (§1.1 · R1 sign-off):** `.sn-ambient` is redefined from the 2026-07-13 plain-white flatten to the canonical Atelier wash (warm paper base + gold/green/slate radial glows, `background-attachment: fixed`, no drift animation). It's already applied by SidebarShell to every dashboard doorway (event/vendor/admin); the launcher swaps its bespoke `.sn-wash-home` splash for `.sn-ambient` (that orphaned duplicate recipe is removed — one wash source of truth), and `(account)/layout.tsx` gains `sn-ambient min-h-dvh` so the spokes sit on the same canvas as the home. Glass panels require a tinted backdrop; this is the precondition of the language.
- **Glass var canon (§1.2):** `--sn-glass-bg` .55→.5, `--sn-glass-line` .75→.72, `--sn-glass-blur` `blur(18px) sat(1.4)`→`blur(22px) sat(1.5)` — so `.sn-glass`/`.sn-card`/`.sn-tile` all share the fidelity-spec frosted recipe.
- **New shell kit primitives (§1.2):** `.sn-tile` (glass, radius-20, pad-18, `--sn-sh-tile`, hover −3px→`--sn-sh-hi`), `.sn-tile-dark` (the obsidian focal — canonical named class; same recipe as the launcher's `.sn-tile-obsidian`), `.sn-row` (opaque tint, NO blur — the blur-budget fallback for repeated rows), `.sn-eye` (gold-700/gold-300 eyebrow, 14px icon), `.sn-h1` (Hanken w800 36px −.03em + `.sn-h1-tail`), `.sn-sec`/`.sn-sec-sub` (sentence-case section heads), `.sn-modal-panel` (frosted modal chrome). `.sn-card` hover retuned to `--sn-sh-hi`.
- **Live-primitive re-point, SCOPED to dashboards (§1.2 · R5):** `.button-primary`/`.button-secondary`/`.input-field` recipes re-pointed to the kit's gold fill / ink outline / gold focus ring **only under `.app-surface`**. Guest (`app/[slug]`, `.sn-editorial`) + marketing surfaces have no `.app-surface` ancestor, so their base rule is byte-identical (verified: `.button-primary` base def untouched; the override selector `.app-surface .button-primary` cannot match the guest tree).
- **Motion additions (§2):** `sn-rise-soft`/`.sn-page-enter` (the 400ms route-transition rise the per-surface `template.tsx` files consume in PR-2/5/6/8) and `.sn-live-dot` (the one sanctioned infinite pulse on genuinely-live signals), both with explicit reduced-motion coverage. `sn-bloom`/`sn-pop-in`/`sn-lens-in`/`sn-chip-pop`/`sn-ring-sweep` already shipped in PR-0.
- **Radius tokens (§6 R9):** `rounded-tile` (20px) / `rounded-card` (18px) added to `tailwind.config.ts` so the kit radii are named tokens the `lint:radius` guard permits.
- **CountUp promotion + ProgressRing sweep (§2c):** the launcher's CountUp island moves to `app/_components/count-up.tsx` (launcher imports the shared one; its local copy removed). ProgressRing's opt-in `sweep={{delayMs}}` prop already shipped in PR-0.
- **Toast reskin (§1.2):** `toast-provider.tsx` panel → `.sn-row` + warm-semantic (`--sn-success/-danger/-info`) icon/border + `sn-rise` 320ms entrance.
- **Violet retirement (§3.4):** dropped the SidebarShell `accent` prop + `.sn-sidebar--violet` hook + `--a-violet` token + `--m-sidebar-accent-violet*` vars; admin Internal badge → info-slate (`--sn-info` on `--sn-info-soft`); RefundForm violet-700/800 → ink primary + gold focus ring; `admin/layout.tsx` no longer passes `accent="violet"`. Admin's identity is now the ShieldCheck + "HQ" label, not a colour fork. Stale doc-comments referencing the removed hooks corrected. (No admin page recomposition — PR-8 territory.)

SPEC IMPACT: App_Wide_Glass_Rollout_Plan_2026-07-15.md (§5 PR-1 row implemented) + DECISION_LOG.md (two rows appended: R1 warm-wash reversal owner-directed 2026-07-15; R2 scope amendment — full redesign + full motion supersedes the 2026-07-12 skin-only framing).
