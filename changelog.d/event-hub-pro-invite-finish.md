## 2026-09-11 · feat(invite): Event Hub Pro finishes the invite themes — the couple's button colour, the eighth Pro item, one reveal on the way in, weddings only, and links to where the look lives

Builds the owner's 2026-09-11 answers **Q2 · Q3 · Q6 · Q7** (`DECISION_LOG.md`, "the seven invite-theme questions") plus one thing that needed no decision. Session 5 of `Design_Invite_Themes_2026-09-10/01_WHAT_IS_LEFT_SESSIONS_2026-09-10.md`. **No migration, no new table, no new SKU — price and SKU unchanged.**

**1 · WHERE TO CHANGE THE LOOK (no decision needed).** The picker told a couple their invite "opens on your reveal background, in your colour" and pointed at nothing, so the only way to change it was to already know which other page owned which field. The sentence now links each thing it names — and there are **three**, not two, because Q2 made the button a *second* colour: the background → `studio/save-the-date`, the mark and its colour → `invitation`, the button → `website/colors`. Naming three things and linking two of them would have sent a couple to the monogram field to change a button that does not read it.

**2 · THE BUTTON IN THE COUPLE'S COLOUR (Q2 = A).** On a **Pro theme only**, the one action on doors 01–03 takes `events.site_button_color`. New pure module `apps/web/lib/invite-button-color.ts` returns the fill **and its label as a pair** — white or ink by measured contrast, and the Setnayan terracotta `#C24E25` whenever neither label clears 4.5:1. 🔑 **The floor is not "invalid hex".** A perfectly valid mid-luminance colour — a dusty sage — is too dark for white *and* too light for ink at the same time, and there is no third label to try; that is the whole mid band, and a guard that only fed the function junk would have passed while a guest looked at a button they could not read. The pair is carried through `DoorShell`'s skin from `app/[slug]/invite/_lib/load-invite-look.ts`, **not from the theme files**, so the button is the couple's colour the moment a skin ships and no theme can paint a different one. `DoorShell` sets two custom properties and one `data-door-action` attribute; **one rule in `globals.css`** reads them (no new stylesheet — `main` sits a hair under a locked 200 KB; the precedent is `.app-surface .button-primary`). House, every other door, and the Event Hub's own copy of the same RSVP card are outside the scope and render byte-identically. The colour is one a couple typed, so it is validated as exactly `#rrggbb` before it reaches a style.

**3 · THE EIGHTH PRO ITEM (Q3 = A).** `WEBSITE_PRO_ITEMS` gains **'Invite link theme'** — the umbrella already withheld the four Pro invite themes from non-buyers and did not name them. ⚠ **THE COUNT LIVES IN FIVE PLACES AND ONLY ONE IS THE ARRAY**: the CTA a couple *presses* ("Unlock all **eight**"), the sentence they *read* in the editor ("One unlock covers all **eight**"), and three docblocks. Every one of those files passed its own suite with the list at eight and the sentence at seven. The buy page's `BENEFITS` **already** named the invite link (it shipped with #5409) — confirmed on `origin/main`, not rebuilt. `says-what-it-includes.test.ts` stays true both ways.

**4 · NO SECOND REVEAL RIGHT AFTER ARRIVING (Q6 = B).** Since #5410 a Pro invite door opens on the couple's reveal; door 03 then hands the guest into the Event Hub, which opened on the **same reveal, from the same event, under the same rule** — so the veil a guest had just lifted came straight back down. Both halves were right on their own; their *sequence* was wrong and neither could see it. New pure module `apps/web/lib/reveal-once-per-visit.ts`: the invite door's overlay records "seen" **per event** in `sessionStorage` when it actually **shows** (`oncePerVisit="record"`), and the Event Hub's reads it on mount and steps aside (`oncePerVisit="defer"`). Session, not local — the owner's answer is *"on that visit"*, and a later visit plays as usual. Keyed per event, because a guest can hold two invitations in one tab. A store that throws reads as "not seen": a refusal must never suppress a paid opening. 🪤 **THE TRAP — stepping aside must still START the Save-the-Date film**, since on the Event Hub the first lift is what starts it. It does, **through the shipped path and not a new one**: the deferral is folded into `active`, so `window.__stdRevealActive` is never set and the film's own 700 ms grace start takes over — exactly as for a couple who chose "No Reveal". `std-reveal-done` is deliberately **not** dispatched: that is the first-lift signal, and firing it on mount would tell the veil↔film handshake a lift happened that never did.

**5 · WEDDINGS ONLY (Q7 = A).** The four Pro themes belong only where the event type may carry the Save-the-Date film — `resolveWeddingOnlyParts(profile).save_the_date_film`, **the reveal's own fence, asked again, never restated**. Three surfaces now hold it and only one of them protects a value that is *already saved*: the picker does not offer them (absent, not disabled — a locked radio means "buy this", and on a birthday no purchase would ever turn it on), `setInviteTheme` refuses them server-side after the couple check and before the write, and **`resolveInviteTheme` renders House even for a value already in the row** — a couple who picked Capiz as a wedding and then changed the celebration's type gets House with no write, exactly as a lapsed unlock does. An unreadable profile is **not** a wedding on all three: every `.catch` falls to the free door.

**Tests — 20 sabotages, every one confirmed RED before the guard was trusted.** New `lib/invite-button-color.test.ts` (8), `lib/reveal-once-per-visit.test.ts` (7), `app/dashboard/[eventId]/guests/invite/the-invite-look-is-finished.test.ts` (11); `lib/invite-themes.test.ts` extended to 11. 🪤 **Two sabotages came back GREEN first and both were real findings, not noise:** (a) a `.catch(() => false)` assertion was satisfied by a *different* `.catch` elsewhere in the same file, so turning the profile fallback into `true` left the guard green — both such assertions are now anchored to their own chain; (b) the first item-1 sabotage rewrote the opening clause but left the three `<Link>`s standing, so it never broke the property — re-done as the true revert to the shipped paragraph. A new guard also pins that no invite door names a `INVITE_LOOK_COLUMNS` column a second time in its own `select`, since `event_type` moved into that shared string (Postgrest would otherwise be handed one column twice, and nothing throws locally).

⚠ **The eighth item's copy was NOT written by the Fable model.** The spine asks for it; the Fable request returned HTTP 429 (*"You've reached your Fable limit"*). The headline and blurb were written in-session, grounded only in what ships, and deliberately carry **no theme count** — there are three ready skins today and there will be five when sessions 2–4 land.

**6 · THE SAVE MUST PROVE A ROW CHANGED (owner, live on 2026-09-11 — hardening, not an outage).** `setInviteTheme` ran `update({ invite_theme }).eq('event_id', …)` and checked only `error`. **A PostgREST UPDATE that matches ZERO rows returns no error**, so the action fell straight through to `?theme=saved` and the picker printed *"Saved — your invite link now opens in this look."* over a database that was never touched — byte-identical to a real save. It now asks for the rows back (`.select('event_id')`) and counts them; zero rows takes the same exit as an error. 🔑 **The shape was already in the file**: the sibling `regenerateInviteQr` counted rows correctly, so this is a copy, not an invention. **And the other half is the screen.** `?theme=error` had been redirected to since the action shipped and the picker rendered **nothing** for it — a refusal came back as a plain page with the old radio still selected, which reads as *"my click didn't register"* and invites the couple to press Save into the same failure. The `saved: boolean` prop is now `notice: 'saved' | 'error' | null` and both are drawn, the failure with `role="alert"` and words that do not claim the look changed. Both halves are guarded, both sabotage-checked; the write guard's window is the **write's own chain**, because `regenerateInviteQr`'s correct row-count sits in the same file and satisfied every assertion on its own.

**Two findings the merge with `origin/main` produced, neither of them predictable from a grep.**
· 🪤 **A REAL DUPLICATE COLUMN.** PR #5470 rewrote `app/[slug]/invite/enter/page.tsx` and its `.select()` names `event_type` itself; `INVITE_LOOK_COLUMNS` started carrying `event_type` on this branch the same day. Merged, that door handed Postgrest the same column **twice** — and nothing throws locally. The door-side guard added earlier in this branch went **RED on the merge commit**, which is the only reason it was seen. Fixed by dropping the door's own `event_type`.
· 🪤 **TWO STALE "SEVEN"s THE SWEEP COULD NOT SEE.** `launch/page.tsx` ("the seven Pro items are ONE purchase") and `editor-shell.tsx` ("One CTA for all seven") are **comments**, and the guard reads source through `stripComments` — the sweep written to catch "seven" was reading the file with exactly that text removed. Both corrected; both now pinned on a **raw** read, with a matching positive assertion so an emptied file cannot pass.

SPEC IMPACT (item 6): None — no decision, no schema, no copy a spec names.

SPEC IMPACT: `EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md` § 5.3 counts seven Pro items and now counts eight — applied directly in the corpus per the 2026-06-04 direct-edit authorization.
