| 2026-07-21 | **Live Studio console is scroll-free on both layouts (PR #3442) — root cause was `aspect-video`.** Owner (with screenshot): *"obs should be scroll free. for both desktop and mobile."* **Cause:** the PROGRAM monitor used `aspect-video`, which derives height from **WIDTH** — on a wide desktop column that is a ~1000px monitor before the sources rail is reached, so the page scrolled and the SETNAYAN overlay's own bottom band fell off screen. Aspect-ratio boxes suit a card in a feed, not a viewport-filling console. **Principle:** an operator surface used during an unrepeatable ceremony must fit one screen — a control below the fold is a control you cannot reach, and whatever you scroll past is a camera you can no longer see. **Fix:** new `lib/panood-console-fit.ts` — available height is **MEASURED** from the console's own `getBoundingClientRect().top`, never summed from the shell's chrome (sticky bar + `py-6` + `pb-20` mobile nav), because any hardcoded sum rots the first time one of those changes. Mobile-nav clearance follows **VIEWPORT** width (the nav is `lg:hidden`), deliberately NOT the board/compact layout mode — layout is device-driven and the two are allowed to disagree (a laptop in Compact has no bottom nav). `env(safe-area-inset-bottom)` subtracted; negative values cannot add height. **Floors at 420px rather than crushing** — on a short window with OBS docked, scrolling a little is the honest outcome. Returns **null** on an unusable measurement (SSR / zero viewport / mid-transition) so the console falls back to natural flow instead of collapsing. **Layout consequences:** console root = fixed-height flex column with `min-h-0` throughout (without it flex children refuse to shrink below content size and the page scrolls anyway); program `<video>` switches **`object-cover` → `object-contain`** since the box is no longer 16:9 and cropping would make an operator misjudge their own shot; **compact** = the tab body is the ONE scroll region with PROGRAM + tab bar pinned; **board** = columns scroll internally with the rails capped at half the column so a long moment list can never squeeze out the monitor. 9 new tests (124 total). ⚠️ Live on merge. | repo PR #3442 · `apps/web/lib/panood-console-fit.ts` |
| 2026-08-21 | 🔎 **FIND SOMEBODY BY NAME — and "both must have an account" becomes the PRODUCT RULE, not a pilot.** PRs [#4691](https://github.com/iscasasola/setnayan-platform/pull/4691) (merged, **verified live: typing "Ana" on the owner's own account returned "Ana & Marco" with an Add button, no email typed**) and [#4695](https://github.com/iscasasola/setnayan-platform/pull/4695). Owner: *"we can search all users of that name as well. so they can also add manually instead of email address"* · *"it will show all people with that name and pick the person they want to add"* · *"just like facebook"* · then, asked directly, *"these people must have an account to be listed as people."* ⚖ **THE DIRECTORY IS A DELIBERATE CHANGE OF POSTURE.** `visible_connection_names` has said since 2026-07-05: *"name-only, confirmed-only, self-scoped — **never a browsable directory**."* Searching all accounts by name IS a directory; the owner, the NPC-registered DPO, asked for it in those words. **Everything else in that rule is untouched**: a result carries a name, a photo, a public id and a mutual hint — never an email, a phone, a slug, a raw `user_id`, their events or anyone's connections — and finding somebody only lets you ASK. `discoverable_by_name` DEFAULT TRUE with a plain opt-out pair on the profile; **a directory with no way out is the version an NPC reviewer objects to.** 🔒 **AND IT IS NOT AN ORACLE** — an opted-out account, a name nobody has, and a name only anonymous drafts carry all return the same empty list and the same sentence. 🚨 **THE ILIKE ESCAPE THIS CODEBASE HAS ALREADY PAID FOR:** `%` and `_` are WILDCARDS — unescaped, a typed `%` searches for EVERYBODY and `_` matches any character, which is exactly how the admin shop-address correction could move a DIFFERENT shop (2026-08-12). Escaped once, in a pure module split out of the `server-only` file **specifically so it could be tested**, and proved from both ends: unit tests plus db tests that seed a person actually named `Maria %_ Test` so a wildcard search must return HER and nobody else. Mutation: removing the escape (1→0) turns **5 tests red**. 🤝 **ADDING IS A HANDSHAKE, AND THE GUARD FOR IT WAS ONE LEVEL TOO LOW.** Owner: *"it needs to be a handshake for them to become friends like facebook."* It already was — but `person_connections_transition_guard` only inspects **UPDATEs**, so an INSERT arriving already `confirmed` was never a transition and walks straight past the guard everyone assumes covers it. `adding-is-a-handshake.test.ts` now reads every insert in the People actions and fails on anything but `pending` (vacuity-checked). The search row also stays put and flips to **Asked** — clearing the list looked like the connection had been made. 📜 **THE PRIVACY NOTICE HAD A PROMISE WITH NOTHING BEHIND IT:** *"a request nobody answers, and a connection that is declined, are both deleted after 30 days"* — **no `DELETE FROM person_connections` existed anywhere**. RA 10173 binds us to the period we DECLARE, so that sentence was the obligation. `expire_stale_connection_requests()` now deletes pending/draft/declined past the window, **never `confirmed`** (a relationship both agreed to has no expiry), hard-delete, cron-free via the traffic-driven runner — and `/privacy` RENDERS the constant the sweep deletes by so copy and code cannot drift. 🚨 **A GUARD OF MINE PROVED NOTHING AND THE MUTATION IS WHAT SAID SO:** the test asserting *"no browser role may run a retention sweep"* stayed **GREEN** when the `REVOKE … FROM authenticated` was deleted — the PGlite replay never granted EXECUTE, so revoking it changes nothing there, while **in production Supabase's default privileges grant EXECUTE to anon and authenticated on every new function in `public`** (the fact the `resolve_or_claim_person` lockdown was written to fix). The revoke is now asserted in the migration TEXT, where it can fail, and re-measuring turns it red. ⚖ **THE RULING, RECORDED ON THE OBJECT:** `kin_pilot_mutual_accounts` already refused a connection unless both endpoints are claimed accounts, but its own migration called itself a pilot and said *"for the full product it is probably not [the right trade]"*, inviting a future session to drop it. **Superseded — applied migrations are never edited, so the correction lives in the function's COMMENT: do NOT drop this trigger.** Somebody without an account is an **alaga** or is **invited to join**, never recorded as a connection. ⏭ That also **retires the mock's "Only you can see this" row by ruling** rather than leaving it pending. | `apps/web/lib/people-search.ts` · `apps/web/lib/people-search-query.ts` · `apps/web/lib/connection-request-expiry.ts` · `supabase/migrations/20271155742397_people_can_find_you_by_name.sql` · `supabase/migrations/20271155852254_requests_do_not_linger.sql` · `apps/web/tests/db/found-by-name.db.test.ts` · `apps/web/tests/db/requests-do-not-linger.db.test.ts` · [[project_setnayan_adding_people_reaches_them]] |
