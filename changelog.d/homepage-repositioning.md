### Homepage repositioning — memory-home lead (PR-H · Direction A)

Shifts the homepage hero from the planning-utility framing ("Plan your whole wedding in one place") to the memory-home pivot the owner chose (Direction A): lead with the lasting promise, keep the planning utility as the how.

- **Hero** (`app/_components/marketing/_sections.tsx`): "Goodbye, Viber chaos. / Hello, Set na 'yan." → **"Your wedding is one day. / Keep it forever."** The brand-origin signature is preserved (eyebrow `SET NA 'YAN`, and the cursive line now reads "'It's all set.' And it stays that way." — bridging the brand meaning to "forever"). Subhead leads with "Keep every photo, every video, every memory — in one place, for life." and keeps the existing planning-utility sentence (guest list · vendors · seating · schedule · free). **CTAs untouched** (button-preservation rule). Same responsive clamps (the "large texts ate the screen" guard).
- **Metadata** (`app/page.tsx`): title + description re-led with the keep-forever promise while retaining the SEO/GEO signals (Filipino wedding · free · verified vendors · 0% commission). JSON-LD feature graph left intact (facts).
- FeaturesNarrative left as-is — its "One home." opening already reinforces the new lead.

SPEC IMPACT: Public positioning shift (utility → memory home). Worth a DECISION_LOG row — the homepage is now the front of the broader retention strategy (memory hub + anniversary + guest→host loop shipped this session).
