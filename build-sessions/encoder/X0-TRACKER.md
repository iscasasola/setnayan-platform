# X0 — lead-time tracker

Owner ruling 2026-09-05: **no calendar waiting** — every item starts now. Update the Status column
in place; this file is the register (a GitHub issue could not be opened from the session that
wrote it — open one and link it here if you prefer).

| # | Item | Owner | Started | Needed by | Status |
|---|---|---|---|---|---|
| 1 | **Apple: accept the updated Program License Agreement** (developer.apple.com → Agreements). Membership is paid. Both `build-desktop` dispatches on 2026-09-05 (02:03Z run 33937991389, 02:52Z run 33940303449) failed ONLY at notarization: `HTTP status code: 403. A required agreement is missing or has expired.` Signing itself succeeded (cert "Indalecio Casasola" found). Windows leg passed both times. After accepting: re-dispatch `build-desktop` → the macOS leg should notarize. | owner | 2026-09-05 | before S11 (macOS half can be built now) | ✅ resolved 2026-09-06 — agreement accepted; re-dispatched run `34016548173` notarized clean (`Notarizing Finished with status Accepted`, Gatekeeper `accepted`, source=Notarized Developer ID) |
| 2 | **Apple identity**: is the Developer ID the PH organisation (needs D-U-N-S, ~15 business days + Apple review) or the individual the CI log shows? This is whose name appears on every couple's Gatekeeper dialog. | owner | 2026-09-05 | before S11 | ✅ resolved 2026-09-06 — owner ruling: **keep the individual identity (Indalecio Casasola)**. It already works (confirmed in run `34016548173`) at no extra cost and no D-U-N-S wait; the PH-org switch was considered and declined. Do not re-raise this unless the owner reopens it. |
| 3 | **Windows OV code-signing cert + cloud signing.** Azure Trusted/Artifact Signing excludes Philippine organisations (learn.microsoft.com, 2026-08-11). Keys must be hardware/cloud since 2023-06-01 → a file cert cannot sign from GitHub Actions. Default route: SSL.com OV (~$129/yr) + eSigner; alternative Certum. Charged at order; validation 3–4 weeks. **S11 shipped the CI gate**: `build-desktop.yml` + `src-tauri/scripts/windows-codesign.ps1` sign via SSL.com eSigner CodeSignTool the moment `SSL_COM_ESIGNER_USERNAME` / `_PASSWORD` / `_CREDENTIAL_ID` / `_TOTP_SECRET` exist as repo secrets — no further code change needed once the cert is issued. | owner | 2026-09-05 | before S11's Windows half | ⏳ open — order now (CI gate ready, waiting on the cert) |
| 4 | **Production env flags** (Vercel → Environment Variables): record the prod values of `NEXT_PUBLIC_LIVE_STUDIO_ROAM_ENABLED`, `NEXT_PUBLIC_PANOOD_STREAMING_ENABLED`, `NEXT_PUBLIC_LIVE_STUDIO_POOL_ONLY`. In neither the repo nor Vercel `get_project`. Without the first two the controller returns `notFound()`. | owner | 2026-09-05 | before S13 / M0 | ◐ **HALF CLOSED 2026-09-08 — MEASURED, NOT ASKED.** `NEXT_PUBLIC_LIVE_STUDIO_ROAM_ENABLED` is **ON in production**: the paid `LIVE_STUDIO` row renders on the anonymous `/pricing` page, and `lib/v2-catalog.ts` `.neq('service_code','LIVE_STUDIO')`s it out of the catalog read whenever `liveStudioRoamEnabled()` is false (mechanism read in code, not from a comment). `NEXT_PUBLIC_PANOOD_STREAMING_ENABLED` and `NEXT_PUBLIC_LIVE_STUDIO_POOL_ONLY` have **no anonymous surface** and remain open — someone must read Vercel's Environment Variables. |
| 5 | **§ 4c free-tier branding** — two owner locks disagree on what the free tier's canvas draws; `program-bridge.tsx` says the flag must not flip until settled. | owner | 2026-09-05 | before S2 | ⏳ open |
| 6 | **R2 GitHub Actions secrets** — `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_PUBLIC_URL` must exist as **GitHub Actions** secrets. ⚠ This is a SEPARATE store from Vercel's environment variables — the same names already set in Vercel do not reach CI. Until they exist, S10's `publish-latest` step cannot upload and `/download` degrades honestly to "not available now". Raised by S10 (PR #5209). 🔑 **AND IT IS FIVE VALUES, NOT FOUR — corrected 2026-09-08 by S14 (PR #5334):** `R2_PUBLIC_URL` must ALSO exist in **Vercel's** environment variables, where it is currently UNSET (`vercel env pull`). The Actions secrets get a build UPLOADED; Vercel's copy is what `lib/desktop-release-server.ts` reads at runtime to SERVE it, and without it `/download` keeps answering 503 over a perfectly good artefact. | owner | 2026-09-06 | **NOW BLOCKING S15** | ⏳ **still open — RE-VERIFIED 2026-09-08 by `gh secret list`: none of the four exists.** Consequence measured the same day: `https://www.setnayan.com/api/download/mac` and `/api/download/windows` both answer **503 "No … build is published right now"**, and the whole `desktop/` prefix **404s** on the live R2 host. **Nobody can install the desktop app today.** |

| 7 | **`media.setnayan.com` is ruled out, and the desktop app was pointed at it anyway.** Measured 2026-09-08: the host returns **NXDOMAIN** from `8.8.8.8`, `1.1.1.1` and `9.9.9.9`, against a working control. The owner **already ruled it is not being set up** (see `scripts/upload-decor-pilot-to-r2.ts`'s header and MB14b's note in `lib/reception-decor-layers-server.ts`) — **this is settled, do not re-ask it and do not propose a DNS migration**; `setnayan.com` is not a Cloudflare zone and an R2 custom domain requires one. But `tauri.conf.json`'s updater endpoint AND `updater.rs`'s `EXPECTED_ENDPOINT_HOST` guard both pin that dead host — so S12's updater cannot work no matter what secrets are set, and its own guard would **refuse** the host production actually uses (`pub-37d64fe618584c2981a88610a55dd439.r2.dev`). Both strings ship inside the signed binary. | engineering | 2026-09-08 | before S15 | ⏳ open — **S14 fixes it** |

| 8 | **`encoder_ipc.rs` names a file that does not exist.** Its docblock cites `apps/web/lib/encoder/go-live-guard.ts` as the caller of `encoder_probe` before `encoder_start`; `find . -name "go-live-guard*"` returns nothing, on any branch. `probeTransport` (`lib/encoder/ipc-envelope.ts`) is real and tested, but NOTHING calls it, so the transport is never probed before a broadcast begins. S18 wired the send path and deliberately did NOT invent this guard — the question of what a probe failure should DO (refuse go-live outright, or warn and continue) is a product decision, and `Envelope::is_zero_copy`'s docblock warns at length that refusing on the JSON envelope alone would refuse every macOS user. | owner + engineering | 2026-09-09 | before S13 | ✅ **resolved 2026-09-10 (S19)** — `go-live-guard.ts` now exists at the cited path and runs once before `encoder_start`. 🔑 **The refusal rule was NOT an open owner decision — S18 was wrong to file it as one.** It was already written in three places that agree (`ipc-envelope.ts`'s docblock, `live-studio-ingest-health.ts`, `S5.md` trap 3): refuse only when `usable` is false, never on the envelope. S5 also allowed a latency refusal from a justified threshold; S0's own data (§§ 3.1/3.4) shows the healthy and degraded latency distributions overlap — degraded mean 155 ms inside healthy p95 151 ms — so no threshold separates them, and slowness is reported, never enforced. |

**Windows artifact from the 02:52Z run** (run 33940303449): `setnayan-desktop-windows-latest`,
1,848,513 bytes, **unsigned** (workflow header: "Windows is still unsigned (Phase 2)"), expires
2026-09-19. SmartScreen will warn until item 3 lands; S10's rehearsal script teaches
"More info → Run anyway".

**Defaults if the owner says nothing** (Launch Plan § 6): Windows route = SSL.com OV + eSigner ·
OS floor stated on `/download` · browser key reveal hidden on desktop · 720p30 default ·
pool as-is with pool-only ON once the pool can broadcast · split-screen out of scope.
