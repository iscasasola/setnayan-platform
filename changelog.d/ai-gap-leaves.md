## 2026-07-20 · feat(taxonomy): 14 gap leaves for non-wedding event types

The 14 taxonomy gap leaves from the owner's "build the gaps" directive (2026-07-17): referee_official · event_medic · tour_activity · tour_guide · travel_insurance · av_production · speaker_talent · performers · kids_entertainer · choreographer · reveal_element · event_insurance · personal_accident_insurance · restaurant_reservation. Head of the taxonomy dependency chain — unblocks `ai-applicable-event-types`, `ai-travel-scheduling` and `ai-per-type-pricing`.

- **Migration `20270825054104`** — 14 new `public.vendor_category` enum values; 12 `canonical_service_schemas` stubs; the REGENERATED full taxonomy seed (5 new tier-1 families: Experience · Dining · Logistics & Safety · Insurance & Protection · Specialty; 12 new tier-2 tiles; 12 new canonical mappings); `applicable_event_types` seeded on the NEW leaves only (tile + canonical grain) exactly where the Part-A leaf-by-type matrix is explicit. Verified against the live prod schema in a rolled-back transaction.
- **Reconciled, not duplicated:** `performers` + `choreographer` already existed as PROGRAM tiles — they gain only couple-side `VendorCategory` values, and their tile scoping stays NULL (universal) pending the matrix-sign-off follow-up. `reveal_element` — previously a checklist-only key (gender_reveal tier2Core) — is now a real taxonomy tile under the exact same id.
- **Code maps extended so tsc catches every consumer:** `lib/taxonomy.ts` (WeddingFolder +5, WeddingTile +12, TAXONOMY_MAP +12), `lib/vendors.ts` (VendorCategory +14 + labels + service groups), `lib/vendor-category-taxonomy.ts` (14 tile anchors), `lib/vendor-service-tools.ts` (category icons), `lib/vendor-dayof-modules.ts` (folder families), explore folder-strip + admin taxonomy folder icons, `plan3d` booth templates (+12, existing chassis/props only).
- **Inert for today's couples:** /explore + shortlists filter tiles through `passesEventTypeFilter`, so scoped new tiles are invisible outside their matrix column; new folders surface only once they hold visible tiles with vendors. `restaurant_reservation` is scoped `{travel}` for now — its anchor type `dinner_date` is not in `event_type_vocab` yet (dinner-date-type is HOLD-OWNER).
- **Tests:** `lib/taxonomy-gap-leaves.test.ts` — 7 unit tests proving the admin-taxonomy data maps render all 14 leaves (tile order/parents/labels, canonical placement, vendor-category anchors, snapshot bucketing, checklist reconciliation).

SPEC IMPACT: Whats_Next_Suite_AI_Pricing §gap-leaves: 14 leaves landed; unblocks ai-applicable-event-types + ai-travel-scheduling + ai-per-type-pricing.
