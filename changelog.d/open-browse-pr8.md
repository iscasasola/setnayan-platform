# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-24 · feat(guest-site): open-browse PR8 — editorial-as-archive + empty/find-mode states

Open-browse PR8 of 11 (council verdict `Guest_Event_Website_Open_Browse_Council_Verdict_2026-07-22.md` §1.1/§1.3/§5.1/§5.2/§5.10). The guest-facing degraded-state layer on top of PR7's phases-as-emphasis engine. **All of it mounts ONLY on the open-browse path (`plan.openBrowse`), which is FALSE for every production event today (`website_open_browse DEFAULT false`), so the flag-off guest site is byte-identical** — the 15 `site-body-plan.test.ts` goldens are untouched and green.

- **Menu-below-editorial + evergreen-below-cover** (§5.1/§5.2 — the owner-flagged visible change): under open-browse the editorial phase keeps its body-replacing takeover, then the browsable site (`normalBody()`: Story / Details / Photos + the persistent menu + the guest's QR in Me) renders BELOW it behind a divider — "editorial leads an archive," not "editorial amputates the site." Precedent: GuestHubBar already persists below the guest editorial. Flag-off editorial is unchanged (the takeover alone). Applies to both identity trees (both call the one `phasedBody`).
- **`_components/empty-states.tsx`** (new): `SectionEmptyPlate` (details/story/photos, null-date-safe, `pastTense` post-event variant) so a menu tab under open-browse never points at a dead/empty section; `FindModeCard` — the anonymous "Me" tab's designed find-mode (§1.1): reason-aware ("that invite is for a different celebration" / "we couldn't find that invitation" / default), with a post-editorial phase ceiling ("Were you a guest? Claim your photos") so a stranger is never funnelled into a finished wedding; `PublicEventDetails` — the anonymous `event_details` public variant (§5.10): EVENT-level facts only (date + venue), since the live `event_details` widget renders per-guest role+side and stays firewalled from the anonymous tier.
- **Anonymous tree wiring:** under open-browse, Details always renders (public event facts + widgets + a teaser plate when empty), Story falls back to a teaser plate when there's no love story, and the Me anchor becomes the find-mode card; the menu's Details/Story tabs are marked present so they're never dead anchors. All guarded by `plan.openBrowse`.

Deferred to a PR8b follow-up (noted): the post-event archive-tense flourish (Countdown "Married [date]", `what_to_bring`/`photo_moments` past-tense copy) and the RSVP closed-card. PR7 already **excludes** those time-bound widgets post-event, so the archive is CORRECT today (never wrong-tense) — the flourish is polish, not correctness. Copy here is a first pass centralized in one file; the brand-voice pass is the owner sign-off item (§5.9). No migration.

SPEC IMPACT: None — implements the council build plan (row 8). Owner sign-off items §5.1/§5.9/§5.10 (menu-below-editorial visible change, empty-state copy voice, event_details field set) are surfaced here for review, not newly decided.
