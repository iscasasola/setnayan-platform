// Loading shell for the Studio Studio — instant animated skeleton while the
// active tab's surface (Website default / Hero video / Reveal Studio / Recaps)
// loads. Matches the FormPageSkeleton the legacy /admin/website loading used.
export { FormPageSkeleton as default } from '@/components/skeletons';
