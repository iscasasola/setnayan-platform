## 2026-08-03 · docs(claude): commit RULE 0 — it was owner-locked on 2026-07-27 and has been living as an uncommitted edit on one machine ever since

**RULE 0 — "FIND IT BEFORE YOU BUILD IT" — is not in git.** Not on `main`, not in any commit, not in any worktree. It exists only as a **40-line uncommitted modification to `CLAUDE.md` in the owner's home checkout.**

Found while bringing that checkout up to date (it was 2,036 commits behind). `git show origin/main:CLAUDE.md | grep -c "RULE 0"` returns **0**; so does the same query against the commit the checkout was sitting on.

**Why that matters more than a normal missing doc.** `CLAUDE.md` is auto-loaded into every Claude Code session as context. RULE 0 is the rule that exists *because the owner paid, more than once, to have a page rebuilt that already existed* — it instructs every session to grep for the shipped component, the existing design, and the settled decision **before writing anything**. A rule that only loads on one machine is a rule that silently does not apply:

- The ~100 stale worktrees pruned earlier the same day all carried a `CLAUDE.md` **without** it.
- Any fresh clone, any CI checkout, any other machine, and any session in a worktree got the pre-2026-07-27 instructions.
- The corpus `CLAUDE.md` explicitly warns that a session may be on a different machine or account with no memory files — this is exactly that failure, one layer down.

It survived today's 2,036-commit fast-forward only because it was checked for and stashed first. It would not have survived a `git checkout -- CLAUDE.md`.

**This commit changes nothing but the file's home.** The diff is **+40 / −0** against `origin/main` — byte-identical to what has been loading in the owner's sessions since 2026-07-27, verified by diffing the working copy against `origin/main:CLAUDE.md` before copying it across. The three upstream lines added to `CLAUDE.md` since that checkout went stale are preserved; zero lines from either side were lost.

Left deliberately uncommitted in the owner's checkout: `apps/mobile/android/app/capacitor.build.gradle` and `apps/mobile/android/capacitor.settings.gradle`, both regenerated by `cap sync` after the push-notifications plugin was added. Those are build output, not a decision, and belong with whoever lands that plugin.

SPEC IMPACT: None — no product decision changes. RULE 0 was already owner-locked on 2026-07-27; this puts the locked text where every session can actually read it.
