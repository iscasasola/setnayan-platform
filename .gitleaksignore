# gitleaks false-positive allowlist (fingerprints only — one per line).
#
# The generic-api-key rule flags the DB column `pakanta_song_r2_key` in the
# Pakanta admin delivery queue. It is an R2 object-key reference (a storage
# path for the delivered song MP3), NOT a credential — no secret is present.
c2d6f7e76835edd3d5f0c6b58b34034fe7f61418:apps/web/app/admin/pakanta/page.tsx:generic-api-key:149
#
# Same rule flags the Instagram media-sync column selection (the _key column
# identifiers are R2 object-key storage paths, not credentials), plus this
# allowlist file's own earlier explanatory text (a self-referential match).
# Both pinned by their introducing commit below — historical commits in the PR
# range are scanned by gitleaks-action, so an inline allow at HEAD is not enough.
3adb3d45550f227fbb1fb9969c1f09157f89ad1e:apps/web/app/vendor-dashboard/instagram-actions.ts:generic-api-key:171
b30742ba764b469dfe954b93fc6acedb3d5e4917:.gitleaksignore:generic-api-key:8
#
# Same rule flags `PAPIC_PASS_SERVICE_KEYS` in lib/papic-guest.ts — a frozen list
# of CATALOG SKU CODES (PAPIC_GUEST_6K / _10K / _TOPUP). They are public product
# identifiers that appear verbatim in platform_retail_catalog_v2 and on the
# pricing page; there is no credential here. The rule fires on the shape
# `..._KEYS = [...]` plus the underscored uppercase literals, not on any entropy.
# Renaming the const would NOT clear this: gitleaks-action scans every commit in
# the PR range, so the introducing commit below stays matchable regardless of HEAD.
3d81839f31fdde5e06efa35c5886dfe3b89061cc:apps/web/lib/papic-guest.ts:generic-api-key:97
#
# Same rule flags the R2 object-key COLUMN references in the Shared Pool
# Gallery reader (thumb_r2_key / display_r2_key / poster_r2_key — storage
# paths, not credentials). The introducing commit predates the inline
# `gitleaks:allow` added at c2f40393b, and gitleaks-action scans every commit
# in the PR range, so the historical fingerprint must be pinned here too.
b616f6804a6ebd405717ff676c0434a85223aa3d:apps/web/lib/papic-pool-gallery.ts:generic-api-key:76
#
# generic-api-key false-positives on the SKU code string 'PAPIC_GUEST_6K'
# (entropy 3.52) inside the two-type-model migration — a catalog service_code
# in an IN (...) list and a seed comment, not a credential. Pinned on the
# introducing commit because gitleaks-action scans every commit in the PR range.
f03e1fcdcfb264660d5c5c5626def658615191fb:supabase/migrations/20271019231590_papic_two_type_model_pool_topups_and_dedicated_one_cameras.sql:generic-api-key:127
f03e1fcdcfb264660d5c5c5626def658615191fb:supabase/migrations/20271019231590_papic_two_type_model_pool_topups_and_dedicated_one_cameras.sql:generic-api-key:842
#
# Same 'PAPIC_GUEST_6K' generic-api-key false positive (entropy 3.52) as the two
# entries above, this time in the generated-llms.txt renderer's REQUIRED_RETAIL
# list — catalog service_codes, not credentials. The inline `gitleaks:allow` was
# added at 4d5f7d890, but gitleaks-action scans every commit in the PR range, so
# the introducing commit's fingerprint must be pinned here too.
dd2a89c2b3f7d57407bb18cb570cc4faa49b63f2:apps/web/lib/llms-txt.ts:generic-api-key:97
#
# Same rule flags a hardcoded `claim_qr_token` fixture value ('legacy-fund-1')
# in the Papic rungs-are-fundable db test — the seat row a legacy multi-camera
# grant is replayed against. It is a literal in a PGlite in-memory database that
# exists for the length of one test process; no seat, event or credential in any
# real environment carries it. The rule fires on the COLUMN NAME (`..._token = '...'`),
# not on entropy, which is why the surrounding fixtures using the same column
# also match this shape. Pinned on the introducing commit because
# gitleaks-action scans every commit in the PR range, so renaming the literal at
# HEAD would not clear it.
ee7f86117c3154443592c975fa0cdc9e1feea3ac:apps/web/tests/db/papic-rungs-are-fundable.db.test.ts:generic-api-key:187
#
# The same `generic-api-key` rule fires on a COLUMN NAME again: the Decisions
# view's Files third copies the chat attachment's stored-asset column into the
# shared-files input, so the line is one property named `..._key` assigned from
# another property of the same name. There is NO literal on it at all — both
# sides are reads. That column holds an r2 stored-asset ref (never a
# credential; see migration 20271215223903), and the file is a 'use client'
# component, so anything secret in it would already be in the page source.
#
# An inline `gitleaks:allow` was added at HEAD, but gitleaks-action scans every
# commit in the PR range, so the introducing commit's fingerprint is pinned too.
8ea4dff28f612638232ae06790b85e43a8a506aa:apps/web/app/_components/chat-message-stream.tsx:generic-api-key:473
#
# ⚠ AND THE ENTRY ABOVE NEEDED A SECOND ONE, because the first version of this
# very comment QUOTED the offending line verbatim to explain it — so gitleaks
# matched the explanation and failed the scan on `.gitleaksignore` itself. The
# comment now describes the shape instead of reproducing it. If you are adding
# an entry here: say what the line is, do not paste it.
561e8614c97f2cbdbf90d03a258adbc4bd245b41:.gitleaksignore:generic-api-key:58

