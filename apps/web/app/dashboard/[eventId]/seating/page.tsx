import { IterationPlaceholder } from '../_components/placeholder';

export const metadata = { title: 'Seating chart' };

export default function SeatingPage() {
  return (
    <IterationPlaceholder
      iteration="Iteration 0008"
      title="Seating chart editor"
      blurb="Free-placed stage · 13-entry table catalog (round / long / family-head / sweetheart / serpentine) · role-tier auto-fill rings · QR-on-publish print pack."
    />
  );
}
