/**
 * apps/web/app/[slug]/_lib/top-corner.ts
 *
 * The id of the guest page's top-right cluster (GuestHubBar's Account
 * control). Controls that belong in that corner — the music button — portal
 * into it instead of fixing a second element on top of it. One string, so the
 * cluster and its guests cannot drift apart.
 */
export const TOP_CORNER_SLOT_ID = 'site-top-corner';
