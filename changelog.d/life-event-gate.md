## 2026-07-17 · feat(events): life-event creation gate — cardinality + soft horizon + measured-type visibility

Council verdict `Event_Creation_Limits_Council_Verdict_2026-07-17.md`, owner "build it now" (2026-07-17). Life events are limited; lifestyle events stay unlimited (owner-verbatim "no rules").

- **Cardinality (the wedding guard generalized):** ONE life event IN PLANNING per (creator account × event type × honoree) for debut · christening · birthday · graduation · gender_reveal. Honoree key = `honoree_dependent_id` → normalized `honoree_label` ("Para kanino?", optional first name) → per-type singleton slot. Slot frees on archive or the event date passing (wedding-guard shape). Grandfather rule: pre-epoch unlabeled rows never block. Wedding keeps `wedding-guard.ts` byte-identical — but the guard is now ALSO wired into the wedding onboarding commit (`app/onboarding/wedding/actions.ts`), closing a recon-confirmed bypass; all four `events`-insert paths now guarded, with a source-scan CI test (`lib/life-event-gate.test.ts`) as the backstop against future unguarded paths.
- **Eligibility = soft horizon advisory, never a wall:** per-type planning windows from the owner-locked prep-months table (debut 18 mo · birthday 9 mo · christening 6 mo · gender reveal 5 mo · graduation 4 mo) — a "Malayo pa 'yan" note on far dates; the form always submits. No birthdate is ever asked.
- **Measured-type visibility (owner 2026-07-17):** with the People layer on, the create grid hides debut/christening unless someone in the account's dependents approaches that moment (18F/21M within horizon; under-8 child); wedding + unmeasured types always visible. A "show all event types" expander is the permanent doorway (wayfinding lock). Flag off → nothing hides.
- **Blocked-state UX:** live-duplicate card with three live doors — open the existing event · different celebrant via "Para kanino?" · archive hint (`?error=life_event_exists&existing=…`, mirroring `wedding_exists`).
- **Schema:** migration `20270820100000` adds `events.honoree_label` (ordinary PI, never on public/vendor/guest reads — public selects are column-scoped) + `events.honoree_dependent_id` (FK dependents, NULL until the person-picker PR). `/privacy` "Event honoree details" clause extended in the same PR.

SPEC IMPACT: `~/Documents/Claude/Projects/Setnayan/Event_Creation_Limits_Council_Verdict_2026-07-17.md` (built; owner resolved sign-offs by "build it now" + hide directive), DECISION_LOG.md row.
