## 2026-07-22 · feat(vendor-billing): Vendor AI ("the AI Chatbot") — a sellable, per-vendor-entitled ₱1,500/28d add-on (free first cycle) that gates the existing auto-reply assistant

Owner-locked 2026-07-22: the vendor Auto-Reply Assistant (`apps/web/lib/vendor-autoreply/*`, previously flag-dark with NO price or entitlement) becomes **Vendor AI** — a **flat ₱1,500 / 28-day add-on** on the PAID tiers (Solo/Pro/Enterprise, verified only), **FREE for the vendor's FIRST 28-day cycle** (one-time per account). The **inbox stays free** — a vendor without the add-on still reads + replies by hand; the ₱1,500 only buys the AI auto-answer. This makes the assistant sellable + per-vendor-entitled WITHOUT rebuilding it.

**Migration** `supabase/migrations/20270905761946_vendor_ai_addon_sku_and_trial.sql` — extends the `vendor_billing_catalog` `offering_type` + `vendor_billing_shape` CHECKs with a new `vendor_addon_recurring` value (same drop/recreate pattern as `20270511762904` / `20270512705572`); INSERTs the admin-managed `vendor_ai_addon` SKU (₱1,500, display_order 82, `ON CONFLICT DO UPDATE` that never stomps an admin price edit); ALTERs `vendor_profiles` ADD `ai_addon_trial_used_at` + `ai_addon_expires_at` (both nullable TIMESTAMPTZ). Idempotent; no new table → no new RLS. The ₱0 free cycle is a price RESOLVER (not a ₱0 catalog row — the `price_php > 0` CHECK forbids it).

**Resolver** `apps/web/lib/vendor-addon-pricing.ts` — pure `resolveVendorAiAddonPricePhp({ trialUsed, cyclePricePhp })` (₱0 first cycle → ₱1,500 catalog fallback after), plus pure `isVendorAiAddonActive` / `nextVendorAiAddonExpiry` (window stacks from the later of now / current expiry) and a `fetchVendorAiAddonPricePhp` / `fetchVendorAiAddonState` DB reader (client passed in, import-type only, so the module stays unit-testable). Tests in `vendor-addon-pricing.test.ts` (10 cases: intro ₱0, ₱1,500 fallback/reprice/invalid, active-window, stacking).

**Purchase action** `apps/web/app/vendor-dashboard/subscription/ai-addon-actions.ts` — `activateVendorAiAddon` rejects free/verified tiers + unverified vendors **before pricing** (mirrors the token-RPC `is_active`/verified guard — `resolveServiceSellability` never checks `vendor_billing_catalog`, so this action is the only gate), re-reads the authoritative ₱1,500 + `is_active` server-side. FREE first cycle → **atomic** trial claim (`UPDATE … WHERE ai_addon_trial_used_at IS NULL`) + a fresh 28-day window + a ₱0 audit order (no payment row). PAID cycle → apply-then-pay `orders`+`payments` (lands in `/admin/payments`).

**Entitlement gate (the not-a-fake-door part)** — `activateVendorAiAddonOrder` hook in `lib/sku-activation.ts` stamps the window on paid-order approval (idempotent via ledger). The inbox-hook enablement boundary (`lib/vendor-autoreply/inbox-hook.ts`) now reads `ai_addon_expires_at` and passes `addonActive` into the tested gate (`inbox-decision.ts` — new `no_addon` skip reason). `NEXT_PUBLIC_VENDOR_AUTOREPLY_V1` stays the GLOBAL master switch (default OFF, NOT flipped); the per-vendor entitlement is the new gate. Gate + orchestrator tests updated (`inbox-decision.test.ts`, `inbox-hook.test.ts`).

**UI** — `subscription/_components/ai-addon-card.tsx` on the "Plan & tokens" hub: honest states (not-eligible upsell · free-first-cycle turn-on · active + "active through" + renew · reactivate), free first cycle shown as "Free first cycle, then ₱1,500 / 28 days".

Verify: `npx tsx --test "lib/**/*.test.ts"` green (incl. new resolver + gate cases); `npx tsc --noEmit` clean.

SPEC IMPACT: New owner-locked vendor SKU + pricing rule (Vendor AI ₱1,500/28d, free first cycle, paid+verified). Not yet reflected in the corpus vendor-billing tables (`Pricing.md` § 0.C / `VENDOR_TIERS_AND_BENEFITS.md`). Code is canonical per the 2026-06-07 ground-truth flip; flagged here for the owner to fold into the pricing corpus + `DECISION_LOG.md` on sign-off.
