# MB10 — The supplier gallery chain + the picker

**Goal:** a gallery photo carries its supplier's credit all the way from library → board → vendor
list.

**Model:** Opus · high effort for the migration and the map.
**Size:** 1 day. **Depends on:** MB0, and MB3 for the picker's mount point.

🔀 **Runs in PARALLEL with the money arc** — a second agent can take MB10 → MB11 while MB5–MB8
proceed. This is where the ~4 days of wall-clock saving comes from.

## Delivers

**Migration first:**
- `event_inspiration_assets.library_asset_id` + a `gallery_pick` source mode
- `moodboard_library_assets.vendor_profile_id`
- the rights-warranty column MB11 will need — add it here so MB11 is not a second migration

**Then the feature:**
- The couple-facing picker — copy `template-gallery.tsx`'s paged browse-and-apply shape.
  **Capped and paged**, never the unbounded query #5113 just killed.
- Supplier credit on every credited photo: *"Bloom & Vine · florist"*
- The **"you saved 2 of their photos"** marker in the vendor list — this is what turns inspiration
  into discovery, and it is the reason suppliers will upload at all
- The slot → vendor-category map as a lookup over `lib/taxonomy.ts`. The taxonomy **already
  carries every category** (`cake_maker`, `wedding_cake`, `florist`, …) — nothing to invent.

## Verify

- full db replay + **both Ugat tests**
- `node apps/web/scripts/lint-events-column-grants.mjs`
- `pnpm exec tsc --noEmit` from `apps/web`
- picker guard: paged, capped, applies via `gallery_pick` — sabotage: fetch-all, confirm red
- marker guard: the marker reaches the **vendor-list render**

## Owner decides first

Nothing.
