# Launch-readiness audit — measured against `origin/main` + prod, 2026-09-21

Read-only session. No app code, PR, or setting was changed. `origin/main` was read directly for
code (this worktree's checked-out branch is 829 commits behind `origin/main` — `git log --oneline
origin/main..main` = 0, `main..origin/main` = 829 — so every code claim below cites `origin/main`,
not this checkout). Prod DB = Supabase project `njrupjnvkjkitfctetvi` (`setnayan-prod`). Prod app =
Vercel project `prj_7VTNk7sjPejgXNsSkZsyiPQRLnwA`. Re-measure everything below before acting on it —
see `a-registers-status-rots-in-both-directions` in MEMORY.md.

Legend: **S** = owner clicks/pastes a setting, hours. **M** = a focused PR, ~1 day. **L** = multi-day
/ needs counsel or a platform migration.

## BLOCKS LAUNCH

| # | Area | Measured today | Missing | Risk | Effort |
|---|------|-----------------|---------|------|--------|
| 1 | Legal — false public claim | `/signup` footer (origin/main `app/signup/page.tsx`) states verbatim **"We never sell your data — RA 10173 compliant."** on a live, real-traffic page. | The repo's own compliance catalog (`apps/web/lib/npc-filing-tasks.ts`, 15 tasks) shows the claim is premature: `t0-1` (route packet to counsel) unresolved, `t3-12` (sign the DPO designation + Privacy Manual + Breach Policy) unresolved, `t3-13` (file the DPS with NPC) unresolved, `t1-5`/`t1-6` (publish faith/minors/biometric disclosures, gate SPI honoree fields) unresolved, `t2-8` (execute DPAs for Supabase/Vercel/Cloudflare/Resend/Sentry/PostHog/Anthropic/Persona/Google/TikTok/Suno) unresolved. | A public, unqualified "RA 10173 compliant" claim next to an unfiled DPS and unsigned DPO designation is the single highest-exposure line in the product — it is evidence against the company if a data-subject complaint reaches the NPC. | S to soften/remove the line; L for the underlying filing (needs counsel — `t0-1` gates everything else). |
| 2 | Legal — clickwrap vs browsewrap | Measured `origin/main app/signup/page.tsx`: the only checkboxes on the form are "Include my wedding in Stories" (opt-in) and "Stay signed in" (defaulted). Terms/Privacy is a footnote below the submit button — no checkbox, no `required` attribute tied to it. | A required, logged "I agree to the Terms and Privacy Policy" checkbox. | Browsewrap ("by using the site you agree") is materially weaker than clickwrap in PH courts and under the NPC's consent standard — an owner betting the ToS is enforceable is betting on the weaker form. | S — one checkbox + one `required`. |
| 3 | Data safety — Supabase plan | `mcp:get_organization` → `{"plan":"free"}` for org `henfjjgsilacblzsmrqu` (measured live, not from a doc). | Confirmation of backup/PITR coverage — no backups-listing tool was available to this session to read retention directly; Supabase's public plan comparison (not verified live this session) says Free tier ships without the paid daily-backup/PITR add-ons Pro includes, and free projects can be **paused for inactivity after 7 days** (this exact sentence is already quoted from Supabase's own docs in `OWNER_ACTIONS.md` line ~1458). | Real couples' PII, 6 real paid orders (₱2,499+₱2,899+₱147+₱49 GCash/BDO, per MEMORY.md) and vendor financial data sit on a database with no independently-verified backup SLA. A pause-for-inactivity is also a real availability risk during a slow week pre-launch. | S to check the Backups tab in the Supabase dashboard and confirm retention in writing; M/L to upgrade to Pro if it's what it looks like. |
| 4 | Accounts — email never verified | Measured `origin/main app/signup/actions.ts` lines ~245-260: every signup is **auto-confirmed via the admin API** immediately after `auth.signUp`, with the code's own comment: *"V1 work-around: Supabase's built-in email sender drops into spam folders and free-tier rate limits are tight... Real email-based verification returns when Supabase's SMTP is pointed at Resend."* | Nothing verifies the signer-upper controls the email address they typed. `OWNER_ACTIONS.md` already has the fix scoped ("Point Supabase Auth at Resend — DO THIS BEFORE THE CAPTCHA SECTION"). | Anyone can create a live, working account against an email address they don't own (typo-squat, impersonation, spam signups) with zero verification loop. Combined with #1's compliance claim, this is a real account-integrity gap for a platform handling PII and money. | M — the fix is already written down as an owner runbook step; needs the Resend-SMTP switch + a real test pass. |

## SHOULD FIX FIRST WEEK

| # | Area | Measured today | Missing | Risk | Effort |
|---|------|-----------------|---------|------|--------|
| 5 | Email — delivery status is genuinely unreadable | Live query, `email_deliveries` table (origin/main, shipped, not a stub): **20/20 rows** have `last_event = NULL`; every row's `error` column reads `"status check: This API key is restricted to only send emails"`. The checker job (`email-delivery-check`, `lib/email-delivery.server.ts`) IS running — `cron_job_runs` shows it claiming a fresh window every ~10 min — it just gets refused by Resend every time. | A Resend API key with **read** scope (today's key is send-only). One key swap in Vercel Production. | The task's premise ("send-only key") is TRUE, but not because nobody built read-back — the read-back mechanism, verdict logic, and admin/Notifications UI are all shipped and tested (`email-delivery-log.test.ts`). It's inert for one permission-scope reason. | S — regenerate the Resend key with read access, paste into Vercel. |
| 6 | Email — allowlist is real and correct | Read `apps/web/lib/notification-emit.ts` in full: `EMAIL_ENABLED_TYPES` (43 types) is transactional-only, `MARKETING_GATED_EMAIL_TYPES` correctly excludes all transactional types after a documented 2026-06 incident where it didn't. | — | None found; this is a pass. | — |
| 7 | Email — key-unset behavior | `sendEmail()` (`lib/email.ts`) returns `{ok:false, reason:'not_configured'}` and no-ops when `RESEND_API_KEY`/DB config absent; `emitNotification` still writes the in-app notification row regardless. | — | If the key were ever unset, users would silently stop getting email but keep getting in-app notifications — no hard failure, no data loss, degrades gracefully. Confirmed this IS the current fail-soft design, not a gap. | — |
| 8 | Monitoring — no external uptime check found | `/api/v1/health` exists (`app/api/v1/health/route.ts`, "useful for uptime monitors"docblock) but no UptimeRobot/BetterUptime/Checkly/Pingdom/healthchecks.io reference anywhere in the repo (`grep -rli` across `*.ts`/`*.tsx`/`*.yml`/`*.md`, zero hits). | An external service polling `/api/v1/health` and paging the owner on failure. | If Vercel or Supabase goes down, nobody is notified except by a customer complaint — the only two automated watchers found (`deploy-drift-monitor.yml`, `migration-drift-monitor.yml`) both verified green and hourly-scheduled today, but they watch deploy/schema drift, not live uptime. | S — wire any free uptime service to the existing endpoint. |
| 9 | Data safety — deletion handshake protects paid suppliers | Read `app/admin/event-deletions/actions.ts` (origin/main): a couple **cannot** self-delete an event once "a bill we confirmed, an official receipt, or a payment nobody has checked yet" exists — it routes to a human-reviewed admin queue instead of a one-click delete. | — | This directly answers "does a supplier lose records when a couple deletes an event" — for any event with money moved, no. Pass. | — |
| 10 | Accounts — captcha is configured, mobile bug is fixed and tested | `curl https://www.setnayan.com/signup` (mobile UA) today shows a real `<div data-turnstile="true" class="cf-turnstile">` + populated `captcha_token` hidden field — site key IS set in Production (contradicts a 2026-09-16 note in `OWNER_ACTIONS.md` saying it wasn't; re-measured, not trusted). The 375px/293×0 mobile zero-height bug (`a-measurement-that-does-not-change-the-advice-was-wasted` memory) was fixed and covered by `lib/turnstile-holder-fits.test.ts` on 2026-09-18 (commit `c5b90a5ee`). | Whether Supabase's server-side CAPTCHA **enforcement** toggle is currently ON cannot be checked from any tool available this session — Supabase's MCP exposes no Auth-config read, confirmed by `OWNER_ACTIONS.md`'s own note ("no path"). `OWNER_ACTIONS.md` step 3a also flags an **unresolved** question: whether Supabase's captcha gate blocks the password-reset `verify` endpoint once enabled — marked "nobody has established" as of the 2026-09-18 fix commit. | If captcha enforcement is ON and step 3a's question was never answered, password resets could be silently dying at the last step. This needs one live test (open a real reset link on a phone), not a code change. | S — owner runs the one manual test named in `OWNER_ACTIONS.md`. |
| 11 | Security — Postgres advisories | `get_advisors(type=security)`: 1 ERROR (`security_definer_view` ×3: `vendor_profiles_self` + 2 others), 42 tables with RLS enabled but no policy (`rls_enabled_no_policy`, INFO), 80 functions with mutable `search_path`, 1 leaked-password-protection disabled (`auth_leaked_password_protection`). | Review of the 3 SECURITY DEFINER views and the leaked-password-protection toggle specifically — both are cheap, high-value fixes; the 42 no-policy tables need per-table judgment (RLS-enabled-no-policy defaults to deny-all, so many are likely intentionally admin-only). | Leaked-password-protection off means Supabase Auth will happily accept a password that's in a known-breach list. | S for leaked-password toggle + reviewing the 3 views; M for auditing the 42. |
| 12 | Dependencies | `gh api .../dependabot/alerts`: **2 open alerts**, both `medium` — `glib` (transitive, likely desktop/Tauri build toolchain) and `@opentelemetry/core`. Zero `high`/`critical` open. | — | Low; medium-severity, no exploit path evident from the alert list alone. | S to review, likely a `pnpm update`. |

## CAN WAIT

| # | Area | Measured today | Note |
|---|------|-----------------|------|
| 13 | Demo/fixture data in prod | `select count(*) from vendor_profiles where is_demo=true` → **0**. `demo-mode.ts` gates all demo content behind admin-only session cookie, non-admins silently see nothing regardless of query params. `select count(*) from users where is_internal` → **1** of 14 total users. | Clean. Code's own docblock sets a "Dec 1, 2026" cutover deadline for demo data — informational, not a current defect. |
| 14 | Cookie consent | `cookie-consent-banner.tsx`: gates PostHog init on consent (`analyticsAllowed()` false until decided), essential cookies unaffected, explicitly suppressed inside the iOS Capacitor shell per a 2026-06-30 App Review note. | Real gate, not cosmetic. Pass. |
| 15 | Public claims — `/features` | Already fully audited 2026-09-06: 7 false claims found across 5/12 sections, all fixed across 3 PRs, permanently fenced by `features-page-says-what-ships.test.ts` (bans each false phrase with the shipped fact attached). Not re-audited line-by-line this session — the guard test's existence was confirmed, not re-run. | Don't re-do this sweep; the mechanism that would catch regressions already exists. |
| 16 | Deploy-drift / migration-drift monitors | `gh run list --workflow=deploy-drift-monitor.yml` and `migration-drift-monitor.yml`: last 5 runs each `completed success`, hourly cadence (`cron: 17 * * * *`) plus `workflow_run` triggers off `deploy-prod`. | Both actually running, both green, today. Pass. |
| 17 | Periodic/cron-free jobs | `select job_key, last_run_at from cron_job_runs`: all 25 registered jobs claimed within the last 4 days; the three retention jobs (`retention-sweep`, `vendor-dossier-retention`, `papic-fullres-drop`) are weekly-gapped and last claimed 4 days ago — inside their window, not stale. | Design is deliberately cron-free (rides admin-page `after()` traffic + a DB compare-and-swap) — "not on a schedule" is not itself a finding here. |
| 18 | Postgres performance advisories | `get_advisors(type=performance)`: 6 findings (`unindexed_foreign_keys`, `auth_rls_initplan`, `no_primary_key`, `unused_index`, `multiple_permissive_policies`, `duplicate_index`) — 1 each. | Not launch-relevant at current traffic (14 users, 6 orders). Revisit at scale. |

## Could not verify (no tool access this session — say so, don't guess)

- **R2 bucket-level versioning/lifecycle for 4 of 5 buckets.** Code confirms a lifecycle rule was scripted for the Papic sampler prefix (`ops/papic-r2-lifecycle`, PR #2187) but there is no Cloudflare API/MCP available this session to read live bucket config for `media`, `thread-files`, `contracts`, `verification`. Owner must check the Cloudflare R2 dashboard directly.
- **Vercel Production environment variables.** `filter_project_envs` returned `403 Forbidden` for this session's Vercel credentials — could not confirm live presence/target of `RESEND_API_KEY`, `SENTRY_DSN`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY` from the API. (The Turnstile site key's presence WAS inferred behaviorally via the rendered `/signup` HTML — see row 10.)
- **Supabase Auth captcha enforcement toggle (on/off) and leaked-password-protection toggle, live state.** No MCP tool exposes Supabase Auth configuration reads; `OWNER_ACTIONS.md` independently confirms no such path exists. Owner must check Supabase Dashboard → Authentication → Attack Protection directly.
- **Whether the 11 named sub-processor DPAs (Supabase, Vercel, Cloudflare, Resend, Sentry, PostHog, Anthropic, Persona, Google, TikTok, Suno) are actually executed.** `npc-filing-tasks.ts` itself marks this `[confirm]` — it is a document-existence question, not something greppable from code or the DB.
- **Sentry: is it actually receiving events in prod right now.** `sentry.server.config.ts` confirms DSN-gated init exists in code and an admin-only "Sentry smoke test button" exists (`admin/settings/_components/sentry-smoke-test-button.tsx`), but firing a real test event requires an authenticated admin session this read-only audit did not use.
