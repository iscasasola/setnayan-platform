import { IterationPlaceholder } from '../_components/placeholder';

export const metadata = { title: 'Invitation site' };

export default function InvitationPage() {
  return (
    <IterationPlaceholder
      iteration="Iteration 0002 — coming after 0001"
      title="Invitation site"
      blurb="Personal guest-facing invitation page with branded QR. Customization via 0004 widgets (hero / our-story / schedule)."
    />
  );
}
