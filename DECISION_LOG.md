| 2026-07-21 | **Live Studio console is scroll-free on both layouts (PR #3442) — root cause was `aspect-video`.** Owner (with screenshot): *"obs should be scroll free. for both desktop and mobile."* **Cause:** the PROGRAM monitor used `aspect-video`, which derives height from **WIDTH** — on a wide desktop column that is a ~1000px monitor before the sources rail is reached, so the page scrolled and the SETNAYAN overlay's own bottom band fell off screen. Aspect-ratio boxes suit a card in a feed, not a viewport-filling console. **Principle:** an operator surface used during an unrepeatable ceremony must fit one screen — a control below the fold is a control you cannot reach, and whatever you scroll past is a camera you can no longer see. **Fix:** new `lib/panood-console-fit.ts` — available height is **MEASURED** from the console's own `getBoundingClientRect().top`, never summed from the shell's chrome (sticky bar + `py-6` + `pb-20` mobile nav), because any hardcoded sum rots the first time one of those changes. Mobile-nav clearance follows **VIEWPORT** width (the nav is `lg:hidden`), deliberately NOT the board/compact layout mode — layout is device-driven and the two are allowed to disagree (a laptop in Compact has no bottom nav). `env(safe-area-inset-bottom)` subtracted; negative values cannot add height. **Floors at 420px rather than crushing** — on a short window with OBS docked, scrolling a little is the honest outcome. Returns **null** on an unusable measurement (SSR / zero viewport / mid-transition) so the console falls back to natural flow instead of collapsing. **Layout consequences:** console root = fixed-height flex column with `min-h-0` throughout (without it flex children refuse to shrink below content size and the page scrolls anyway); program `<video>` switches **`object-cover` → `object-contain`** since the box is no longer 16:9 and cropping would make an operator misjudge their own shot; **compact** = the tab body is the ONE scroll region with PROGRAM + tab bar pinned; **board** = columns scroll internally with the rails capped at half the column so a long moment list can never squeeze out the monitor. 9 new tests (124 total). ⚠️ Live on merge. | repo PR #3442 · `apps/web/lib/panood-console-fit.ts` |
| 2026-08-20 | 🔒 **OWNER RULING — NO VAT IS SHOWN OR ADDED ANYWHERE. ₱499 IS ₱499.** Owner, verbatim: *"just stay with 499. remove the 12% let's keep it simple and effective for everybody."* Setnayan is **not VAT-registered** (sole prop under ICASA ENTERPRISE, 8% flat; VAT only at the ₱3M tripwire) and `platform_settings.default_vat_rate_pct` is **0**. PR [#4621](https://github.com/iscasasola/setnayan-platform/pull/4621) removes the last four surfaces that said otherwise: the **admin quote screen** (printed `requested_total_php * 1.12` as *"₱499 · buyer pays ₱559 incl. 12% VAT"* and labelled its input *"Buyer pays base × 1.12 incl. VAT"*) · the **customer checkout drawer** (a FIXED *"incl. 12% VAT"* under an already-correct price) · the **supplies cart** (*"Final price + 12% VAT are confirmed…"*) · and the **shortfall notice**, which described a 12% VAT as being inside the figure it quoted. 🔑 **THE PATTERN IS THE FINDING AND IT REPEATED FOUR TIMES: THE ARITHMETIC WAS ALREADY RIGHT AND THE WORDS WERE WRONG.** The rate had been moved into settings long ago; what was left behind each time was a **hardcoded sentence beside a correct number**. **Fixing the calculator does not fix the label** — and every one of these was found only because a person opened that particular screen, which is why the guard SWEEPS every money surface instead of asserting on the four known files. ⚖ **The admin screen never overcharged anyone** — the money path already read the configured rate — but the operator writes the *"Note to couple"* from that card, so a figure ₱60 too high was one copy-paste from being quoted and chased. ⚖ **DERIVED, NOT DELETED:** every surface reads the rate from settings and says nothing while it is 0, so crossing ₱3M is **one number** and every line returns by itself; **nothing hardcodes a zero.** ⚠ The guard bans the LITERAL (`12% VAT`, `× 1.12`), never the concept, and deliberately does NOT ban bare `1.12` — it is a CSS line-height and a 3D coordinate in this repo, and a guard that cries wolf goes unread. | `apps/web/app/admin/payments/page.tsx` · `apps/web/app/admin/payments/actions.ts` · `apps/web/app/dashboard/[eventId]/_components/inline-checkout-drawer.tsx` · `apps/web/app/dashboard/[eventId]/studio/supplies-marketplace/_components/cart-drawer.tsx` · `apps/web/lib/no-invented-tax.test.ts` |
| 2026-08-20 | 🚨 **AN UNPAID ORDER OUTLIVES THE EVENT IT WAS PLACED FOR, AND THE BUYER CAN NEVER REACH IT AGAIN — FOUND BY SIGNING IN.** Measured in production: the owner's ₱499 Setnayan AI order (`S89O-GCR6BDC4Z6` · ref `SNEYGV00WY` · status `submitted`) is **alive with `event_id = NULL`** because the birthday event it belonged to was deleted during the session. `orders_event_id_fkey` is **ON DELETE SET NULL**, the buyer's only route to an order is `/dashboard/<eventId>/orders/<orderId>`, and **`/dashboard/orders` is Page not found when signed in** — there is no account-level orders list. ⇒ The customer keeps the debt and loses the screen naming the amount, the reference and where to send the money; the order sits in the admin quote queue attached to nothing. 🔑 **THE MONEY GATE ON EVENT DELETE ONLY COUNTS `paid`/`fulfilled`/`refunded`** (`SETTLED_ORDER_STATUSES`) — an order that has been **quoted and is awaiting payment** does not block deletion, so the couple-facing delete removes it cleanly. Its own comment justifies the gate as protecting *"a receipt, a BIR record and a support case"*; **a live unpaid commitment is all three in waiting.** ⏭ **OWNER DECISION, NOT TAKEN UNILATERALLY because it is money semantics:** either REFUSE deletion while an order is outstanding ("settle or cancel this first"), or CANCEL the outstanding order as part of the delete. Recommendation given: cancel, because blocking traps somebody who just wants a test event gone. ⚠ **This class is invisible from outside the login** — it was found only after the owner placed a signed-in account in the browser, which is exactly the gap every prior readiness pass had declared unmeasured. | `apps/web/lib/event-deletion-gate.ts` · `apps/web/app/dashboard/[eventId]/delete-actions.ts` · `orders_event_id_fkey` |
| 2026-08-20 | 🔒 **OWNER RULING — AN UNPAID ORDER EXPIRES IN 15 DAYS, WARNED FIRST, CANCELLED NOT DELETED.** Owner, asked what happens when a payment never reflects and given a recommendation of 14: *"okay 15 days."* plus **ok** to warning before expiry and **ok** to cancel-not-delete. PR [#4630](https://github.com/iscasasola/setnayan-platform/pull/4630). 🔑 **15 IS BETTER THAN THE 14 I PROPOSED AND THE 7 THE OLD SPEC ASSUMED: PH payroll lands on the 15th and the 30th, so a fifteen-day window always contains EXACTLY ONE PAYDAY whichever day the order is placed** — seven can miss both. ⚖ **And the expensive failure is NOT an order lingering** — an unpaid order unlocks nothing (`ACTIVE_STATUSES` admits only paid/fulfilled, owner 2026-06-18) and merely sits — **it is MONEY ARRIVING AGAINST AN ORDER THAT HAS ALREADY CANCELLED ITSELF**, which a shorter window makes likelier; the window is generous on purpose. **Built:** the deadline is stamped by a **column DEFAULT** rather than app code, because orders are minted from several paths and the one somebody forgets is the one that never expires — the "gate with no handle" shape; a sweep that nudges at the halfway point and cancels at the deadline, mirroring the cron-free `sweepLapsedSubscriptions` precedent (**it can only ever run LATE, never early — the forgiving direction**); both notification types on the **email allowlist**, since a deadline learned only inside the app is the tray-badge half-mechanism #4595 fixed; and the deadline printed at checkout, because **we promised no deadline anywhere before this** and a rule the customer was never told is a surprise, not a rule. ⏭ **Same PR closes the stranded ₱499:** deleting an event now CANCELS its unpaid orders instead of orphaning them (`orders.event_id` is ON DELETE SET NULL, and there is no account-level orders page, so the buyer kept the debt and lost the only screen naming it). ⚖ **VENDOR ORDERS DELIBERATELY EXCLUDED** — the ruling was about a customer; cancelling a supplier's unpaid booking fee may unpick a booking a couple relies on. Widening later deletes a predicate; narrowing after it has cancelled somebody's booking is not possible. | `supabase/migrations/20271150847627_an_unpaid_order_expires_in_15_days.sql` · `apps/web/lib/order-payment-window.ts` + `.server.ts` · `apps/web/app/dashboard/[eventId]/delete-actions.ts` · `apps/web/tests/db/unpaid-order-window.db.test.ts` |
| 2026-08-20 | 🪤 **THREE OF MY OWN MISTAKES ON THAT BUILD, ALL CAUGHT BY TESTS, ALL REUSABLE.** **(1) THE FIELD GUARD COULD NEVER FIRE.** It used this repo's usual `current_user NOT IN ('authenticated','anon')` idiom **inside a `SECURITY DEFINER` function**, where `current_user` is the FUNCTION OWNER — so the condition is ALWAYS TRUE and the gate admits everybody. **Migration `20271141980127` had already written the exact table down** after being bitten by it (`SET LOCAL ROLE authenticated` ⇒ `current_user`=postgres · `current_setting('role')`=authenticated). **The idiom is only safe in a SECURITY INVOKER context; in a DEFINER one it inverts.** **(2) TWO db TESTS PASSED VACUOUSLY FIRST.** The buyer fixture had no identity, so **RLS refused the UPDATE outright** and *"a buyer cannot move their own deadline"* was green **for the wrong reason**; giving the fixture a real `auth.uid()` turned both RED and is what exposed (1). 🔑 **GIVE A FIXTURE A REAL IDENTITY BEFORE TRUSTING A DENIAL** — and **`SET LOCAL ROLE` outside a transaction is a NO-OP**, so the role switch is now asserted (`current_user` read back) exactly as the anon-RPC suite does. **(3) `SECURITY DEFINER` WAS UNNECESSARY** and the anon-callable-definer db guard said so on the full run — the function touches no table, only rewrites NEW. Dropping to INVOKER narrowed the surface **and** fixed (1) a second way. 🔑 **The pattern across all three: every one was a GREEN that meant nothing, and each was only exposed by fixing a different green that meant nothing.** | `supabase/migrations/20271150847627_*.sql` · `apps/web/tests/db/unpaid-order-window.db.test.ts` |
