## 2026-07-17 · feat(creator): P2 spine — chapter Book CTA, CTA-click attribution, viewer promo, inquiry-source labels, inquiries-driven, Pro-gate (PR-C)

The Creator Economy P2 spine, one PR (simplest-approach council verdict §5 step 4 — six connectors, one migration), routing creator-driven bookings into the EXISTING inquiry→quote pipeline plus the owner's new inquiry-source taxonomy (2026-07-17). Everything owner-ratified 2026-07-16; paper locks honored: CTA-click attribution (no windows/multi-touch) · public read whitelists `audience_rate_terms` only · metric word "inquiries driven" (never "bookings") · fulfilled/unfulfilled + discount↔chapter link, NO clawback · viewer disclosure at the Book CTA · vendor Creators surface tightened to PRO-AND-UP.

- **`supabase/migrations/20270819553697_creator_p2_spine_inquiry_source_attribution.sql`** — `chat_threads.referring_chapter_id` (FK → creator_chapters, partial index) + `chat_threads.inquiry_source` CHECK (`shortlist · first_pick · favorites · influencer · website · editorial · auto_build · degree`; NULL = Website default) + `chat_threads.is_returning` companion flag + `vendor_creator_offers.fulfilled_at` + `users.creator_accepts_offers` (opt-out storage the enforcement requires — the one addition beyond the plan's item-1 list) + notification enum `chapter_drove_inquiry`. CREATE OR REPLACE (never mutate merged migrations): `unlock_vendor_event` **and** `consume_lead_token_hold` stamp `spend_source='lead_unlock'` on the burn ledger when the unlocked (vendor,event) has an attributed thread — BOTH unlock paths, so attributed unlocks count regardless of the lead-hold flag; `offer_creator_reach_hold` gains the CREATOR_OFFERS_OFF opt-out check + the TIER_BELOW_PRO_NO_REACH pro-and-up tighten; `link_creator_offer_deliverable` + `respond_creator_offer` stamp `fulfilled_at`. Header also corrects the overstated 20270819350491 §5 "no spend path is blind" claim (the legacy direct-burn path doesn't subtract lead holds — recorded, not fixed).
- **`apps/web/lib/inquiry-source.ts`** (new) — the owner's 9 labels ("Shortlist Inquiry", "First Pick Recommendation", "Favorites", "Influencer Recommendation", "Website Inquiry", "Editorial Inquiry", "Auto Build Recommendation", "Degree Recommendation", + "Returning Customer" companion). `degree` is enum+label ONLY (People-layer, counsel-gated, k-anonymity-required — no fake stamping); `shortlist`/`favorites` unwired (no live trigger surface creates inquiries).
- **`apps/web/lib/inquiry-attribution.ts`** (new) — server-validated `ref_chapter` referral (published + public profile + substrate credits THIS vendor), returning-customer signal (the token bands' prior-unlock predicate), once-only provenance stamp, audience-rate whitelist read, vendor thread attribution, collab-active marker, the "inquiries driven" counter (two guards: exclude self-owned-vendor unlocks; dedup per couple/event — COUNT DISTINCT events), and the `chapter_drove_inquiry` creator notification (in-app only).
- **Book CTA** — `apps/web/app/u/[userSlug]/c/[chapterId]/page.tsx`: shoppable vendor cards gain "Book through this chapter" → `/v/[slug]?ref_chapter=…`, "Viewer promo: {terms}" and the ratified disclosure line when an accepted collab carries an audience rate.
- **Stamping** — `app/v/[slug]/inquiry-actions.ts` (`startServiceInquiry` carries + validates referral/source; new threads only) · `app/v/[slug]/page.tsx` + `inquiry-composer.tsx` (`ref_chapter`/`src=editorial` intake) · `vendor-credit-chip.tsx` + realstories gallery (`src=editorial`) · `unlock-category.ts` (`first_pick` dashboard / `auto_build` onboarding fan-out + held-picks flush).
- **Vendor side** — thread header source chip + Returning Customer chip + "Referred by [Storyteller] · via [chapter]" + promised audience rate + "Creator collab active" (`vendor-dashboard/messages/[threadId]/page.tsx`); source chips on the messages inbox + clients list; "What type of customer inquires" SourceBreakdown on `vendor-dashboard/performance` (`lib/vendor-funnel.ts fetchInquiriesBySource`).
- **Quote** — `proposal-maker.tsx` viewer-promo banner + the discount line labeled "Viewer promo — {terms}" (informational null-amount line when no peso entered); `/proposals/[publicId]` reflects it through the existing line_items render.
- **Inquiries driven** — raw number on `/u` (renders nothing at 0) + the vendor Creators browse cards; creator notified on unlock of an attributed thread (accept covers both unlock paths).
- **Pro-and-up gate** — `/vendor-dashboard/creators` page (VendorTierGate) + the RPC floor; `fetchEligibleCreators` hides opted-out creators; ratified verdict §6 copy replaces the stale held/refund lines; audience-rate field's dormant label goes live.
- **Opt-out** — "Accept vendor offers" toggle (default ON) on `/dashboard/creator`, enforced in `offer_creator_reach_hold` + hidden from browse.
- **`/privacy`** — one factual section on chapter→inquiry attribution + inquiry-source labels (updated 2026-07-17).

Known gap (documented): the anon compose-first stash (`lib/pending-vendor-inquiry.ts`) doesn't carry a chapter referral — an anonymous viewer who converts later lands as a Website inquiry.

SPEC IMPACT: Creator_Economy_Simplest_Approach_Council_Verdict_2026-07-16.md §5.4 (PR-C delivered as specified) + Creator_Economy_Discount_Collab_Build_Plan_2026-07-16.md § Inquiry-source taxonomy (owner's types shipped; wired = influencer/editorial/first_pick/auto_build/website-default/returning; unwired = shortlist/favorites/degree — no live trigger surface / counsel-gated). DECISION_LOG.md row appended in the corpus.
