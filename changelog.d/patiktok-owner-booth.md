## 2026-06-22 · feat(patiktok): paid owners land on the booth, buy tiers hidden when owned

Owner-locked rule (2026-06-22): a paid SKU must auto-surface in its natural in-context place once the couple owns it — a couple who PAID for Patiktok should land on their working booth, not a buy page. "Owns" for our manual payment rails means admin-APPROVED (`eventSkuActive`, bundle-aware — covers a direct `PATIKTOK_COMPILER` order AND the `MEDIA_PACK` bundle that includes it per `BUNDLE_CHILD_SKUS`; refund/cancel/lapse releases it), not the pending-inclusive `eventOwnsSku`.

Applied to the Patiktok studio surface (`PATIKTOK_COMPILER`):

- **Buy tiers hidden when owned** (`apps/web/app/dashboard/[eventId]/studio/patiktok/page.tsx`): the SKU is resolved ONCE near the page's data fetches via `eventSkuActive(createAdminClient(), eventId, 'PATIKTOK_COMPILER')` — read with the ADMIN client because ownership is an event-level fact and `orders` RLS is purchaser-scoped (a co-host who didn't personally place the order would otherwise be mis-gated onto the buy view). When active, the `<PricingTiers>` buy block is HIDDEN and a new `BoothLaunchPanel` (a prominent "Open booth dashboard" CTA deep-linking to `/studio/patiktok/booth`) plus the existing `<YourRenders>` list are promoted to the top of the page. When NOT active, today's buy-tiers view renders unchanged (tiers + renders), so non-owners are unaffected. Graceful-degrade on a missing/legacy `orders` table (`42P01` / `42703` → not active) keeps pre-bootstrap DBs on the buy view rather than crashing. The booth-launch + renders components already existed on the page — they are gated/reordered, not rebuilt.
- **Owner entry deep-links to the booth** (`apps/web/app/dashboard/[eventId]/studio/about/[addon]/page.tsx`): the Studio row for Patiktok routes through the App Store `/studio/about/patiktok` interstitial (via `appStoreDetailHref`). A patiktok-scoped owned-redirect was added to that route file: when `addon === 'patiktok'` AND `eventSkuActive(...)` is true, it `redirect()`s straight to `/dashboard/[eventId]/studio/patiktok/booth` (the operator dashboard) instead of rendering the marketing About page. Scoped to Patiktok only — every other feature keeps its About page. Same admin-client + bundle-aware + graceful-degrade contract; on an entitlement-read miss it falls through to the About page. This was done in the patiktok-relevant route render logic (NOT `lib/add-ons-catalog.ts` — another PR is touching a different entry there — and NOT the shared `studio/page.tsx` grid or `_components/addon-detail-view.tsx`).

Wired against the existing bundle-aware reader (no new helper invented): `eventSkuActive` from `apps/web/lib/entitlements.ts`, with `PATIKTOK_COMPILER` as a `MEDIA_PACK` child in `BUNDLE_CHILD_SKUS`. Booth surface `apps/web/app/dashboard/[eventId]/studio/patiktok/booth/page.tsx` and the `YourRenders` list were already present.

Follow-up (out of scope, noted to avoid collisions): the shared studio chrome — the per-row status badge in `studio/page.tsx` and `_components/addon-detail-view.tsx` — was deliberately not refactored; a separate pass should surface an "Owned" badge / direct booth CTA on the Studio grid card itself.

SPEC IMPACT: 0017 Patiktok — paid owners land on the booth, buy tiers hidden when owned (admin-approved `PATIKTOK_COMPILER`, directly or via `MEDIA_PACK`). Logged in `DECISION_LOG.md`.
