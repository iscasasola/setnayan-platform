## 2026-06-20 · content(journal): 78 future-dated articles — Facebook drip through Dec 18

The Journal (`/blog`) is the content engine that feeds Facebook: each published article auto-syndicates as an FB teaser (curiosity hook + native link card) via `sweepJournalArticles` in `lib/social/flush.ts`, gated on the article's `publishedAt`. The 10 existing articles all date ≤ Jun 16, so the FB queue drains ~Jun 21. This adds **78 new articles** dated **Mon/Wed/Fri 2026-06-22 → 2026-12-18** (3×/week, owner-chosen), so the FB journal pillar never runs dry through year-end. Topics span planning, vendors, culture (incl. regional + faith), and seasonal hooks (rainy season, ber-months, Christmas/December, NYE), plus 3 `news` brand pieces — none duplicating the existing 10.

- **`apps/web/lib/blog-batches/*.ts`** (8 new modules, 78 `BlogArticle` objects + 78 FB hooks): authored per-theme — `capture-coverage` · `food-styling` · `guests-paper` · `money-legal` · `rituals-symbols` · `regional-faith` · `seasonal-rainy` · `december-news`. Each exports `ARTICLES_*` + `HOOKS_*`; each `import type`s `BlogArticle` from `@/lib/blog` (erased → no runtime import cycle). 600–900 words each, allowed blocks only (`p`/`h2`/`ul`/`quote`/`cta`), market price RANGES only (no Setnayan SKU prices · public-surface hygiene), internal CTAs to `/signup` · `/explore` · the 10 core slugs.
- **`apps/web/lib/blog.ts`** — the inline 10 became `CORE_BLOG_ARTICLES`; `BLOG_ARTICLES` is now the concat (core + 8 batches). New **`publishedBlogArticles()`** gate (`publishedAt <= PH-today`) drives the PUBLIC surface; `generateStaticParams` + `findBlogArticle` stay UNGATED so an FB teaser link resolves on its day instead of 404-ing.
- **`apps/web/app/blog/page.tsx`** + **`app/sitemap-blog.xml/route.ts`** — index, hero pick, JSON-LD `blogPost`, category chips, and sitemap read `publishedBlogArticles()`. Both routes render on demand (index dynamic, sitemap hourly-ISR), so a future article appears on its exact PH date with no redeploy.
- **`apps/web/lib/social/journal-hooks.ts`** — spreads the 8 `HOOKS_*` maps into `JOURNAL_SOCIAL_HOOKS` (existing excerpt fallback unchanged).
- **`apps/web/public/blog/*.webp`** (10 new covers, ~40–149KB each, 1600×900, Recraft + Pillow-recompressed to the existing covers' web weight): `photo` · `catering` · `florals` · `guests` · `invites` · `culture` · `regional` · `rainy` · `christmas` · `news`. Placeholder posture per the 2026-06-15 "AI now, swap to real photography later" decision; reused across same-theme articles.

Verified: `tsc --noEmit` clean · `next lint` (no new warnings) · `lint:retired` clean · `next build` green · runtime smoke (real env) — `/blog` lists exactly the 10 live articles and 0 future, sitemap matches, future slugs (Jun 22 / Dec 18) render 200 on demand, unknown slug 404s.

SPEC IMPACT: editorial content only — no schema, pricing, or product-surface change. Extends iteration 0038 (Editorial). Logged as a row in `DECISION_LOG.md` (3×/week journal drip through 2026-12-18; FB cadence stays governed at ≤3/day by the existing social governor).
