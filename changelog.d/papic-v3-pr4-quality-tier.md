## 2026-07-20 · feat(papic): per-event quality/fidelity tier — one column, two seams (brief PR-4)

Papic v3 brief PR-4 (`0012_papic/Papic_Build_Brief_2026-07-17.md` ruling #2 · `Papic_v3_Whats_Next_2026-07-18.md` §8 `papic-v3#3`). NET-NEW work — the 2026-07-19 grep confirmed neither `papic_quality` nor `papic_quality_tier` existed in code, so this introduces the column and BOTH seams fresh (no live write/read mismatch to repair).

**Migration `20270825539466`** — `events.papic_quality_tier TEXT NOT NULL DEFAULT 'full_res' CHECK (full_res|optimal|high_efficiency)`. INERT on apply: the default is exactly the shipped behavior (originals stored 1:1, no ingest processing), so auto-apply-on-merge changes nothing for existing rows or envs.

**One column, two seams (the whole point — no fake door):**
- **WRITE seam** — new "Photo quality" section on the couple's Papic setup surface (`studio/papic`): `QualityPicker` (StylePicker idiom, form-per-card) → `setPapicQualityTier` server action (couple-gated, allow-list validated against the shared vocabulary) → `events.papic_quality_tier`. Weddings see a "Recommended" badge on **Optimal (~4256px · ~12 MP · A3-sharp)**; **High efficiency (~2560px · ~4 MP)** is the Papic Lite tier; **Full resolution** = keep 1:1 (the default). Downscaling choices ask a confirm first (open-risks invariant "irreversible fidelity downscale confirm"); copy states it applies only to photos captured after the change.
- **READ seam** — both capture ingest paths (`app/papic/actions.ts` seat captures + `app/api/papic/guest-capture/route.ts` guest disposables) call `applyEventFidelityToOriginal` (new `lib/papic-ingest-fidelity.ts`) FIRST in their `after()` chains, so screen/derivatives/Drive/downloads all flow from the tiered original. A downscaling tier replaces the just-uploaded still's R2 original in place (same bucket+key — every stored ref stays valid) with a long-edge-capped JPEG, retaining EXIF incl. GPS via `.withMetadata()` (corpus: geo strips on OUTBOUND only; the R2 original retains it). STILLS only (no server ffmpeg — clips stay client-governed 1080p). Photos already at/below the cap store verbatim (one-compression-pass rule). Best-effort, never throws; absent/legacy/pre-migration rows resolve to `full_res` = byte-for-byte today's behavior.

New shared vocabulary `lib/papic-fidelity.ts`: type **`PapicFidelityTier`** (deliberately distinct from adaptive-quality's NETWORK tier `PapicQualityTier` in `lib/papic-adaptive-quality.ts`), `asPapicFidelityTier` fallback coercion, and the single `fidelityIngestParams` tier→parameter mapping both seams import. Unit tests (`lib/papic-fidelity.test.ts`, 6 tests): mapping matches the GBB §5 ladder, default fallback = no-processing, vocabulary == the column CHECK.

Verify: `tsc --noEmit` clean · lint clean · `test:unit` 2194/2194 pass.

SPEC IMPACT: `0012_papic/Papic_v3_Whats_Next_2026-07-18.md` — brief-PR-4 shipped: `papic_quality_tier` single-column write/read, no fake door. Note for the corpus: GBB §5's "quality is fixed per product, no picker" simplification was superseded by the brief's PR-4/PR-8 single-column + picker shape (this build); wedding default remains full_res at the DB level with Optimal as the recommended pick — flag to owner if the fixed-per-product (auto-derive, no picker) reading was intended instead.
