## 2026-07-12 · feat(pricing): Setnayan AI → single ₱1,499 one-time SKU + ₱200 token display sync

Pricing follow-through for the 2026-07-12 owner locks. TWO parts:

**Part A — Setnayan AI collapses to ONE ₱1,499 one-time SKU (owner FINAL 2026-07-12).** The owner's final decision: Setnayan AI is a SINGLE ₱1,499 one-time purchase per event — no ₱4,999 pass, no ₱499 entry, no monthly, no annual, no tiers. End state = exactly ONE active Setnayan AI door: `SETNAYAN_AI` · ₱1,499 · `billing_period='one_time'` · `is_active=true`, granting the existing PERMANENT `events.setnayan_ai_active` unlock.

- `supabase/migrations/20270729889508_setnayan_ai_reprice_single_one_time_1499.sql`: idempotent `UPDATE public.platform_retail_catalog_v2 SET retail_price_php = 1499, billing_period='one_time', is_active=true WHERE service_code='SETNAYAN_AI'` (₱499 → ₱1,499). Per-event-pricing flag stays OFF (since `20270714262264`) so no lapsing window is stamped — the unlock is permanent. CHECK constraints satisfied (an UPDATE on the existing row; `one_time` ∈ billing_period check). Timestamped after PR #3138's `20270728*` files and after every migration on `origin/main`.
- REMOVED the interim ₱4,999 EVENT PASS this same draft PR had floated: deleted its seed migration `supabase/migrations/20270729719932_setnayan_ai_event_pass_sku.sql` (never applied to prod → clean un-create), and reverted the `SETNAYAN_AI_EVENT_PASS` wiring in `apps/web/lib/sku-activation.ts` (activation hook + both reversal-path references + the `AI_EVENT_PASS_SKU` const/doc). Zero `SETNAYAN_AI_EVENT_PASS` references remain anywhere. The existing `SETNAYAN_AI` permanent-unlock activation + its reversal are exactly as they were before this PR.
- Deliberately NOT changed: `SETNAYAN_AI_SUB` (dormant ₱499/28-day per-user subscription) stays `is_active=false` — the couple monthly/annual is dropped, NOT reactivated; `SETNAYAN_AI_RENEW` stays `is_active=false`; no feature flag flipped (per-event-pricing, per-user, paywall all left as-is).

**Part B — token ₱200 display sync (display only, no charge logic).** After PR #3138 the CHARGE is 1 token = ₱200 (DB migrations `20270728100000`/`20270728200000`), but display constants still showed ₱100 × old 1–3 region bands. Synced every vendor/admin/public display to a flat ₱200 / flat-1-token burn:

- `apps/web/lib/v2/region-token-burn.ts`: `TOKEN_PRICE_PHP` 100 → 200; header/band docs updated to note the flat-1 burn (band machinery kept intact — DB column + resolver still support 1–3 for a future admin re-band).
- `apps/web/lib/region-source.ts`: `STATIC_REGIONS` fallback `burn_band` flattened to 1 for all 19 regions (mirrors live `regions.burn_band=1` after #3138); type + DB hydrator untouched.
- `apps/web/app/admin/pricing/_surfaces/token-bands-surface.tsx`: hardcoded `burn_band * 100` → `burn_band * TOKEN_PRICE_PHP` (now ₱200); header/intro copy → ₱200/token (1/2/3 = ₱200/400/600, currently flat 1).
- `peso-per-lead-card.tsx` (vendor) + `peso-per-lead-admin-card.tsx` (admin): the ₱/token figures derive from `TOKEN_PRICE_PHP` (auto ₱200); "1–3 region-banded" hints → flat 1 token (₱200).
- `vendor-dashboard/page.tsx`: cost-to-answer banner → "flat 1 token (₱200), anywhere".
- `buy-tokens-cta.tsx`: fallback per-token price 100 → 200 (primary value already derives from the live pack catalog); SRP doc refreshed to ₱200/token packs.
- `vendor-dashboard/earnings/page.tsx` + `web-nudge-banner.tsx`: ₱100 → ₱200 in copy/prop-doc.
- `public/llms.txt` (AI-crawler surface) + `lib/llms-price-fixture.ts` (drift-guard allow-list): token section synced to ₱200 flat + flat-1-token unlock; pack table repriced (₱800/₱2,000/₱5,000/₱10,000/₱20,000); footer changelog entry added; drift guard reconciled and re-verified GREEN (no unapproved/unused/duplicate figures).

**Part C — stale-copy cleanup (Gap 5a + token-comment sync · copy/comments only, no logic).**

- `apps/web/app/dashboard/[eventId]/studio/setnayan-ai/page.tsx`: the buy surface still described the SKU as a "₱499 / 28-day subscription that … auto-ends" — contradicting the ONE-TIME PERMANENT model. Rewrote the third `WHAT_YOU_GET` card ("On until your wedding day" → "Yours for the whole wedding" · one-time purchase, no renewals/28-day cycle) and the buy-block line ("Active until your wedding day, then it ends" + `₱X / 28 days` fallback → "One-time purchase · yours for the whole wedding" + plain-peso fallback). The live-catalog `priceLabel` drives the shown price (now ₱1,499) and is unchanged. The two doc comments (file header + priceLabel note) were refreshed to the single-₱1,499 one-time framing. No hardcoded ₱499/₱4,999 AI figure remains in the page copy.
- Token ₱100 → ₱200 in code comments the Part B sweep missed (comments only): `lib/region-source.ts` (band legend), `lib/vendor-tier-caps.ts` (lifetime-token note), `lib/chat-actions.ts` (burn-on-answer band note), `lib/vendor-peso.ts` (₱/token source note), `app/admin/token-bands/actions.ts` (×3: header + flat-1:1 note + inline), `app/admin/pricing/_surfaces/token-bands-surface.tsx` (doc note). Also synced ONE stale VISIBLE display string the Part B sweep missed on the same surface: the token-bands table header `Tokens (= ₱×100)` → `₱×200` (it sat directly under the already-synced ₱200 intro — a display-only fix, consistent with Part B). Flagged: `TOKEN_BUY_PRICE_PHP = 100` in `vendor-tier-caps.ts` is unused dead code — left as-is per the comments-only scope.

DID NOT: run `supabase db push`; touch PR #3138's migrations; enable any flag; auto-merge (DRAFT PR). No charge logic changed — Parts B & C are display/copy only.

FOLLOW-UP (out of this PR's scope): `public/llms.txt` (public AI-crawler feed) still lists Setnayan AI at ₱499 one-time in ~8 places (unchanged here — those figures pre-date this PR, and ₱499 is shared with Live Background / Camera Bridge, so the fixture allow-list can't simply drop it). Syncing that public marketing copy to ₱1,499 is a separate, deliberate llms.txt content refresh (its own dated changelog note).

SPEC IMPACT: DECISION_LOG.md 2026-07-12 FINAL LOCK — **Setnayan AI is a single ₱1,499 one-time SKU** (supersedes the interim ₱4,999-pass / ₱499-entry split floated in this same draft; the ₱499/28-day SUB stays retired). Also DECISION_LOG.md 2026-07-12 (PRICING LOCK — token ₱100→₱200 + flat-1-token burn) covers Part B. Apply the DECISION_LOG row + `Pricing.md § 00` update to ₱1,499 one-time on approval/merge.
