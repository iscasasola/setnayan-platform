## 2026-07-03 · feat(admin): 6-menu respine — Overview · Accounts · Content · Marketing · Performance · System Settings

Owner-locked 2026-07-03: Setnayan HQ's spine is re-cut from the 8-group ops layout to the owner's six topic menus, with the 2026-06-08 ops insight preserved INSIDE Overview — the owner declared **Overview the task inbox** (vendor-home pattern): the `/admin` pulse plus every act-now queue (requests · approvals · transactions · reports · disputes).

- **Desktop sidebar** (`admin-sidebar.tsx`): Home+Work merge into **Overview** (key `queues` kept for localStorage); Directory → **Accounts** (key `directory`); Content & Media → **Content** (key `media`); NEW **Marketing** group (key `marketing`) takes Social queue (from Work), Spotlight Awards + Journal Spotlights (from Directory), Discount codes + Referrals (from Monetization); App Performance group → **Performance** (key `funnels`); **System Settings** (key `settings-group`) absorbs the dissolved Monetization config lane + Data Structure. No surface dropped, no URL changed; queue badges follow item keys so Social queue keeps its live count in Marketing.
- **Mobile bottom nav** (≤5 ruleset honored): Overview · Accounts · Marketing · Performance · More. The Work tab dissolves into Overview (its rolled-up badge moves there); the Money tab dissolves (config → System Settings inside More). Performance lands directly on the App Performance cockpit. New additive `activeMatchAlsoExact` on the shared `BottomNavItem` lets the merged Overview tab combine the exact `/admin` landing with the queue-route prefix umbrella.
- **Landings:** new `/admin/marketing` card grid; `/admin/directory` retitled Accounts (route kept); `/admin/more` re-cut to Content + System Settings (App Performance section removed — own tab now); `/admin/money` + `/admin/insights` stay as bookmark landings.
- **Nav registry defaults:** `admin.bottom-nav.home` → "Overview", `.directory` → "Accounts", `.work` + `.money` defaults removed with their tabs, `.marketing` + `.performance` added. Item-level `admin.sidebar.*` slots unchanged (items only moved groups), so admin overrides survive.
- **Overview page:** header re-written for the new IA + a "Platform upgrades" link beside "Open the work list" pointing at the App Performance Action Center (the owner's "things we need to upgrade" — linked, not duplicated).

SPEC IMPACT: corpus DECISION_LOG.md 2026-07-03 row (6-menu respine + Overview-as-task-inbox owner decision); memory `project_setnayan_nav_icon_menu_registry` + `project_setnayan_app_performance_cockpit` updated.
