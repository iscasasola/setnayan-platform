# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-24 · feat(guest-site): open-browse PR7 — phases-as-emphasis engine (additive, per-event flag-dark)

Open-browse PR7 of 11 (council verdict `Guest_Event_Website_Open_Browse_Council_Verdict_2026-07-22.md` §1.2/§1.3, build-plan row 7). Under the per-event `events.website_open_browse` switch (migration `20270919912384`, `DEFAULT FALSE` — the go-live hold), lifecycle phases stop being GATES and become EMPHASIS: every published, non-empty, audience-permitted widget renders in every phase behind the five-tab menu; the phase decides only what Home leads with. **Purely additive and dormant in production** — the column defaults FALSE for every event (flipped for demo events + new events at PR11), so with it FALSE the guest site renders byte-for-byte as today. The 15 flag-off golden tests (`lib/site-body-plan.test.ts`) are unchanged and green — the byte-identical guarantee.

- **`lib/invitation-widgets.ts`** — the open-browse engine, landing ALONGSIDE (never replacing) `WIDGET_PHASES` (which stays the flag-off gate, deleted only post-flip at PR11): `WIDGET_SPOTLIGHT` (exhaustive `Record<WidgetType, {weight, spotlightPhases}>` — ordering weight + Home-spotlight eligibility, never a render boolean); `resolveWidgetTerminalState` + `isTerminalRenderable` (§1.3 time-bound terminal states — `rsvp`→closed / `countdown`,`what_to_bring`,`photo_moments`→archive post-event; PR7 excludes non-`active` widgets so the dormant flag-on path is never wrong-tense, leaving the archive/closed COPY to PR8); `hasContent` (the shared emptiness predicate both menu + renderer consume, so they can never disagree); `openBrowseSectionVisible` / `openBrowseWidgetVisibleTo` (the `is_visible`-OR-`mode` reconciliation + `audience` dial — the migration's load-bearing rule, honoring couple force-hides across the PR9 cutover); `openBrowseWidgetsInOrder` (the widened, weight-ordered, mode/audience/content/terminal-filtered list). `InvitationWidgetRow` gains optional `mode` + `audience`.
- **`lib/public-capability.ts`** (new) — the anonymous-tier capability object (§1.2 "PII guarantee made real"). The single USER of the exported `PUBLIC_WIDGET_ALLOWLIST` firewall; constructed with zero guest input by design. `canRenderWidget` ANDs the allow-list with the open-browse mode/audience reconciliation.
- **`lib/site-body-plan.ts`** — `resolveSiteBodyPlan` gains optional `openBrowse` (default FALSE → verbatim pre-PR7 path) + `content` inputs, and three new plan fields (`openBrowse`, `spotlight`, `widgetTerminalStates`) that are inert (`false`/`null`/`{}`) on the flag-off path. When TRUE: `qr_card` + `greeting` widen to every phase (§1.2, fixing the live `WIDGET_PHASES` strip); RSVP follows its terminal state; the widget lists come from `openBrowseWidgetsInOrder` (guest audience for the guest tree, anonymous + capability-firewalled for the public tree); `pickHomeSpotlight` picks Home's identity-aware lead (anonymous rsvp-phase → find-mode, never an RSVP imperative).
- **Renderer wiring** (`app/[slug]/_components/site-body.tsx`, `_lib/loaders.ts`, `_lib/types.ts`) — the widget SELECT + events SELECT add `mode`/`audience`/`website_open_browse`; `SiteBody` feeds `openBrowse` + a `content` presence map into the plan (byte-inert when the column is FALSE); the widened lists flow through the existing render loops unchanged; a minimal, palette-respecting `SpotlightCard` (new) mounts on Home in both trees, guarded by `plan.spotlight` (so it renders nothing in prod).

PR7→PR8 seam (documented in code): the per-widget `archive`/`closed` degraded COPY, the public `event_details` variant, the find-mode invite card, and the menu-below-editorial visible change are PR8 (owner copy sign-off). PR7's spotlight card + terminal handling are the functional-but-minimal lead PR8 restyles. No migration (the PR4/PR5 columns already ship inert in prod).

SPEC IMPACT: None — implements the already-signed-off council build plan (row 7); no pricing, SKU, or schema change. Owner sign-off items §5 (empty-state copy, menu-below-editorial, event_details field set) remain deferred to PR8/PR11 per the plan.
