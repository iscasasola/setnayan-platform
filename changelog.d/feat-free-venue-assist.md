## 2026-07-09 · feat(setnayan-ai): FREE first-venue-shortlist carve-out — Suri builds the couple's first reception-venue shortlist, free, once

Owner-locked 2026-07-09 (Pricing.md § 00): Suri's reception-VENUE finding is the free taste that introduces the full Setnayan AI subscription (₱499 first 28-day cycle → ₱799/28d). Refined same day: the benefit is NOT perpetual venue-category AI — it is **one free build of the couple's FIRST reception-venue shortlist**, an introduction to what Suri does. Every other category and the guard/secretary/briefing layer keeps the normal gate.

- **`lib/setnayan-ai-free-assist.ts`** (new · pure · unit-tested, 10 tests): the carve-out's single source of truth. `SURI_FREE_ASSIST_CATEGORIES = ['venue']` (the reception member of `VendorCategory`; plan group derived → `reception_venue`), `isSuriAssistFreeForCategory` / `-ForPlanGroup` / `-DecisionId`, `FIRST_VENUE_SHORTLIST_CAP = 5`, and the offer-visibility rule `isFirstVenueShortlistOfferAvailable`: offered ⇔ the event's venue shortlist is EMPTY. **"First" semantics need NO migration** — any venue pick (Suri-built or manual) consumes the offer; the shortlist state itself records consumption. Couple-facing copy strings (badge · chip · offer · upsell · confirmation) are unit-locked here.
- **`app/dashboard/[eventId]/progress/_actions/free-venue-shortlist.ts`** (new): `buildFirstVenueShortlist` — RLS-scoped (events membership gate before the emptiness gate), deliberately **NOT AI-gated** (works for free accounts), idempotent (`already_has_shortlist` no-op once the shortlist is non-empty), cap 5, fail-soft per candidate. **Reuse over reinvention:** candidates come from the shipped `searchCategoryVendors` (ranked + faith/event-type/compat/radius-scoped — the Category Search overlay's exact machinery); writes go through the shipped `attachMarketplaceVendorToCategory` (`status='considering'`, `source='host_marketplace_search'` — no new enum, no schema).
- **Progress page** (`/dashboard/[eventId]/progress`): free state only — the offer ("Let Suri find your first venue shortlist — free" + `✦ Setnayan AI · Free` badge + the one quiet upsell line) embeds under the venue decision item when the board renders one, else stands alone atop the decisions board (fresh events with an empty board still see it). Post-build confirmation is the upsell beat: "Suri shortlisted N venues that fit your date, budget & area — this is what the full Suri does. ₱499 first 28 days → ₱799/28d." AI-active state renders exactly as before.
- **Vendors bench** (`shortlist-categories.tsx`): presentational "First shortlist free ✦" chip on the reception-venue category row while its shortlist is empty. No touch to the 3-state engine, packages, finalize gates, availability, or reach logic.

SPEC IMPACT: Pricing.md § 00 free-venue-assist carve-out (owner-locked 2026-07-09) — already applied in the corpus; this entry records the code landing (first-shortlist refinement of the same lock).
