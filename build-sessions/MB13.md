# MB13 — The OKLCH progressive recommender

**Goal:** the specced, unit-tested recommender finally lands and feeds 02.

**Model:** Sonnet · high effort — spec and tests already exist; this is faithful translation.
**Size:** half-day. **Depends on:** MB4 (same docblock gate, same colour space), MB5.

## ⏭ This is the designated slip

If the arc runs long, **this is the first thing to move a week.** Section 02 works without it and
nothing else depends on it. Everything else in MB0–MB14 earns its slot.

## Delivers

- The scratchpad spec ported as a lib, with its unit tests
- Integration into 02's suggestion flow — cumulative recommendations that account for the colours
  already chosen, not one-shot suggestions

## Scope reminder

Setnayan AI advises on the **create-your-own** path. On an applied theme it stays quiet until the
board becomes the couple's own. That predicate already exists from the prototype work — reuse it,
do not re-derive it.

## Verify

- `pnpm exec tsc --noEmit` from `apps/web`
- the ported unit tests
- the rank-invariant suite unaffected

## Owner decides first

Nothing beyond MB4's gate.
