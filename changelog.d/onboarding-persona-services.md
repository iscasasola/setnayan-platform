## 2026-06-25 · feat(onboarding): per-type persona services — pre-surface in-app services per persona (0053, flag-gated)

Iteration 0053 Phase 3 follow-up. PR #2135 added per-type persona packs of vendor CATEGORIES, but the generic `/onboarding/[type]` flow still hardcoded `interestedServices: []` — so non-wedding events never pre-surfaced the in-app Setnayan SERVICES the wedding wizard already lines up per persona (`EXP_PERSONAS[].services` → `style_preferences.interested_services`). This restores that richness, type- + persona-appropriately.

- **`lib/onboarding/persona-packs.ts`** — added a `servicesByPersona: Record<PersonaKey, readonly string[]>` dimension to every `TypePersonaPack`, mirroring the existing `byPersona` category shape. Authored type-appropriate service lists for all 8 enabled non-wedding types × 6 personas. Couple-only SKUs (`pakanta`) are excluded from corporate + tournament. New pure resolver `derivePackServices(packKey, personaKey, effort)` returns the persona's services, deduped, **intersected against the canonical `VALID_SERVICE_KEYS` registry** (= `INAPP_TO_SERVICE_CODE` from onboarding-pricing.ts — the same key space the wedding wizard writes), then sized by effort (simple=2 / balanced=3 / allout=5). No pack / unknown persona → `[]` (the safe PR2 fallback). Exported `VALID_SERVICE_KEYS` so the test can assert the only-valid-keys invariant.
- **`app/onboarding/[type]/_components/generic-onboarding.tsx`** — computes `planServices` via `derivePackServices(personaPackKey, personaKey, axes.effort)` (memoized) and sets `interestedServices: planServices` in the `handleCreate` payload (was `[]`). No UI change on the reveal screen (kept low-risk; services are written silently).
- **`lib/onboarding/persona-packs.services.test.ts`** (new) — every enabled type has services for all 6 personas; only-valid-keys invariant; effort scaling (2/3/5) + default-3; priority-order/prefix; dedupe; no-pack → []; unknown/null persona → []; corporate/tournament never include `pakanta`. 12 tests, all green (443 total).

Pre-surfaced only (NOT purchased) — no paywall, no pricing/free-vs-paid decision touched. The whole generic flow stays **dark** behind `NEXT_PUBLIC_EXPERIENCE_QUIZ_ENABLED`, so no user-visible change until owner go-live. Wedding is untouched (its own wizard never routes through `/onboarding/[type]`). No migration — `interested_services` is an existing `style_preferences` JSONB field.

SPEC IMPACT: onboarding (iteration 0053 Phase 3) — per-type/per-persona in-app service pre-surfacing content. Fills the `servicesByPersona` data seam; engine + write path unchanged. Logged in `DECISION_LOG.md`.
