## 2026-07-08 · feat(plan3d): mood-board dance-floor mural + monogram, all surfaces

The dance floor across the three 3D seat-plan surfaces now renders the couple's mood board as a painted MURAL (Fable dossier §3.7) instead of a flat accent plane — or, on two surfaces, nothing at all.

- `lib/dance-mural-texture.ts` (new): CanvasTexture mural from `events.role_palette`. Derives `[bg, accent1, accent2]` via the LED-wall math (`ledPaletteFromMoodBoard`, site-palette.ts) over a new dark `DANCE_MURAL_TEMPLATE` triple — thin/grey palettes fall back to the template verbatim, so every room gets a floor. Paints a terrazzo/gobo-wash motif (two accent radial blooms + `mulberry32(fnv1a(key))`-seeded stone chips + inlaid border ring + edge vignette) into a rounded-rect alpha frame; optionally composites the couple's CANONICAL monogram at centre via `monogramSourceToDataUri` (async `<img>` decode → one `needsUpdate` flip; failure degrades to mural-sans-mark). Rasterized ONCE per `(palette triple, monogram identity)` cache key in a module cache — never per frame, never disposed (same lifetime contract as `floorRoughnessMap`). Pure parts (`muralPalette` / `muralCacheKey` / `monogramMuralKey`) unit-tested in `lib/dance-mural-texture.test.ts` (11 tests).
- `app/_components/plan3d/dance-floor-mural.tsx` (new): the shared `<DanceFloorMural>` mesh — MonogramPlane discipline (`meshBasicMaterial` + `toneMapped:false` + `alphaTest` + `depthWrite:false` + raycast off, so floor-tap roam passes through).
- Couple lab (`seating-lab-3d.tsx`): the flat accent dance plane is replaced by the mural, fed by `rolePalette` (the DEMO material switcher keeps recolouring walls/linen but never repaints the couple's floor). The static mark bakes into the mural ONLY when the dance floor sits away from the room centre — a centred dance floor is already covered by the origin medallion, so the floor never shows two marks. The paid ANIMATED_MONOGRAM bloom (`MonogramPlane` + `lib/animated-monogram.ts` gate) is untouched.
- Homepage demo (`plan3d-scene.tsx`) + public guest venue walk (`guest-venue-3d.tsx`): the MISSING dance-floor mesh added (both rooms fed `floor.dance` into walk obstacles but rendered nothing — avatars dodged an invisible rectangle). Both render the shared mural, palette-only: the anonymised public payload carries no monogram source (PII posture unchanged — no per-guest data involved).

Typecheck + lint clean; full `test:unit` suite green (1143 tests).

SPEC IMPACT: 0008_seating_chart_editor/0008_3DPlan_Fable_Design_2026-07-08.md §3.7 (built as specced; one judgment call — lab mural carries the static mark only when the dance floor is off-centre, to avoid doubling the origin medallion)
