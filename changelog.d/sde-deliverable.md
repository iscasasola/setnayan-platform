## 2026-06-22 · feat(sde): Same-Day Edit deliverable — admin upload → auto-shows on day-of page + recap

Ships the Same-Day Edit (SDE) film as one coherent feature: the Setnayan crew uploads the finished cut from a new HQ surface, and it auto-publishes to the couple's public surfaces the moment it lands — no separate couple-publish step (owner rule: a paid feature auto-shows the moment it exists).

Migration `20270213100000_sde_output.sql` (mirrors the Panood watch-url migration): three additive, idempotent columns on `public.events` — `sde_video_r2_key` (encodeR2Ref stored-asset ref), `sde_poster_r2_key`, `sde_published_at`. No new RLS (events UPDATE is host-scoped; the public page reads via the admin client like every other landing column). Applied to prod statement-by-statement via `db query` (a pre-existing parallel-session ledger drift — local-only `20270212791181_integration_meta_social.sql` — blocked `supabase db push`; ledger row inserted manually). All three columns confirmed on `public.events`.

- **Admin upload** — new `/admin/sde` (`page.tsx` + `actions.ts` + `sde-uploader.tsx`, copy-adapted from `/admin/hero-video`). Lists every event holding SDE (direct `orders.service_key='SDE'` or the Media Pack bundle that includes it) with a per-event upload widget. `saveSdeFilm({eventId,videoKey,posterKey})` asserts admin → service-role update of the three columns with `sde_published_at=now()` (AUTO-PUBLISH) → revalidates the couple's `/${slug}` + `/${slug}/recap`. Uploader does presigned R2 PUT (via `/api/upload`) then calls the action. Sidebar link added under the Content group.
- **Studio card** — one new `ADD_ONS` entry (`key:'sde'`, `serviceKey:'SDE'`, `Icon: Film`, `studioGroup:'capture'`, `status:'web_v1'`, poster copied from `panood`); the three-state ownership pill resolves via `pillFor` with no studio hub edit. New `/dashboard/[eventId]/studio/sde/page.tsx` (mirrors `studio/indoor-blueprint`): owned → "appears on your day-of page + recap" + inline film preview when delivered (else a "being cut" stand-in); not-owned → what-it-is + `InlineCheckoutDrawer` Get CTA (catalog-priced via `formatV2Sku('SDE')`).
- **Auto-show surfaces** — `[slug]/page.tsx`: in the day-of live/recap block, gate on `eventSkuActive('SDE')` + read the three `sde_*` columns, presign the film + poster, thread `sdeFilm` (+ `sdeOwnedPending`) into PublicLanding and InvitationSite, render a new `<SdeFilmBlock>` across the live AND post (recap) windows. `lib/auto-recap.ts` `assembleRecapModel`: resolves `sdeFilmUrl`/`sdeFilmPosterUrl` when SDE is active + delivered; `[slug]/recap/page.tsx` renders a new `<SdeFilm>` section between `RecapHero` and `RecapStats`, only when present.
- **Gate hygiene** — `editorial/data.ts` §8: 'SDE' is listed under "services availed" only when `eventSkuActive('SDE')` (admin-approved, bundle-aware), so a pending/unverified SDE order can't advertise a deliverable on the public editorial. SDE-only — other labels untouched.

Every `sde_*` read graceful-degrades on `42703`/`42P01` (and any error) → treated as not-delivered, so the surfaces are safe on a pre-migration / drift-blocked DB. `pnpm typecheck` + `pnpm lint` clean.

SPEC IMPACT: 0024 / 0012 — the SDE film is delivered via admin upload and auto-shows on the couple's day-of page + recap, gated `eventSkuActive('SDE')` (bundle-aware via MEDIA_PACK). Logged in `DECISION_LOG.md`.
