## 2026-07-22 · feat(papic): one-pool metering foundation — Papic One per-camera grant + Pool binding + Free 50-pt pool (ships DARK)

Implements the buildable half of `0012_papic/Papic_One_Pool_Model_Spec_2026-07-22.md` §0 (owner-locked FINAL numbers), bundling spec PR3 + PR4 + PR5 as ONE PR. The whole point: make the event pool "apply" whenever an event holds ANY `papic_event_point_grants` row (not only a `pass_service_codes` order), so **Free** (a 50-pt `free_grant` seeded at event creation), **Papic One** (a `camera_grant` per paid camera), and **Papic Pool** (`PAPIC_GUEST*` `topup_order` grants) all flow into the SAME pool and are metered by the SAME `papic_reserve_event_points` RPC. This routes **around** the `20270828140000` §11 fence RAISE because it never touches `pass_service_codes`. Nothing is flipped `is_active`/`status` — Pool/One stay dark; no new guest doorway is surfaced.

**Three additive, idempotent migrations (allocator-sourced prefixes, re-allocated to sort strictly after main's tail `20270901120000`; no interleaving with `20270832487160` from #3491):**
- `20270901123354_papic_one_camera_grant_config.sql` — legalizes `free_grant` + `camera_grant` in the grant `source` CHECK (both were rejected by the live CHECK today); adds admin-tunable `papic_event_pool_config.camera_grant_points` (default 250); flips `papic_tier_config.points_per_day → NULL` for **BOTH `mini` (Papic One) AND `free`** so their seats meter ONLY against the shared event pool (window-total, one gate, **NO per-seat reserve** — supersedes the applied `20270828150000` `mini=200` assertion via a forward UPDATE, not an edit to the applied file; `free` was never asserted there); adds `papic_grant_camera_points(event, order)` — counts the order's mini seats (250 × N in one grant), idempotent by `order_id` **serialized by a `pg_advisory_xact_lock` on the order** so a concurrent double-approval can't double-grant, repeatable across distinct orders.
- `20270902100836_papic_free_event_pool_seed.sql` — admin-tunable `free_grant_points` (default 50) + an `AFTER INSERT ON events` trigger seeding ONE 50-pt `free_grant` per event (no per-seat reserve; new events only, no backfill).
- `20270902148488_papic_pool_binding_grants.sql` — `CREATE OR REPLACE papic_event_pool_status` to apply on flat-pass OR any grant, with the guest-derived base computed ONLY for the legacy flat pass (a grant-only event meters at exactly `SUM(grants)`, never the `clamp(guests×150,5000,30000)` ceiling — so a ₱100 One buyer is capped at 250, a Free event at 50); and `CREATE OR REPLACE` the 6-arg `papic_record_guest_capture` so a grant-driven event may record via guest phones (ownership-via-pool) and its per-guest-150 yields to the pool (the reserve is the real cap). `papic_event_points_remaining` + `papic_reserve_event_points` read pool_status, so both re-point with no edit.

**FIX (red-team high · Fix 1): FREE tier no longer keeps a per-seat reserve.** The metering flip previously touched only `mini`, leaving `papic_tier_config.free.points_per_day = 20` — so a free event's 3 seats still metered a hidden 20-pt/day per-seat budget, contradicting §0's "ONE shared 50-pt pool, NO per-seat reserve" invariant AND the free-seed migration's own header comment. Flipping `free → NULL` (mirroring exactly what the PR did for `mini`) makes the per-camera reserve pass through, so a free seat draws the single 50-pt `free_grant` pool exactly like guest phones. Proven by the new test (d).

**Code:**
- `lib/sku-activation.ts` — `grantPapicCameraPoints` hook registered on service_key **`PAPIC_CAMERAS`** (the per-camera buy order — NOT the mini seat sku_code `PAPIC_CAMERA_MINI_DAY`); delegates to `papic_grant_camera_points`; non-fatal; reversal already handled by the existing `reversePapicPassPoints` (deletes grants by `order_id` regardless of source).
- `app/api/papic/guest-capture/route.ts` — the guest capture gate meters the ONE event pool: an orphan-byte pre-check (fail-OPEN, MAXINT no-op for non-applies events) + an AUTHORITATIVE, fail-CLOSED reserve around the record RPC, releasing on record failure. Symmetric to the seat path (`app/papic/actions.ts`, already wired). Replaced the per-guest `fetchGuestQuota` gate. The two pool-gate RPC calls are now **extracted into `lib/papic-event-pool-gate.ts`** (`papicEventPoolPreCheckExhausted` + `papicReserveEventPoolForCapture`) — server-only-free so the route's reserve-before-record ordering + FALSE→409 mapping are unit-testable (the route itself pulls the Next `server-only` virtual module and can't be imported in the test runner); behavior byte-identical.
- `lib/papic-tier-copy.ts` — `papicFreeGrantPoints` + `PAPIC_FREE_GRANT_POINTS_FALLBACK` (50) so the free-pool size is config-sourced, never a scattered literal.

**Tests** — `lib/papic-pool-metering.test.ts` (end-to-end over the real replayed schema in PGlite): (a) **Pool binding** — a priced grant binds on photo (1 pt) AND clip (3 pts), refuses at N+1, never partially books; (b) **Free pool cap** — the trigger seeds exactly 50, the pool applies, the 51st capture is refused, and a free event records via the pool; (c) **One grant** — 2 paid mini cameras = 500 pooled pts, idempotent by order; **(d · Fix 1) Free shared pool** — a `tier='free'` seat's per-camera reserve passes through with no `papic_seat_day_usage` row (30 reserves, well past the retired 20/day cap) and a seat capture decrements the same 50-pt pool a guest phone reads, with the 51st across BOTH refused; **(e · Fix 3) Route gate** — the route's extracted pool gate books to exhaustion and refuses the (N+1)th with the verdict the route maps to 409 `camera_points_exhausted`. Plus a `papic-copy-guardrails.test.ts` guard that the free-pool 50 comes from config.

**Migration-replay harness:** added `20270832295038_setnayan_ai_event_reach_matrix.sql` (merged from main via #3489) to `tests/db/replay-migrations.ts` `ALLOWED_SKIP` — it writes `applicable_event_types = ARRAY[…,'dinner_date']` but no migration seeds `dinner_date` into `event_type_vocab` (prod-only vocab, HOLD-OWNER per `20270825054104`), so the `validate_applicable_event_types` trigger rejects it on a FRESH replay. Prod-data dependency, not a schema bug; flagged for a separate main-side vocab-seed fix.

**Owner sign-offs surfaced (do NOT auto-merge):** (1) Papic One (`mini`) is LIVE today (is_active=TRUE from #3486) — its metering now becomes 250/camera pooled + window-total; if One must stay dark until the redesign, that's a separate `is_active=FALSE`. (2) `mini.points_per_day→NULL` AND `free.points_per_day→NULL` are live metering changes. (3) every NEW event gets a 50-pt `free_grant` + ownership-via-pool ⇒ `/api/papic/guest-capture` accepts up to 50 captures on ANY event (intended universal-free-Papic; no new doorway surfaced this PR). (4) hook keys on `PAPIC_CAMERAS`, not the seat sku_code the spec §4-PR3 text names. (5) numbers follow §0 (250/₱100/repeatable, Free 50), superseding §4-PR3/PR5's pre-override 300/₱150/90.

SPEC IMPACT: Implements `0012_papic/Papic_One_Pool_Model_Spec_2026-07-22.md` §0 (PR3/PR4/PR5 foundation). No corpus edit required — the code now conforms to the already-written §0 owner-locked numbers; §4-PR3/PR5's pre-override figures (300 pts / ₱150 / Free 90 / 1-non-repeatable seat) are explicitly superseded by §0 per the spec's own "§0 wins" rule. Owner sign-offs above are flagged for review; the paid rungs remain provisionally priced per §1.4/R1 (unbuilt purge) and ship dark.
