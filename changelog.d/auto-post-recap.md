# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-05 · feat(social): auto-post event recap to Setnayan FB + IG

When an event completes and the couple publishes their public `/[slug]/recap`, Setnayan now auto-shares that recap to its OWN Facebook Page + Instagram Business account through the EXISTING social auto-publish pipeline — no new posting path, no cron.

- **New `lib/social/recap-post.ts` · `composeRecapSocialPost(eventId)`**: renders the polished recap card (reuses `renderRecapOgJpeg` · `lib/social/recap-card.tsx`) → uploads it to R2 `setnayan-media` at a deterministic key `social/recap/{eventId}.jpg` (a STABLE public URL, never a presigned/expiring one — Facebook's `/photos` branch needs an image-looking URL, Instagram requires a public `image_url`) → inserts one `social_posts` row (`source_type='event_recap'`, `source_ref=event_id`). Composed-ONCE per event via the partial-unique index; NEVER-THROWS; gated on a per-feature toggle.
- **Trigger** (`app/dashboard/[eventId]/studio/papic/recap/actions.ts` `publishRecap`): fires `after(composeRecapSocialPost + runSocialFlush)` — off the couple's critical path, cron-free (house `after()` style). Inert when Meta isn't configured (nothing dispatches until the owner arms `autopublish_enabled` + supplies `META_PAGE_ID`/`META_PAGE_ACCESS_TOKEN` + `IG_USER_ID`).
- **Dispatch reuse**: the existing `lib/social/flush.ts` dispatch publishes the row to Setnayan's FB (`postToFacebookPage`) AND IG (`postToInstagramFeed` — already present in `lib/social/instagram.ts`, two-step `/media`→`/media_publish`, Graph v21.0) with the same governor slot, per-row claim, and master-switch + per-platform enabled+configured gates as every other auto-post. IG credentials already flow through `resolveMetaConfig().igUserId`.
- **New `event_recap` card**: `lib/social/card.tsx` gains an `event_recap` `CardContext` + body, and `/api/social/card/[postId]` resolves it via `loadRecapCardData` — the on-the-fly fallback image when R2 is unconfigured. Admin Social Queue surface + flush `PostRow` union carry the new source type (label "Event recap").
- **Migration `20270519100000_social_event_recap_source.sql`**: extends `social_posts.source_type` CHECK to allow `event_recap`; adds `social_publish_settings.recap_autopost_enabled BOOLEAN DEFAULT TRUE` (per-feature toggle, independent of the master switch). Guarded/idempotent; RLS unchanged (both tables already admin-only).
- **Wave 3 brief** (corpus): `03_Strategy/Auto_Post_To_User_Pages_Wave3_Brief_2026-07-05.md` — extending this to a vendor's/couple's OWN FB Page + IG Business account (needs `pages_manage_posts` + `pages_read_engagement` + `instagram_content_publish`, Meta App Review + Live mode, per-user OAuth reusing the Wave 2 `vendor_ig_connections` pattern). NOT built here.

Owner arm-up env vars (all optional / inert-until-set): `META_PAGE_ID`, `META_PAGE_ACCESS_TOKEN`, `IG_USER_ID` (or the Integration Console DB equivalents), `R2_PUBLIC_URL` + R2 creds for the baked card, plus flipping `social_publish_settings.autopublish_enabled` + `instagram_enabled` ON. `recap_autopost_enabled` defaults ON.

SPEC IMPACT: None — additive reuse of the shipped social auto-publish pipeline (`Social_Sharing_Program_2026-06-12.md` § 8) for a new `event_recap` source type; no pricing change, no new SKU. New Wave 3 strategy brief added to the corpus (`03_Strategy/`) as forward-looking design only.
