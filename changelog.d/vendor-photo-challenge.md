## 2026-07-22 · feat(vendor-billing): Photo Challenge — a sellable, per-(vendor,event) ₱400 add-on that gates the flag-dark Papic Games vendor challenges

Owner-locked 2026-07-22: **Photo Challenge** — a booked **Pro/Enterprise (verified)** vendor pays a **flat ₱400 / event** to SPONSOR guest photo-engagement missions (the already-built, flag-dark Papic Games / missions feature, `apps/web/lib/papic-games*.ts` + `papic-missions.ts`) at a booked event where **Papic is active**. **PAPIC-GATED** (only offerable when Papic is active on that event), **booked-vendors-only on their OWN events** (no third-party placement), and **FREE + inclusive for guests/couple** — the vendor pays, guests play free. This resolves the ⚠ PRICING GAP flagged in `20270902380131` (the vendor custom-challenge RPC enforced only the buildable Pro+-unlimited half because there was no per-(vendor,event) entitlement marker). The underlying Papic Games runtime is UNCHANGED and stays behind the GLOBAL master switch `NEXT_PUBLIC_PAPIC_GAMES_V1` (default OFF, **not flipped**).

**Migration** `supabase/migrations/20270907628470_vendor_photo_challenge_sku.sql` — (1) extends the `vendor_billing_catalog` `offering_type` + `vendor_billing_shape` CHECKs with a new **`vendor_addon_per_event`** value (same drop/recreate pattern as `20270905761946`; includes every prior value incl. `vendor_addon_recurring`); (2) INSERTs the admin-managed **`vendor_photo_challenge`** SKU (₱400, `vendor_addon_per_event`, display_order 83, `ON CONFLICT DO UPDATE` that never stomps an admin price edit); (3) creates **`papic_photo_challenge_sponsorships`** (per-(event, vendor_profile) entitlement · `UNIQUE(event_id, vendor_profile_id)` · RLS at CREATE TABLE — vendor-own read + couple/coordinator read + admin all); (4) `CREATE OR REPLACE`s **`papic_create_vendor_challenge`** to require a paid sponsorship row for (event, caller) on top of the existing booked + Pro/Enterprise/Custom gates. Idempotent.

**Resolver + gate** `apps/web/lib/vendor-photo-challenge.ts` — pure `resolveVendorPhotoChallengePricePhp(cyclePricePhp)` (catalog value → ₱400 fallback; no trial), pure `photoChallengeEligibility({ tier, verification, booked, papicActive, alreadySponsored })` (tier→verification→booked→papic→sponsored, shared by the action + UI), plus `fetchVendorPhotoChallengePricePhp` / `fetchPhotoChallengeSponsored` DB readers (client passed in, so the module stays unit-testable). Tests in `vendor-photo-challenge.test.ts` (13 cases: price flow/fallback + every deny reason + gate order).

**Purchase action** `apps/web/app/vendor-dashboard/clients/[eventId]/photo-challenge-actions.ts` — `sponsorPhotoChallenge` rejects tier<Pro · unverified · not-booked-on-event · Papic-not-active · already-sponsored **before pricing** (the orders spine has no other gate for a vendor add-on), then re-reads the authoritative ₱400 + `is_active` server-side. Apply-then-pay: a `submitted` `orders` row (event_id + vendor_profile_id set) + a pending `payments` row → `/admin/payments`. **No free first cycle** (per-event, not a subscription — the owner set a trial only for the AI + 3D add-ons).

**Entitlement gate (the not-a-fake-door part)** — `activatePhotoChallengeSponsorship` hook in `lib/sku-activation.ts` writes the `papic_photo_challenge_sponsorships` row on paid-order approval (idempotent via ledger + `ON CONFLICT DO NOTHING`). The `papic_create_vendor_challenge` RPC now requires that row before a vendor may author a challenge — so paying is the ONLY path to a live vendor Photo Challenge, and admin approval is the payment-verified handshake.

**UI** — `clients/[eventId]/_components/vendor-challenge-section.tsx` (booked-only mount) surfaces honest states: **Sponsored** badge + author box · eligible → "Sponsor a Photo Challenge — ₱400" buy CTA (`photo-challenge-buy.tsx`, BDO/GCash apply-then-pay) · not-eligible → the plain reason (below Pro / unverified / Papic not active).

**Wired vs. needed:** the ₱400 sale, the per-(vendor,event) entitlement, and the RPC gate are fully wired end-to-end (buy → admin approve → sponsorship row → author gate). Still needed to make it visibly live for guests: the owner flips the GLOBAL `NEXT_PUBLIC_PAPIC_GAMES_V1` master switch (kept OFF), and the migration must be pushed (`supabase db push`).

**⚠ Reconciliation with the just-merged #3515 (`20270906348207`, owner, same day):** that PR made custom challenges eligible for **Solo/Pro/Enterprise/Custom** at ₱400/event but **deferred collection** ("a per-event apply-then-pay gate is an owner decision for when paying vendors exist"). This PR IS that gate — so it (a) replaces free-during-launch authoring with a **paid ₱400 apply-then-pay** requirement (which #3515 anticipated), and (b) per THIS task's owner-lock, **narrows the tier gate back to Pro/Enterprise only** (drops `solo`). The Solo-inclusion question is the one genuine contradiction between the two same-day owner decisions — **owner must confirm which wins.** Flipping it back to Solo is a one-line change in the migration's tier list + `isTierAtLeast('solo')` in `lib/vendor-photo-challenge.ts`.

SPEC IMPACT: New owner-locked vendor SKU + pricing rule (Photo Challenge ₱400/event, Pro/Enterprise + verified + booked + Papic-active). It also **puts the previously-free custom challenge behind ₱400/event** and **narrows #3515's Solo eligibility to Pro/Enterprise** (see reconciliation note above — needs owner sign-off on the Solo question). Not yet reflected in the corpus (`Pricing.md`, `0012_papic/Papic_Games_and_Vendor_Missions_Spec_2026-07-21.md` § 3.4). Code is canonical per the 2026-06-07 ground-truth flip; flagged for the owner to fold into the pricing corpus + `DECISION_LOG.md` on sign-off.
