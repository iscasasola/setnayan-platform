## 2026-09-20 · fix(deps): close 10 of 13 open Dependabot alerts, document the 3 that stay open

Pre-launch Dependabot sweep (13 open alerts: 5 high, 7 moderate, 1 low). Fixed via
`pnpm.overrides` in the root `package.json` (no direct-dependency bumps were
needed — every hit was transitive) except where noted below:

| Alert | Severity | Package | Manifest | Action |
|---|---|---|---|---|
| #96 | high | js-yaml | pnpm-lock.yaml | Bumped override target `js-yaml@>=4.0.0 <4.3.1 -> ^4.3.1` to `<4.3.2 -> ^4.3.2` (the prior floor, 4.3.1, was itself inside the newly-disclosed vulnerable range). eslint/`@eslint/eslintrc` only — lint-time, not shipped. |
| #93 | moderate | baseline-browser-mapping | pnpm-lock.yaml | New override `>=2.0.0 <2.11.0 -> ^2.11.0` (resolved 2.11.25). Transitive via browserslist/babel/autoprefixer — build-time target resolution only, not imported by any runtime route. |
| #92, #47 | high | sharp | apps/mobile/package-lock.json | **Dismissed as dev-only — already documented.** `apps/mobile/package.json`'s own `comments.sharp-and-uuid-left-alone` (npm `overrides`, separate from the root pnpm workspace) already decided this: sharp 0.32.6 is a devDependency of `@capacitor/assets`, runs only by hand on the maintainer's machine over the repo's own icon files, never in CI, never at app runtime. Bumping crosses three sharp release lines (0.32 -> 0.35) that removed APIs and repackaged prebuilt binaries. No action taken this sweep; owner already signed off. |
| #91, #90, #89 | moderate | fflate | pnpm-lock.yaml | Three overrides, one per parent's pinned minor, all for the same infinite-loop advisory (GHSA-px8p-9vwx-vf98): `>=0.6.0 <0.6.11 -> ^0.6.11` (three-stdlib, via @react-three/drei — client 3D), `>=0.4.5 <0.4.9 -> ^0.4.9` (posthog-js — client analytics), `>=0.7.0 <0.7.5 -> ^0.7.5` (`@shuding/opentype.js` via satori, backs the live `/api/og/*` routes). 0.8.3 (@types/three chain) was already out of every vulnerable range. Two of the three parents are real runtime paths (browser + server route), not dev tooling. |
| #83, #82 | high | browserslist | pnpm-lock.yaml | New override `<=4.28.6 -> ^4.28.7` (resolved 4.29.0) covers both GHSA-c83g-rgw3-j3cx and GHSA-73wf-gq98-2v4g at once — same package/version. Build-time only (autoprefixer/babel), confirmed by `grep -rn "from 'browserslist'"` across `apps/web` returning zero hits. |
| #81 | low | postcss-selector-parser | pnpm-lock.yaml | New override `>=6.1.0 <6.1.3 -> ^6.1.3` (resolved 6.1.4). Transitive via tailwindcss — build-time only. |
| #32 | moderate | @opentelemetry/core | pnpm-lock.yaml | **Left open — already documented, owner sign-off needed only if this changes.** `package.json`'s existing `comments.opentelemetry-core-1x-left-alone` explains why: the advisory's only patched line is 2.8.0+, there is no patched 1.x, and forcing `@sentry/node@8.55.2`'s 1.30.1 copy to 2.8.0 removes two exports (`VERSION`, `isWrapped`) Sentry imports by name, breaking server-side instrumentation at import time. `pnpm audit --prod` confirms this is now the ONLY remaining pnpm-side advisory. Real fix is a `@sentry/nextjs` major (v9+ ships OTel 2.x) — its own PR, not a lockfile bump. **For the owner: dismiss #32 on GitHub as "risk accepted" with a link to this entry, or leave open as a tracked exception — Claude Code does not dismiss Dependabot alerts via the API.** |
| #16 | moderate | uuid | apps/mobile/package-lock.json | **Dismissed as dev-only — already documented**, same `sharp-and-uuid-left-alone` comment as #92/#47: uuid 7.0.3 is used only by `@capacitor/assets`' own tooling chain, never at app runtime; the only patched line is 11.1.1 (4 majors up). No action taken. **For the owner: dismiss #16 on GitHub with the same reasoning, or leave tracked.** |
| #2 | moderate | glib | src-tauri/Cargo.lock | **Left open — no safe fix exists.** `cargo update -p glib --precise 0.20.0` fails: `gtk@0.18.2` (required by `tauri@2.11.5`, already the newest stable 2.x — the only newer release is `3.0.0-alpha.1`, a pre-release) pins `glib = "^0.18"`. The vulnerable code (`glib::VariantStrIter`, GTK/webkit2gtk backend) is Linux-only; `.github/workflows/build-desktop.yml` builds only `macos-latest` and `windows-latest` — this repo never produces a Linux build, so the vulnerable path is unreachable in any shipped artifact. **For the owner: dismiss #2 on GitHub as "not used" (Linux-only GTK backend, we ship macOS + Windows only), or track until Tauri 3 stabilizes.** |

**Verified:** `pnpm typecheck` (turbo, both packages) green · `pnpm --filter @setnayan/web test:unit` green · `pnpm --filter @setnayan/web build` (production) green · repo lint guards green. `pnpm audit --prod` now reports exactly 1 finding (`@opentelemetry/core`, the documented exception above) — down from the pre-existing baseline plus the 6 newly-disclosed advisories this sweep closes.

SPEC IMPACT: None — no product-facing behavior changed, dependency patch/minor bumps only.
