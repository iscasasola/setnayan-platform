## 2026-07-15 · feat(design): account spokes recomposition — Atelier-Glass (Glass PR-5)

The `(account)` spoke pages re-expressed onto the Atelier-Glass language (App_Wide_Glass_Rollout_Plan_2026-07-15 §5 PR-5 row · §3.2 treatment). PR-1 (#3251) already added `sn-ambient` to `(account)/layout.tsx`, so the spokes already sat on the home's warm canvas; this PR recomposes each spoke's expression. Every data source, server action, route, copy-fact, and feature flag is unchanged — this is a composition/expression pass, not a behaviour change. Token names (`terracotta`/`mulberry`/`bg-cream`/`warn-*`/`danger-*`) are preserved per the rollout's R6 name-preserving-remap rule (mass rename is a post-PR-9 sweep).

- **New `(account)/template.tsx` (§2a):** 3-line server component wrapping `{children}` in `.sn-page-enter`, so every in-shell spoke navigation gets the 400ms `sn-rise-soft` entrance (search-param changes don't remount → in-page filters don't replay). Mirrors the event-shell `[eventId]/template.tsx` idiom.
- **Setnayan AI spoke — the surface's ONE obsidian focal (§1.3):** the subscription-active box + the weekly-digest section are folded into a single `.sn-tile-dark` (`sn-bloom` entrance) carrying **plan state** (Active + covered-through date / Not subscribed / Coming soon — real `user_ai_subscription.active_until` + the per-user flag) and **guards on watch** (the live `computeUserAiDigest` money-guard interventions / digest, rendered only when there's real data). No other spoke gets a dark tile. Buy/coming-soon panel → `.sn-tile`; the subscribe component's Total box → `.sn-row` with mono ₱ amount.
- **Per-spoke chrome (§3.2):** back-links → ghost `.sn-chip` (`sn-press`); headers → `.sn-eye` eyebrow + `.sn-h1` (retires `text-3xl font-semibold` and the residual `font-serif` h1s on year/samahan/samahan-new); flat bordered `bg-cream` section containers → `.sn-tile`; repeated rows/option cards → `.sn-row` (no blur — blur-budget fallback); `font-mono` ALL-CAPS section labels → `.sn-sec`, micro eyebrow labels → `.sn-eye`; forms consume the PR-1-re-pointed `.input-field`/`.button-primary` inside tiles.
- **Library tab strip (§3.2):** the border-bottom tab bar → a `.sn-chip` row (`.selected` on active); the tab body wrapped in `key`-remounted `.sn-lens-swap` so switching tabs cross-fades. Life-Flash entry → `.sn-card`.
- **Notifications (§3.2):** page chrome recomposed (chip back-link + eyebrow + `.sn-h1`). The feed list is the SHARED `app/_components/notifications/notifications-list.tsx` (also consumed by `vendor-dashboard/notifications`), left untouched to keep the vendor surface byte-identical until its own coordinated PR — see deviations.
- **Profile (§3.2):** long settings page — header → chip/eyebrow/`.sn-h1`; all `font-mono` section labels → `.sn-sec`; bordered `bg-cream` panels (password, sessions, privacy, face-profile, share-consents) → `.sn-tile`; the info `dl` Rows, checkbox labels, personalize fieldset, and share-consent items → `.sn-row`; account-ID / dates stay mono. Warm-semantic status callouts (deletion-pending warn, delete/face-erasure danger) keep their tints. (Profile has no tab strip today, so the §3.2 "profile tab strip → chips" note is n/a; the tab strip that exists is on **library**, which got that treatment.)
- **Numerals/dates mono, one gold primary per view, warm semantics, reduced-motion covered, `lint:radius` clean.** Scope untouched: `(launcher)/`, `[eventId]/**`, `vendor-dashboard/**`, `admin/**`, marketing, `app/[slug]`.

Spokes touched: setnayan-ai (+subscribe cmp), notifications, profile, profile/concierge, year, api-keys, library, people, create-event, life-flash, samahan, samahan/new.

**Deviations (surfaced for sign-off):**
- **create-event** — §3.2 says "single centered `.sn-tile`", but the flow is the owner-locked (2026-07-10) full-width "maximize screen space" event-type GRID, not a narrow 3-question card. Wrapping it in a constraining tile would fight that lock, so the page chrome is recomposed and the grid stays full-width. Zero flow/copy changes, as required.
- **notifications feed** — the row list is a shared component with `vendor-dashboard/notifications` (a do-not-touch scope). Left intact so vendor renders byte-identical; the `.sn-row`+`.sn-live-dot` feed treatment lands when vendor notifications are recomposed coherently (PR-6/7 territory).
- **samahan/[communityId]** (597-line detail hub, a mini-dashboard) — beyond the spoke-hero scope of this PR; sits correctly on the ambient + template entrance already, its internals a follow-up.

SPEC IMPACT: App_Wide_Glass_Rollout_Plan_2026-07-15.md (§5 PR-5 row implemented; §3.2 treatment applied). No corpus decision reversal — the two rollout decisions (R1 wash reversal, R2 scope amendment) were logged with PR-1.
