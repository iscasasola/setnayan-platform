# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-08 · fix(plan3d): carry FigureSpec.scale through the instanced seated crowd (pixel-identity guard)

Triage of the instancing slice caught a second latent pixel-divergence: `<InstancedSeatedCrowd>` silently dropped the figure's per-root `scale`. The individual `<Figure>` mounts its whole rig (body + status ring) under `<group scale={spec.scale ?? 1}>` (figure.tsx), but the batch composed only `seat.matrix × bakedLocal[part]` at unit scale — so any seated occupant whose spec carried `scale !== 1` (the documented "kids / principal-sponsor / VIP sizing" field on `FigureSpec`) would render full adult size in the batch while the individual `<Figure>` it replaces rendered scaled. At the walk-in `'done'` handoff (individual → batch) that guest would pop size — the exact break the pixel-identity guarantee exists to prevent. Latent today (no seated batched caller set `scale`), but an invitation to silently break the crowd the moment child/VIP sizing is adopted.

- **`kit/instanced-seated-crowd.tsx`** — `SeatedInstance` gains an optional `scale` field; a new `seatInstanceMatrix()` composes `seat.matrix × scale × partLocal` (uniform scale about the figure-root origin === mounting the part under `<group scale={sc}>`), applied to every body part AND the status ring so a scaled occupant lands byte-identical to the individual figure. `sc === 1` keeps the existing allocation-free unscaled path (the neutral crowd pays nothing). The placement helpers (`seatRootMatrix` / `seatedFigureMatrix`) stay pure position+yaw — scale is composed once, in the component, from the `SeatedInstance` data.
- **`plan3d-scene.tsx` (demo) + `seating-lab-3d.tsx` (lab)** — the two callers that carry a `spec` now pass `scale: spec.scale` / `tok.spec.scale` into the batch, so child/VIP sizing flows through instead of being dropped. The public walk feeds neutral strangers with no spec (scale 1) — unchanged.
- **`lib/figure-sit-bake.test.ts` (+1 test · now 8)** — pins the new invariant: `seatRoot × scale × baked` matches an individual figure whose root is `<group scale=s>` at s ∈ {0.8, 1, 1.1} (≤1e-8), across all 14 parts.

**Three-surface coverage (restated for the owner):** all THREE seat-plan surfaces — the phone-first **public guest walk** (`guest-venue-3d.tsx`), the homepage **demo** (`plan3d-scene.tsx`), and the couple **lab** (`seating-lab-3d.tsx`) — now collapse their STATIC seated crowd into `<InstancedSeatedCrowd>` at **low** quality (the walk is always low; demo + lab gate on `quality==='low' || reducedMotion` / `crowdLow || far || reduced`). At **high** quality every seated figure stays an individual `<Figure>` and keeps its `FigureFrameDriver` **idle sway** (±torso sway + occasional head turn) — the shipped high-quality look is untouched on every surface. The **walk→sit handoff is transform-identical**: the instanced steady-state reproduces `<SeatedAvatar>` / the individual `<Figure>` bit-for-bit (now including per-root scale), so when the walk-in `'done'` state hands off to the batch the figure lands on the same world transform — no pop, gap, or size jump.

**Before/after draw-call across surfaces** (seated crowd, per surface, at N occupied seats): **before** ≈ `14 × N` color-pass draws + `N` per-figure `useFrame` subscribers (the public walk at 250 pax ≈ **3,200 draws + 250 subs**); **after** ≈ **14 InstancedMesh draws (+1 optional status-ring batch) + 0 per-figure `useFrame`** — flat in occupant count on all three surfaces at low quality.

SPEC IMPACT: None — no product surface, pricing, or schema change; the seated look is preserved pixel-for-pixel at every quality, now including scaled (child/VIP) occupants. Rendering-direction sign-off note from the slices below still stands (no PR opened).

## 2026-07-08 · perf(plan3d): wire InstancedSeatedCrowd into the demo + couple lab (static path only)

Completes the follow-up flagged in the slice below ("demo + lab left individual"): both remaining seat-plan surfaces now collapse their STATIC seated crowd into `<InstancedSeatedCrowd>`, mirroring the already-shipped public walk. The gate is the SAME condition that already makes an individual `<Figure>` static, so at `quality='high'` + motion every seated figure stays individual and keeps its `FigureFrameDriver` idle sway — **the shipped `quality='high'` look is untouched on both surfaces.**

- **`app/_components/plan3d/plan3d-scene.tsx` (homepage demo · WIRED)** — collect predicate `quality==='low' || reducedMotion`. Non-photo seated guests (not the active walk/sit/roam guest) drop their body into one room-level batch; matrices via `seatRootMatrix(pos.x, pos.z, heading)` (the demo's flat position+heading placement — the individual figure here has no table→seat nudge/flip), per-instance tint = the SAME resolved attire colour the individual `<Figure>` wears (`spec.outfitColor`, post-palette-v2 `resolveAttirePaletteColor`), ring = `spec.statusColor` (`SIDE_COLOR[side]`). `GuestToken` gained a `bodyless` prop: an instanced guest's body moves into the batch but its per-guest QR-mint hit/hover cylinder (`GUEST_HIT_GEO`) stays individual, so tap-to-open-card + the hover shell keep working. Photo guests (billboard head) + the active guest stay full `<GuestToken>`. Batch `quality={quality}` so its shadow pass matches the individual figures'.
- **`app/dashboard/[eventId]/seating/lab/_components/seating-lab-3d.tsx` (couple lab · WIRED)** — instances only the STATIC steady-state `<SeatedAvatar>` crowd, gated `crowdLow || far || reduced` (the exact condition that makes a lab figure static). The batch is mounted INSIDE each table's animated `<group>` and fed TABLE-LOCAL matrices (`seatedFigureMatrix` with an identity table = the seat group × the −0.04 nudge / π flip `<SeatedAvatar>` renders), so it rides the drag slide-lag / scale-pop / rotation exactly like the individual figure did — the parent group matrix applies to both paths. Only CONFIRMED (opacity 1), non-photo real guests join; tentative guests (translucent-ring semantics), photo seats, and the "+1 reserved" ghost stay individual. Seated 3D figures carry no tap interaction in the lab (swap is driven from the 2D panel via `onGuestTap`), so no hit-proxy is needed here. Tint = `tok.spec.outfitColor`, ring = `tok.spec.statusColor`.
- **`lib/figure-sit-bake.test.ts` (+1 test · now 7)** — pins the lab's decomposition invariant: `parentGroup(home, tableFaceY) × seatedFigureMatrix(identity-table, seat)` equals the full-world `seatedFigureMatrix(home, tableFaceY, seat)` at rest, and equals an individual figure mounted under the SAME table group when that group is drag-scaled (1.06). This is the math behind the seamless settle handoff.

**Walk→sit→settle transform-identity (the operator's live-verify concern):** during populate-Play a lab guest is drawn individually the whole way — walk `<Figure>` → live `<SitController>` (flush-seated) → CrowdField `'done'` end-state (already transform-identical to `<SeatedAvatar>`, per its existing guarantee). The instanced steady-state figure reproduces `<SeatedAvatar>` bit-for-bit (proven), so when `settleCrowd` unmounts `<Crowd>` and the guest reappears in the batch, it lands on the identical world transform — **no pop/gap/jump.**

**CrowdField `'done'` / reduced agents left individual (deliberate — SURFACED for owner):** the change-map listed the CrowdField end-state as an instancing target, but it is TRANSIENT (Crowd unmounts on `settleCrowd`, handing directly to the now-instanced steady `<SeatedAvatar>`), and `<InstancedSeatedCrowd>` keys its InstancedMeshes on occupant count — so instancing the incrementally-growing `'done'` set would force a full GPU batch recreate on every single sit during the walk-in finale, exactly the moment the operator live-verifies. Since the persistent draw-call win already lives entirely in the steady `<SeatedAvatar>` path and the handoff stays seamless without it, instancing the transient end-state is churn/flicker risk for ~zero benefit. Left as individual figures; flagged here so the owner can override if they want the end-state instanced too.

SPEC IMPACT: None — no product surface, pricing, or schema change; the seated look is preserved pixel-for-pixel at every quality on both surfaces. Same rendering-direction note as the slice below (instanced seated crowd awaits owner sign-off before shipping; no PR opened, operator live-verifies the lab walk→sit handoff first).

## 2026-07-08 · fix(plan3d): scope InstancedSeatedCrowd to static figures (pixel-identity guard)

Triage of the instanced-seated-crowd slice caught a latent pixel-divergence footgun: `<InstancedSeatedCrowd>` defaulted to `quality='high'` and its docstring claimed a blanket "pixel-identity" with the individual `<Figure pose="sit">`. But the batch bakes the CONSTANT `sitPose()` once and has NO per-frame path — its `quality` prop only gates the shadow pass — whereas an individual seated `<Figure>` at `quality='high'` (its own default, motion allowed) mounts `FigureFrameDriver`, which layers a live `idleSway(id, t)` (a slow ±torso sway + occasional head turn) over `SIT_BASE` every frame. So the two are byte-identical only against a STATIC figure (`quality='low'` OR reduced motion), not at the component's own advertised default. The single shipped call site (`guest-venue-3d.tsx`) already passes `quality='low'`, so there is NO divergence in production today; the `'high'` default + unqualified guarantee were an invitation to reintroduce the sway-vs-frozen mismatch the moment another surface mounted the batch on defaults.

- **`kit/instanced-seated-crowd.tsx`** — default `quality` flipped `'high'` → `'low'` (a careless mount now matches the static bake it replaces; the wired call site was already explicit `'low'`, so runtime behaviour is unchanged). Docstring pixel-identity guarantee re-scoped to STATIC seated figures, with an explicit note that the batch never animates and `quality` here gates only shadows. `quality`-prop JSDoc corrected to say the same.

**Draw-call win (unchanged by this fix — restated for the owner):** the seated crowd drew ~14 non-instanced meshes PER occupant, one `<Figure pose="sit">` per occupied seat, no cap/LOD/instancing. On the phone-first public guest walk at 250 pax that was ≈ **14 × N = ~3,200 color-pass draws + ~250 `useFrame` subscribers**. Instanced, the whole seated room collapses to **~14 InstancedMesh draws (+1 optional status-ring batch) + 0 per-figure `useFrame` subscribers** — the phone draw ceiling drops from **14×N to ~14**, flat in occupant count.

**LOD / hard-cap recommendation (evaluated, NOT shipped — needs owner sign-off):** a seat cap or distance-LOD would push the phone budget further, but BOTH change the locked look (a cap renders fewer figures than were seated; an LOD swaps silhouettes at range) — so they are deliberately out of this slice. Recommendation: the instancing win alone already flattens the ceiling to ~14 draws, so a cap/LOD is not needed for the 250-pax phone target and should ship (if ever) only as an explicit owner-approved look change, never folded in silently.

SPEC IMPACT: 0008_seating_chart_editor/0008_3DPlan_Fable_Design_2026-07-08.md — seated crowd instanced (pixel-identical); draw-call ceiling on phones cut from 14×N to ~14. LOD/hard-cap options evaluated but NOT shipped — pending owner sign-off as they alter the locked look.

## 2026-07-08 · perf(plan3d): instanced seated crowd — collapse the phone-walk draw ceiling

> ⚠ OWNER SIGN-OFF PENDING — this changes the seated-crowd RENDERING PATH (individual articulated meshes → instanced), which brushes the owner-locked "one articulated figure everywhere" direction. The LOOK is preserved pixel-for-pixel; the direction nuance is flagged for sign-off before this ships. No PR opened.

The seated crowd drew ~14 non-instanced meshes PER occupant, and the three seat-plan surfaces mounted one `<Figure pose="sit">` per occupied seat with no cap/LOD/instancing — the phone-first public guest walk hit ≈3.2k color-pass draws + ~250 no-op `useFrame` subscribers at 250 pax (quality 'low' dropped shadow casters + per-frame joints but NOT the draw count or the subscription). Because the sit pose is a CONSTANT (`sitPose()` — no id-variation, no time term), every seated figure shares the identical baked joint transform, so the whole seated crowd collapses to ~14 InstancedMesh draws.

- **`lib/figure-sit-bake.ts` (new, pure + unit-tested)** — the SINGLE SOURCE of the rig proportions, per-mesh leaf offsets/scales, joint-length denominators, and `applyPose`, hoisted out of `kit/figure.tsx` (which now imports them; render output byte-identical). `buildSitBakedLocals()` reads each of the 14 seated body parts' local matrix under the sit pose ONCE; `seatRootMatrix` / `seatedFigureMatrix` compose a seat's figure-root world matrix (the latter reproduces the exact table→seat→nudge nesting the surfaces mount `<Figure>` under). One definition drives both the rendered figure AND the baker, so the instanced crowd can never silently diverge from the individual figure it replaces.
- **`lib/figure-sit-bake.test.ts` (new · 6 tests)** — the pixel-identity proof in math form: baked locals equal an INDEPENDENT reference rig (figure.tsx's nesting re-typed with literal proportions), and `seatRootMatrix`/`seatedFigureMatrix × bakedLocal[part]` reproduces the world position an individual nested-group `<Figure>` lands each part at (≤1e-8). Plus determinism, part-count = 14, and the `setColorAt` tint rule (white ≡ neutral, hex ≡ tint).
- **`kit/instanced-seated-crowd.tsx` (new)** — `<InstancedSeatedCrowd>`: one InstancedMesh per body part (reusing the IDENTICAL geometry buffers now exported from `figure.tsx`) + one shared white `MeshStandardMaterial` at the mannequin roughness/metalness, tint via `setColorAt` (`instanceColor` multiplies over white → pixel-identical to `mannequinMaterial(tint)`). Optional instanced status-ring batch (unlit, DoubleSide, per-instance colour; occupants without one zero-scale out; skipped entirely when nobody has a ring). `key={count}` recreates on occupant-count change; `frustumCulled={false}` + `castShadow` honoring quality, matching `InstancedChairs`/`<Figure>`. NO per-figure `useFrame`.
- **`kit/figure.tsx`** — consumes the shared constants; the per-frame joint loop moved into a `<FigureFrameDriver>` child mounted ONLY while animating, so a STATIC (quality 'low' / reduced-motion) individual figure now holds ZERO `useFrame` subscriptions instead of one no-op subscriber apiece. Geometry buffers + ring placement exported for the instanced path.
- **`app/[slug]/venue/_components/guest-venue-3d.tsx` (WIRED — the goal surface)** — neutral, ringless strangers collapse into ONE room-level `<InstancedSeatedCrowd>`; photo seats (billboard head) + the viewer's own seat (accent + gold ring) stay individual. Draw math at 250 pax: ~3.2k seated-figure color draws + 250 `useFrame` subs → ~14 instanced draws + 0 subs. Pixel-identical because this walk is ALWAYS quality 'low' (static baked pose).

**LOD / cap deliberately NOT built (evaluated, left for owner sign-off):** the goal called for the pixel-identical instancing core only. A seat cap or distance-LOD would change the shipped look (fewer/again-different figures), so those are out of this slice.

**Demo (`plan3d-scene.tsx`) + couple lab (`seating-lab-3d.tsx`) left individual (evaluated):** their seated figures animate an idle sway at quality 'high', so instancing them there would DROP that motion = a look change; pixel-identity holds only for STATIC ('low') seated figures. They also attach a per-figure status ring AND per-figure interaction (demo: tap-to-open-card hit target; lab: chair taps + the sit-choreography walk→sit→done handoff + tentative-opacity ghosts). Correctly wiring them needs a quality-gated split (instance at 'low', individual at 'high') plus the ring batch (already supported) plus interaction hit-proxies — a follow-up once the direction is signed off. The `useFrame`-split already benefits their static figures.

SPEC IMPACT: None — no product surface, pricing, or schema change; the seated look is preserved pixel-for-pixel. The rendering-direction change (instanced seated crowd) awaits owner sign-off before shipping; no corpus edit applied yet.
