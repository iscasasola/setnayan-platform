# G2 — Vendor-portfolio Papic: the ledger and the price

**Model:** Fable 5.1 · **Effort:** high · **Wave:** after G1 (landed 2026-09-05) · **Blocks:** G3

Measured against `origin/main` @ `dd2288ebc` and the plan in `build-sessions/GIFTS-PLAN.md`
on 2026-09-05. Re-fetch and re-measure before you act.

🛑 **TWO OWNER ANSWERS MUST BE FILLED IN BELOW BEFORE THIS SESSION STARTS.** Both govern money.
If either line still reads `___`, stop and ask the owner; do not guess, do not "assume the
narrow reading".

Start a new Claude Code session and paste EVERYTHING below the rule.

---

Read the repo's own CLAUDE.md and the corpus CLAUDE.md first, then follow RULE 0: assume what you
are about to build already exists, and locate it before writing anything. On this stream RULE 0
has already paid twice — the couple's free-credit writer turned out to be a DB trigger, not the
module whose docblock claimed it; and "vendors can buy their own Papic" turned out to be a
DIFFERENT, already-shipped product with a different rate.

## The two answers this session cannot start without

- **OPEN #1 — Does the 2026-08-26 host-visible upload lane survive?** Owner answer: *"base it all from the supplier's shots per event not from what the host gives them."* (answered 2026-09-05 in the session that opened PR #5201 — this prompt has been RUN; do not run it again)
  The 2026-08-26 "SUPPLIER LANE, RULED IN FIVE PARTS" row in `DECISION_LOG.md` locked five things:
  the ₱5/point rate, video at 800 credits, uploads per event, buying shots, and host-approved
  access (`papic_missions.source = 'vendor'`, the DPO answer). On 2026-09-05 the owner said
  the new portfolio product **replaces** that allowance. Replacing the RATE is clear. Whether the
  supplier-uploads-finished-work-into-the-HOST's-gallery lane is retired too, or now runs on the
  new credits, is not — and the owner described the new product as *"not shared to the guests
  but their personal portfolio"*, the opposite destination. `retire` → delete that lane's
  credit plumbing. `keep on new credits` → repoint it. Either way the access chain
  (host approves, guest consent per photo) is untouched — it is a DPO ruling, not a rate.
- **OPEN #2 — "minimum of 1000" is the CEILING, yes?** Owner answer: *"yes. that is the maximum from booking fee."* (answered 2026-09-05, same session)
  He said *"minimum of 1000"* to the question "cap: 1,000 or 2,000?". Read as a ceiling of
  1,000. A FLOOR of 1,000 would contradict his own ₱1,000 → 50 rule and make the ₱500/25 pack
  pointless. One word from him settles it.

## Owner decisions already made — do not re-ask any of these

Verbatim, 2026-09-05, all in `DECISION_LOG.md` (the 🎁/📸 rows dated 2026-09-05):

- *"vendors get 5% of the amount they paid for on booking fee. so if they paid 1000 pesos for
  the booking fee, they get 50 papic credits for that event. if they import a user and get to
  sync with them for free. they pay 500 pesos for 25 papic credits. since they did not pay for
  booking fee, they only pay for the photo importation fee for their portfolio."*
- *"when we approve the payment"* — credits land on admin payment approval, never at order
  submission, never self-reported.
- *"replace it."* — this product supersedes the 2026-08-26 allowance. Both this stream and a
  Fable review had recommended keeping two pools; overruled. Record it in `DECISION_LOG.md` as
  a **reversal**, the way the 2026-08-26 row itself recorded its reversal of 2026-07-18.
- The product is the vendor's PRIVATE portfolio: their own dedicated shots, never the host's
  gallery, never a guest's.
- No floor. A ₱0 fee earns nothing and the vendor buys the pack. Tiny grants still land
  (Fable: *"grant the crumbs, sell the loaf next to them"*) with the ₱500/25 pack as the CTA
  beside them when the grant is under 25.

## 0. Measure first — never a local checkout, never a line number

```bash
git fetch origin
gh pr list --state open --limit 40 --json number,title,headRefName   # who is mid-flight
git worktree list                                                    # what this machine is building
git log origin/main --oneline -15
git worktree add --detach /tmp/wt-g2 origin/main && cd /tmp/wt-g2 && pnpm install --prefer-offline
```

A fresh worktree has no `node_modules`; before install, tests and tsc "pass" resolving nothing.
Anchor on strings with `git grep <symbol> origin/main -- <path>`; a line number rots between
fetches. Confirm PRs #5192 and #5193 are MERGED or still open — G2 does not depend on either,
but a still-open #5193 means `comp_grants.event_id` is not on main yet; irrelevant here unless
you touch `comp_grants`, which you should not.

## 1. What exists — find these before writing anything

- **The rate you are replacing:** `VENDOR_PAPIC_PHP_PER_POINT`, `VENDOR_PAPIC_BASE_GIFT_POINTS`,
  `VENDOR_PAPIC_MAX_POINTS`, `vendorPapicPointsForBookingFee` in `apps/web/lib/vendor-papic-tier.ts`.
  Wired through `fetchVendorBookingFeePaidPhp` → `allowancePointsFor` / `captureAllowance` in
  `apps/web/lib/vendor-papic-grants.ts`, pinned by `the-fee-reaches-the-allowance.test.ts`.
  🔑 Keep its two rules when you replace its number: **`null` is a failed read, never ₱0**
  (a failed read grants nothing); and **`waived_free5` means they paid nothing**.
- **The ledger:** `vendor_papic_capture_grants` (migration slug `vendor_papic_capture_counsel_gated`).
  It already has a `tier` column (`lite | ltd | unli`) and a per-(vendor,event) shape. Read it
  and the `VendorPapicTier` model before deciding whether the new product is a new `source`/
  `kind` on this table or something else. **A second table is the last resort** — and if one is
  born, `ugat-concept-coverage.db.test.ts` will demand a node in `apps/web/lib/ugat/graph.ts`.
- **The booking fee:** `booking_fee_charges`, read by `fetchVendorBookingFeePaidPhp`. Imports
  carry NO fee by design — `BookingFeeAttribution = 'sourced' | 'import'` → `waived_import` in
  `apps/web/lib/booking-fee-gate.ts`. So for an import the 5% branch has nothing to read; the
  pack purchase is the only path, and "when we approve the payment" means approval of THAT
  order.
- **The price table:** `vendor_billing_catalog` (`sku_code, title, price_php, offering_type,
  is_active`), migration slug `v2_pricing_table_alignment`, with `CHECK (price_php > 0)`. Every
  vendor SKU is a lowercase `vendor_*` row. Prices are admin-managed at render; **never derive a
  price from a code comment or from this file — read the table.**
- **The apply-then-pay approval:** find where a vendor order's payment is approved by an admin
  (`grep -rn "approve_vendor_subscription\|approveVendorOrder\|payment_approved" apps/web/app/admin`)
  — that is the hook where credits must land. Do not invent a second approval.
- **The video threshold:** `canCapture`'s `video_not_allowed` branch and the 800-credit rule in
  `vendor-papic-tier.ts`. "Replace it" did not say what happens to this. Do not silently keep or
  drop it — it goes in the PR body as a question, and the code keeps today's behaviour until
  answered.
- **What a vendor sees of their own shots today:** `apps/web/lib/vendor-own-captures.ts` — their
  captures on OTHER people's events. G3 builds the portfolio surface; G2 must leave a reader
  G3 can call, not a screen.

State, in one line each, what exists · what is missing · the delta. If you cannot name the
existing constant you are retiring and the existing ledger row shape, you have not searched enough.

## 2. The delta

1. **Pure function, replacing the old one:** `vendorPortfolioCreditsForFee(feePhp)` =
   `floor(fee × 0.05)`, capped at the OPEN #2 answer, **no floor**, `null`/`NaN`/`≤0` → 0.
   Unit tests: ₱1,000 → 50; ₱20 → 1 (crumbs still land); ₱0 → 0; ₱30,000 → cap; `null` → 0.
   Retire `vendorPapicPointsForBookingFee` and its three constants in the SAME PR; update
   `the-fee-reaches-the-allowance.test.ts` rather than deleting it — the wire it pins still
   matters, only the number changed.
2. **The pack SKU:** one `vendor_billing_catalog` row, ₱500 → 25 credits, `is_active = TRUE`,
   title in the owner's words. Migration via `pnpm migration:new` — never a hand-typed prefix.
   `check-migration-timestamps.mjs` enforces UNIQUE; a prefix below prod's head still applies
   (`db push --include-all`) — the ordering rule is for the PGlite replay only.
3. **Credits on approval:** in the approval hook found in §1, when a vendor booking-fee payment is
   approved → grant `vendorPortfolioCreditsForFee(fee)` to the ledger for (vendor, event); when a
   pack order is approved → grant 25. Idempotent per order (a partial unique index or an
   `ON CONFLICT` keyed on the order id — read how `papic_event_point_grants` did it for
   `topup_order`). ⚠ A NOT NULL DEFAULT is a write nobody made — read
   `a-not-null-default-is-a-write-nobody-made` before adding a BEFORE INSERT trigger.
4. **Ledger separation:** the new credits carry their own `source`/`kind` value so the private
   portfolio balance can never be spent through the host-gallery path and vice versa (OPEN #1
   decides whether that path still exists). A db test proves a portfolio grant is invisible to
   the host-gallery reader.
5. **The under-25 CTA is G3's** — but G2 exposes `{ credits, packPricePhp, packCredits }` from
   one reader so G3 has one call to make.

## 3. Prove it

- Unit: the pure function (above) + `the-fee-reaches-the-allowance.test.ts` updated.
- DB (from `apps/web`): the new ledger test, `tests/db/ugat-schema-claims.db.test.ts`,
  `tests/db/ugat-concept-coverage.db.test.ts`, plus every existing `tests/db/*papic*` file —
  `npx tsx --test tests/db/vendor-papic*.db.test.ts tests/db/papic-*.db.test.ts`.
- `node scripts/check-migration-timestamps.mjs` from the root.
- **All 29 CI guards** — they are separate steps, NOT `pnpm lint`. Extract and run them exactly
  like this (macOS `sed` does not know `\s`; `run:` left in place executes a command named
  `run:` and every guard "fails" 127; and one step, `lint-page-masthead.mjs`, has
  `working-directory: apps/web`):

  ```bash
  grep -E "^[[:space:]]+run: (cd apps/web && )?(RADIUS_LINT_STRICT=1 )?node [^ ]*lint-[a-z0-9-]+\.mjs" .github/workflows/ci.yml \
    | sed -E 's/^[[:space:]]*run: //' | sort -u > /tmp/guards.txt
  while IFS= read -r line; do bash -c "$line" >/dev/null 2>&1; echo "$? $line"; done < /tmp/guards.txt
  (cd apps/web && node scripts/lint-page-masthead.mjs)
  ```

  Every line must print `0`. `zsh` does not word-split a command held in a variable — that is
  why it is `bash -c "$line"`.
- **Full tsc, under the lock:** `mkdir ~/Documents/Claude/Projects/.tsc.lock` is the atomic
  test-and-set (wait if it exists; `rmdir` it when done, even on failure). Then from `apps/web`:
  `NODE_OPTIONS=--max-old-space-size=6144 npx tsc --noEmit -p tsconfig.json > $S/tsc.out 2>&1; echo "TSC_EXIT=$?" >> $S/tsc.out`
  where `$S` is the session scratchpad — **never `/tmp`**, which comes back empty on the next
  call. Exit 134 = heap (raise to 6144 if you ran the default); 144 = killed (8192 did that
  with no peer running). Read `TSC_EXIT=` and the `error TS` count together; the wrapper's
  exit code is the `echo`'s.
- An empty log after a kill is a NON-RESULT: not a pass, not a failure. Say so.

## 4. Land it

- `changelog.d/vendor-portfolio-papic-ledger.md` — dated `## 2026-09-… · feat(papic): …`
  block with a `SPEC IMPACT:` line. It is not "None": add the `DECISION_LOG.md` row (the
  reversal, quoting *"replace it"*), then the corpus mirror this stream deferred — the
  `0012_papic` iteration `.md` (find the live one: `ls ~/Documents/Claude/Projects/Setnayan/0012_papic/`),
  regenerate its `.docx` via pandoc per `COWORK.md`. Say in the PR body that you did the mirror,
  or that you did not and why.
- Commit with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Push, `gh pr create`,
  then immediately `gh pr merge <PR#> --auto --merge`. Body ends with
  `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
- Count the checks after opening: a CONFLICTING PR runs NO CI and reports zero failing and zero
  running. `gh pr view <PR#> --json mergeable,statusCheckRollup --jq '{mergeable, n:(.statusCheckRollup|length)}'`.
- `git worktree remove /tmp/wt-g2 --force && git worktree prune` the moment auto-merge is
  armed. Never batch cleanup to the end.

## 5. Report

What exists · what you retired · what you added · every number and where it came from (a table
row, an owner quote — never a guess) · the video-800 question, verbatim, for the owner · what
G3 can now call.
