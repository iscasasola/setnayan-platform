# MB26 — two map rows and ten dead rows

**Model · effort: Sonnet · high.** Two verbatim additions to a map that already has a guard, and
one retire-only migration. No pixels, no new shape.

**Owner rulings (2026-09-05, "follow your recommendations"):**

1. `overall` **includes coordinators.** MB16 already gives coordinators the same colour powers
   as stylists; a full-room photo is exactly what a coordinator's portfolio holds.
2. `stage` **admits lights-and-sound shops.** `backdrop` does **not** change — a backdrop is a
   stylist's work.
3. `media.setnayan.com` **is not being set up now.** The ten pilot `venue_scene` rows that point
   at it are retired.

## What oversight measured (2026-09-05, `origin/main`)

- `MOODBOARD_SLOT_TRADES` in `lib/moodboard-gallery.ts` today:
  `stage: ['stylist_decorator', 'av_production']` ·
  `overall: ['reception', 'stylist_decorator', 'lights_sound']` ·
  `backdrop: ['stylist_decorator', 'led_wall']`.
- `coordinator` **is a legal tile key**: it is a member of the `WeddingTile` union in
  `lib/taxonomy.ts` (label "Coordinator / Planner", group `planning`). `lights_sound` is already
  used by `ceiling` and `overall`. Nothing new has to be invented; both additions are existing keys.
- The map's own guard is `lib/moodboard-gallery.test.ts` (MB18). MB18's report: adding a stylist
  entry to `flowers` polluted MB16's "florist only" fixture in `lib/moodboard-finalization.test.ts`
  and had to be re-pointed. **Expect the same shape** — adding `coordinator` to `overall` may
  touch any fixture that assumed an `overall` photo could only come from those three trades.
  Strengthen the fixture (move it to a tile the new entry cannot reach); never loosen the guard.
- The ten dead rows: `moodboard_library_assets` where `asset_type = 'venue_scene'` and
  `storage_path LIKE 'https://media.setnayan.com/%'` — seeded by `20271194970382` (decor-layers
  pilot, 2026-09-03), all `approved_at IS NULL`, subtypes `backdrop` ×5 and `ceiling` ×5. The host
  does not resolve (`dig +short media.setnayan.com` is empty) and the objects also 404 on the
  working `pub-…r2.dev` host. Nothing a customer can see today; nothing that should ever become
  approvable by accident.

## The build

### Part 1 · The map (two rows, verbatim)

```ts
stage:   ['stylist_decorator', 'av_production', 'lights_sound'],
overall: ['reception', 'stylist_decorator', 'lights_sound', 'coordinator'],
```

Nothing else in the map changes. Extend `lib/moodboard-gallery.test.ts` with the two rows the
way MB18 pinned its four: assert the exact array, so a later "tidy-up" that drops a trade goes
red. Sabotage: remove `coordinator` → red; remove `lights_sound` from `stage` → red; add
`lights_sound` to `backdrop` → red (the rule says backdrop does NOT change; pin that too).

### Part 2 · Retire the ten (migration)

`pnpm migration:new` (above `20271206413595`). `UPDATE … SET retired_at = NOW()` on exactly the
predicate above, guarded by a `DO $$ … RAISE` if the row count is not 10 — a different count means
the world changed under the migration and someone should look, not a silent partial retire.
**Retire, never delete** ([[inspiration-gallery-owner-decisions-2026-09-04]]).

Extend `tests/db/no-placeholder-photo-is-ever-live.db.test.ts` (MB23) with one assertion: no live
row's `storage_path` is on `media.setnayan.com`. Do **not** add a host allowlist or a DNS check to
`lib/moodboard-library-placeholder.ts` — the owner may set that domain up later, and a guard that
has to be undone is a guard that gets deleted in a hurry.

## Out of lane

`backdrop`. Every other slot. The watermark work (MB27). Anything under `public/moodboard-seed/`.

## Report

The four lines in `MB-OVERSIGHT.md`, plus the exact `moodboard-gallery.test.ts` diff and which
fixture (if any) the `overall` change collided with.
