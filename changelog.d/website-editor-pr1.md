# Changelog fragment — collected into CHANGELOG.md by scripts/changelog-collect.mjs

## 2026-07-25 · feat(website): unified website editor — shell, live preview, two-way sync (PR-1)

PR-1 of 5 for the Unified Website Editor (design + build plan: `Design_Unified_Website_Editor_2026-07-25/`). Owner 2026-07-25: editing was scattered across ~18 routes and two paradigms, and every "preview/edit" link jumped into the legacy `/site-editor` ("an old website"). This PR lands the one editing surface and re-points every link at it — **which is the fix for that jump.**

- **NEW `/dashboard/[eventId]/website/editor`** — the unified editor. Left rail groups the whole website the way a couple thinks about it (**① Site** URL · who-can-view · open browsing · Colors [PRO] · Background music [PRO] · **② Sections** hero · our story · details & schedule · gallery [PRO] · dress code · photo moments · special message · what to bring · show/hide & reorder · **③ Chapters** Save-the-Date · After/editorial [PRO]); right pane is the couple's REAL `/[slug]` page in a same-origin iframe with the four lifecycle phase tabs. Absorbs the Launch surface (owner "rebuild the launch page and improve it"): `LaunchStdButton` go-live/schedule sits in the rail, status chips come from the same reads. Mobile gets an Edit ⇄ Preview toggle.
- **Two-way sync** — `app/[slug]/_components/editor-bridge.tsx` (new): rail row → `postMessage {scrollTo}` → preview scrolls + highlights; tapping a section IN the preview → `{edit}` → that rail row activates and scrolls into view. Both directions verify `event.origin`. The bridge finds sections through the anchors the site ALREADY renders (`SITE_MENU_ANCHORS`), so `site-body.tsx` needed only ONE gated mount instead of editor attributes threaded through every branch.
- **Host-gated `?editor=1`** — mirrors the existing `?phase=` fence but STRICTER (real host membership only; no demo-event shortcut), and the lookup fires only when the param is present. `editorMode` is FALSE for every guest/anonymous visitor, so **guest HTML is unchanged byte-for-byte** — `site-body-plan` (19) + `site-menu` (4) + `anonymous-zero-guest` (4) all green.
- **Link re-point (the bug fix):** the sidebar "Launch" nav item, `LaunchStdButton`'s preview link, the Launch page's URL/map/theme cards, the website board's editor + per-phase edit links, and `WebsiteLaunchPreview`'s edit hrefs now all land on the unified editor instead of `/site-editor`.

Scope note: PR-1 rail rows deep-link to the editor that already owns each setting; PR-3/PR-4 convert them to inline panels calling those SAME server actions (never a new write path). `/site-editor` and `/website/launch` still resolve — PR-2 retires them to redirects. Remaining `/site-editor` references are lib route-builders (`routes.ts`, `add-ons-catalog.ts`, `customer-menu.ts` activeMatch) + the `studio/[addon]` redirect; PR-2's redirect makes them land correctly, then PR-5 cleans them up. No migration.

SPEC IMPACT: Applied — `DECISION_LOG.md` 2026-07-25 (Unified Website Editor + Launch merge) + `Design_Unified_Website_Editor_2026-07-25/`.
