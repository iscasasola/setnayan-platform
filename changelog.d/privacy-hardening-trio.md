## 2026-07-05 · fix(privacy): authenticate /api/notify, mask pre-reveal vendor logo, add invite-QR rotation

Three privacy hardening fixes from the 2026-07-04 Data Flow Map audit (gaps #3, #6, #9), shipped in one PR:

1. **Authenticated `/api/notify`** (gap #3). The Supabase DB-webhook target that sends push notifications and stamps `chat_threads.last_push_notified_at` now enforces a shared-secret header (`x-webhook-secret`) via a constant-time compare (`node:crypto` `timingSafeEqual`) against `NOTIFY_WEBHOOK_SECRET`. Missing/wrong header → 401 (fail closed). If `NOTIFY_WEBHOOK_SECRET` is itself unset, the route fails OPEN with a `console.warn` so a fresh deploy doesn't break push before the owner provisions the secret. (Replaces the prior `SUPABASE_WEBHOOK_SECRET` check that failed closed with a 500 when unset.)

2. **Hybrid-anonymity logo leak fixed** (gap #6). The couple's pre-reveal thread list (`dashboard/[eventId]/messages`) masked the vendor's name but passed the real `logo_url` unconditionally. The logo is now nulled until reveal using the exact same predicate that masks the name — `isVendorNameRevealed` (the single source of truth behind `resolveVendorDisplayName`) — so logo and label can never drift; pre-reveal the avatar falls back to screen-name initials. Auditing the other couple-facing pre-reveal surfaces surfaced the **same leak class on the Vendors shortlist card** (`dashboard/[eventId]/vendors`, `marketplaceCardByVendorId`), which likewise resolved the name through the anonymity gate but passed `logo_url` straight through — now gated by the identical `isVendorNameRevealed` predicate. The thread DETAIL page selects `logo_url` but never renders it (no leak there); the broader public marketplace browse grid is intentionally left as-is (that's a discovery surface, not the inquiry pre-reveal context, and changing it is a larger vendor-value-proposition decision out of scope for this hardening PR).

3. **Event join-QR rotation** (gap #9). `event_join_tokens.revoked_at`/`expires_at` were honored by all 4 read sites but nothing wrote them, so a leaked/printed QR stayed valid forever. Added a couple-only "Regenerate QR" action on the guests → Invite surface (`dashboard/[eventId]/guests/invite`) that mints a fresh 32-char hex token — matching the DB generator `generate_event_join_token()` / the `event-qr` rotation precedent — via a single in-place UPDATE on the per-event token row (the table is UNIQUE per `event_id`), clearing `revoked_at`. Overwriting the token string is what revokes the old QR (all read sites match on `event_id + token`). Gated behind a destructive confirm dialog warning that shared/printed QRs stop working. New action lives in the page's own `actions.ts` (NOT `dashboard/[eventId]/actions.ts`, per parallel-work de-confliction).

OWNER ACTION (item 1): set `NOTIFY_WEBHOOK_SECRET` in Vercel env AND add the matching `x-webhook-secret` header to the Supabase `chat_messages` webhook config. Until then, `/api/notify` accepts unauthenticated posts (fail-open) and logs a warning on every call.

SPEC IMPACT: None. (No schema change, no SKU/pricing/taxonomy change; corpus specs are archive-only per the 2026-06-07 source-of-truth flip.)
