// Loading shell for the Accounts Studio — instant table-shaped skeleton while
// the active tab's surface (Users / Events) loads. Matches the pattern used by
// the legacy /admin/users + /admin/events loading files.
export { TablePageSkeleton as default } from '@/components/skeletons';
