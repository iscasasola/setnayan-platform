## 2026-07-23 · fix(payments): close three paths that let a non-admin reach `paid`/`matched` or provision a SKU without full payment

MONEY / apply-then-pay lock: `admin/payments/actions.ts` (service-role) is the ONLY legitimate writer of `orders.status='paid'`. Three holes let that be bypassed; all three now closed.

**(a) `orders_owner_write` / `payments_owner_insert` had no status guard** (migration `20260513150000_iteration_0034_payments.sql:77,119` — both `WITH CHECK (user_id = auth.uid())` only). An authenticated couple could `PATCH /rest/v1/orders {status:'paid'}` — or self-`INSERT` a `payments` row at `status='matched'` to satisfy the admin shortfall arithmetic — straight from the browser and self-provision. New migration `20270920010000_order_payment_write_guard.sql` adds three RESTRICTIVE policies (ANDed on top of the permissive owner policies, `TO authenticated`, split INSERT/UPDATE so SELECT/DELETE reads are untouched): a non-admin, non-service-role writer may only set an order's NEW-row status to `draft`/`submitted`/`awaiting_payment`/`cancelled` (the money-final `paid`/`fulfilled`/`refunded`/`lapsed` are blocked), and may only INSERT a payment at `status='pending'`. `auth.role()='service_role'` (the admin/service client) and `is_admin()` pass through. Allowed sets enumerated from the real writers — every papic free-order `'fulfilled'` insert and the vendor self-comp `'paid'` insert already run through `createAdminClient()` (service_role) and are unaffected. A `DO $$` post-condition asserts RLS stays ENABLED on both tables and that all three guards exist AS RESTRICTIVE. RLS never disabled; no `USING (true)`.

**(b) `createOrder` self-comp branch provisioned on an arbitrary event** (`app/dashboard/[eventId]/orders/actions.ts`). The branch minted `status='paid'` + ran SKU activation on a caller-supplied `event_id`, gated only by "is owner/admin of this vendor_profile" — trivially self-granted (a self-registered vendor auto-owns their profile) and saying nothing about the target event, so a vendor could provision a paid SKU onto a STRANGER'S event. Fix: the caller must ALSO be a `member_type='couple'` member of the target event (self-comp = "comp for MYSELF" / my own wedding). Decision extracted to pure, tested `lib/self-comp-authority.ts` (`decideSelfCompAuthority`). The per-quarter `enforce_vendor_self_comp_quota` trigger on `comp_grants` still bounds volume.

**(c) admin reconciliation provisioned on partial payment** (`app/admin/payments/actions.ts`). SKU activation fired on EVERY approval (moved outside the promote/shortfall guard by a 2026-06-15 "dead-unlock repair"), so approving a ₱1 payment on a ₱X order with "mark paid" unchecked still provisioned the full SKU — contradicting the block's own notice ("the order was NOT marked paid"). Fix: activation now gated on `shouldProvisionOnApproval({ promoteOrder, reconciledToPaid })` — fires only when the order actually reaches `paid` (promoted AND matched-total fully reconciles owed). Shortfall predicate extracted to `orderReconciledToPaid` + `ORDER_SHORTFALL_TOLERANCE_PHP` in `lib/orders.ts` (replaces the inline `SHORTFALL_TOLERANCE_PHP`/comparison). The Deep-Search `after()` deferral and idempotency contract are preserved.

Tests — new `lib/self-comp-authority.test.ts` (6 cases incl. the exact vendor-owner-but-not-couple exploit) + 10 new cases in `lib/orders.test.ts` for the reconciliation gate. Red/green: mutating `>=`→`>` (reconcile) fails the ₱1-tolerance case; `&&`→`||` (provision gate) fails the promote-unchecked + short cases; dropping the couple check fails the exploit case. Full `lib/**` suite green (2882), typecheck clean. The (a) guards are SQL and asserted at migrate-time; scripted reconciliation reasoning is in the PR body (no local Postgres in this env).

SPEC IMPACT: None. Enforces the already-locked apply-then-pay invariant (CLAUDE.md "Payment system" · `admin/payments/actions.ts` is the sole `paid` writer); no SKU, price, schema-rename, or feature change. The deliberate vendor self-comp feature (§ 3.1a, quota-bounded) is preserved, only scoped to the vendor's own events.
